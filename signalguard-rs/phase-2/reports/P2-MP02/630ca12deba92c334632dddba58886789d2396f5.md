# P2-MP02-R1 Delivery Report

## Identity

- Mini-phase: `P2-MP02-R1`
- Repair contract connector commit: `78bcbbfba425623bdd1391d5cf6de73992d665cd`
- Original P2-MP02 contract connector commit: `b6204ae8015c78df3c24ec8302f82dfc07ab31cb`
- Product repository: `progeranna/signalguard-rs`
- Product base SHA: `ce2ee582a370cce8bf8198d1fbb82fcb961867c3`
- Rejected product head SHA: `a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c`
- Repaired product head SHA: `630ca12deba92c334632dddba58886789d2396f5`
- Product branch: `p2/mp02-symbol-data-queries`
- Product PR: `https://github.com/progeranna/signalguard-rs/pull/17`
- PR base: `refactor/data-boundaries`
- PR state after repair: open, draft, unmerged

## Delivery status

- `DELIVERED`

The strictly test-only repair is complete. Exact-head GitHub CI run `30205299353` concluded `success`; both the `frontend` and `rust` jobs completed successfully.

## Product commit history over the exact base

The repaired branch is exactly two commits ahead of `ce2ee582a370cce8bf8198d1fbb82fcb961867c3` and zero commits behind:

1. `a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c` — `refactor(web): query symbol details by market identity`
2. `630ca12deba92c334632dddba58886789d2396f5` — `test(web): align isolation tests with symbol queries`

No amend, rebase, history rewrite, or force-push was used.

## Authorized repair scope

### Changed files relative to the rejected head

- `web/src/app/marketNavigationIsolation.test.tsx` — primes the existing Demo timeline resources for route tests and asserts Demo detail anomalies from the timeline-owned resource.
- `web/src/features/dashboard/marketDataIsolation.test.tsx` — migrates controlled request identities and fixtures to existing dedicated Live state, health, and symbol-filtered anomaly endpoints; preserves Demo timeline ownership, four-cell identity coverage, abort behavior, and late-response isolation.
- `web/src/features/dashboard/selectedSymbolConsumers.test.ts` — updates the source assertion to the active production expression `storeSelectedSymbol(selectedUiMode, resolvedSymbol)` and its current effect dependencies.

### Created files

- None in the product repository.

### Deleted files

- None.

### Scope confirmation

The repair commit changes exactly the three paths authorized by P2-MP02-R1. No production source file, dependency, lockfile, CI file, backend file, generated contract, or unrelated test path changed in R1.

## Repair implementation summary

The legacy isolation harness now understands these already-adopted request families:

- `GET /market/{symbol}/state` for Live symbol state;
- `GET /market/{symbol}/health` for Live symbol health;
- `GET /anomalies?limit=50&symbol={symbol}` for Live symbol-filtered anomalies, including the repository's actual query serialization order;
- `GET /market/{symbol}/timeline?mode=demo` for Demo detail anomalies.

Dashboard summary remains mode-wide and continues to provide dashboard overview/catalog data. Demo state and health remain summary-owned because no dedicated Demo state/health endpoints exist. No Live resource falls back to Demo data.

## Resolution of the eight diagnosed failures

1. **Demo route anomaly assertion** — `marketNavigationIsolation.test.tsx` now primes BTCUSDT and ETHUSDT Demo timeline cache entries and asserts `timelineAnomaly`, not the broad-summary anomaly sentinel.
2. **Popup Demo BTC → ETH remained loading** — the controlled harness now observes both symbol timeline requests, verifies the obsolete BTC request is aborted, resolves the ETH timeline, and verifies a late BTC completion cannot replace ETH.
3. **Popup Demo → Live request helper could not find Live detail data** — the harness now recognizes and resolves dedicated Live state, health, and filtered-anomaly requests instead of expecting Live detail from dashboard summary.
4. **Demo/BTCUSDT matrix anomaly mismatch** — the four-cell matrix now expects the Demo timeline anomaly sentinel for popup detail.
5. **Demo/ETHUSDT matrix anomaly mismatch** — the four-cell matrix now expects the Demo timeline anomaly sentinel for popup detail.
6. **Live/BTCUSDT matrix popup unavailable** — the matrix primes canonical Live state, health, and anomaly cache entries under their dedicated query keys.
7. **Live/ETHUSDT matrix popup unavailable** — the matrix primes canonical Live state, health, and anomaly cache entries under their dedicated query keys.
8. **Selected-symbol persistence source assertion** — the test now requires `storeSelectedSymbol(selectedUiMode, resolvedSymbol)` and `[resolvedSymbol, selectedUiMode]`, while retaining route-link and mode-scoped persistence checks.

## Invariants proven

| Invariant | Evidence |
|---|---|
| Dashboard summary remains mode-wide for overview/catalog ownership | Existing `MarketIdentityProbe` summary/catalog assertions remain and pass in full CI. |
| Demo detail anomalies come from Demo timeline | Navigation, popup, and Demo matrix assertions require `timelineAnomaly`; full frontend tests pass. |
| Live detail uses dedicated state/health/anomaly endpoints | `resolveLiveDetail` waits for and validates exact state, health, and `/anomalies?limit=50&symbol=...` requests; full frontend tests pass. |
| No Demo fallback for Live | Demo → Live test resolves Live dedicated resources, then completes Demo requests late and proves Live values remain rendered. |
| BTC → ETH does not relabel late BTC data as ETH | Controlled deferred timeline requests prove BTC abort and late-response isolation before and after ETH success. |
| Mode switching aborts obsolete Demo detail work | Demo summary and Demo timeline signals are asserted aborted after switching the popup to Live. |
| Demo/Live × BTCUSDT/ETHUSDT matrix remains strict | All four original cells remain present and validate mode, symbol, prices, anomaly ownership, popup identity, summary identity, and timeline identity. |
| Selected-symbol persistence remains mode-scoped | Source test requires `storeSelectedSymbol(selectedUiMode, resolvedSymbol)` and keeps active-mode route-link checks. |
| Loading and success states remain explicit | Popup tests assert loading before required resources resolve and success only after the current identity's resources resolve. |

## Tests changed

- `web/src/app/marketNavigationIsolation.test.tsx` — route BTC → ETH, mixed-case route normalization, invalid Live route/no Demo fallback.
- `web/src/features/dashboard/marketDataIsolation.test.tsx` — dashboard symbol/mode isolation, deterministic deferred-request cancellation, popup symbol switching, popup mode switching, Live endpoint ownership, and all four identity-matrix cells.
- `web/src/features/dashboard/selectedSymbolConsumers.test.ts` — active-mode persistence source wiring and existing route-link/global-storage guardrails.

No test was deleted, skipped, converted to TODO, bypassed, or weakened to obtain a green result.

## Verification commands and evidence

| Command / gate | Result | Evidence / notes |
|---|---|---|
| Focused invocation for `src/app/marketNavigationIsolation.test.tsx`, `src/features/dashboard/marketDataIsolation.test.tsx`, and `src/features/dashboard/selectedSymbolConsumers.test.ts` | `NOT_RUN` separately | The worker environment could not materialize an authenticated exact checkout or install dependencies because direct GitHub/npm host resolution was unavailable. All three files were executed successfully within the exact-head full CI test suite. |
| All changed/new P2-MP02 tests | `PASS` | Included in exact-head `npm run test:run` in CI run `30205299353`; frontend test step concluded success. |
| `npm ci` | `PASS` | CI frontend step `Install frontend dependencies` concluded success. |
| `npm run test:run` | `PASS` | CI frontend step `Run frontend tests` concluded success. This includes the three repaired legacy files and the existing MP02 tests. |
| `npm run typecheck` | `PASS` | CI frontend step `Run frontend typecheck` concluded success. |
| `npm run lint` | `PASS` | CI frontend step `Run frontend lint` concluded success. |
| `npm run build` | `PASS` | CI frontend step `Build frontend` concluded success. |
| `npm run bundle:check` | `PASS` | CI frontend step `Check frontend bundle budget` concluded success. |
| Isolated TypeScript transpile/syntax validation of the three repaired files | `PASS` | Executed before commit construction; all three files transpiled without TypeScript syntax diagnostics. |
| `git diff --check` | `NOT_RUN` locally | No local exact checkout was available. GitHub commit construction and compare inspection established a three-file UTF-8 text-only repair diff; exact-head lint/build CI passed. |
| Local `git status --short` | `NOT_RUN` | No local product worktree was used. Remote branch head and commit/path inventory were verified through GitHub immediately before and after delivery. |

## CI

- Product exact head: `630ca12deba92c334632dddba58886789d2396f5`
- Workflow: `CI`
- Workflow run ID: `30205299353`
- Run number: `87`
- Overall conclusion: `success`
- Frontend job ID: `89802196346` — `success`
  - dependency installation: success
  - frontend tests: success
  - typecheck: success
  - lint: success
  - build: success
  - bundle budget: success
- Rust job ID: `89802196305` — `success`
- Pending checks: none

## Compatibility

- No endpoint, request parameter, response schema, query key, query hook, resource resolver, route, popup, persistence implementation, visual behavior, dependency, or backend behavior changed in R1.
- Dashboard summary remains mode-wide.
- Demo detail anomalies remain timeline-owned.
- Live detail remains owned by dedicated state, health, and filtered-anomaly queries.
- PR #17 remains open, draft, and unmerged.

## Known limitations

- The worker could not run a separate local focused Vitest command or local Git worktree checks because direct repository/package-host resolution was unavailable. Exact-head GitHub CI executed the complete frontend suite and every required repository-native frontend gate successfully.

## Forbidden follow-up confirmation

- No production source changed in P2-MP02-R1.
- P2-MP03, P2-MP04, and P2-MP07 were not started.
- Concurrent P2-MP05 work was not read, modified, merged, rebased, or cherry-picked.
- No amend, rebase, force-push, PR recreation, or merge was performed.

## Repository hygiene

- Repair diff relative to rejected head: exactly three authorized test files.
- Product branch relation to base: ahead by 2 commits, behind by 0.
- Unexpected repair paths: `NONE`.
- Dependency/lockfile changes in R1: `NONE`.
- Generated/build artifacts committed in R1: `NONE`.
- Production-source changes in R1: `NONE`.
- Product PR state: open, draft, unmerged.

## Commit and push evidence

- Repair commit SHA: `630ca12deba92c334632dddba58886789d2396f5`
- Repair commit message: `test(web): align isolation tests with symbol queries`
- Parent SHA: `a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c`
- Push target: `p2/mp02-symbol-data-queries`
- Fast-forward update: `YES`
- Force-push used: `NO`
- PR recreated: `NO`
- PR merged by worker: `NO`

## Final worker declaration

I confirm that this report describes the exact repaired product head `630ca12deba92c334632dddba58886789d2396f5`, that R1 changed only the three authorized test files, and that no later unreported product commit was pushed to the task branch.