# P2-MP03-R2-R1 Repair Report

- Original P2-MP03-R2 contract: `54a93405d55b8dc1bd1b2129db2a0abaa50134a8`
- Repair contract: `ca2c49313da44fbbd925a12281b8bc7974222496`
- Clean product base: `1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`
- Rejected product head: `d2be5b35d8beb433d3bafdcd9f9986ae5a292f2d`
- Repaired product head: `9174f5797b5d21bd917497a668f0cd7e1c3e35aa`
- Product branch: `p2/mp03-market-view-models-r2`
- Product PR: https://github.com/progeranna/signalguard-rs/pull/19
- PR base: `refactor/data-boundaries`

## Exact changed product paths

- `web/src/features/dashboard/marketViewModel.ts`
- `web/src/features/dashboard/marketAdapters.ts`
- `web/src/features/dashboard/marketAdapters.test.ts`
- `web/src/pages/DashboardPage.tsx`
- `web/src/pages/SymbolDetailPage.tsx`
- `web/src/pages/SymbolDetailPage.test.tsx`

## Repair semantics

The shared readonly market view model now carries explicit `{ route, popup }` display variants for anomaly observed and threshold values. Route values use generic `en-US` numeric formatting with at most four fractional digits and no anomaly suffix. Popup values retain type-aware percentages, durations, `/m`, `gap`, and `limit` units. Null and NaN values remain `—`; `spread_spike` 2.5 maps to route `2.5` and popup `2.500%`.

The view model carries `stateAvailable`. Absent state leaves all state-derived values unavailable and `depthGaps` as `—`; present state with a real zero gap count renders `0`. The route keeps the existing Latest normalized state empty-state shell when state is absent.

The route passes `{ mode: selectedUiMode, symbol: canonicalRouteSymbol ?? resourceState.resource.symbol }`; the popup passes `{ mode: identity.mode, symbol: identity.symbol }`. Existing resource, summary, and anomaly identity guards remain intact.

## Focused tests

- `market resource adapters` — 15 tests: route/popup variants, spread precision, duration/burst/depth units, null values, absent state, fabricated-zero prevention, real zero, health/status, Demo/Live separation, identity preservation and mismatch rejection.
- `symbol detail route resource ownership` — 5 tests: shared contract, route/popup expected-identity wiring, route latest-state shell guard, and raw DTO formatting architecture guard.
- Existing `DashboardPage.popup.test.tsx` — 9 tests passed.
- Existing `symbolPopupResource.test.tsx` — 7 tests passed.
- Focused result: 4 files, 36 tests passed.
- Existing mode/symbol switching and shell behavior remained green in the full suite.

## Local gates

- `npm ci`: PASS (existing Node 16 engine warnings only)
- `npm run test:run`: PASS — 22 files, 281 tests
- `npm run typecheck`: PASS
- `npm run lint`: PASS
- `npm run build`: PASS
- `npm run bundle:check`: PASS — 746869 bytes / 753664-byte budget
- `git diff --check`: PASS
- Changed-path inventory: PASS — six authorized product paths only
- Repository hygiene: PASS — no forbidden product paths, dependencies, lockfiles, frontend configuration, or backend files changed
- Copy/CSS/query/backend inspection: PASS — no existing user-visible strings, CSS classes, accessibility labels, layout, query, endpoint, or Rust changes
- Dashboard scope inspection: PASS — imports and the existing symbol-detail popup success path only

## Exact-head CI

- Run ID: `30216901488`
- Head SHA: `9174f5797b5d21bd917497a668f0cd7e1c3e35aa`
- Conclusion: `success`
- Frontend and Rust jobs: passed
