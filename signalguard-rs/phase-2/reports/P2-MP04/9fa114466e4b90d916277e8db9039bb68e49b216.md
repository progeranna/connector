# P2-MP04-R2 extractor rejection response repair

- Original contract: `signalguard-rs/phase-2/prompts/P2-MP04.md` at `5d0950bf1f6cbcb30e637773c7c273a291586001`.
- Continuation: `signalguard-rs/phase-2/repairs/P2-MP04-C1.md` at `5d0950bf1f6cbcb30e637773c7c273a291586001`.
- R1 contract: `signalguard-rs/phase-2/repairs/P2-MP04-R1.md` at `1a4ae7e01d51c15072e9b2d19bd5dfe756eb9308`.
- R2 execution continued from product head `ccbf8967bfaafab51a2785883d666eae80aba7de`.

Product repository: `progeranna/signalguard-rs`  
Worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p2-mp04`  
Branch: `p2/mp04-api-contract`  
Remote phase base: `origin/refactor/data-boundaries` = `1a226b5b5661a2b99dabec579aeeecf8b0e4be85`  
Repair commit: `9fa114466e4b90d916277e8db9039bb68e49b216`  
PR: https://github.com/progeranna/signalguard-rs/pull/21 (draft, base `refactor/data-boundaries`)

## Preflight

The dedicated worktree and branch were clean and exact at the required starting head. `origin/p2/mp04-api-contract` matched `ccbf8967bfaafab51a2785883d666eae80aba7de`; `origin/refactor/data-boundaries` matched `1a226b5b5661a2b99dabec579aeeecf8b0e4be85`; remote comparison was `0 2`. The stale local `refactor/data-boundaries` ref was not used.

## Router probe matrix

Probes use the real Axum router and existing application-state helpers in `api::routes`.

| Endpoint/request | Source | Status | Content-Type | Body |
|---|---|---:|---|---|
| `GET /dashboard/summary?mode=prod` | Axum query extractor | 400 | `text/plain; charset=utf-8` | text |
| `GET /market/BTCUSDT/timeline?mode=prod` | Axum query extractor | 400 | `text/plain; charset=utf-8` | text |
| `GET /market/BTC-USDT/timeline?mode=demo` | handler `ApiError::InvalidSymbol` | 400 | `application/json` | `{error,message}` |
| `GET /anomalies?limit=wat` | handler `ApiError::InvalidRequest` | 400 | `application/json` | `{error,message}` |
| `GET /anomalies?symbol=BTC-USDT` | handler `ApiError::InvalidSymbol` | 400 | `application/json` | `{error,message}` |
| `POST /runtime/mode`, malformed JSON | Axum JSON extractor | 400 | `text/plain; charset=utf-8` | text |
| `POST /runtime/mode`, `{"mode":42}` | Axum JSON extractor | 422 | `text/plain; charset=utf-8` | text |
| `POST /runtime/mode`, missing `mode` | Axum JSON extractor | 422 | `text/plain; charset=utf-8` | text |
| `POST /runtime/mode`, missing content type | Axum JSON extractor | 415 | `text/plain; charset=utf-8` | text |
| `POST /runtime/mode`, `text/plain` content type | Axum JSON extractor | 415 | `text/plain; charset=utf-8` | text |
| `POST /runtime/mode`, unsupported mode | handler/supervisor `ApiError::InvalidRequest` | 400 | `application/json` | `{error,message}` |
| `POST /runtime/mode`, switching disabled | handler `ApiError::Forbidden` | 403 | `application/json` | `{error,message}` |

The probe test asserts exact status and media type, JSON body structure for handler errors, and text body kind for extractor failures.

## Contract repair

`contracts/web-console.openapi.json` remains OpenAPI 3.0.3 and is regenerated by `src/bin/export-api-contract.rs`.

`src/api/contract.rs` now distinguishes `HandlerJson` and `ExtractorText` response metadata. Handler errors reference `ApiErrorResponse`; extractor failures reference `ExtractorRejectionBody` (`type: string`, `text/plain; charset=utf-8`). Status `400` on timeline and runtime POST advertises both media types because both are proven reachable. Runtime POST includes exactly `400`, `403`, `409`, `415`, `422`, and `500` responses.

Write: `cargo run --quiet --bin export-api-contract -- --write`  
Check: `cargo run --quiet --bin export-api-contract -- --check`  
OpenAPI validation: `cargo run --quiet --bin export-api-contract -- --validate`

The validator is `openapiv3` 2.2.0 deserialization of the actual committed artifact into `openapiv3::OpenAPI`; it passed locally and in CI. The artifact equality, intentional drift, determinism, route/method, schema, nullability, and error-media tests all passed.

## Compatibility tests

`validateCompatibility` in `web/src/features/dashboard/api-contract.test.ts` now verifies handler JSON references, extractor text media/schema, query-extractor separation, all runtime POST extractor statuses, and the existing endpoint/schema compatibility contract. Ten focused tests passed. Isolated cloned fixtures are rejected through the validator for removing extractor media type, replacing extractor text schema with `ApiErrorResponse`, removing a required runtime POST status, and replacing a handler JSON error with text-only; all previous mutation fixtures also remain covered.

## Gates and integrity

Rust gates passed: formatting, check, clippy with `-D warnings`, full all-target tests (378 passed, 3 pre-existing environment-dependent ignored), OpenAPI validation, contract write/check twice with unchanged status, and `git diff --check`.

Frontend gates passed: `npm ci`, focused compatibility (10 passed), full suite (23 files/293 tests), typecheck, lint, build, and bundle check.

R2 changed paths against previous head: `.github/workflows/ci.yml`, `Cargo.lock`, `Cargo.toml`, `contracts/web-console.openapi.json`, `src/api/contract.rs`, `src/api/routes.rs`, `src/bin/export-api-contract.rs`, `web/src/features/dashboard/api-contract.test.ts`.

All changed paths against the remote phase base are limited to the prior P2-MP04 paths plus the R2 modifications. Forbidden paths unchanged against the phase base: `src/api/handlers.rs`, `src/api/error.rs`, `src/storage/redis.rs`, `tests/redis_cache.rs`, and `web/src/features/dashboard/types.ts`. No runtime behavior, serialization, status behavior, routes, database/cache behavior, frontend queries, view models, or presentation code changed.

Final remote comparison: `origin/refactor/data-boundaries...HEAD` = `0 3`.

Exact-head CI: run `30277459548`, conclusion `success`; Rust and frontend jobs passed, including `Validate OpenAPI 3.0.3 contract`.
