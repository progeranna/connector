# P2-MP02 Review — a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c

## Decision

`REJECT — REPAIR_REQUIRED`

## Reviewed identity

- Product repository: `progeranna/signalguard-rs`
- Product branch: `p2/mp02-symbol-data-queries`
- Product base: `ce2ee582a370cce8bf8198d1fbb82fcb961867c3`
- Product head: `a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c`
- Product PR: `https://github.com/progeranna/signalguard-rs/pull/17`
- Contract: `progeranna/connector@b6204ae8015c78df3c24ec8302f82dfc07ab31cb`
- Delivery report: `signalguard-rs/phase-2/reports/P2-MP02/a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c.md`

## Repository state

- Exact base/head relation: one commit ahead, zero behind.
- PR base: `refactor/data-boundaries`.
- PR state: open, draft, unmerged.
- Changed paths: nine files, all under the allowed `web/src/**` lease.
- No dependency, lockfile, Rust, Redis, PostgreSQL, CI, or generated-contract changes.

## Implementation assessment

The implementation direction is consistent with the contract:

- canonical symbol-owned query-key families exist for state, health, timeline, and anomalies;
- Live route/popup ownership uses existing dedicated state, health, and filtered-anomaly endpoints;
- Demo uses mode-aware timeline together with Demo summary data;
- route and popup share one symbol-market resource identity layer;
- new focused MP02 tests passed within the full suite before the legacy-suite failures were reached.

No acceptance is possible because the required complete frontend gate is red.

## Blocking evidence

Product workflow run:

- run ID: `30203823930`;
- Rust job: success;
- frontend dependency installation: success;
- `npm run test:run`: failure;
- typecheck, lint, build, and bundle checks: skipped.

An orchestrator diagnostic reran the exact product head and persisted the complete failure tail:

- diagnostic run: `30204380568`;
- request: `signalguard-rs/phase-2/diagnostic-requests/P2-MP02/a83d63b-diag1.json`;
- result: `signalguard-rs/phase-2/diagnostic-results/P2-MP02/a83d63b-diag1/result.json`;
- log tail: `signalguard-rs/phase-2/diagnostic-results/P2-MP02/a83d63b-diag1/vitest-tail.log`.

Exact result:

- 3 test files failed;
- 8 tests failed;
- 18 test files passed;
- 257 tests passed.

Failing legacy test files:

1. `web/src/app/marketNavigationIsolation.test.tsx`
2. `web/src/features/dashboard/marketDataIsolation.test.tsx`
3. `web/src/features/dashboard/selectedSymbolConsumers.test.ts`

Failure classes:

- legacy Demo expectations still read anomaly sentinels from broad dashboard summary instead of the adopted Demo timeline resource;
- legacy Live popup/route harnesses do not supply the new dedicated state, health, and filtered-anomaly requests, leaving the resource unavailable or loading;
- controlled mode-switch helpers do not recognize the new Live request families;
- a source-level selected-symbol persistence assertion still requires the old `selectedSummary.symbol` expression even though the new route preserves the same mode-scoped behavior through `resolvedSymbol`.

## Acceptance boundary

The repair must not merely delete, skip, loosen, or bypass the existing isolation matrix. It must migrate the existing tests to the new resource ownership while preserving:

- Demo/Live × BTCUSDT/ETHUSDT coverage;
- route/popup parity;
- no stale symbol relabeling;
- abort and late-response isolation;
- mode-scoped selected-symbol persistence;
- explicit loading/error/unavailable/success behavior.

Production code changes are not authorized in the first repair. If the migrated tests expose a real product defect, the worker must stop and report it for a separate repair decision.

## Integration

`NOT_INTEGRATED`
