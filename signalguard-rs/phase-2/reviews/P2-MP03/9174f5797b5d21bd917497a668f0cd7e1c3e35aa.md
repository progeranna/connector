# P2-MP03-R2-R1 Review

## Verdict

`ACCEPT`

## Reviewed delivery

- Product repository: `progeranna/signalguard-rs`
- Branch: `p2/mp03-market-view-models-r2`
- PR: https://github.com/progeranna/signalguard-rs/pull/19
- PR base: `refactor/data-boundaries`
- Clean phase base: `1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`
- Rejected head: `d2be5b35d8beb433d3bafdcd9f9986ae5a292f2d`
- Repaired exact head: `9174f5797b5d21bd917497a668f0cd7e1c3e35aa`
- Repair commit: `fix(web): preserve market view-model presentation semantics`
- Repair contract: `signalguard-rs/phase-2/repairs/P2-MP03-R2-R1.md` at connector commit `ca2c49313da44fbbd925a12281b8bc7974222496`
- Delivery report: `signalguard-rs/phase-2/reports/P2-MP03/9174f5797b5d21bd917497a668f0cd7e1c3e35aa.md` at connector commit `a58c95033d80b7a41a78cb4329094847311e2cc5`

## Structural validation

- PR is open, draft, mergeable, and targets `refactor/data-boundaries`.
- Exact PR head is `9174f5797b5d21bd917497a668f0cd7e1c3e35aa`.
- Repair is exactly one additional commit after rejected head `d2be5b35d8beb433d3bafdcd9f9986ae5a292f2d`.
- Repair commit message exactly matches the contract.
- Repair changes only the same six authorized frontend paths:
  - `web/src/features/dashboard/marketViewModel.ts`
  - `web/src/features/dashboard/marketAdapters.ts`
  - `web/src/features/dashboard/marketAdapters.test.ts`
  - `web/src/pages/DashboardPage.tsx`
  - `web/src/pages/SymbolDetailPage.tsx`
  - `web/src/pages/SymbolDetailPage.test.tsx`
- No API hooks, query keys, DTO schemas, dependencies, lockfiles, frontend tooling, CI, or backend/Rust paths changed.

## Repair findings

The four blocking findings from the rejected head are resolved:

1. Route and popup anomaly presentation semantics are preserved through one readonly view-model contract with explicit `{ route, popup }` display variants.
   - `spread_spike` value `2.5` maps to route `2.5` and popup `2.500%`.
   - Route retains generic `en-US` numeric formatting with at most four fractional digits.
   - Popup retains type-aware percentage, duration, `/m`, `gap`, and `limit` units.
   - Null and NaN remain `—`.

2. Partial-state behavior is preserved.
   - The view model exposes `stateAvailable`.
   - Missing state leaves state-derived fields unavailable.
   - Missing state maps `depthGaps` to `—`, not fabricated zero.
   - Present state with a real zero gap count maps to `0`.
   - `SymbolDetailPage` retains the existing Latest normalized state empty shell when state is absent.

3. Expected canonical identity is now passed by both production consumers.
   - Route passes the current mode and canonical route symbol.
   - Popup passes popup identity mode and symbol.
   - Existing resource, summary, and anomaly identity guards remain active.

4. The connector delivery report exists at the unique required path and its claims match the product head, PR, test evidence, and CI result.

## Test and CI evidence

Local evidence reported and consistent with the delivered tests:

- focused tests: 4 files, 36 tests passed;
- full frontend suite: 22 files, 281 tests passed;
- typecheck: passed;
- lint: passed;
- build: passed;
- bundle check: passed;
- `git diff --check`: passed;
- changed-path and repository-hygiene checks: passed.

Exact-head product CI:

- run: `30216901488`;
- head: `9174f5797b5d21bd917497a668f0cd7e1c3e35aa`;
- frontend job: success;
- Rust job: success;
- overall conclusion: success.

## Acceptance basis

The repaired delivery satisfies the clean replacement contract and the R1 repair contract. The shared adapter/view-model boundary is explicit, deterministic, identity-safe, preserves existing route and popup presentation semantics, preserves partial-state behavior, and remains confined to the authorized frontend scope.

No blocking finding remains.