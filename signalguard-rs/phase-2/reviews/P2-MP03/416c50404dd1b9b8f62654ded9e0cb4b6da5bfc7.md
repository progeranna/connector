# P2-MP03 Review — Contaminated branch head

## Verdict

`REJECTED_AND_SUPERSEDED`

## Product evidence

Product repository: `progeranna/signalguard-rs`

Assigned branch: `p2/mp03-market-view-models`

Original exact base: `74dd6e3985649d05e59f1f4cb7f653e405b31cb0`

Original incomplete head: `641078b5bba452a377a5f644a70453b2fdf9c6ad`

Unexpected current head: `416c50404dd1b9b8f62654ded9e0cb4b6da5bfc7`

Comparison from the immutable incomplete head to the unexpected current head proves:

- branch is ahead by 6 additional commits;
- no authorized repair worker had performed its first write before those commits appeared;
- changed paths are:
  - `web/src/features/dashboard/marketAdapters.test.ts`;
  - `web/src/features/dashboard/marketAdapters.ts`;
  - `web/src/features/dashboard/marketViewModel.ts`;
  - `web/src/pages/DashboardPage.tsx`;
  - `web/src/pages/SymbolDetailPage.test.tsx`;
  - `web/src/pages/SymbolDetailPage.tsx`.

## Blocking findings

The final commit `416c50404dd1b9b8f62654ded9e0cb4b6da5bfc7` is not a reviewable P2-MP03 delivery.

Its `DashboardPage.tsx` patch contains unrelated and contract-forbidden presentation changes, including:

- changing user-visible labels such as `Detected` to `Time`;
- removing existing text classes from an anomaly message;
- changing markup outside the narrowly scoped popup migration;
- malformed Tailwind class text split as `text-` / `slate-500`;
- malformed function formatting/signature around `buildVisibleTimelineAnomalies`;
- arbitrary whitespace damage in unrelated dashboard regions.

No focused tests, full frontend gates, exact-head CI, PR, or connector delivery report exist for this head.

The append-only R1 stop condition was correctly triggered when the worker discovered that the branch had moved before its first authorized repair write.

## Disposition

- Do not continue, amend, rebase, reset, force-push, merge, or open a PR from `p2/mp03-market-view-models`.
- Do not cherry-pick or mechanically copy commits from this branch.
- Preserve the branch only as rejected evidence.
- Replace it with `p2/mp03-market-view-models-r2` from current phase SHA `1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`.
- The replacement must be implemented and validated in a complete local Git worktree with one atomic product commit.
