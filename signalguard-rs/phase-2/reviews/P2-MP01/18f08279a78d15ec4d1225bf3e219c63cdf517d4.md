# P2-MP01 Orchestrator Review

## Verdict

`ACCEPT_WITH_NOTES`

The product commit is accepted for integration into `refactor/data-boundaries`.

## Identity

- Contract commit: `c0aa94d7ff9f67a4a4d02a49e18d192bc7b917f6`
- Product base: `5e15a06169445461a9003e17fa1ae5a648d5a1a1`
- Product branch: `p2/mp01-query-key-factories`
- Reviewed product head: `18f08279a78d15ec4d1225bf3e219c63cdf517d4`
- Product PR: `https://github.com/progeranna/signalguard-rs/pull/15`
- Worker report commit: `2548c8ddd5e41f501337a4e70ce763a635113f63`

## Repository and Git validation

- PR state at review: open, unmerged, mergeable, draft.
- PR base: `refactor/data-boundaries`.
- PR base SHA: exact required base `5e15a06169445461a9003e17fa1ae5a648d5a1a1`.
- PR head: exact reported SHA `18f08279a78d15ec4d1225bf3e219c63cdf517d4`.
- Branch comparison: exactly one commit ahead, zero behind.
- Commit message: `refactor(web): centralize market query keys`.
- Changed paths: exactly the three primary allowed paths.
- No dependency, lockfile, backend, CI, route, styling, script, or documentation changes.

## Changed paths

- `web/src/features/dashboard/api.ts`
- `web/src/features/dashboard/queryKeys.ts`
- `web/src/features/dashboard/queryKeys.test.ts`

## Semantic review

Accepted behavior:

- dashboard summary identity includes mode and remains mode-wide;
- market timeline identity includes mode and canonical symbol;
- invalid or absent symbols retain the disabled `null` identity;
- runtime mode identity remains separate from market resources;
- query keys are readonly, serializable tuples;
- `api.ts` compatibility exports are preserved;
- existing endpoint, fetch, AbortSignal, enabled-state, and refresh behavior remain unchanged;
- no presentation state is introduced into server-resource identity;
- P2-MP02 was not started.

The implementation uses the existing `parseSymbolId` boundary, whose normalization trims, uppercases, and rejects non-alphanumeric symbols. Hook usage in `api.ts` consumes the centralized factories without changing query functions or policies.

## Test and CI validation

GitHub Actions run `30200943210` completed successfully for the reviewed PR tree.

Frontend job `89790651859` passed:

- dependency installation;
- full frontend tests;
- typecheck;
- lint;
- production build;
- bundle budget.

Rust job `89790651867` passed:

- formatting;
- cargo check;
- clippy with warnings denied;
- tests;
- replay target discovery;
- Docker Compose validation;
- script syntax validation.

The focused query-key Vitest command was not executed as a separate local invocation. This is a non-blocking evidence limitation because `queryKeys.test.ts` was included in and passed the full repository frontend CI suite. `git diff --check`, isolated TypeScript validation, and supplementary runtime identity assertions were reported as passed.

## Scope and risk assessment

- Correctness blocker: none found.
- Architecture blocker: none found.
- Scope violation: none found.
- Compatibility regression: none found.
- Integration conflict risk: low; Wave 1 backend task owns disjoint paths.

## Integration authorization

Integrate only if PR #15 still points to exact head SHA:

`18f08279a78d15ec4d1225bf3e219c63cdf517d4`

Use squash merge into `refactor/data-boundaries`.
