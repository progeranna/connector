# P2-MP05-R1 Repair Contract — Formatting and Executable Redis Proof

## Role

You are the repair worker for the rejected P2-MP05 product head.

Perform only this repair. Do not start P2-MP06 or any other Phase 2 work.

## Immutable context

Original contract commit:

`c0aa94d7ff9f67a4a4d02a49e18d192bc7b917f6`

Original contract:

`signalguard-rs/phase-2/prompts/P2-MP05.md`

Rejected review:

`signalguard-rs/phase-2/reviews/P2-MP05/6f4ec2c757dc05b208f11b41cd218edb8a6aa4ce.md`

Product repository:

`progeranna/signalguard-rs`

Product branch:

`p2/mp05-atomic-redis-state`

Current rejected product head:

`6f4ec2c757dc05b208f11b41cd218edb8a6aa4ce`

Open PR:

`https://github.com/progeranna/signalguard-rs/pull/16`

Current phase branch head after parallel MP01 integration:

`ce2ee582a370cce8bf8198d1fbb82fcb961867c3`

Failed CI run and job:

- run: `30201414838`
- Rust job: `89791871372`
- failed step: `cargo fmt --all --check`

Control-repository Redis proof workflow:

`.github/workflows/signalguard-redis-proof.yml`

## Start conditions

Before modifying anything:

1. Read this repair contract completely from the exact connector commit supplied by the launcher.
2. Read the original P2-MP05 contract and rejected review.
3. Verify PR #16 is still open and unmerged.
4. Verify `p2/mp05-atomic-redis-state` still points exactly to `6f4ec2c757dc05b208f11b41cd218edb8a6aa4ce`.
5. Verify the branch contains exactly the rejected one-commit implementation over product base `5e15a06169445461a9003e17fa1ae5a648d5a1a1`.
6. Verify no later unreported product or connector repair files exist.
7. Stop on any mismatch.

Do not rebase, merge the phase branch into the task branch, force-push, amend, or rewrite the rejected commit.

## Authorized repair scope

This repair explicitly authorizes one additional product commit on the same task branch. This is a repair exception to the original one-product-commit rule.

Allowed product path:

- `src/storage/redis.rs`

No other product path may change.

Do not change:

- Lua atomicity semantics unless compilation proves a narrowly necessary correction;
- Redis key names;
- JSON serialization;
- public cache API or `CacheError` variants;
- startup/reset behavior;
- dependencies or lockfile;
- frontend;
- CI files in `signalguard-rs`;
- P2-MP06 code.

## Required product repair

1. Materialize the exact product branch checkout when the environment supports it.
2. Run `cargo fmt --all`.
3. Confirm rustfmt changes only `src/storage/redis.rs` and are formatting-only.
4. When a local checkout/toolchain is unavailable, inspect the exact formatting diff from failed GitHub Actions job `89791871372`, apply the canonical rustfmt result, and use the next PR CI run as authoritative verification.
5. Run `git diff --check` when a checkout is available.
6. Create one additional product commit:

   `style(cache): apply rustfmt to atomic Redis change`

7. Push only `p2/mp05-atomic-redis-state` without force.
8. Do not close or recreate PR #16. The existing PR must update to the repaired head.

## Required product CI

Wait for the GitHub Actions PR CI associated with the exact repaired head.

All steps must pass, including:

- formatting;
- cargo check;
- clippy with warnings denied;
- ordinary Rust tests;
- replay target discovery;
- Docker Compose validation;
- frontend CI.

Do not proceed to delivery when product PR CI is pending, cancelled, or failed.

## Required real-Redis proof

After product PR CI succeeds for the exact repaired product head, create exactly one proof request in `progeranna/connector`:

`signalguard-rs/phase-2/proof-requests/P2-MP05/<REPAIRED-PRODUCT-HEAD-SHA>.json`

Required JSON shape:

```json
{
  "mini_phase": "P2-MP05",
  "product_repository": "progeranna/signalguard-rs",
  "product_sha": "<REPAIRED-PRODUCT-HEAD-SHA>",
  "product_pr": "https://github.com/progeranna/signalguard-rs/pull/16",
  "purpose": "Execute full Rust and real-Redis proof for repaired atomic market-state registration"
}
```

Commit only that unique request file to connector. Do not edit the workflow, control/status files, prompts, templates, prior reports, reviews, or another worker's files.

The request automatically triggers `SignalGuard Redis Proof`. Wait for completion and record the run ID and URL.

Every workflow step must pass, especially:

- `cargo fmt --all --check`;
- `cargo check --all-targets --all-features`;
- `cargo clippy --all-targets --all-features -- -D warnings`;
- `cargo test --all-targets --all-features`;
- `cargo test --test redis_cache -- --ignored --test-threads=1` against real Redis;
- `cargo test --lib atomic_market_state_write -- --ignored --test-threads=1` against real Redis;
- exact checked commit confirmation.

A product PR CI success without the connector Redis-proof success is insufficient for acceptance.

## Repaired delivery report

After both product PR CI and connector Redis proof succeed, create a new report:

`signalguard-rs/phase-2/reports/P2-MP05/<REPAIRED-PRODUCT-HEAD-SHA>.md`

Do not modify or delete the rejected report keyed by `6f4ec2c757dc05b208f11b41cd218edb8a6aa4ce`.

The new report must include:

- this repair-contract connector commit SHA;
- original rejected head and repaired head;
- exact two product commits over the original product base;
- the formatting-only repair diff;
- unchanged atomic mechanism;
- product PR CI run ID, jobs, and conclusions;
- connector proof-request path and commit SHA;
- connector Redis-proof run ID, URL, jobs, and conclusions;
- exact real-Redis test commands and results;
- changed paths and scope confirmation;
- confirmation of no force-push, rebase, merge, or P2-MP06 work;
- final PR state open and unmerged.

Commit only the unique new report path to connector.

## Stop conditions

Stop and report without further product changes when:

- the product branch or PR head moved unexpectedly;
- rustfmt requires a non-formatting product change;
- any file other than `src/storage/redis.rs` changes in the product repository;
- product PR CI fails after repair;
- the connector Redis-proof workflow fails;
- a dependency, public API, key namespace, startup/reset, or adjacent-phase change appears necessary;
- exact SHA evidence cannot be established.

Do not merge PR #16.

## Final response

Return only:

- repaired product head SHA;
- product repair commit SHA and commit message;
- PR URL;
- product PR CI run ID and conclusion;
- connector proof-request path and commit SHA;
- connector Redis-proof run ID, URL, and conclusion;
- repaired delivery-report path and connector commit SHA;
- concise gate summary;
- any blocker.