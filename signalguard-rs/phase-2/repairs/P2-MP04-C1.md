# SignalGuard RS P2-MP04-C1 — Continue API Contract Work from Current Phase

## Purpose

Continue `P2-MP04` with local Codex from the current Phase 2 tree after integration of P2-MP06.

This contract supersedes only the original base/start conditions and the obsolete concurrent MP06 lease language in `signalguard-rs/phase-2/prompts/P2-MP04.md`. Every product requirement, forbidden path, test requirement, delivery rule, and stop condition from the original contract remains binding unless explicitly changed here.

## Immutable product state

Product repository:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p2-mp04`

Assigned branch:

`p2/mp04-api-contract`

Parent phase branch:

`refactor/data-boundaries`

Exact current phase SHA and required initial local/remote branch head:

`1a226b5b5661a2b99dabec579aeeecf8b0e4be85`

Required product commit message:

`feat(api): publish validated web console contract`

Required PR base:

`refactor/data-boundaries`

## Start and recovery rules

The remote MP04 branch was empty and was fast-forwarded from the old base to the exact current phase SHA above. It contains no MP04 implementation commit.

Before any edit:

1. Fetch all product refs.
2. Verify `origin/refactor/data-boundaries` equals the exact current phase SHA.
3. Verify `origin/p2/mp04-api-contract` equals the exact current phase SHA.
4. Inspect the dedicated worktree if it exists.
5. If the worktree contains any uncommitted or staged file, stop without modifying, resetting, stashing, cleaning, or deleting it. Return the exact status and changed paths for recovery review.
6. If the worktree is absent, create it from `origin/p2/mp04-api-contract`.
7. If the worktree exists and is clean but its local branch is on the previous base, fast-forward it normally to `origin/p2/mp04-api-contract`. Do not reset or force anything.
8. Require a clean worktree at exact SHA `1a226b5b5661a2b99dabec579aeeecf8b0e4be85` before editing.

## Current source of truth

P2-MP06 is integrated. Read the current exact phase versions of handlers, DTOs, routes, frontend network types, and CI when inventorying the API.

The obsolete instruction not to read the MP06 branch is removed because MP06 is now part of the phase tree. Do not read or copy any superseded local/web-worker execution source.

The following product paths remain strictly forbidden because MP04 is a contract/drift task and must not change runtime cache or handler behavior:

- `src/api/handlers.rs`
- `src/storage/redis.rs`
- `tests/redis_cache.rs`
- `web/src/features/dashboard/types.ts`
- frontend query implementation or query keys
- view models or presentation components
- Docker/deployment/runtime behavior

Reading `src/api/handlers.rs` to inventory the actual API is required; modifying it is forbidden.

## Required result

Implement the complete original P2-MP04 contract from the exact current phase tree:

- one deterministic checked OpenAPI 3.x contract, or an equivalently precise machine-readable contract only if OpenAPI is technically unsuitable;
- typed backend contract definitions as the generation source;
- complete current JSON web-console endpoint inventory and actual parameter names;
- accurate optional versus nullable semantics;
- accurate decimal-string, UUID, date-time, enum, array, nested-object, and identity representations;
- explicit `/metrics` Prometheus-text disposition;
- deterministic write and no-write check commands;
- executable backend determinism, drift, schema, and route-inventory tests;
- executable frontend compatibility tests reading the checked artifact;
- a CI drift gate without weakening existing Rust or frontend gates;
- no runtime endpoint, serialization, status-code, frontend query, or presentation change.

The generated contract must reflect the integrated P2-MP06 dashboard behavior without adding or changing any public endpoint or response field.

## Commit discipline

Create exactly one product commit after every required gate passes:

`feat(api): publish validated web console contract`

Do not amend, rebase, reset, merge, or force-push.

Push only:

`p2/mp04-api-contract`

Open one draft PR into:

`refactor/data-boundaries`

PR title:

`feat(api): publish validated web console contract`

## Required gates

Run every gate in the original P2-MP04 contract, including:

- contract write command;
- deterministic contract check twice;
- proof that check mode leaves tracked files unchanged;
- focused backend contract and route-inventory tests;
- `cargo fmt --all` and `cargo fmt --all --check`;
- `cargo check --all-targets --all-features`;
- `cargo clippy --all-targets --all-features -- -D warnings`;
- `cargo test --all-targets --all-features`;
- `git diff --check`;
- `npm ci`;
- focused frontend contract compatibility tests;
- full frontend tests, typecheck, lint, build, and bundle check;
- exact changed-path and repository-hygiene inventory;
- proof that `src/api/handlers.rs`, `src/storage/redis.rs`, and `tests/redis_cache.rs` are unchanged against the exact base;
- proof that no runtime endpoint or user-visible behavior changed.

Stop without commit or push when any required gate fails or is skipped.

## Report

After green exact-head product CI, create:

`signalguard-rs/phase-2/reports/P2-MP04/<PRODUCT-HEAD-SHA>.md`

The report must cite both:

- `signalguard-rs/phase-2/prompts/P2-MP04.md`;
- `signalguard-rs/phase-2/repairs/P2-MP04-C1.md`.

It must include the exact current base, exact changed paths, generator/library decision, artifact path and version, endpoint inventory, `/metrics` disposition, write/check commands, backend/frontend proof, all local gates, exact-head CI, and forbidden-path integrity evidence.

Do not modify connector status, control, prompts, repairs, reviews, integration records, proof records, or another task report.

## Final response

Return only:

- repository path;
- worktree path;
- branch;
- verified exact base SHA;
- product commit SHA;
- draft PR URL;
- exact changed paths;
- contract library and rationale;
- typed source module(s);
- artifact path and OpenAPI/contract version;
- complete endpoint inventory and `/metrics` disposition;
- deterministic write command;
- deterministic check command;
- backend contract/route test summary;
- frontend compatibility test summary;
- full Rust gate summary;
- full frontend gate summary;
- forbidden-path integrity proof;
- exact-head CI run ID and conclusion;
- connector report path and commit SHA;
- any blocker.
