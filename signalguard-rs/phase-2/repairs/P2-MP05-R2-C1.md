# P2-MP05-R2-C1 Continuation Contract — Commit After Clean Reverification

## Purpose

This continuation contract authorizes the existing local Codex task to continue from the already prepared `P2-MP05-R2` worktree after a non-product sandbox-permission failure.

The earlier local apply contract correctly stopped commit and push because an initial required Redis command failed. No product commit, push, PR, connector proof request, or delivery report was created.

Subsequent execution in the same worktree reportedly completed all Rust gates and both real-Redis suites successfully. This contract does not accept that report as final evidence by itself. It authorizes one clean reverification cycle followed by commit and push.

This contract supersedes only the prior rule that permanently prohibited commit after the initial sandbox-permission failure. Every other restriction in `P2-MP05-R2-LOCAL-APPLY.md` remains in force.

## Immutable context

Product repository:

`progeranna/signalguard-rs`

Primary local repository:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p2-mp05-r2`

Assigned branch:

`p2/mp05-atomic-redis-state-r2`

Exact base and current remote branch SHA:

`ce2ee582a370cce8bf8198d1fbb82fcb961867c3`

Verified remote state at contract release:

- ahead by 0 commits;
- behind by 0 commits;
- identical to the exact phase base.

Allowed product path:

`src/storage/redis.rs`

Verified candidate source:

`signalguard-rs/phase-2/candidate-results/P2-MP05/03fa30b-ci2/src/storage/redis.rs`

Required SHA-256 for the local candidate file:

`f8ed711ae4421efadb1d9c6ad520862b58f44ae67ee49130bafb60e82f6777be`

Required product commit message:

`fix(cache): write market state atomically`

Required PR base:

`refactor/data-boundaries`

## Mandatory pre-commit identity checks

Before running final gates:

1. Enter the dedicated worktree, not the primary checkout.
2. Fetch product remote references.
3. Confirm the current branch is exactly `p2/mp05-atomic-redis-state-r2`.
4. Confirm local `HEAD` is exactly `ce2ee582a370cce8bf8198d1fbb82fcb961867c3`.
5. Confirm `origin/p2/mp05-atomic-redis-state-r2` still points to the same SHA.
6. Confirm the branch is ahead by 0 and behind by 0 before the commit.
7. Confirm `git status --short` contains only the intended modification to `src/storage/redis.rs`.
8. Confirm there are no untracked files, staged files, or changes in any other path.
9. Compute SHA-256 of local `src/storage/redis.rs` and require the exact candidate checksum above.
10. Run `git diff --check` and require success.

Stop without commit or push on any mismatch.

## Mandatory clean reverification cycle

After the identity checks, rerun every required gate from the original local apply contract in the current successful execution environment.

At minimum, all of the following must pass in this new continuation run:

1. `cargo fmt --all --check`
2. `cargo check --all-targets --all-features`
3. `cargo clippy --all-targets --all-features -- -D warnings`
4. `cargo test --all-targets --all-features`
5. the complete existing ignored Redis integration suite against isolated Redis 7, with all 7 tests passing;
6. the ignored `atomic_market_state_write` proof suite against isolated Redis 7, with all 3 tests passing;
7. `git diff --check` after all commands;
8. final repository hygiene and exact changed-path check.

Using the same permission-escalated or host execution method that produced the successful 7/7 and 3/3 Redis results is authorized when required by the local sandbox. The Redis instance must remain isolated and disposable.

A prior successful run before reading this continuation contract is supporting evidence only and is not a substitute for this new clean reverification cycle.

Stop without commit or push if any command fails, is skipped, or cannot establish exact evidence.

## Authorized Git delivery

Only after every continuation gate passes:

1. Stage exactly `src/storage/redis.rs`.
2. Verify the staged path list contains exactly that one file.
3. Create exactly one product commit with message:

   `fix(cache): write market state atomically`

4. Confirm the new commit has parent `ce2ee582a370cce8bf8198d1fbb82fcb961867c3`.
5. Push only `p2/mp05-atomic-redis-state-r2`.
6. Do not force-push, amend, rebase, merge, or modify another branch.
7. Opening a PR into `refactor/data-boundaries` is optional. If `gh` is unavailable, stop after a successful push; the Orchestrator will open it.

Do not create a connector proof request or delivery report. The Orchestrator will perform product-head validation, open the PR when needed, trigger the exact-head Redis proof, review, and integration.

## Forbidden actions

Do not:

- change any file other than `src/storage/redis.rs`;
- alter the verified candidate implementation;
- continue the superseded `p2/mp05-atomic-redis-state` branch or PR #16;
- read from or modify `p2/mp02-symbol-data-queries`;
- change dependencies, lockfiles, CI, frontend, Redis key names, JSON format, public API, startup/reset behavior, or P2-MP06 code;
- claim PASS for a gate that was not executed in the continuation run.

## Final response

Return only:

- repository path;
- worktree path;
- branch;
- verified base SHA;
- verified candidate SHA-256;
- resulting product commit SHA;
- exact changed paths;
- continuation Rust gate summary;
- continuation real-Redis gate summary;
- push result;
- PR URL if opened;
- any blocker.
