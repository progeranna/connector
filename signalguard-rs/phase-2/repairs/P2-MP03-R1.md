# P2-MP03-R1 Repair Contract — Complete Market View-Model Adoption

## Role

You are the repair worker for `P2-MP03-R1` only.

This contract repairs an incomplete append-only delivery. It does not authorize P2-MP04, P2-MP06, P2-MP07, backend work, dependency changes, or visual redesign.

## Immutable identity

Product repository:

`progeranna/signalguard-rs`

Control repository:

`progeranna/connector`

Assigned product branch:

`p2/mp03-market-view-models`

Original exact product base:

`74dd6e3985649d05e59f1f4cb7f653e405b31cb0`

Incomplete immutable product head:

`641078b5bba452a377a5f644a70453b2fdf9c6ad`

Current parent phase branch at repair release:

`refactor/data-boundaries`

Current phase SHA at repair release:

`1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`

Original contract:

`signalguard-rs/phase-2/prompts/P2-MP03.md`

Incident review:

`signalguard-rs/phase-2/reviews/P2-MP03/641078b5bba452a377a5f644a70453b2fdf9c6ad.md`

## Verified starting state

The incomplete branch contains exactly one commit over its original assigned base and adds only:

`web/src/features/dashboard/marketViewModel.ts`

The published file defines readonly UI-facing market-detail types but does not include adapters, route/popup migration, focused tests, PR, report, or CI evidence.

The task branch and current phase branch have diverged only because P2-MP05 was integrated into the phase branch after P2-MP03 started. P2-MP05 changed backend Redis code only. Do not merge, rebase, cherry-pick, or otherwise import that commit into the task branch. The Orchestrator will validate merge compatibility against the current phase branch.

## Superseded delivery rule

The original requirement for exactly one total product commit is superseded only as follows:

- the incomplete commit must remain unchanged;
- amend, rebase, reset, history rewrite, branch recreation, and force-push remain forbidden;
- a finite append-only completion sequence is authorized on the same branch;
- at most 10 additional product commits may be created;
- every additional commit must be a fast-forward descendant of `641078b5bba452a377a5f644a70453b2fdf9c6ad`;
- each contents-API commit must change exactly one authorized path and use a descriptive message;
- no unrelated cleanup or opportunistic edits are allowed;
- the accepted PR will be squash-merged by the Orchestrator, so the integrated phase history will still contain one P2-MP03 commit.

Before the first product write, prepare the complete intended final path inventory and final contents for every changed file. Do not discover the implementation incrementally by committing speculative files.

## Product-write method

A normal complete Git checkout and one conventional completion commit are preferred when available.

When only GitHub connector contents operations are available:

- contents writes are permitted under this repair because they create one commit per path;
- keep the sequence append-only and bounded by the limit above;
- do not modify a path more than twice;
- use exact current blob SHAs for updates;
- verify the branch head after every write;
- stop immediately if the branch head moves unexpectedly.

## Required final implementation

Complete the original P2-MP03 goal without changing its architecture or semantics:

1. Keep `marketViewModel.ts` as the explicit readonly UI-facing contract, revising it only when required for correctness.
2. Add a narrowly scoped pure adapter module such as `marketAdapters.ts`.
3. Convert the existing P2-MP02 successful market resource/DTO boundary into `MarketDetailViewModel`.
4. Preserve canonical `{mode, symbol}` identity.
5. Reject or return an explicit non-success result for any resource whose state, health, timeline, or anomaly identity does not match the requested symbol/mode.
6. Move repeated display formatting, status text/tone derivation, missing-value representation, and anomaly-row mapping out of route/popup presentation.
7. Make the dedicated symbol route and popup consume the same view-model contract.
8. Preserve loading, error, unavailable, empty-anomaly, and success shells.
9. Preserve Live dedicated state/health/filtered-anomaly ownership.
10. Preserve Demo summary-backed state/health and Demo timeline anomaly ownership.
11. Preserve selected-symbol persistence, routes, popup return context, query behavior, refresh behavior, layout, CSS classes, accessibility, and user-visible copy.
12. Do not introduce P2-MP07 source/availability semantics.

## Required focused tests

The final repaired head must include executable tests proving at least:

- full Live mapping;
- full Demo mapping without Live fallback;
- null optional values remain the existing missing-value display and never become zero;
- healthy/degraded/unhealthy text and tone mapping;
- explicit empty-anomaly view;
- exact anomaly identity and symbol preservation;
- BTCUSDT cannot expose ETHUSDT state, health, timeline, or anomaly values;
- Demo and Live models remain distinct for the same symbol;
- route and popup consume structurally equal view models for the same successful identity;
- existing loading/error/unavailable/success behavior remains;
- deterministic symbol-switch and mode-switch isolation remains;
- an architecture guard prevents migrated presentation from directly joining/formatting raw DTO values.

Do not delete, skip, weaken, replace, or convert existing tests to TODO merely to obtain a green result.

## Authorized paths

The final branch diff relative to the original assigned base may change only paths already authorized by the original P2-MP03 contract:

- `web/src/features/dashboard/marketViewModel.ts`;
- narrowly scoped adapter/view-model modules and tests under `web/src/features/dashboard/**`;
- `web/src/features/dashboard/symbolMarketResource.ts` only as needed without query changes;
- `web/src/features/dashboard/symbolPopupResource.ts` and its tests;
- `web/src/pages/SymbolDetailPage.tsx` and its tests;
- exact existing popup presentation consumer files under `web/src/**` that render the successful P2-MP02 resource;
- source-level architecture guard tests under `web/src/**`.

Every changed path must be justified in the final report.

## Forbidden paths and changes

Do not change:

- `web/src/features/dashboard/api.ts`;
- `web/src/features/dashboard/queryKeys.ts`;
- `web/src/features/dashboard/queryKeys.test.ts`;
- `web/src/features/dashboard/types.ts`;
- package manifests or lockfiles;
- frontend tool configuration;
- CI workflows;
- Rust/backend files;
- Redis/PostgreSQL code;
- endpoints, DTO schemas, query parameters, or refresh policies;
- route definitions or route names;
- browser-storage behavior;
- P2-MP04, P2-MP06, or P2-MP07 work.

## Required validation

When a complete local checkout is available, run from `web/`:

1. `npm ci`
2. focused adapter/view-model and changed-consumer tests
3. `npm run test:run`
4. `npm run typecheck`
5. `npm run lint`
6. `npm run build`
7. `npm run bundle:check`
8. `git diff --check`
9. repository status and hygiene checks

When the worker environment cannot run repository-native commands, record each unavailable command accurately as `NOT_RUN` or `DEFERRED`. Do not claim local PASS.

Exact-head GitHub CI must complete successfully before delivery can be accepted.

## PR delivery

After the append-only completion sequence is finished:

- verify the final branch contains no more than 11 total commits over the original base: the one incomplete commit plus at most 10 repair commits;
- verify all paths are authorized;
- open one draft PR from `p2/mp03-market-view-models` into `refactor/data-boundaries`;
- use PR title `refactor(web): adapt market DTOs into view models`;
- do not merge;
- do not rebase onto the moved phase branch;
- wait for exact-head CI;
- stop and report if CI fails.

## Connector report

After exact-head CI succeeds, create exactly one report:

`signalguard-rs/phase-2/reports/P2-MP03/<FINAL-PRODUCT-HEAD-SHA>.md`

The report must include:

- original and repair contract connector SHAs;
- original base, incomplete head, and final repaired head;
- complete append-only commit list;
- PR URL/base/state;
- changed/created/deleted paths;
- DTO/resource types consumed;
- view-model types and adapter functions introduced;
- route and popup consumers migrated;
- formatting/status derivation moved out of presentation;
- identity and missing-data invariants with test names;
- exact local commands/results or accurate `NOT_RUN` reasons;
- exact-head CI run ID and result;
- compatibility, scope, and hygiene confirmation.

Do not edit connector control, status, prompt, review, repair, diagnostic, proof, integration, or another worker's report paths.

## Stop conditions

Stop without further product writes when:

- the product branch no longer points to the expected latest repair descendant;
- more than 10 additional commits would be needed;
- a path outside the authorized lease is required;
- a forbidden network, DTO, query, dependency, CI, backend, route, persistence, or P2-MP07 change is required;
- exact-head CI fails;
- route/popup parity or identity isolation cannot be preserved.

## Final response

Return only:

- product branch;
- original base SHA;
- incomplete head SHA;
- final repaired product head SHA;
- append-only repair commit list;
- product PR URL;
- exact changed paths;
- view-model and adapter modules introduced;
- route/popup consumers migrated;
- focused/full gate summary;
- exact-head CI run ID and conclusion;
- connector report path and commit SHA;
- any blocker.
