# P2-MP07-R1 Repair Contract — Complete Availability Semantics

## Role

You are the local repair executor for SignalGuard RS mini-phase `P2-MP07-R1`.

Repair the existing P2-MP07 candidate in place. Preserve its accepted backend/source/availability foundations while closing the exact independent-review findings.

## Immutable identity

Product repository:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p2-mp07`

Assigned branch:

`p2/mp07-market-availability`

Parent and future PR base:

`refactor/data-boundaries`

Assigned phase base:

`856b58177d30772bc691ec870fe537c05aab3dba`

Immutable rejected starting head:

`9ed0d1e12fc385b19aabe0cbf32b0d4b726dd8fb`

Required repair commit message:

`fix(ui): honor market availability states`

The final branch must contain exactly two normal commits above the assigned phase base.

## Prohibited history operations

Do not:

- amend either commit;
- reset or rebase;
- squash locally;
- force-push;
- replace the branch or worktree;
- create or modify a product PR;
- merge;
- modify the connector repository.

The Orchestrator owns independent review, PR creation, visual acceptance, and integration.

## Preflight

Before editing:

1. Fetch `origin/refactor/data-boundaries` and `origin/p2/mp07-market-availability`.
2. Confirm `origin/refactor/data-boundaries` is `856b58177d30772bc691ec870fe537c05aab3dba`.
3. Confirm the local and remote P2-MP07 branch both point to `9ed0d1e12fc385b19aabe0cbf32b0d4b726dd8fb`.
4. Confirm branch comparison against `origin/refactor/data-boundaries` is ahead `1`, behind `0`.
5. Confirm the worktree is clean and nothing is staged.
6. Read the immutable original contract and independent review:
   - `signalguard-rs/phase-2/prompts/P2-MP07.md` at connector commit `2701189895eaf4748f47190be29e618732143c93`;
   - `signalguard-rs/phase-2/reviews/P2-MP07/9ed0d1e12fc385b19aabe0cbf32b0d4b726dd8fb.md`.

Stop before editing if identity or cleanliness differs.

## Repair requirements

### 1. Complete the frontend OpenAPI compatibility gate

Modify `web/src/features/dashboard/api-contract.test.ts` so its reusable validator checks the generated artifact for all P2-MP07 fields:

- required non-null `source` on:
  - `DashboardSummaryResponse`;
  - `DashboardSymbolSummary`;
  - `MarketStateResponse`;
  - `MarketHealthResponse`;
  - `MarketTimelineResponse`;
  - `AnomaliesResponse`;
  - `SymbolsResponse`;
- required non-null `availability` on:
  - `DashboardSymbolSummary`;
  - `MarketStateResponse`;
  - `MarketHealthResponse`;
- exact `PublicDataMode = ["demo", "live"]`;
- exact `MarketAvailability = ["observed", "configured", "awaiting", "unavailable"]`;
- the required fields reference the correct schemas;
- the previous `catalogAvailability` representation is absent.

Add independent mutation fixtures that fail when:

- a required `source` field is removed;
- a required `availability` field is removed;
- either field is made optional or nullable;
- either enum is changed;
- a source/availability schema reference is replaced incorrectly;
- `catalogAvailability` is introduced in place of `availability`.

Preserve every existing P2-MP04 extractor/handler error compatibility assertion.

### 2. Make header status availability-aware

Update `web/src/app/AppShell.tsx` so a healthy pipeline cannot produce `Data Healthy` when there are zero `observed` symbols.

Required behavior:

- if at least one observed symbol exists, preserve current health/anomaly precedence;
- if every symbol is non-observed, use a neutral status derived from explicit availability;
- do not infer observation from `state !== null` alone;
- do not alter header layout, menus, navigation, focus behavior, or responsive styling.

Add or update an authorized `web/src/app/*test.tsx` test proving all-non-observed catalogs do not report healthy observed data.

### 3. Prevent dashboard chart and metric leakage for non-observed symbols

Update `web/src/pages/DashboardPage.tsx` and directly related authorized tests so:

- Live timeline requests are enabled only when the selected symbol has `availability === "observed"`;
- configured/awaiting/unavailable symbols never render historical chart points or timeline anomalies;
- configured/awaiting/unavailable symbols do not render price, spread, trades/min, freshness, health score, or anomaly count rows;
- pipeline age is never presented as selected-symbol freshness for a non-observed symbol;
- the exact required empty-state copy is used:
  - configured: `Configured for Live; Live ingestion is not active.`
  - awaiting: `Awaiting first Live market data.`
  - unavailable: `Live market data is unavailable.`
- observed Demo and Live behavior remains unchanged;
- source badge and existing selected-market/status surface remain additive and visually narrow.

### 4. Prevent route and popup placeholder metrics for non-observed symbols

Update the shared resource/adapter/view-model boundary plus route/popup presentation so:

- `anomalyCount` is not `0` for a non-observed resource; use unavailable representation or do not render it;
- metric strip and market metric rows are not rendered for configured/awaiting/unavailable symbols;
- generic `No recent anomalies` is not used as evidence of zero anomalies for non-observed symbols;
- route and popup use the same explicit availability-specific copy;
- route and popup use explicit source identity and never default an absent Live view model to a Demo badge;
- observed resources retain current metrics, chart, health, and anomalies;
- unknown symbols may remain a distinct not-found resource state, but known configured/awaiting/unavailable symbols must not collapse into a generic 404 presentation.

### 5. Preserve backend behavior

Do not change accepted backend semantics unless a focused test-only correction is necessary.

Preserve:

- exact source/availability enum and derivation matrix;
- configured+registered Live union;
- canonical deduplication and lexicographic order;
- one `list_symbols` read and exactly one bulk `get_market_states` call;
- no per-symbol awaited cache state read;
- no Demo fallback into Live;
- existing endpoint/error behavior;
- P2-MP04 OpenAPI error model;
- P2-MP05 atomic Redis write;
- P2-MP06 bulk Redis behavior.

## Repair path lease

The original P2-MP07 production path lease remains authoritative.

For this repair, the following exact test/support paths are additionally authorized:

- `web/src/features/dashboard/api-contract.test.ts`;
- `web/src/pages/DashboardPage.popup.test.tsx`;
- `web/src/test/marketFixtures.ts`.

The latter two additions explicitly regularize the paths already modified in the rejected first commit. No other path expansion is authorized.

Forbidden paths from the original contract remain forbidden, including Cargo files, CI, routes, API errors, runtime, Redis implementation/tests, query-key factories, selected-symbol persistence, popup identity, Docker, deployment, and dependencies.

## Required tests

Add or update executable tests proving:

1. frontend OpenAPI validator and mutation fixtures cover every required source/availability field and enum;
2. all-non-observed header state is neutral, not healthy;
3. configured dashboard symbol does not issue/render timeline data and uses exact configured copy;
4. awaiting dashboard symbol does not issue/render timeline data and uses exact awaiting copy;
5. unavailable dashboard symbol does not issue/render timeline data and uses exact unavailable copy;
6. no non-observed dashboard surface shows symbol freshness from pipeline age;
7. no non-observed route or popup shows metric values, health score, anomaly count `0`, chart data, or generic zero-anomaly copy;
8. route and popup source/availability labels and empty-state text match;
9. observed Demo and Live metrics/charts/anomalies remain unchanged;
10. all existing isolation and late-response tests remain green.

Use controlled promises for races; do not add sleep-based tests.

## Verification

Run all original P2-MP07 verification commands, including:

- `cargo fmt --check`;
- OpenAPI `--validate` and `--check`;
- `cargo check`;
- clippy with warnings denied;
- all-target/all-feature Rust tests;
- `git diff --check`;
- `npm ci`;
- focused contract/header/dashboard/route/popup/resource tests;
- full frontend test suite;
- typecheck;
- lint;
- build;
- bundle check;
- exporter write then immediate check;
- final changed-path and forbidden-path proof against `856b58177d30772bc691ec870fe537c05aab3dba`.

If local Docker smoke is available, test Demo plus controlled Live observed/configured/awaiting/unavailable states. Otherwise report the exact environment limitation.

## Commit and delivery

After all required gates pass:

1. Stage only authorized paths.
2. Create exactly one additional commit:

   `fix(ui): honor market availability states`

3. Confirm comparison against `origin/refactor/data-boundaries` is ahead `2`, behind `0`.
4. Push normally to `origin/p2/mp07-market-availability`.
5. Do not create a PR.
6. Do not merge.

## Required report

Return:

- exact starting and final SHAs;
- exact two-commit branch history;
- exact changed paths against the phase base;
- exact R1 delta paths against `9ed0d1e12fc385b19aabe0cbf32b0d4b726dd8fb`;
- forbidden-path proof;
- OpenAPI validator/mutation evidence;
- header zero-observed evidence;
- dashboard non-observed no-query/no-render evidence;
- route/popup no-metric evidence;
- full gate counts;
- manual smoke result or environment blocker;
- pushed remote SHA;
- blockers, if any.
