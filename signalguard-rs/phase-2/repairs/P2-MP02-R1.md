# P2-MP02-R1 Repair Contract — Align Legacy Isolation Tests

## Role

You are the repair worker for rejected mini-phase `P2-MP02`.

Perform only this repair. Do not start P2-MP03, P2-MP04, P2-MP07, or any backend work.

## Immutable context

Product repository:

`progeranna/signalguard-rs`

Product branch:

`p2/mp02-symbol-data-queries`

Exact rejected head:

`a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c`

Product PR:

`https://github.com/progeranna/signalguard-rs/pull/17`

Product base:

`ce2ee582a370cce8bf8198d1fbb82fcb961867c3`

Original contract:

`signalguard-rs/phase-2/prompts/P2-MP02.md`

Rejected review:

`signalguard-rs/phase-2/reviews/P2-MP02/a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c.md`

Diagnostic evidence:

- workflow run `30204380568`;
- `signalguard-rs/phase-2/diagnostic-results/P2-MP02/a83d63b-diag1/result.json`;
- `signalguard-rs/phase-2/diagnostic-results/P2-MP02/a83d63b-diag1/vitest-tail.log`.

## Start conditions

Before editing:

1. Read this repair contract completely from the exact connector commit supplied by the launcher.
2. Read the original P2-MP02 contract, rejected review, and diagnostic tail.
3. Verify PR #17 is open, draft, and unmerged.
4. Verify `p2/mp02-symbol-data-queries` points exactly to `a83d63bbe5e9b0ff1add4e6d2235c00a8732a70c`.
5. Verify the branch contains exactly one product commit over `ce2ee582a370cce8bf8198d1fbb82fcb961867c3`.
6. Stop on any mismatch.

Do not amend, rebase, force-push, recreate the PR, merge another branch, or modify the concurrent MP05 work.

## Authorized repair scope

This repair authorizes exactly one additional product commit.

Allowed product paths:

- `web/src/app/marketNavigationIsolation.test.tsx`
- `web/src/features/dashboard/marketDataIsolation.test.tsx`
- `web/src/features/dashboard/selectedSymbolConsumers.test.ts`

No production source file is authorized.

Do not change:

- API functions or query hooks;
- query keys;
- symbol resource resolvers;
- route or popup production code;
- endpoint families or parameters;
- selected-symbol persistence behavior;
- dependencies or lockfiles;
- global query defaults;
- visual UI;
- Rust/backend/cache code;
- CI files in the product repository.

If a production source change appears necessary, stop and report the exact failing invariant. Do not make that change under R1.

## Required repair

### 1. Migrate legacy request harnesses

Update the existing controlled-fetch and matrix fixtures so Live symbol detail supplies the dedicated existing resources adopted by P2-MP02:

- `GET /market/{symbol}/state`;
- `GET /market/{symbol}/health`;
- `GET /anomalies?symbol={symbol}&limit=50` using the repository's actual query serialization order;
- no broad dashboard summary as the primary Live symbol-detail payload.

Keep dashboard summary mode-wide where it still owns overview/catalog data.

### 2. Preserve Demo ownership explicitly

For Demo symbol detail:

- state/health may continue to come from the Demo dashboard summary where no dedicated Demo state/health endpoint exists;
- symbol anomalies used by the new detail resource come from `GET /market/{symbol}/timeline?mode=demo`;
- legacy expectations must assert the Demo timeline anomaly sentinel rather than a broad-summary anomaly sentinel for the route/popup detail resource;
- no Live fallback is allowed.

### 3. Preserve race and cancellation guarantees

Update controlled request identities/helpers so existing tests still prove:

- BTC → ETH before resolution never renders BTC under ETH;
- Demo → Live detaches/aborts Demo requests;
- late Demo completion does not replace Live data;
- route and popup use the same canonical symbol resource identity;
- loading, unavailable, error, and success states remain explicit.

Do not replace deterministic deferred-promise control with timers or sleeps.

### 4. Preserve the four-cell matrix

The existing Demo/Live × BTCUSDT/ETHUSDT matrix must remain present and strict.

For each cell, continue validating alignment among:

- selected mode;
- selected symbol;
- summary/state values;
- anomaly source/value;
- popup identity;
- route identity where applicable.

Do not delete cells or reduce assertions merely to obtain a green suite.

### 5. Update the brittle persistence source assertion

`SymbolDetailPage` now persists the successfully resolved canonical symbol through:

`storeSelectedSymbol(selectedUiMode, resolvedSymbol)`

Update the source-level consumer test to assert this active-mode-scoped behavior instead of requiring the previous `selectedSummary.symbol` expression.

Keep the other selected-symbol persistence and route-link checks intact.

## Required gates

From `web/`, run when the environment permits:

1. focused tests:
   - `src/app/marketNavigationIsolation.test.tsx`
   - `src/features/dashboard/marketDataIsolation.test.tsx`
   - `src/features/dashboard/selectedSymbolConsumers.test.ts`
2. all changed/new MP02 tests;
3. `npm run test:run`;
4. `npm run typecheck`;
5. `npm run lint`;
6. `npm run build`;
7. `npm run bundle:check`.

Also run:

- `git diff --check`;
- repository status/hygiene checks.

GitHub CI for the exact repaired head must be fully green. A test-only local or isolated syntax check is insufficient.

## Product delivery

After the repair is complete:

1. verify only the three authorized test files changed relative to the rejected head;
2. create exactly one additional commit:

   `test(web): align isolation tests with symbol queries`

3. push only `p2/mp02-symbol-data-queries` without force;
4. keep PR #17 open, draft, and unmerged.

## Repaired report

After green exact-head product CI, create a new report:

`signalguard-rs/phase-2/reports/P2-MP02/<REPAIRED-HEAD-SHA>.md`

Do not modify or delete the rejected report.

The new report must include:

- this repair contract connector commit SHA;
- rejected and repaired product SHAs;
- the exact two product commits over the base;
- changed-file inventory;
- how each of the eight legacy failures was resolved;
- focused and full frontend command results;
- exact GitHub CI run ID and job conclusions;
- confirmation that no production source changed in R1;
- confirmation of no amend, rebase, force-push, merge, or adjacent-phase work.

## Stop conditions

Stop without further product changes when:

- the branch head moved unexpectedly;
- any production source change appears necessary;
- any path outside the three authorized test files changes;
- a required frontend gate remains red;
- exact SHA evidence cannot be established.

## Final response

Return only:

- repaired product head SHA;
- repair commit SHA and message;
- PR URL;
- exact changed paths;
- focused test summary;
- full frontend gate summary;
- product CI run ID and conclusion;
- repaired delivery-report path and connector commit SHA;
- any blocker.
