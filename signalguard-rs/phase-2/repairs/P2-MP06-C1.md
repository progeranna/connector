# P2-MP06-C1 — Continuation after focused test fixture mismatch

## Status

This continuation authorizes resuming the existing local P2-MP06 work after the first focused dashboard test failed before any product commit or push.

It supersedes only the original permanent stop caused by that failed focused gate. All other P2-MP06 requirements remain binding.

## Immutable product state

Product repository:

`progeranna/signalguard-rs`

Assigned branch:

`p2/mp06-bulk-redis-state`

Exact assigned base and current verified remote head:

`1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`

Dedicated local worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p2-mp06`

No product commit, push, PR, connector report, or product CI run exists yet.

Current local changed paths reported by the executor:

- `src/storage/redis.rs`;
- `src/api/handlers.rs`;
- `tests/redis_cache.rs`.

These are exactly the P2-MP06 authorized paths.

## Reported implementation state

Reported bulk cache API:

`get_market_states(&[Symbol]) -> Result<Vec<(Symbol, Option<MarketState>)>, CacheError>`

Reported Redis strategy:

- one Redis `MGET` for all requested market-state keys;
- explicit symbol-to-result association;
- no per-symbol awaited `GET` loop.

Reported focused cache tests: `2/2` passed.

Reported focused dashboard failure:

- one assertion expected decimal string `"1.00"`;
- the constructed value serialized as `"100"`.

The current public DTO behavior serializes `rust_decimal::Decimal` using the decimal value and scale represented by the fixture. Existing product tests demonstrate scale-preserving strings such as `Decimal::new(6505425, 2) -> "65054.25"`.

## Authorized correction

Before changing code, inspect the exact failing assertion and its fixture.

Correct only the test inconsistency:

1. If the intended fixture value is numeric `100`, keep the fixture and expect `"100"`.
2. If the intended fixture value is numeric `1.00`, construct it with scale 2, such as `Decimal::new(100, 2)`, and expect `"1.00"`.
3. Do not change production DTO serialization, response fields, Redis payload encoding, or dashboard formatting merely to satisfy the test.
4. Prefer the option that makes the test intent explicit and consistent with adjacent fixture values.
5. Add a brief test-local comment only if the chosen scale would otherwise be ambiguous.

No unrelated behavior correction is authorized by this continuation.

## Required pre-commit verification

Continue in the existing dedicated worktree and verify:

1. current branch is `p2/mp06-bulk-redis-state`;
2. `HEAD` is still `1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`;
3. remote assigned branch is still the same exact SHA;
4. only the three authorized paths are modified;
5. no staged files, untracked generated artifacts, dependency changes, CI changes, or frontend changes exist;
6. the P2-MP05 atomic Lua write behavior remains unchanged.

Stop without commit or push if any condition is false.

## Required clean continuation gates

After correcting the test inconsistency, run a new complete gate cycle. Previous partial results do not count as acceptance evidence.

Required focused gates:

1. focused cache bulk-read tests;
2. focused dashboard bulk-mapping tests;
3. all tests covering empty input, stable input order, missing first/middle state, malformed JSON, embedded-symbol mismatch, in-memory behavior, and dashboard state/health mapping.

Required full Rust gates:

1. `cargo fmt --all`;
2. `cargo fmt --all --check`;
3. `cargo check --all-targets --all-features`;
4. `cargo clippy --all-targets --all-features -- -D warnings`;
5. `cargo test --all-targets --all-features`;
6. `git diff --check`;
7. repository hygiene/status checks.

Required real-Redis gates against an isolated Redis 7 instance:

1. `cargo test --test redis_cache -- --ignored --test-threads=1`;
2. any focused ignored bulk-read Redis tests introduced by P2-MP06;
3. existing P2-MP05 atomic Redis proof tests must continue to pass.

Every required continuation gate must pass in this new cycle. Stop without commit or push if any gate fails or is skipped.

## Delivery after green gates

After all continuation gates pass:

1. stage only the three authorized paths;
2. create exactly one product commit with message:

   `perf(api): load dashboard market states in bulk`

3. push only `p2/mp06-bulk-redis-state` without force;
4. open one draft PR into `refactor/data-boundaries`;
5. do not merge, amend, rebase, or force-push;
6. wait for exact-head product CI;
7. stop and report if exact-head CI fails;
8. after green exact-head CI, create the required unique connector report:

   `signalguard-rs/phase-2/reports/P2-MP06/<PRODUCT-HEAD-SHA>.md`

Use the original P2-MP06 contract and report template for all remaining delivery requirements.

## Concurrency boundary

P2-MP03-R1 may run concurrently on frontend-only paths.

Do not read, modify, merge, rebase onto, or cherry-pick from `p2/mp03-market-view-models`.

## Final response

Return only:

- repository path;
- worktree path;
- product branch;
- verified base SHA;
- corrected test intent and exact correction;
- product commit SHA;
- product PR URL;
- exact changed paths;
- bulk cache API signature;
- Redis bulk command strategy;
- focused gate summary;
- full Rust gate summary;
- real-Redis gate summary;
- product CI run ID and conclusion;
- connector report path and commit SHA;
- any blocker.
