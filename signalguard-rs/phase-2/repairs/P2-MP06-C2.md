# P2-MP06-C2 Continuation Contract — Resolve Redis Test Type Inference

## Role

Continue the existing local `P2-MP06` implementation after the `P2-MP06-C1` full gate stopped at a Rust type-inference error in a newly added Redis integration test.

This is a narrow continuation. It does not authorize redesign, reimplementation, history rewriting, or production behavior changes.

## Immutable product state

Product repository:

`progeranna/signalguard-rs`

Dedicated local worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p2-mp06`

Assigned branch:

`p2/mp06-bulk-redis-state`

Exact required local `HEAD` before continuation:

`1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`

Exact required remote branch SHA before continuation:

`1447a3ccb2fa3020738cd2dd3f8d145be6cd202a`

The remote branch has been independently verified as ahead by 0, behind by 0, and identical to this base. No product commit, PR, report, or CI run exists yet.

## Preserved local implementation

The existing uncommitted implementation may remain in the worktree.

Expected modified paths:

- `src/storage/redis.rs`
- `src/api/handlers.rs`
- `tests/redis_cache.rs`

Expected implementation properties:

- bulk API:
  `get_market_states(&[Symbol]) -> Result<Vec<(Symbol, Option<MarketState>)>, CacheError>`;
- one Redis `MGET` for all requested market-state keys;
- stable input/result ordering;
- explicit requested-symbol association;
- missing state represented as `None`;
- malformed payload and embedded-symbol mismatch reported as cache data errors;
- dashboard uses the bulk API rather than awaited per-symbol Redis `GET` calls;
- P2-MP05 atomic Lua write remains unchanged.

The previous decimal assertion correction is accepted as test-only evidence:

- fixtures represented numeric values `100` and `200` with scale `0`;
- expectations were corrected from `"1.00"` / `"2.00"` to `"100"` / `"200"`;
- production decimal serialization was not changed.

## Failure being continued from

The focused gates passed:

- 2 bulk-cache tests;
- 11 dashboard handler tests.

The full gate stopped at:

`cargo check --all-targets --all-features`

because a newly added Redis integration test had a Rust type-inference error.

Real-Redis gates were not run after that failure.

## Narrow repair authorization

Before editing:

1. Verify local `HEAD` and remote assigned branch still equal the exact base SHA.
2. Verify only the three expected paths are modified.
3. Verify nothing is staged.
4. Capture the complete compiler diagnostic, including error code, file, line, expression, and inferred/required types.
5. Confirm the diagnostic originates in newly added P2-MP06 test code.

Authorized correction:

- add the smallest explicit Rust type annotation or generic type argument needed to make the test expression unambiguous;
- prefer annotating the local binding or Redis command return type at the failing test line;
- keep the test assertion and behavior unchanged;
- keep the correction inside `tests/redis_cache.rs`.

Examples of acceptable correction shapes, only when they match the actual diagnostic:

- `let value: Option<String> = ...;`
- `let _: () = connection.set(...).await?;`
- `query_async::<_, ExpectedType>(...)` using the signature supported by the repository's pinned Redis crate;
- an explicit integer/result type on a Redis command whose return type is otherwise unused.

## Forbidden correction

Do not:

- suppress the error with broad casts, `unwrap_or_default`, ignored results, or dead-code allowances;
- remove, skip, weaken, or replace the integration test;
- change production bulk API types;
- change Redis serialization/deserialization behavior;
- change dashboard DTOs or response fields;
- change the P2-MP05 Lua script;
- modify files outside the three original authorized paths;
- modify production code merely to satisfy test type inference;
- amend, rebase, reset, force-push, or create an intermediate commit.

If the compiler diagnostic is not inside newly added P2-MP06 test code, or fixing it requires a production-code change, stop and report the exact diagnostic without committing.

## Required gate cycle after the correction

Run a completely new gate cycle from the beginning. Previous passes do not substitute for this cycle.

### Focused gates

Run the exact focused tests covering:

- empty input;
- BTCUSDT and ETHUSDT in both orders;
- missing first or middle state;
- malformed JSON;
- embedded-symbol mismatch;
- in-memory cache behavior;
- dashboard state and health mapping;
- the new real-Redis bulk retrieval integration test.

### Full Rust gates

Run:

1. `cargo fmt --all`
2. `cargo fmt --all --check`
3. `cargo check --all-targets --all-features`
4. `cargo clippy --all-targets --all-features -- -D warnings`
5. `cargo test --all-targets --all-features`
6. `git diff --check`
7. repository status and hygiene checks

### Isolated Redis 7 gates

Use the existing isolated Redis execution method and run:

1. `cargo test --test redis_cache -- --ignored --test-threads=1`
2. any focused ignored P2-MP06 bulk-read test filter, when separately available
3. `cargo test --lib atomic_market_state_write -- --ignored --test-threads=1`

Every required gate must pass. A failed or skipped gate requires stopping without commit or push.

## Product delivery after all gates pass

1. Verify only these paths differ from the base:
   - `src/storage/redis.rs`
   - `src/api/handlers.rs`
   - `tests/redis_cache.rs`
2. Stage only those paths.
3. Create exactly one product commit:

   `perf(api): load dashboard market states in bulk`

4. Push only:

   `p2/mp06-bulk-redis-state`

5. Open a draft PR into:

   `refactor/data-boundaries`

6. Do not amend, rebase, merge, reset, or force-push.
7. Wait for exact-head GitHub CI.
8. Stop and report if exact-head CI fails.
9. After green CI, create the required connector delivery report:

   `signalguard-rs/phase-2/reports/P2-MP06/<PRODUCT-HEAD-SHA>.md`

## Concurrent work isolation

`P2-MP03-R1` may be running concurrently on frontend-only paths.

Do not read from, modify, merge, rebase onto, or cherry-pick from:

`p2/mp03-market-view-models`

## Final response

Return only:

- repository path;
- worktree path;
- product branch;
- verified base SHA;
- exact compiler diagnostic and error code;
- exact type annotation correction;
- product commit SHA;
- product PR URL;
- exact changed paths;
- bulk cache API signature;
- Redis bulk command strategy;
- focused gate summary;
- full Rust gate summary;
- real-Redis gate summary;
- product CI run ID and conclusion;
- connector report path and commit SHA;
- any blocker.
