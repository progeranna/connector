# P3-MP00 — Post-Phase-2 Route, Component, Visual, and Path-Lease Inventory

## Result

`ACCEPT`

P3-MP00 is a connector-only control/inventory task. It makes no product change and creates no product commit.

## Exact starting identity

- product repository: `progeranna/signalguard-rs`
- authoritative starting branch: `main`
- exact starting main SHA: `6b57938d87e05d3b4fa4858f9c34c27739877821`
- Phase 3 branch: `refactor/dashboard-modules`
- initial Phase 3 branch SHA: `6b57938d87e05d3b4fa4858f9c34c27739877821`
- branch divergence at Phase 3 start: ahead `0`, behind `0`
- stable preview worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-preview`
- candidate preview worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-candidate-preview`
- stable preview port: `5173`
- candidate preview port: `5174`

The Phase 3 branch was created directly from the final merged Phase 2 main SHA and contains no Phase 3 product commit at this inventory point.

## Entry and routing graph

### Application entry

- `web/src/main.tsx`
  - mounts `<App />` under React Strict Mode;
  - imports global CSS.
- `web/src/app/App.tsx`
  - mounts `AppProviders` and `RouterProvider`;
  - owns no page-specific presentation.
- `web/src/app/providers.tsx`
  - provider composition; high-impact shared infrastructure.

### Router and layout

- `web/src/app/router.tsx`
  - centralized browser-router composition;
  - route `/` → dashboard index;
  - route `/dashboard` → dashboard;
  - route `/symbols/:symbol` → canonical symbol wrapper;
  - route `/anomalies` → anomaly page;
  - high-conflict path reserved for Wave 6 routing tasks.
- `web/src/app/ConsoleLayout.tsx`
  - wraps child routes in `AppShell` and `Outlet`.
- `web/src/app/CanonicalSymbolRoute.tsx`
  - canonicalizes mixed-case symbol deep links;
  - preserves the direct symbol route.
- `web/src/app/symbolRoutePath.ts`
  - canonical route-path helper.

No 404 route or route-level error boundary exists at the Phase 3 starting tree; those remain Wave 6 work.

## Global shell and ticker ownership

### `web/src/app/AppShell.tsx`

Current ownership:

- global product header;
- symbol selector and search menu;
- Demo/Live selector and explanatory menu;
- selected-symbol/header-route coordination;
- global data status;
- responsive header layout;
- `GlobalMarketTicker` placement;
- page content container.

This is a high-conflict path. It is forbidden during Wave 0 and reserved later for explicitly leased semantic, selector-accessibility, or shell work.

### `web/src/app/GlobalMarketTicker.tsx`

Current ownership:

- ticker query and market ordering;
- ticker item structure;
- status, price, spread, and anomaly text;
- duplicated scrolling item groups;
- ticker-specific formatting and status classes.

The ticker is a permanent Phase 3 forbidden path and forbidden behavior.

### `web/src/shared/styles/globals.css`

Current ownership:

- global theme variables and base styles;
- shared `.sg-panel` class;
- `.sg-ticker-track` animation and hover/mobile behavior;
- `@keyframes sg-ticker-scroll`.

Because this file contains ticker CSS, it is forbidden in Wave 0 and may not be modified by reduced-motion or other Phase 3 work without an explicit contract that proves ticker rules remain untouched. The ticker selectors and keyframes themselves are forbidden throughout Phase 3.

## Page and composition inventory

### `web/src/pages/DashboardPage.tsx`

The starting file is the primary Phase 3 extraction hotspot. It currently owns all of the following in one module:

- dashboard top-level data/query shell;
- selected-market timeline panel;
- Recharts composition;
- timeline tooltip presentation;
- timeline point normalization;
- price and time-domain calculations;
- visible anomaly marker calculation;
- selected-market snapshot/status surface;
- Market Health preview shell;
- Market Health desktop table;
- Market Health mobile cards;
- health-score presentation;
- Recent Anomalies preview shell;
- Recent Anomalies desktop table;
- Recent Anomalies mobile cards;
- all-markets modal content;
- all-anomalies modal content;
- symbol-detail popup container and success presentation;
- inline modal primitive `DashboardTableModal`;
- status, severity, anomaly, data-age, and display formatting helpers;
- modal return-context orchestration.

Current modal primitive behavior:

- `role=dialog` and `aria-modal=true`;
- title labelling;
- Escape close;
- backdrop close;
- body scroll lock.

Current modal primitive does not provide the complete Wave 5 target by itself: no portal, full focus trap, controlled initial focus, or explicit focus-return contract is centralized yet.

`DashboardPage.tsx` is forbidden for Wave 0 workers. Later waves split pure models first, then independent components, then compositor wiring. No two active workers may edit it simultaneously.

### `web/src/pages/SymbolDetailPage.tsx`

Current ownership:

- direct symbol route container;
- route identity and selected-symbol synchronization;
- shared Phase 2 market resource/view-model consumption;
- symbol status/source header;
- metric strip and state rows;
- route anomaly desktop/mobile presentation;
- loading/error/not-found/non-observed states.

It remains unchanged during Wave 0 and is reserved for the shared symbol-section migration wave.

### `web/src/pages/AnomaliesPage.tsx`

Current state:

- route exists and remains public;
- presentation is a deliberate placeholder/explainer page;
- uses `PageHeader`, `MetricCard`, `StatusBadge`, and a symbol route link;
- no Phase 3 Wave 0 modification is authorized.

## Phase 2 data-boundary inventory retained by Phase 3

Phase 3 must consume, not repeat, the following accepted boundaries:

- `web/src/features/dashboard/queryKeys.ts`
  - semantic mode/symbol/filter query identity.
- `web/src/features/dashboard/api.ts`
  - validated request/query hooks and cancellation.
- `web/src/features/dashboard/types.ts`
  - Zod DTO boundary including explicit source/availability.
- `web/src/features/dashboard/marketAdapters.ts`
  - DTO/resource to view-model adaptation.
- `web/src/features/dashboard/marketViewModel.ts`
  - route/popup shared view-model contracts.
- `web/src/features/dashboard/symbolMarketResource.ts`
  - route resource-state composition.
- `web/src/features/dashboard/symbolPopupResource.ts`
  - popup resource composition.
- `web/src/features/dashboard/symbolPopup.ts`
  - popup identity and return context.
- `web/src/features/dashboard/selectedSymbol.ts`
  - mode-scoped selected-symbol preference.
- `web/src/features/dashboard/uiMode.ts`
  - public Demo/Live mode ownership.
- `web/src/features/dashboard/marketCatalog.ts`
  - explicit API-owned catalog consumption.
- `web/src/features/dashboard/marketOrder.ts`
  - deterministic market ordering.

Wave 0 may not alter these established data flows except for adding a new pure descriptor module under its own lease. No worker may reintroduce frontend availability synthesis, BTC fallback, Demo injection into Live, or alternate query-key ownership.

## Shared component and style inventory

Existing shared components include:

- `web/src/shared/components/ErrorPanel.tsx`;
- `web/src/shared/components/LoadingSkeleton.tsx`;
- `web/src/shared/components/MetricCard.tsx`;
- `web/src/shared/components/PageHeader.tsx`;
- `web/src/shared/components/StatusBadge.tsx`.

No shared `web/src/shared/components/Tooltip.tsx` exists at the starting SHA. The path is available for P3-MP02.

`web/src/shared/lib/status.ts` owns generic visual tone values and the generic tone palette. P3-MP03 must not replace or restyle this module. Domain-qualified system/market/anomaly/data-age semantics belong in a new pure dashboard descriptor module.

## Test and build inventory

### Tooling

- React 18;
- React Router 6;
- TanStack Query 5;
- Recharts 3;
- Zod 3;
- Vitest with jsdom;
- Testing Library and jest-dom;
- no tooltip or dialog dependency is installed or required for Wave 0.

Existing required frontend commands:

- `npm run test:run`;
- `npm run typecheck`;
- `npm run lint`;
- `npm run build`;
- `npm run bundle:check`.

`package.json`, lockfiles, Vite config, Vitest config, and TypeScript config are forbidden in Wave 0. Existing tooling is sufficient.

### Existing relevant tests

- `web/src/app/marketNavigationIsolation.test.tsx`
  - route BTC→ETH identity;
  - canonical mixed-case route;
  - invalid/absent Live symbol with no Demo fallback.
- `web/src/features/dashboard/marketDataIsolation.test.tsx`
  - mode/symbol and late-response isolation.
- `web/src/pages/DashboardPage.popup.test.tsx`
  - popup identity, return context, mode ownership, and non-observed popup presentation.
- `web/src/pages/SymbolDetailPage.test.tsx`
  - route resource and availability behavior.
- `web/src/features/dashboard/symbolPopupResource.test.tsx`
  - popup resource-state behavior.
- `web/src/features/dashboard/queryKeys.test.ts`
  - query-key dimensions.
- `web/src/test/marketFixtures.ts`
  - deterministic Demo/Live and BTC/ETH fixture support.
- `web/src/test/setup.ts`
  - jest-dom registration and automatic cleanup.

The existing suite provides strong identity coverage but no single reusable structured smoke-matrix manifest covering every required surface, mode, symbol, viewport, resource state, switch sequence, and dialog behavior. P3-MP01 fills only that test-infrastructure gap.

## Responsive and visible-state inventory

Current responsive variants:

- header changes from stacked to centered grid at large widths;
- ticker scroll animation is desktop-only and disabled below `1024px`;
- dashboard Market Health and Recent Anomalies use desktop tables and mobile cards;
- modal market/anomaly content uses desktop tables and mobile lists/cards;
- symbol route anomaly content uses desktop table and mobile rows;
- dashboard top area uses responsive chart/snapshot layout.

Required reusable smoke states:

- loading;
- error;
- empty;
- unavailable/non-observed;
- observed success;
- Demo and Live;
- BTCUSDT and ETHUSDT;
- dashboard, popup, and direct route;
- desktop and mobile widths;
- rapid mode switching;
- rapid symbol switching;
- route/popup parity;
- no stale data flash;
- dialog keyboard behavior when applicable.

## Wave 0 exact path leases

### P3-MP01 — Reusable local UI smoke matrix

Branch:

`p3/mp01-ui-smoke-matrix`

Dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-mp01`

Required commit:

`test(ui): define reusable smoke matrix`

Authorized paths:

- new `web/src/test/uiSmokeMatrix.ts`;
- new `web/src/test/uiSmokeMatrix.test.ts`.

Optional only when strictly necessary to reuse existing canonical fixture types:

- `web/src/test/marketFixtures.ts`.

No production file may change. The task defines a typed, deterministic, reusable matrix and executable completeness tests; it does not redesign, render, or migrate UI.

### P3-MP02 — Tooltip primitive foundation

Branch:

`p3/mp02-tooltip-primitive`

Dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-mp02`

Required commit:

`feat(ui): add accessible tooltip primitive`

Authorized paths:

- new `web/src/shared/components/Tooltip.tsx`;
- new `web/src/shared/components/Tooltip.test.tsx`.

No caller migration, page edit, global CSS edit, dependency edit, Recharts tooltip edit, or visual-semantic copy change is allowed.

### P3-MP03 — Pure status vocabulary and descriptor model

Branch:

`p3/mp03-status-descriptors`

Dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-mp03`

Required commit:

`feat(ui): define status descriptor model`

Authorized paths:

- new `web/src/features/dashboard/statusDescriptors.ts`;
- new `web/src/features/dashboard/statusDescriptors.test.ts`.

The module is pure TypeScript with no JSX, React hook, network request, store access, DOM access, CSS, or caller wiring. It may import only stable type/tone contracts needed for compile-time consistency.

### P3-MP04 — Deterministic semantic fixtures

Not yet execution-authorized. It follows accepted P3-MP03 and will receive a separate immutable contract and exact base.

Reserved prospective paths:

- new `web/src/features/dashboard/statusDescriptorFixtures.ts`;
- new `web/src/features/dashboard/statusDescriptorFixtures.test.ts`.

## Wave 0 common forbidden paths

All Wave 0 workers are forbidden from modifying:

- `web/src/pages/DashboardPage.tsx`;
- `web/src/pages/SymbolDetailPage.tsx`;
- `web/src/pages/AnomaliesPage.tsx`;
- `web/src/app/AppShell.tsx`;
- `web/src/app/GlobalMarketTicker.tsx`;
- `web/src/app/router.tsx`;
- `web/src/app/App.tsx`;
- `web/src/app/ConsoleLayout.tsx`;
- `web/src/app/CanonicalSymbolRoute.tsx`;
- `web/src/shared/styles/globals.css`;
- existing Phase 2 API/query/resource/identity/adaptor files;
- `web/package.json` and lockfiles;
- Vite/Vitest/TypeScript/Tailwind configuration;
- backend, OpenAPI, contract, Docker, deployment, and CI files;
- docs and public claims;
- any ticker text, structure, order, scrolling, animation, or CSS.

No worker may edit another Wave 0 worker's leased paths.

## Checkpoint 0 baseline

Before and after each Wave 0 integration:

- the Phase 3 stable preview must remain visually identical to final Phase 2;
- all routes and existing interactions must remain unchanged;
- full frontend gates must stay green;
- the upper ticker must be byte-for-byte unchanged;
- no backend or public contract change is expected.

P3-MP00 is complete. P3-MP01, P3-MP02, and P3-MP03 may now receive immutable worker contracts and branches from exact base `6b57938d87e05d3b4fa4858f9c34c27739877821`.
