# Phase 3 Roadmap Recovery — Consolidated Inventory Evidence

Status: `P3_RECOVERY_INVENTORIES_ACCEPTED_FOR_SYNTHESIS`

## 1. Authority and identity

- Product repository: `progeranna/signalguard-rs`
- Product SHA: `ba31a348dc5055935c45f6be81073688caedd925`
- Product tree: `f629b6ea4339c92d03223c3bd8024cd4cb4571da`
- Phase branch: `refactor/dashboard-modules`
- Roadmap: `signalguard-rs/phase-3/control/EXECUTION_PLAN.md`
- Mandatory resumption entry point: `signalguard-rs/phase-3/control/RESUMPTION_PLAN.md`

The product SHA and tree were independently verified from PR #68, its worker tree, and a zero-diff comparison between the worker head and merge commit.

## 2. Accepted worker inputs

The orchestrator received and reviewed the complete terminal outputs from:

- `P3-RECOVERY-INV-A-WAVE3.md`
- `P3-RECOVERY-INV-B-WAVE4.md`
- `P3-RECOVERY-INV-C-DIALOGS.md`
- `P3-RECOVERY-INV-D-PERF-RESPONSIVE.md`

Terminal results:

- A returned `P3_RECOVERY_INV_A_BLOCKED_BY_MISSING_EVIDENCE` only because its tool surface did not expose the commit tree SHA. Its substantive inventory was complete. The missing tree evidence was independently supplied by the orchestrator, so A is accepted as `SUBSTANTIVELY_COMPLETE_TREE_EVIDENCE_SATISFIED`.
- B returned `P3_RECOVERY_INV_B_COMPLETE`.
- C returned `P3_RECOVERY_INV_C_COMPLETE`.
- D returned `P3_RECOVERY_INV_D_COMPLETE`.

No worker modified either repository.

## 3. Common conclusions

All four inventories agree on the binding continuation order:

1. `P3-MP18R` and `P3-MP20R` Wave 3 modal-only closure;
2. Checkpoint 2R;
3. semantic Bridge 01 and Bridge 02;
4. agreed semantic Wave 4 `P3-MP21…P3-MP30` and Checkpoint 3;
5. dialogs/accessibility `P3-MP31…P3-MP35`;
6. routing/loading/performance `P3-MP36…P3-MP41`;
7. responsive/final `P3-MP42…P3-MP46`;
8. only then a new product Phase 4.

The modal-only product-owner override remains binding:

- `/` and `/dashboard` are the only visual console pages;
- `/symbols/:symbol` and `/anomalies` are replacement redirects;
- markets open Symbol Detail modal;
- anomalies open exact UUID-keyed Anomaly Detail;
- All Anomalies rows never open Symbol Detail;
- modal state is local and ephemeral;
- URL-backed modal state and standalone symbol/anomaly pages are forbidden.

## 4. Wave 3 closure findings

### P3-MP18R

Current defect:

- Symbol Detail desktop anomaly rows are inert.
- Symbol Detail mobile anomaly cards call `onOpenSymbolDetail(anomaly.symbol)` and advertise market-detail activation.
- No controller state represents Anomaly Detail opened from a specific Symbol Detail resource.
- mobile focus restoration can target a hidden desktop duplicate because initial-focus lookup uses a bare `querySelector`.

Required identity:

- store exact `anomalyId` plus parent `SymbolPopupIdentity`;
- resolve the anomaly by UUID from the current symbol resource;
- never store a stale full anomaly object;
- Back restores the same Symbol Detail identity and exact visible row/card focus;
- mode or symbol replacement clears the old UUID and prevents stale content.

Accepted proposed MP18R lease:

Production:

- `web/src/pages/DashboardPage.tsx`
- `web/src/features/dashboard/SymbolDetailAnomalies.tsx`
- `web/src/features/dashboard/SymbolDetailHeader.tsx`
- `web/src/features/dashboard/SymbolDetailMetrics.tsx`
- `web/src/features/dashboard/symbolPopup.ts`

Tests:

- `web/src/pages/DashboardPage.popup.test.tsx`
- `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`
- `web/src/features/dashboard/SymbolDetailHeader.test.tsx`
- `web/src/features/dashboard/SymbolDetailMetrics.test.tsx`
- `web/src/features/dashboard/symbolPopup.test.ts`

MP18R also owns removal of the unreachable Symbol Detail return context `"anomalies"` and ignored single-literal `variant="popup"` / `surface="popup"` props. It must not change resources, queries, API schemas, routes, shared Dialog architecture, Wave 4 vocabulary, global CSS or ticker ownership.

### P3-MP20R

Current residue:

- `MarketDisplayVariants` still contains `route` and `popup` values after standalone route removal.
- adapters still calculate route-only anomaly formatting.

Accepted proposed MP20R lease:

- `web/src/features/dashboard/marketViewModel.ts`
- `web/src/features/dashboard/marketAdapters.ts`
- `web/src/features/dashboard/marketAdapters.test.ts`

MP20R owns only pure view-model and formatter cleanup. It follows accepted MP18R and must not reopen MP18R-owned paths.

### Checkpoint 2R

Required flows across Demo/Live, BTCUSDT/ETHUSDT, 1440px/390px, pointer/keyboard:

- Dashboard → Symbol Detail;
- Symbol Detail anomaly → exact Anomaly Detail → Back;
- All Markets → Symbol Detail → exact Anomaly Detail → Back;
- All Anomalies → exact Anomaly Detail → Back;
- exact visible focus restoration;
- Tab/Shift+Tab containment;
- Escape, Close, backdrop and body-lock behavior;
- rapid mode and symbol switching;
- no route navigation or stale-content flash;
- zero unexpected console errors.

## 5. Semantic bridge findings

Exactly two bridge mini-phases are required.

### Bridge 01 — backend semantic/data contract

The backend already owns detector thresholds, health thresholds, health signals, penalties and evaluation data. Missing or private facts include:

- an authoritative delayed-data threshold;
- pipeline evaluation time and degraded/unhealthy thresholds in public DTOs;
- rich dashboard health facts;
- deterministic primary issue;
- explicit measurement unit/comparison/exceeded-by semantics;
- Demo threshold alignment with runtime configuration.

The delayed threshold must become a validated backend setting with:

`0 <= delayed_after_ms <= stale_after_ms`.

Proposed Bridge 01 lease:

- `src/config.rs`
- `src/health/scoring.rs`
- `src/api/dto.rs`
- `src/api/handlers.rs`
- `src/api/demo_data.rs`
- `src/api/contract.rs`
- `contracts/web-console.openapi.json`

No endpoint identity changes are required. Changes must be additive and preserve Demo/Live isolation.

### Bridge 02 — frontend preservation and pure adapters

The frontend currently drops rich `/market/:symbol/health` fields and lacks pure semantic adapter ownership.

Proposed Bridge 02 lease:

- `web/src/features/dashboard/types.ts`
- `web/src/features/dashboard/types.test.ts`
- `web/src/features/dashboard/api-contract.test.ts`
- `web/src/features/dashboard/marketViewModel.ts`
- `web/src/features/dashboard/marketAdapters.ts`
- `web/src/features/dashboard/marketAdapters.test.ts`
- `web/src/features/dashboard/symbolMarketResource.ts`
- `web/src/features/dashboard/marketDataIsolation.test.tsx`
- `web/src/test/marketFixtures.ts`
- new `web/src/features/dashboard/semanticAdapters.ts`
- new `web/src/features/dashboard/semanticAdapters.test.ts`
- new `web/src/features/dashboard/semanticTooltipFacts.ts`
- new `web/src/features/dashboard/semanticTooltipFacts.test.ts`

The synthesis must account for the fact that Bridge 02 reopens `marketViewModel.ts`, `marketAdapters.ts` and `marketAdapters.test.ts` after MP20R. This is sequential ownership, not a conflict: MP20R first removes dead route variants; Bridge 02 later extends the resulting surface-neutral model with authoritative semantic facts.

## 6. Wave 4 readiness and safe grouping

The approved vocabulary already exists in `statusDescriptors`; visible wiring is mostly incomplete.

Readiness:

- MP21 System indicator: partial;
- MP22 chart anomaly indicator: partial;
- MP23 Data Age: partial and blocked on runtime thresholds;
- MP24 Market Health vocabulary: partial;
- MP25 Recent Anomalies semantics: partial;
- MP26 selected-market status/facts: partial;
- MP27 Demo/Live explanation: partial, nearest completion;
- MP28 health-score tooltip: partial and currently duplicates backend score thresholds;
- MP29 observed/threshold/exceeded-by: partial;
- MP30 unified tooltip audit: infrastructure only.

Maximum safe sequence after Bridge 02:

- G2 parallel: MP21, MP22, MP24;
- G3 parallel: MP23, MP25, MP27;
- G4: MP26;
- G5: MP28;
- G6: MP29;
- G7: MP30 final audit.

Collision rules:

- MP21 and MP27 both own `AppShell.tsx`, so they cannot run together.
- MP22, MP23 and MP26 own `TimelinePanel.tsx`, so they are sequenced as above.
- MP24, MP25, MP26, MP28 and MP29 touch `DashboardPage.tsx`; the sequence above prevents overlap.
- MP24 and MP28 share Market Health callers.
- MP25 and MP29 share anomaly callers.
- MP30 audits only the accepted combined tree.

No new frontend dependency is justified. Bundle budgets remain unchanged.

## 7. Dialog/accessibility findings

Current inline behavior is substantial but incomplete:

- no portal;
- no `aria-describedby` for subtitles;
- boundary-only focus trap;
- fragile responsive initial focus;
- no exact All Markets return-focus identity;
- body locking is insufficiently tested;
- modal code remains concentrated in `DashboardPage.tsx`.

### MP31

Create and test a shared primitive only:

- new `web/src/shared/components/Dialog.tsx`
- new `web/src/shared/components/Dialog.test.tsx`

No caller migration or visible change.

### MP31A

Required structural decomposition before parallel migrations:

- `web/src/pages/DashboardPage.tsx`
- `web/src/pages/DashboardPage.test.tsx`
- new `web/src/features/dashboard/modals/LegacyDashboardTableModal.tsx`
- new `web/src/features/dashboard/modals/SymbolDetailModal.tsx`
- new `web/src/features/dashboard/modals/AllMarketsModal.tsx`
- new `web/src/features/dashboard/modals/AnomalyModals.tsx`

MP31A moves code only and does not adopt the new primitive.

### MP32–MP34

After Wave 4 Checkpoint 3, MP31 and MP31A:

- MP32 owns `SymbolDetailModal.tsx` and its new focused test.
- MP33 owns `AllMarketsModal.tsx`, its new test, and the controller paths needed for exact market return focus.
- MP34 owns `AnomalyModals.tsx` and its new test; it preserves UUID identity and nested Back behavior.

The temporary legacy shell remains until sequential MP35 cleanup.

### MP35

Must turn the declarative smoke matrix into executed integrated keyboard/focus coverage and add `all-markets-modal`. It also deletes the legacy shell after zero-import verification.

## 8. Routing/loading/performance findings

Recovered objectives:

- MP36: explicit wildcard 404 while preserving compatibility redirects;
- MP37A: Dashboard/render boundary distinct from 404 and query errors;
- MP37B: modal-local boundary reset by modal type, mode, symbol and UUID;
- MP38: measured optional modal-feature split, not Dashboard route splitting;
- MP39: `SUPERSEDED_BY_ACCEPTED_IMPLEMENTATION` because Recharts is already removed and native SVG is accepted;
- MP40: accessible lazy loading/import-failure presentation only after accepted MP38;
- MP41: measurement checkpoint, no product write by default.

Important ownership resolution:

- D's MP37B lease was proposed against the pre-decomposition tree and names `DashboardPage.tsx`. The synthesis must translate that lease to the accepted post-MP31A/post-MP35 modal/controller ownership rather than blindly retaining current-tree paths.
- MP38 must target the accepted post-MP35 modal modules. It may wire dynamic imports but must not perform extraction.
- MP36 and the modal-boundary implementation may be prepared in parallel only when their exact post-decomposition leases are disjoint.
- MP37A follows MP36 because both own router files.

MP38 is a controlled experiment, not emergency budget repair. Accept only when initial JS is lower than the 387,239-byte baseline, a real async edge exists, total/largest budgets pass, modal identity remains correct, and accessible cold/failure states exist.

## 9. Responsive/final findings

- MP42: full keyboard model for mode and symbol selectors, including Arrow keys, Home/End, filtered-result navigation and Escape focus restoration.
- MP43: reduced-motion support for non-ticker motion only. `GlobalMarketTicker`, `.sg-ticker-track` and ticker keyframes remain protected.
- MP44: Timeline responsive regression, test-first; production fixes require separate narrow leases.
- MP45: Market Health and Recent Anomalies responsive regression, test-first; production fixes require separate narrow leases.
- MP46: final modal-only browser smoke; no production writes.

The final smoke matrix must add:

- All Markets modal;
- explicit 404 routes;
- Dashboard and modal boundaries;
- cold lazy success/failure when MP38 is accepted;
- complete selector keyboard behavior;
- normal and reduced-motion runs;
- real 1440px/390px browser evidence;
- screenshots and zero unexpected console errors.

## 10. Resolved conflicts

The inventories contain no unresolved architectural contradiction after applying these resolutions:

1. A's missing tree evidence is satisfied independently.
2. MP18R owns controller/UI cleanup; MP20R owns pure route-variant removal.
3. Bridge 02 may later reopen MP20R model/adapter paths because it is sequential and has a different result.
4. Wave 4 collision files are handled by the accepted G2–G7 sequence.
5. MP31A is mandatory before MP32–MP34 parallel migration.
6. D's current-tree modal-boundary and lazy-loading leases must be rewritten against the post-decomposition tree by synthesis.
7. MP39 requires no implementation.
8. reduced-motion work may not alter ticker behavior.

## 11. Synthesis requirements

The synthesis worker must now publish:

- `signalguard-rs/phase-3/control/MICRO_PHASE_LEDGER.md`
- `signalguard-rs/phase-3/control/IMPLEMENTATION_SEQUENCE.md`
- updated `signalguard-rs/phase-3/control/STATUS.md`

Only Wave 3 closure implementation may be authorised by this synthesis. Semantic bridge and Wave 4 remain sequenced future work until Checkpoint 2R.

No product branch, commit or PR is authorised during synthesis.
