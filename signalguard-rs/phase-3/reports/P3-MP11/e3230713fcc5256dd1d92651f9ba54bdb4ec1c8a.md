# P3-MP11-WEB2 Connector Report

## Status

**BLOCKED — immutable remote read-back failure.**

The replacement branch was created from the exact assigned base and received exactly one normal product commit. Remote blob verification then found that the committed test blob was not byte-identical to the locally hardened candidate: quote characters in two source-guard regular expressions were present remotely as `#`. That makes the test inventory invalid. The WEB2 contract forbids a second product commit, amend/reset/rebase, force-push, branch rewrite, or any reuse/mutation of the rejected WEB1 branch. Work therefore stopped before PR creation.

## Contract identity

- Contract repository: `progeranna/connector`
- Contract path: `signalguard-rs/phase-3/prompts/P3-MP11-WEB2.md`
- Contract commit: `2ac40644aaeebe06afd1d84be6ac011287e9be5e`
- Product repository: `progeranna/signalguard-rs`
- Replacement branch: `p3/mp11-market-health-desktop-r1`
- PR base: `refactor/dashboard-modules`
- Exact assigned base: `93a870010730c458417ccfff392cb97aff23d6c9`
- Product commit: `e3230713fcc5256dd1d92651f9ba54bdb4ec1c8a`
- Commit message: `feat(ui): add market health desktop table`

## Quarantined WEB1 evidence

- Rejected branch: `p3/mp11-market-health-desktop`
- Rejected head: `3b5e3e0b5efd1ca5291c08cb0d7f9e3ab36ea596`
- Rejected PR: `#33`, closed and unmerged
- The rejected ref was read only; it was not modified or used as the replacement branch base.

## Divergence and path lease

Before the product write:

- `93a870010730c458417ccfff392cb97aff23d6c9...p3/mp11-market-health-desktop-r1`: ahead `0`, behind `0`

After the single product commit:

- `93a870010730c458417ccfff392cb97aff23d6c9...e3230713fcc5256dd1d92651f9ba54bdb4ec1c8a`: ahead `1`, behind `0`
- Exact changed paths:
  - `web/src/features/dashboard/MarketHealthDesktopTable.tsx` — added, 254 lines
  - `web/src/features/dashboard/MarketHealthDesktopTable.test.tsx` — added, 430 lines
- No other product path changed.
- `DashboardPage.tsx` was not modified or wired.
- P3-MP15 was not started.

## Remote blobs

- `web/src/features/dashboard/MarketHealthDesktopTable.tsx`
  - Remote blob SHA: `e57f8562f3b23a2a1f364f7709c49bf3f140b589`
  - UTF-8 read-back: successful
  - Matches the intended component blob.
- `web/src/features/dashboard/MarketHealthDesktopTable.test.tsx`
  - Remote blob SHA: `0df628d26ea0169c0c3714f88b1de3f0a39dbec8`
  - Locally hardened candidate Git blob SHA: `10ba9852fcb413cdae68d2ade3a6851ab6e0fb96`
  - UTF-8 read-back: successful
  - Byte identity: **failed**

Observed remote corruption:

```text
/intended:  from\s+["'][^"']*...
/remote:    from\s+[#'][^"']*...

/intended:  ...from\s+["']\.\/marketHealthPreviewModel["'];
/remote:    ...from\s+[#']\.\/marketHealthPreviewModel["'];
```

The remote test would not match the component's double-quoted imports and cannot be accepted as a valid complete test inventory.

## Corrected WEB1 regression evidence

The locally hardened candidate corrected the ownership guard to target transformations of the `rows` collection specifically:

```ts
const rowTransformation =
  /\brows\s*\.\s*(?:sort|toSorted|slice|splice|reverse)\s*\(/;

expect(componentSource).not.toMatch(rowTransformation);
expect(rowTransformation.test("rows.slice(0, 7)")).toBe(true);
expect(rowTransformation.test("rows.toSorted(compareRows)")).toBe(true);
expect(rowTransformation.test("value.slice(1)")).toBe(false);
expect(componentSource).toContain("value.slice(1)");
```

This directly addresses the WEB1 regression: collection transformations are rejected, while harmless string capitalization using `value.slice(1)` is permitted.

## Local hardening completed before the product commit

- TypeScript `transpileModule` diagnostic count: `0` for both TSX files
- UTF-8/static source audit: passed
- Six-column structure audit: passed
- `aria-label="Market health"`: present
- Supplied-order rendering via `rows.map((row) => ...)`: present
- Direct `rows.sort/toSorted/slice/splice/reverse`: absent
- `value.slice(1)`: present and explicitly allowed by the focused guard
- Stable `row.key` identity test: present
- Score-first tone-precedence cases: present
- Observed/configured/awaiting/unavailable coverage: present
- Forbidden dependency/ownership guards: present

## Gates

### Product-local full gates

Unavailable in this isolated runner because the repository could not be cloned: direct GitHub network access was unavailable and GitHub CLI was not installed. Therefore these commands were not claimed as locally executed:

- `cd web && npm ci`
- `npm run test:run`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run bundle:check`
- `cargo fmt --check`
- `cargo check --all-targets --all-features`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --all-targets --all-features`

### Remote exact-head CI

No workflow run was available for `e3230713fcc5256dd1d92651f9ba54bdb4ec1c8a` at the hard stop. Remote read-back had already proved the test blob invalid, so the contract required stopping rather than opening a PR to trigger pull-request CI.

### Current merge-tree CI

Unavailable because no PR was opened after the failed remote read-back.

## PR

No draft PR was opened. This is intentional: the contract requires valid remote blobs and complete green hardening before PR publication.

## Ticker proof

The exact base-to-head comparison contains only the two leased Market Health desktop table paths. No ticker component, ticker model, ticker test, layout shell, or other frontend path changed.

## Forbidden actions not taken

- No merge
- No amend/reset/rebase
- No force-push
- No branch rewrite or deletion/recreation
- No rejected WEB1 branch mutation
- No `DashboardPage.tsx` wiring
- No P3-MP15 work

## Required recovery

A new immutable recovery contract and a new replacement branch are required. The existing branch and commit must remain preserved as failed evidence; repairing them would violate the current WEB2 history constraints.
