# P3-MP11-WEB1 — Market Health Desktop Table Delivery Report

## Delivery status

`BLOCKED_FRONTEND_TEST`

The product implementation, one-commit branch publication, and draft PR creation completed. Post-commit verification found a deterministic false-positive in one focused source-guard assertion. The assertion rejects every `.slice()` call in the component source and therefore also rejects the required current status-capitalization implementation `value.slice(1)`, even though the component does not slice or limit rows. GitHub Actions confirms that the frontend job fails at the test step. The immutable lease forbids amend, reset, rebase, history rewrite, force-push, squash, or an additional normal product commit, so the defect was not concealed or repaired by violating the contract.

## Contract identity

- Contract repository: `progeranna/connector`
- Contract path: `signalguard-rs/phase-3/prompts/P3-MP11-WEB1.md`
- Contract commit: `0945af06bc5153336412d73bb9ca6dbc87927ac0`
- Product repository: `progeranna/signalguard-rs`
- Product branch: `p3/mp11-market-health-desktop`
- Required PR base: `refactor/dashboard-modules`
- Exact assigned base: `01bf6edae2795a5e118148ad7b291a285a70a8d8`
- Final product head: `3b5e3e0b5efd1ca5291c08cb0d7f9e3ab36ea596`
- Product commit message: `feat(ui): add market health desktop table`
- Draft PR: `#33` — `https://github.com/progeranna/signalguard-rs/pull/33`

## Initial branch/base verification

Before implementation:

- `refactor/dashboard-modules` compared identical to `01bf6edae2795a5e118148ad7b291a285a70a8d8` (`ahead 0`, `behind 0`).
- `p3/mp11-market-health-desktop` compared identical to `01bf6edae2795a5e118148ad7b291a285a70a8d8` (`ahead 0`, `behind 0`).
- Both authorized product paths were absent.

## Final commit and divergence

- Final head: `3b5e3e0b5efd1ca5291c08cb0d7f9e3ab36ea596`
- Parent: `01bf6edae2795a5e118148ad7b291a285a70a8d8`
- Final divergence from assigned base: `ahead 1`, `behind 0`, `total commits 1`.
- Push/ref update used a non-force update.
- Draft PR base SHA: `01bf6edae2795a5e118148ad7b291a285a70a8d8`.
- Draft PR head SHA: `3b5e3e0b5efd1ca5291c08cb0d7f9e3ab36ea596`.

## Exact changed paths

Only these product paths were added:

1. `web/src/features/dashboard/MarketHealthDesktopTable.tsx` — 254 additions
2. `web/src/features/dashboard/MarketHealthDesktopTable.test.tsx` — 466 additions

No existing product file changed. Total PR diff: 720 additions, 0 deletions, 2 files.

Committed blob identities:

- Component: `e57f8562f3b23a2a1f364f7709c49bf3f140b589`
- Test: `a2cf3c0394088a14eca7692e04f974c7b14985fb`

## Exported component API

```ts
export type MarketHealthDesktopTableProps = Readonly<{
  rows: readonly MarketHealthPreviewRow[];
  onOpenSymbolDetail: (symbol: string) => void;
}>;

export function MarketHealthDesktopTable(
  props: MarketHealthDesktopTableProps,
): JSX.Element;
```

The component consumes accepted `MarketHealthPreviewRow` values only. It does not accept `DashboardSummary` or `DashboardSymbolSummary`.

## Exact presentation parity

### Desktop root and table

- Root classes preserved exactly: `hidden w-full min-w-0 max-w-full overflow-x-auto overscroll-x-contain border-y border-white/10 lg:block`.
- Table accessibility preserved exactly: `aria-label="Market health"`.
- Fixed layout classes preserved: `w-full table-fixed border-collapse text-left`.
- Desktop-only behavior remains hidden below `lg` and shown at `lg`.
- Horizontal overflow and overscroll behavior remain unchanged.

### Columns

Exact order and widths:

| Column | Width |
|---|---:|
| Market | 18% |
| Health Score | 22% |
| Last Price | 16% |
| Spread | 11% |
| Trades/min | 14% |
| Status | 19% |

### Row interaction and keyboard behavior

- Each table row is focusable with `tabIndex={0}`.
- Each table row has `role="button"`.
- Accessible label is exactly `Open <SYMBOL> market detail`.
- Click invokes `onOpenSymbolDetail(row.symbol)`.
- Enter invokes the same callback and calls `preventDefault()`.
- Space invokes the same callback and calls `preventDefault()`.
- Unrelated keys do not activate the row.
- Hover, focus ring, transition, row separator, and last-row classes are copied from the assigned-base table.

### Identity

- Rows render directly with `rows.map(...)`.
- React identity is `key={row.key}`.
- Array index is not accepted or used as identity.
- Duplicate symbol rows with distinct accepted keys remain distinct.

### Symbol and View presentation

- Current symbol typography is preserved.
- The conditional `View` copy remains hidden until `2xl` and uses the assigned-base classes.

### Health Score

- Displayed number is `score` or `—` when null.
- Bar width is `0%` for null; otherwise `Math.max(score, 4)%`.
- Tone precedence is preserved exactly:
  - healthy status or score >= 80 -> emerald;
  - degraded status or score >= 50 -> amber;
  - unhealthy status or score < 50 -> rose;
  - otherwise neutral/slate.
- Current number and bar classes are preserved.

### Metric formatting

- Last Price: falsy/absent -> `—`; otherwise preserve the supplied decimal string.
- Spread: null/undefined/NaN -> `—`; otherwise `toFixed(2)%`.
- Trades/min: null/undefined/NaN -> `—`; otherwise shared `formatCompactNumber`.
- Numeric zero is preserved as data: score `0`, price string `0.0000`, spread `0.00%`, trades/min `0`.

### Status wording and classes

- Observed: capitalize current health status or `Unknown`.
- Configured: `Configured`.
- Awaiting: `Awaiting data`.
- Unavailable: `Unavailable`.
- `StatusBadge` and `toStatusTone(..., "neutral")` are reused, preserving current status classes and tone mapping.

## Row-state matrix

| Accepted state | Health Score | Last Price | Spread | Trades/min | Status |
|---|---|---|---|---|---|
| `observed` | current score/bar | formatted | formatted | formatted | capitalized health status or `Unknown` |
| `configured` | visually empty | visually empty | visually empty | visually empty | `Configured` |
| `awaiting` | visually empty | visually empty | visually empty | visually empty | `Awaiting data` |
| `unavailable` | visually empty | visually empty | visually empty | visually empty | `Unavailable` |

## Accepted-model ownership proof

- Component type-imports `MarketHealthPreviewRow` from `./marketHealthPreviewModel`.
- Accepted model blob remained unchanged at `d98d5af769e6f9ca74ef84c686a633511aea4336`.
- Component does not sort, reverse, reorder, limit, or slice `rows`.
- Component does not inspect `hiddenCount` or `hasMore`.
- Component does not recalculate accepted identity.
- Component does not fabricate metrics.
- Component does not synthesize Demo/Live fallback.
- Component does not change source or availability.

The component contains `value.slice(1)` only to preserve the current observed-status capitalization logic. It does not call `rows.slice(...)` or any other row transform.

## Focused test coverage authored

Explicit RTL/Vitest assertions cover:

1. exact table aria label and column order;
2. exact colgroup widths and fixed-layout classes;
3. desktop-only root classes;
4. supplied-order rendering and more-than-preview-limit input;
5. click callback symbol;
6. Enter activation and default prevention;
7. Space activation and default prevention;
8. exact accessible row label and button-row semantics;
9. observed score/status/price/spread/trades including numeric zero;
10. score tone and bar-width rules;
11. configured/awaiting/unavailable empty metrics and exact status wording;
12. no input mutation or row limiting;
13. accepted `row.key` identity and no array-index identity;
14. no section title, loading/empty shell, view-all button, modal, or mobile markup;
15. no Wave 4 vocabulary or tooltip;
16. no query/API/router/popup/time/random/network dependency.

Snapshots are not used as the sole evidence.

## Verification results

### Passed

- Initial branch and base identity: PASS.
- Exact one-commit final divergence: PASS.
- Exact two-path product scope: PASS.
- Non-force branch update: PASS.
- `git diff --check` over the authored product files: PASS.
- Local Git blob hashes match committed GitHub blob hashes: PASS.
- TypeScript isolated transpile/syntax check for both authored TSX files: PASS.
- Static accepted-model import audit: PASS.
- Direct `rows.map` and `row.key` identity audit: PASS.
- Row ordering/limiting audit scoped to `rows`: PASS.
- Raw-summary dependency audit: PASS.
- Query/API/router/popup/time/random/network dependency audit: PASS.
- Demo/Live fallback and Wave 4 vocabulary audit: PASS.
- GitHub Actions dependency installation: PASS.
- GitHub Actions Rust/global job: PASS, including rustfmt, generated API contract, OpenAPI validation, cargo check, clippy, tests, replay target discovery, Docker Compose validation, and demo/smoke shell syntax.

### Failed

- GitHub Actions frontend test step: FAIL.
- Deterministic cause: the authored source guard uses `/\.(?:sort|toSorted|slice|splice|reverse)\s*\(/` against the entire component source. It therefore matches the assigned-base-compatible string operation `value.slice(1)` in status capitalization. The component itself does not slice rows.

### Skipped by CI after frontend test failure

- Frontend typecheck.
- Frontend lint.
- Frontend build.
- Frontend bundle-budget check.

### Unavailable locally

- `npm` focused/full frontend execution was unavailable because this runtime had no cached frontend dependency set and the configured registry returned HTTP 503 during installation.
- Browser-based responsive visual inspection was unavailable in this runtime.
- Because the focused CI test failed first, no authoritative full frontend typecheck/lint/build/bundle results are available for this head.

## Forbidden-path and ticker proof

Unchanged blobs at final product head:

- `web/src/pages/DashboardPage.tsx`: `deb2eee919938d3b1807e353d309c046b08ba6f5`
- `web/src/app/GlobalMarketTicker.tsx`: `727f591706a60327b3b219f3287b153a06d1160d`
- `web/src/shared/styles/globals.css`: `65ebeb30cc78704fde5aa3cbd836c68c492772a9`
- `web/src/features/dashboard/marketHealthPreviewModel.ts`: `d98d5af769e6f9ca74ef84c686a633511aea4336`
- `web/package.json`: `c658f42c6783988628be7fc18ccf54804bb06018`

The compare result contains only the two authorized added paths. Therefore no page wiring, accepted model, CSS, package/lock/config, backend, ticker, API, router, popup, modal, mobile, deployment, script, or P3-MP15 path changed.

## Blocker and required next action

The current head is not merge-ready because frontend CI is red. Repair requires changing the focused source guard to reject row transforms specifically (for example, `rows.slice(...)`) rather than rejecting all string `.slice()` usage, or changing the capitalization implementation while retaining exact visible behavior. Either repair requires a product-history mutation or an additional product commit, both forbidden by this immutable worker contract after its required single commit was published. A new authorized follow-up lease is required.
