# P3-CHECKPOINT-2R Blocker Report

Status: `P3_CHECKPOINT_2R_BLOCKED`

Validation date: 2026-08-03

## Authority and scope

This report records the dedicated local, read-only validation of `P3-CHECKPOINT-2R — Combined modal-only recovery validation`.

The authoritative contract was read completely and authenticated before validation:

- repository: `progeranna/connector`
- path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-LOCAL.md`
- commit: `4a33c95993a8b9791883848e2c096f7d80ec0515`
- verified blob: `72467c9adae47853586fc4665cbffe93dabfbebd`
- verified status: `P3_CHECKPOINT_2R_LOCAL_VALIDATION_AUTHORIZED`

The current execution entry point was also authenticated and read completely:

- path: `signalguard-rs/phase-3/control/CURRENT_EXECUTION.md`
- connector commit: `4682385ac874aaca715dfb035ef6d6759227d600`
- verified blob: `3a4dda78aa995c9f1799a65ca0c18cd22f71e9c4`
- verified status: `P3_CHECKPOINT_2R_LOCAL_VALIDATION_AUTHORIZED`

The complete required control set and all three accepted MP18R/MP20R integration reports were read before product validation. The contradictory MP20R chat marker was treated as superseded by the durable integration and terminal-correction reports. MP20R was not rerun, recreated, reverted, or reintegrated.

## Exact product and CI identity

Product repository: `progeranna/signalguard-rs`

Target branch: `refactor/dashboard-modules`

Authenticated HTTPS fetch and GitHub branch read-back both resolved the target exactly:

- integrated commit: `8bbef01d7d9979c4996954171a0e7c3748f02538`
- integrated tree: `d8f9e71e7aec5fcf7b472011a68247a6df42bbac`

Validation used a separate detached worktree at that exact commit. The worktree was clean before command execution and clean after all validation artifacts were removed.

Accepted CI evidence was independently read back:

- workflow: `CI`
- run: `30844622613`
- event: `pull_request`
- status: `completed`
- conclusion: `success`
- tested synthetic merge recorded by the accepted integration evidence: `9dd56162af7a23cc18a21dab8fe83428d521d667`
- synthetic tree independently verified: `d8f9e71e7aec5fcf7b472011a68247a6df42bbac`
- integrated tree independently verified: `d8f9e71e7aec5fcf7b472011a68247a6df42bbac`

The target identity gate passed. An initial SSH fetch attempt closed before updating the local tracking ref; it was discarded as stale local state and repeated successfully over authenticated HTTPS before validation began.

## Command gates

Every prescribed command gate passed.

Frontend, from `web/`:

| Command | Result |
|---|---|
| `node --test scripts/check-bundle-size.test.mjs` | PASS — 25/25 tests |
| `npm run test:run` | PASS — 44 files, 603 tests |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS — zero warnings allowed |
| `npm run build` | PASS — 134 modules transformed |
| `npm run bundle:check` | PASS — 25/25 policy tests; 389,451-byte initial/largest/total JS |

Rust and repository-wide gates, from the repository root:

| Command | Result |
|---|---|
| `cargo fmt --check` | PASS |
| `cargo run --quiet --bin export-api-contract -- --check` | PASS |
| `cargo run --quiet --bin export-api-contract -- --validate` | PASS |
| `cargo check` | PASS |
| `cargo clippy --all-targets --all-features -- -D warnings` | PASS |
| `cargo test` | PASS — 379 passed, 0 failed, 3 environment-dependent library tests ignored; service-dependent integration tests ignored as declared |
| `cargo test --test replay_e2e` | PASS — target discovered; 2 service-dependent tests ignored as declared |
| `docker compose config` | PASS |
| `docker compose --profile app config` | PASS |
| `bash -n scripts/demo-replay.sh` | PASS |
| `bash -n scripts/smoke.sh` | PASS |

Bundle measurements remained within the unchanged budgets:

- initial JS: 389,451 / 409,600 bytes
- largest JS: 389,451 / 409,600 bytes
- total JS: 389,451 / 414,720 bytes

## Manifest, lockfile, and budget integrity

The following SHA-256 values were recorded after command execution and matched the exact committed checkout. No corresponding file changed:

| File | SHA-256 |
|---|---|
| `Cargo.toml` | `f53610c4a491f126fba19888c24b0fba859e0265586a400e8476335bb29bb27a` |
| `Cargo.lock` | `9597ab08c34d06b6e9cc16c6781bf3e435a7fecdcf8f6cc2f9ebf7b208fd301f` |
| `web/package.json` | `a993dca0278f26be9f13f441d819e47ea36e576aa929348803a12e1b9ff376f4` |
| `web/package-lock.json` | `aca14d4436c6bddaaf1c4b944fa3707eb4ec5cbb0043057e649feb7543c531e4` |
| `web/bundle-size-budget.json` | `bea1504b7c10d07cc3e71ffcb34647bf65c88d1fb75e4db0d3904dfc98e1ee96` |
| `web/scripts/check-bundle-size.mjs` | `a71729142cc34ae606f9d4811e4aefc3a94bdd95ba2f87f3895e23a36dca4572` |
| `web/scripts/check-bundle-size.test.mjs` | `3267baf17ead89322023183c789ec9097afd98a1a8319f52cdb8ef4f49e0f9e0` |

The prescribed build generated only transient ignored build output. The external dependency link and generated `web/dist` and `target` trees were removed after execution. Final `git status --porcelain=v1` and `git status --short --ignored` were empty. Final HEAD/tree remained the exact authorized identity.

## Browser blocker

The required browser matrix cannot be completed because both required `View all` entry points are unreachable with the real deterministic Demo response at the accepted product identity.

The frontend preview limits are both 7:

- `MARKET_HEALTH_PREVIEW_LIMIT = 7`
- `RECENT_ANOMALIES_PREVIEW_LIMIT = 7`

The real Demo dashboard response contains exactly:

- 7 markets
- 3 recent anomalies

Both section actions render only when `preview.hasMore` is true. Therefore the Dashboard renders zero buttons named `View all`. This prevents both required flow families:

1. All Markets → Symbol Detail → exact Anomaly Detail → Back.
2. All Anomalies → exact Anomaly Detail → Back.

The failure was reproduced deterministically in production preview for Demo/BTCUSDT and Demo/ETHUSDT at both `1440×900` and `390×844`. Since there is no activatable control, both pointer and keyboard validation are blocked. No route change occurred during reproduction. Console errors, page errors, and unhandled-rejection/page-error events were zero in all four accepted reproductions.

The checkpoint stopped at this mandatory browser-flow failure. The remaining modal lifecycle matrix, rapid switching matrix, redirect matrix, 16-shot success inventory, and complete Live matrix are not claimed as validated.

## Exact reproduction

1. Check out detached commit `8bbef01d7d9979c4996954171a0e7c3748f02538`.
2. Start isolated PostgreSQL and Redis instances, apply the repository migrations, and start the backend on an isolated port using the repository replay input.
3. Build the frontend and start `vite preview` on an isolated port.
4. Open `/dashboard` at `1440×900` or `390×844`.
5. Keep `Demo Mode`; select either `BTCUSDT` or `ETHUSDT`.
6. Inspect the Market Health and Recent Anomalies headings and attempt pointer or keyboard activation of `View all`.

Expected:

- a visible, focusable All Markets entry point and a visible, focusable All Anomalies entry point so the two mandatory checkpoint flow families can be exercised.

Actual:

- neither entry point exists in the accessibility tree or DOM; the count of buttons named `View all` is zero.

## Screenshot evidence

Screenshots were written outside both repositories under the isolated validation directory and were not added to the product repository.

| Filename | Mode | Symbol | Viewport | Flow step | SHA-256 |
|---|---|---|---|---|---|
| `checkpoint-2r-blocker-demo-btcusdt-desktop-1440x900.png` | Demo | BTCUSDT | 1440×900 | Dashboard; required View-all controls absent | `836797b418bd9df6fbd96b5a2edb02d3274f0a61e52e01cab98ce8e668c8206c` |
| `checkpoint-2r-blocker-demo-ethusdt-desktop-1440x900.png` | Demo | ETHUSDT | 1440×900 | Dashboard; required View-all controls absent | `c2a4d44216be4f3d75bb868daca71ce135584d633684facc58dd7c7b30ab99c3` |
| `checkpoint-2r-blocker-demo-btcusdt-mobile-390x844.png` | Demo | BTCUSDT | 390×844 | Dashboard; required View-all controls absent | `1a1a4d543ac73904a1b004aaed39f39429988f9c27b886ed48cbd0d95833a883` |
| `checkpoint-2r-blocker-demo-ethusdt-mobile-390x844.png` | Demo | ETHUSDT | 390×844 | Dashboard; required View-all controls absent | `1bbc660b0900dcb906ef6b2107c5af966e05c8b6d69b687ebff09f73f3a90c55` |

Input method for every row: pointer and keyboard are both blocked because the required trigger is absent.

## Classification and proposed recovery lease

Classification: `PRE_EXISTING_ACCEPTANCE_REACHABILITY / ENVIRONMENTAL-DATA CONTRACT`.

This is not classified as an MP18R exact-anomaly-identity regression or an MP20R route-presentation-residue regression. The modal implementations exist, but the accepted real Demo dataset cannot expose their required entry points under the current `hasMore` condition. Reproduction is deterministic.

Smallest proposed recovery lease, subject to separate independent authorization:

- production: `web/src/pages/DashboardPage.tsx` only, limited to making the All Markets and All Anomalies controls reachable whenever their corresponding collections are non-empty;
- tests: `web/src/pages/DashboardPage.test.tsx` and `web/src/pages/DashboardPage.popup.test.tsx` only, limited to control reachability at counts below, equal to, and above the preview limit and preservation of the existing modal flows;
- follow-up: rerun this checkpoint from the separately accepted integrated recovery commit with real Demo and real unavailable/empty or available Live state.

No fix was implemented. No product branch, product commit, product push, or product PR was created.

## Authorization boundary

- zero repository-controlled product files were changed;
- no product dependency, lockfile, API artifact, route, CSS, modal logic, ticker behavior, test, or bundle budget was changed;
- `STATUS.md` was not changed;
- Semantic Bridge 01, Semantic Bridge 02, and semantic Wave 4 did not begin;
- MP18R and MP20R were not rerun or reintegrated.

Terminal status: `P3_CHECKPOINT_2R_BLOCKED`
