# P3-CHECKPOINT-2R-RERUN Blocker Report

Status: `P3_CHECKPOINT_2R_RERUN_BLOCKED`

Validation date: 2026-08-08

## Authority and scope

This report records the dedicated local, strictly read-only product validation of `P3-CHECKPOINT-2R-RERUN — Combined modal-only recovery validation after R1`.

The immutable contract was fetched over authenticated HTTPS, read completely, and verified before product validation:

- repository: `progeranna/connector`
- path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-RERUN-LOCAL.md`
- contract commit: `089aabefe712654258e0b339815422b895a13c04`
- verified contract blob: `42ed3a70ea82a96a8ab0d78a96e6d5d01feb9f3c`
- verified status: `P3_CHECKPOINT_2R_RERUN_LOCAL_VALIDATION_AUTHORIZED`

The complete required control, original Checkpoint 2R blocker, R1 implementation/review/integration contract, and R1 integration evidence set was read before product validation. The latest published `CURRENT_EXECUTION.md` and `STATUS.md` were also read from connector `main` at `903ffab265238da1ab7f01e74b55b194e60dfeb8`; both authorize only this rerun and keep Bridge 01/02 and Wave 4 blocked.

## Exact product identity

Product repository: `progeranna/signalguard-rs`

Target branch: `refactor/dashboard-modules`

An authenticated HTTPS fetch resolved the remote target exactly:

- commit: `9cedbeb9c9e5e59ad634123a3b2d6217555a5c96`
- tree: `9bdfdcf331ef08cdeaf8f21a8ab66adee3092fe8`
- ordered merge parent 1: `8bbef01d7d9979c4996954171a0e7c3748f02538`
- ordered merge parent 2: `f793b8447076a9feb8227447c2b851622475ef7c`

Validation used a new detached worktree created solely for this rerun:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-checkpoint-2r-rerun-9cedbeb`

No MP18R, MP20R, R1, prior Checkpoint 2R, or `p35-manual-qa` worktree was reused.

The detached worktree was clean before validation. After validation, the dependency symlink and generated `web/dist` output were removed, isolated services were stopped and deleted, and every isolated port was free. Final evidence:

- HEAD: `9cedbeb9c9e5e59ad634123a3b2d6217555a5c96`
- tree: `9bdfdcf331ef08cdeaf8f21a8ab66adee3092fe8`
- `git status --porcelain=v1 --untracked-files=all`: empty
- `git status --short --ignored`: empty
- no tracked, untracked, ignored, generated, or service residue remained

## Accepted PR #71 and CI identity

Durable GitHub evidence was independently read back:

- PR: `#71`, closed and merged
- final merge: `9cedbeb9c9e5e59ad634123a3b2d6217555a5c96`
- synthetic merge: `3b7f85eb12f78a729206d61a6a4e86b29f6d0961`
- synthetic tree: `9bdfdcf331ef08cdeaf8f21a8ab66adee3092fe8`
- synthetic ordered parents: exact base `8bbef01d7d9979c4996954171a0e7c3748f02538`, exact worker `f793b8447076a9feb8227447c2b851622475ef7c`
- final tree and synthetic tree: identical
- workflow run: `31252099540`, event `pull_request`, attempt 1, completed successfully
- frontend job: `93089901530`, success
- rust job: `93089901697`, success
- both job logs fetched `refs/pull/71/merge` and recorded checkout HEAD `3b7f85eb12f78a729206d61a6a4e86b29f6d0961`
- frontend CI: 44 files and 612 tests passed
- CI bundle: 389,453 bytes initial/largest/total; unchanged budgets passed

The merged PR synthetic ref is no longer advertised after merge, so the synthetic commit/tree/parents were verified through GitHub's durable Git commit API and the exact checkout SHA was verified from both preserved job logs.

## Command gates

Every prescribed command gate passed before browser validation.

Frontend, from `web/`:

| Command | Result |
|---|---|
| `node --test scripts/check-bundle-size.test.mjs` | PASS — 25/25 tests |
| `npm run test:run` | PASS — 44/44 files, 612/612 tests |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS — zero warnings allowed |
| `npm run build` | PASS — 134 modules transformed |
| `npm run bundle:check` | PASS — policy 25/25; budget PASS |

Rust/global, from repository root:

| Command | Result |
|---|---|
| `cargo fmt --check` | PASS |
| `cargo run --quiet --bin export-api-contract -- --check` | PASS |
| `cargo run --quiet --bin export-api-contract -- --validate` | PASS |
| `cargo check` | PASS |
| `cargo clippy --all-targets --all-features -- -D warnings` | PASS |
| `cargo test` | PASS — 379 passed, 0 failed, 3 service-dependent library tests ignored; declared service-dependent integration targets ignored |
| `cargo test --test replay_e2e` | PASS — target discovered; 2 declared service-dependent tests ignored |
| `docker compose config` | PASS |
| `docker compose --profile app config` | PASS |
| `bash -n scripts/demo-replay.sh` | PASS |
| `bash -n scripts/smoke.sh` | PASS |

Bundle budgets were unchanged and passed:

- initial JS: 389,453 / 409,600 bytes
- largest JS: 389,453 / 409,600 bytes
- total JS: 389,453 / 414,720 bytes

## Manifest, lockfile, budget, and API integrity

Pre-run and post-cleanup SHA-256 inventories were byte-identical:

| File | SHA-256 |
|---|---|
| `Cargo.toml` | `f53610c4a491f126fba19888c24b0fba859e0265586a400e8476335bb29bb27a` |
| `Cargo.lock` | `9597ab08c34d06b6e9cc16c6781bf3e435a7fecdcf8f6cc2f9ebf7b208fd301f` |
| `web/package.json` | `a993dca0278f26be9f13f441d819e47ea36e576aa929348803a12e1b9ff376f4` |
| `web/package-lock.json` | `aca14d4436c6bddaaf1c4b944fa3707eb4ec5cbb0043057e649feb7543c531e4` |
| `web/bundle-size-budget.json` | `bea1504b7c10d07cc3e71ffcb34647bf65c88d1fb75e4db0d3904dfc98e1ee96` |
| `web/scripts/check-bundle-size.mjs` | `a71729142cc34ae606f9d4811e4aefc3a94bdd95ba2f87f3895e23a36dca4572` |
| `web/scripts/check-bundle-size.test.mjs` | `3267baf17ead89322023183c789ec9097afd98a1a8319f52cdb8ef4f49e0f9e0` |
| `contracts/web-console.openapi.json` | `4b9448b1e5a6318046effab182b3adbb7a502891f284eff5d180cb044f22e42f` |

No dependency, manifest, lockfile, API artifact, bundle policy, or budget drift occurred.

## Isolated real-browser environment

The accepted product ran from the exact detached commit with a production build and Vite production preview. A local reverse proxy forwarded real `/api` traffic to the accepted backend; no frontend data was mocked or injected.

Isolation:

- compose project: `p3c2rrerun`
- PostgreSQL: `127.0.0.1:55439`
- Redis: `127.0.0.1:56389`
- backend: `127.0.0.1:18089`
- Vite production preview: `127.0.0.1:15189`
- browser-facing validation proxy: `127.0.0.1:15190`

The environment did not use or modify ports 5173, 8080, 5432, or 6379 and did not touch `p35-manual-qa`.

Real backend responses were confirmed before interaction:

- Demo: exactly 7 markets and 3 recent anomalies
- Demo BTCUSDT exact anomaly UUID used: `3d6ce59a-fd8b-4180-9864-08933b9d4168`
- Live: actual observed BTCUSDT and ETHUSDT state with live-only UUIDs; no Demo substitution was introduced

The former R1 blocker passed its early regression gate in the first core cell:

- Market Health `View all`: visible, enabled, and keyboard focusable at real Demo cardinality 7
- Recent Anomalies `View all`: visible, enabled, and keyboard focusable at real Demo cardinality 3
- All Markets: opened successfully
- the exact nested BTCUSDT anomaly detail opened successfully

## Blocking defect — All Markets Back focus identity

The complete browser matrix cannot be accepted because the required All Markets nested Back flow restores the correct parent modal but not the exact visible originating market row/card.

### Exact reproduction — desktop

Configuration:

- mode: Demo
- symbol: BTCUSDT
- viewport: `1440×900`
- route: `/dashboard`
- input sequence: Enter on Market Health `View all` → Space on BTCUSDT All Markets row → Enter on exact anomaly row → Enter on `Back to symbol detail` → Enter on `Back to all markets`

Observed sequence:

1. All Markets opened.
2. BTCUSDT Symbol Detail opened with identity `demo:BTCUSDT:symbols`.
3. exact Anomaly Detail opened for UUID `3d6ce59a-fd8b-4180-9864-08933b9d4168`.
4. `Back to symbol detail` restored BTCUSDT Symbol Detail and correctly focused the exact visible UUID row.
5. `Back to all markets` restored All Markets, but focus landed on the visible `Close` button.
6. the exact visible `Open BTCUSDT market detail` row did not receive focus.
7. pathname remained `/dashboard`.

Expected: All Markets restores with focus on the exact visible originating BTCUSDT row.

Actual: All Markets restores with focus on `Close`.

### Exact reproduction — mobile

Configuration:

- mode: Demo
- symbol: BTCUSDT
- viewport: `390×844`
- route: `/dashboard`
- input: pointer card activation through All Markets and nested detail; Enter for both Back actions

The same result reproduced: the exact nested anomaly Back restored the visible BTCUSDT anomaly card correctly, then Back to All Markets focused `Close` instead of the visible BTCUSDT market card.

### Determinism and classification

- deterministic: yes
- reproduced in the original matrix attempt and in two targeted reruns
- reproduced at both required viewport sizes
- classification: `MP18R focus-restoration regression`
- not an MP20R presentation regression
- not an R1 `View all` reachability regression; both R1 entry points are now reachable
- not caused by mocked or fabricated data

The controller returns from Symbol Detail with `{ type: "symbols" }` but does not preserve the originating market trigger identity for initial focus in the remounted All Markets modal. This is a read-only diagnosis; no fix was implemented.

Smallest proposed recovery lease, subject to separate independent authorization:

- production: `web/src/pages/DashboardPage.tsx` only, limited to preserving the exact All Markets origin symbol and restoring focus through the existing visible-element selection logic;
- tests: `web/src/pages/DashboardPage.popup.test.tsx` only, adding desktop-row and mobile-card assertions for All Markets → Symbol Detail → exact Anomaly Detail → Back → Back exact visible focus restoration;
- no routes, CSS, resources, modal copy, preview limits, ticker, dependencies, or bundle budgets.

## Console observation

The targeted desktop run also recorded one browser console error from an automatic request to `http://127.0.0.1:15190/favicon.ico`, which returned 404. Page errors and unhandled promise rejections were zero. The mobile run in the same browser process recorded zero console errors because the browser had already cached the favicon result.

This pre-existing missing-static-asset observation is not causal to the focus defect, but it is preserved because the contract requires a zero-unexpected-console-error audit. If independent review treats this browser-generated request as in-scope, the smallest separate lease is `web/index.html` or one `web/public` favicon asset plus a static production-preview smoke assertion. No such change was made.

## Screenshot evidence

Screenshots were captured outside the product and connector repositories. The blocker stopped the success-only 16-shot matrix; no complete-matrix or success inventory is claimed.

| Filename | Mode | Symbol | Viewport | Input | Flow step | SHA-256 |
|---|---|---|---|---|---|---|
| `checkpoint-2r-rerun-blocker-demo-btcusdt-desktop-1440x900-all-markets-back-focus.png` | Demo | BTCUSDT | 1440×900 | Enter/Space keyboard sequence | Post-Back All Markets; actual visible focus is Close | `81f7de6924e6306dd767f972dba566f653508f317ec90689c3215ab98f5e3d50` |
| `checkpoint-2r-rerun-blocker-demo-btcusdt-mobile-390x844-all-markets-back-focus.png` | Demo | BTCUSDT | 390×844 | pointer cards + Enter Back | Post-Back All Markets; actual visible focus is Close | `793ec1f34db02aff62ec01da65797a4823a945fab97218136c271376bbba27d0` |

## Matrix disposition

The rerun stopped at the first mandatory focus-restoration failure in Demo/BTCUSDT/desktop and then performed only the narrow desktop/mobile determinism check required for defect evidence.

Therefore the following are not claimed as complete:

- all 8 Demo/Live × BTCUSDT/ETHUSDT × desktop/mobile cells;
- complete All Anomalies lifecycle coverage;
- complete rapid mode/symbol replacement coverage;
- complete redirect matrix;
- at least 16 success screenshots;
- full zero-console-error acceptance.

Unit/component coverage and passing command gates are not substituted for the missing real-browser acceptance.

## Product write boundary and continuation

- zero repository-controlled product or test files were modified;
- no product branch, commit, push, PR, or merge was created;
- no product defect was fixed;
- no dependency or lockfile was updated;
- no route, modal logic, CSS, ticker, API artifact, bundle budget, test, or Demo/Live data was changed;
- `CURRENT_EXECUTION.md` and `STATUS.md` were not modified;
- Bridge 01, Bridge 02, Wave 4, and later work did not begin;
- the isolated validation services and transient build artifacts were removed;
- the detached product worktree is clean at the exact authorized commit/tree.

Terminal status: `P3_CHECKPOINT_2R_RERUN_BLOCKED`
