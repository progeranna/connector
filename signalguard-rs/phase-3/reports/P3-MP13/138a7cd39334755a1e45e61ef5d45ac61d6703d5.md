# P3-MP13-WEB1 Delivery Report

## Delivery identity

- Contract: `signalguard-rs/phase-3/prompts/P3-MP13-WEB1.md`
- Immutable contract commit: `008b0d8f0b157f7099263760fed03a923569b73b`
- Product repository: `progeranna/signalguard-rs`
- Assigned branch: `p3/mp13-recent-anomalies-desktop`
- Required PR base: `refactor/dashboard-modules`
- Immutable assigned base: `01bf6edae2795a5e118148ad7b291a285a70a8d8`
- Product commit: `138a7cd39334755a1e45e61ef5d45ac61d6703d5`
- Product commit message: `feat(ui): add recent anomalies desktop table`
- Draft PR: `#37` — `https://github.com/progeranna/signalguard-rs/pull/37`
- PR head: `p3/mp13-recent-anomalies-desktop`
- PR base: `refactor/dashboard-modules`
- PR state at publication: open, draft, mergeable, unmerged

## Preflight and publication proof

Initial preflight verified both the assigned worker branch and `refactor/dashboard-modules` at the immutable assigned base. Each compared `ahead 0 / behind 0` against `01bf6edae2795a5e118148ad7b291a285a70a8d8`.

The product candidate was created as one normal commit whose sole parent is the immutable assigned base. Before the branch ref moved, the detached candidate was compared against that base and proved:

- status: ahead;
- ahead: `1`;
- behind: `0`;
- total commits: `1`;
- changed files: exactly `2`, both authorized additions.

The assigned branch then advanced through a normal fast-forward ref update with `force=false`. No amend, rebase, reset, merge, squash, force-push, update-branch operation, or history rewrite was used.

Final immutable-base comparison remains:

- ahead: `1`;
- behind: `0`;
- total candidate commits: `1`;
- merge base: `01bf6edae2795a5e118148ad7b291a285a70a8d8`.

After preflight, the moving phase branch advanced independently to `93a870010730c458417ccfff392cb97aff23d6c9`. The worker candidate was preserved unchanged under the moving-base rule. Final comparison against the current phase branch is diverged:

- candidate ahead: `1`;
- candidate behind: `1`;
- merge base: `01bf6edae2795a5e118148ad7b291a285a70a8d8`.

GitHub reports the draft PR mergeable. PR CI checked synthetic merge commit `4cbb4f672ff1a666c68aa8c905b2e52d5cb3b9ae`, proving compatibility with the advanced phase branch without modifying the worker commit.

## Exact product paths

Exactly two new product paths were added and no existing product path changed:

1. `web/src/features/dashboard/RecentAnomaliesDesktopTable.tsx`
2. `web/src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx`

Remote blob proof:

- component blob: `3a5d8adbbf874b2c8645fad4e437ac44662a0117`;
- focused test blob: `6126314862a76d90127d459a65e06a1239ca274b`.

The immutable-base comparison and draft PR metadata both report one commit, two changed files, 757 additions, and zero deletions.

## Exported component API

The component module exports:

```ts
export type RecentAnomaliesDesktopTableProps = Readonly<{
  rows: readonly RecentAnomaliesPreviewRow[];
  onOpenSymbolDetail: (symbol: string) => void;
}>;

export function RecentAnomaliesDesktopTable(
  props: RecentAnomaliesDesktopTableProps,
): JSX.Element;
```

`RecentAnomaliesPreviewRow` is imported as a type from the accepted `./recentAnomaliesPreviewModel` module. The component does not accept a raw dashboard summary, raw anomaly DTO, mode/source value, modal controller, hidden-count metadata, or preview-limit value.

## Desktop presentation parity

The extracted component preserves the exact assigned-base desktop table presentation:

- wrapper: `hidden w-full min-w-0 max-w-full overflow-x-auto overscroll-x-contain border-y border-white/10 lg:block`;
- table: `aria-label="Recent anomalies"` and `w-full table-fixed border-collapse text-left`;
- six columns, in order: Time, Market, Type, Severity, Observed, Threshold;
- colgroup widths: `15%`, `16%`, `20%`, `19%`, `15%`, `15%`;
- current header, market, detector, severity, observed, threshold, hover, focus-visible, and separator classes;
- no mobile cards, section header, loading/empty shell, view-all action, modal rows, Context/message column, Detected-at column, source badge, combined active label, severity explanation, icon, or tooltip.

The detector cell renders the accepted `row.detectorLabel`. The severity cell renders the accepted `row.severityDescriptor.label` and uses the accepted descriptor tone to select the unchanged compact badge and observed-value classes.

## UUID, ordering, keyboard, and callback evidence

### UUID identity

- React identity is exactly `key={row.id}`.
- `row.id` is the accepted anomaly UUID from P3-MP08.
- No array index, row position, symbol, message, event time, created time, random value, or reconstructed identifier is used.
- The focused test reverses two rows and proves each UUID-keyed DOM row is retained across reordering.

### Ordering and ownership

- Rendering uses one direct `rows.map(...)` pass.
- Supplied row order is preserved exactly.
- Nine supplied rows all render; there is no local preview limit.
- Duplicate symbols are permitted and rendered independently.
- Frozen arrays and row objects render without mutation.
- Source checks prove no `.sort(...)`, `.slice(...)`, local `Set`, duplicate rejection, or throw policy.

### Pointer and keyboard activation

Each row preserves:

- `tabIndex={0}`;
- `role="button"`;
- exact accessible label `Open <SYMBOL> market detail`;
- click activation;
- Enter activation;
- Space activation;
- `preventDefault()` for Enter and Space;
- exact callback payload `onOpenSymbolDetail(row.symbol)`.

Focused DOM tests explicitly verify click, Enter, Space, default prevention, focusability, accessible label, callback count, and callback symbol.

## Complete anomaly formatting matrix

| Input/category | Observed output | Threshold output | Notes |
|---|---|---|---|
| Timestamp | `Intl.DateTimeFormat("en-US", { hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false })` | n/a | Uses truthy `eventTime`; otherwise `createdAt`; invalid text is preserved; empty input remains `Unavailable`. |
| `null`, `undefined`, or `NaN` numeric value | `—` | `—` | Numeric zero and negatives are not treated as absent. |
| `spread_spike` | `value.toFixed(3)%` | `value.toFixed(3)%` | Example: `0` → `0.000%`; `-1.25` → `-1.250%`. |
| `price_move` | `value.toFixed(3)%` | `value.toFixed(3)%` | Example: `-0.125` → `-0.125%`; `0` → `0.000%`. |
| `event_lag_spike` under 1000 ms | locale number, max 3 decimals, plus ` ms` | same | Example: `999.5` → `999.5 ms`. |
| `event_lag_spike` at/over 1000 ms | `(value / 1000).toFixed(1) s` | same | Example: `1500` → `1.5 s`. |
| `stale_data` | current duration policy | current duration policy | Example: `0` → `0 ms`; `-250` → `-250 ms`. |
| `quote_stuck` | current duration policy | current duration policy | Example: `1000` → `1.0 s`; `2500` → `2.5 s`. |
| `trade_burst` | locale integer plus ` /m` | locale integer plus ` /m` | Example: `1234.4` → `1,234 /m`; `-2` → `-2 /m`. |
| `depth_sequence_gap` | locale integer plus ` gap` | locale integer plus ` limit` | Example: `0` → `0 gap`; `-2` → `-2 limit`. |
| Unknown/custom detector | locale number, max 3 decimals | same | Example: `1234.5678` → `1,234.568`; `-0.1254` → `-0.125`. |

Severity presentation remains:

| Accepted descriptor tone | Visible label source | Badge classes | Observed-value class |
|---|---|---|---|
| `critical` | accepted capitalized descriptor label | `border-rose-400/35 bg-rose-400/10 text-rose-200` | `text-rose-300` |
| `warning` | accepted capitalized descriptor label | `border-amber-400/35 bg-amber-400/10 text-amber-200` | `text-amber-300` |
| `info` | accepted capitalized descriptor label | `border-sky-400/35 bg-sky-400/10 text-sky-200` | `text-sky-200` |

## Accepted-model ownership proof

The component has one product-model dependency: type-only `RecentAnomaliesPreviewRow` from `recentAnomaliesPreviewModel.ts`.

Accepted model and descriptor files were not modified:

- `web/src/features/dashboard/recentAnomaliesPreviewModel.ts` base/head blob: `f91a47474e2f63af6bcc6285820e9295d35e97de`;
- `web/src/features/dashboard/statusDescriptors.ts` base/head blob: `ca2eedddb746de5752cba9292ce0fb45984c5414`.

Ownership boundaries preserved:

- accepted P3-MP08 owns UUID propagation, duplicate rejection, canonical ordering, preview limiting, hidden counts, detector labels, and descriptor selection;
- this component owns only the current desktop rendering and interaction of supplied rows;
- no raw snake-case DTO field, raw `DashboardSummary`, raw `DashboardAnomaly`, descriptor formatter, query hook, API client, router, popup controller, current-time read, random value, network call, storage access, mode/source fallback, or synthesized semantic value appears in the component.

## Focused and full verification

### Worker-side focused/static gates

Passed before publication:

- TypeScript TSX syntax/transpilation for the component;
- TypeScript TSX syntax/transpilation for the focused test file;
- isolated strict TypeScript semantic compile of the production component against the accepted row/React API shape;
- exact local Git blob identity against the published component and test blobs;
- trailing-whitespace and tab scan;
- final-newline check;
- conflict-marker scan;
- exact two-path and one-commit immutable-base comparison;
- source scans for forbidden ordering, limiting, duplicate policy, identity alternatives, raw DTO/model ownership, API/query/router/popup/network/time-now/random dependencies, and Wave 4/shell/mobile/modal markup.

The focused test file contains 23 explicit cases after table-driven expansion and uses DOM assertions rather than snapshots. A separate repository-native focused-only Vitest command was unavailable in the isolated connector worker because there was no authenticated product checkout or installed product dependency tree. This limitation is not represented as a passed command. The same focused test file executed successfully within the authoritative full frontend test gate below.

### GitHub Actions full gates

Workflow `CI`, run `30368142923`, run number `195`, completed successfully.

Frontend job `90304649790`:

- install frontend dependencies: success;
- full frontend tests, including `RecentAnomaliesDesktopTable.test.tsx`: success;
- frontend typecheck: success;
- frontend lint: success;
- frontend build: success;
- frontend bundle-budget check: success.

Rust/global job `90304649896`:

- Rust formatting: success;
- generated API contract check: success;
- independent OpenAPI 3.0.3 validation: success;
- `cargo check`: success;
- clippy with warnings denied: success;
- Rust tests: success;
- replay E2E target discovery: success;
- Docker Compose base/profile validation: success;
- demo and smoke script syntax checks: success.

The workflow checked PR merge ref `4cbb4f672ff1a666c68aa8c905b2e52d5cb3b9ae`, combining unchanged product head `138a7cd39334755a1e45e61ef5d45ac61d6703d5` with current phase head `93a870010730c458417ccfff392cb97aff23d6c9`.

## Forbidden-path and ticker proof

The immutable-base comparison contains only the two authorized new paths. Therefore no existing page, route, component, CSS, model, descriptor, fixture, smoke matrix, Tooltip, package, lockfile, config, API, query, resource, adapter, identity, backend, OpenAPI, CI, Docker, deployment, script, ticker, or another worker lease changed.

Read-only blob identity proof:

- `web/src/app/GlobalMarketTicker.tsx` immutable-base blob: `727f591706a60327b3b219f3287b153a06d1160d`;
- `web/src/app/GlobalMarketTicker.tsx` product-head blob: `727f591706a60327b3b219f3287b153a06d1160d`;
- `web/src/pages/DashboardPage.tsx` immutable-base blob: `deb2eee919938d3b1807e353d309c046b08ba6f5`;
- `web/src/pages/DashboardPage.tsx` product-head blob: `deb2eee919938d3b1807e353d309c046b08ba6f5`.

`DashboardPage.tsx` was intentionally not modified or wired. P3-MP14 mobile files and P3-MP15 work were not touched or started.

## Blockers and unavailable checks

No implementation, publication, PR, compatibility, or CI blocker remains.

Unavailable as a distinct command:

- repository-native focused-only Vitest execution against the exact worker commit in the isolated worker runtime.

Reason: the runtime had no product checkout, no `gh`, no installed project dependency tree, and direct GitHub/npm network access was unavailable. The focused file nevertheless executed successfully as part of the full PR frontend suite, and strict/transpile/static focused checks passed before publication.

The phase branch advancement is recorded as integration divergence, not a worker-side blocker. Per contract, the candidate remains the required single immutable commit above its assigned base and was not rebased or rewritten.

## Completion statement

One product commit, one draft PR, and this one connector delivery report were published. Nothing was merged. No branch other than the assigned worker branch was modified. No other worker lease, control file, or forbidden product path was changed, and P3-MP15 was not started.
