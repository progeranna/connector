# P3-MP13-WEB2 Delivery Report

## Identity

- Contract: `signalguard-rs/phase-3/prompts/P3-MP13-WEB2.md`
- Contract commit: `03c9d90d06054a4eb84e136a1ad58bc0c664d3c3`
- Product repository: `progeranna/signalguard-rs`
- Replacement branch: `p3/mp13-recent-anomalies-desktop-r1`
- Required PR base: `refactor/dashboard-modules`
- Exact assigned base: `93a870010730c458417ccfff392cb97aff23d6c9`
- Final product head: `78aa90f6479397ff8d7907a895969db2a93a5eab`
- Product commit message: `feat(ui): add recent anomalies desktop table`
- Draft PR: `#40` — https://github.com/progeranna/signalguard-rs/pull/40
- Authoritative CI run: `30370164163` (`CI` run `#202`)

## Initial lease verification

Before any product write, the replacement branch and exact assigned base compared as identical:

| Ref | Compared with assigned base | Ahead | Behind |
| --- | --- | ---: | ---: |
| `p3/mp13-recent-anomalies-desktop-r1` | identical | 0 | 0 |

Immediately before PR creation, `refactor/dashboard-modules` still resolved to the exact assigned base and the replacement branch was exactly one commit ahead and zero behind.

## Superseded original branch quarantine

The immutable WEB2 contract superseded `p3/mp13-recent-anomalies-desktop`. That branch was not used, checked out, copied from, updated, rebased, reset, force-pushed, or otherwise modified by this worker.

The current remote state changed after the contract was authored: the original branch now has draft PR `#37` and compares with the WEB2 assigned base as diverged, one ahead and one behind, with merge base `01bf6edae2795a5e118148ad7b291a285a70a8d8`. This is recorded only as race/supersession evidence. Its implementation was not used as a source.

## Changed paths

Exactly two product paths were added:

1. `web/src/features/dashboard/RecentAnomaliesDesktopTable.tsx`
2. `web/src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx`

No existing product path changed. The exact assigned-base-to-final-head compare reports one commit, 725 additions, zero deletions, and only these two added files.

## Remote committed blobs

Both committed files were fetched back from GitHub at the exact final SHA using UTF-8 decoding and inspected through their complete content:

| Path | Remote Git blob | Lines | Encoding/completeness proof |
| --- | --- | ---: | --- |
| `web/src/features/dashboard/RecentAnomaliesDesktopTable.tsx` | `8a634c6493842e26688ebf99921cca97eac0800a` | 212 | `utf-8`; complete TSX through final formatter closing brace |
| `web/src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx` | `a92b45aff0fcc4c7c87dfd4e428be3ec55607a4e` | 513 | `utf-8`; complete test module through final `});` |

The authoritative repository frontend test, typecheck, lint, and build steps all executed these committed contents successfully.

## Component API and ownership

```ts
export type RecentAnomaliesDesktopTableProps = Readonly<{
  rows: readonly RecentAnomaliesPreviewRow[];
  onOpenSymbolDetail: (symbol: string) => void;
}>;
```

The component is presentation-only:

- consumes the accepted `RecentAnomaliesPreviewRow` model by type-only import;
- iterates supplied rows directly with `rows.map` in caller-provided order;
- uses accepted `row.id` as React identity;
- invokes only the supplied `onOpenSymbolDetail(row.symbol)` callback;
- renders accepted `row.detectorLabel` directly;
- renders accepted `row.severityDescriptor.label` and `.tone` directly;
- does not sort, slice, limit, filter, reverse, deduplicate, reject duplicates, or mutate rows;
- does not reconstruct IDs, detector labels, severity descriptors, active state, timestamps, source, mode, or hidden counts;
- owns no query, API, router, popup, network, timer, storage, current-time, random, shell, loading, empty, modal, tooltip, or Wave 4 behavior.

## Desktop presentation parity

| Surface | Preserved output/classes |
| --- | --- |
| Responsive wrapper | `hidden w-full min-w-0 max-w-full overflow-x-auto overscroll-x-contain border-y border-white/10 lg:block` |
| Table | `aria-label="Recent anomalies"`; fixed layout, collapsed borders, left alignment |
| Columns | `15% / 16% / 20% / 19% / 15% / 15%` |
| Headers | `Time`, `Market`, `Type`, `Severity`, `Observed`, `Threshold` in current order and header classes |
| Row identity | `key={row.id}` with accepted UUID-shaped identity |
| Row semantics | focusable `<tr>`, `role="button"`, exact label ``Open ${row.symbol} market detail`` |
| Row styling | current bottom separator, pointer, transition, hover background, focus outline/ring, and last-row border removal |
| Time cell | current nowrap, padding, font size/weight, slate color, and `2xl` size |
| Market cell | current min-width, truncation, monospace, weight, color, and responsive size |
| Type cell | current min-width, word breaking, padding, weight, line height, slate color, and responsive size |
| Severity | accepted descriptor label in current compact uppercase pill and tone classes |
| Observed | current nowrap/padding/weight/size with accepted severity-tone color |
| Threshold | current nowrap/padding/weight/slate color and responsive size |

No section shell, title, loading/empty ownership, mobile cards, view-all control, modal, context text, message, active label, severity description, tooltip, or Wave 4 semantics were added.

## Interaction matrix

| Input | Result | Default handling |
| --- | --- | --- |
| Mouse click on row | `onOpenSymbolDetail(row.symbol)` | native click behavior |
| `Enter` on focused row | same callback exactly once | `preventDefault()` |
| `Space` on focused row | same callback exactly once | `preventDefault()` |
| Any other key | no callback | not prevented |
| Parent rerender/reorder | DOM identity follows accepted `row.id`, not array position | supplied order remains visible |
| Duplicate accepted IDs | no local rejection policy; supplied rows still render | React owns duplicate-key diagnostics |

## Formatting matrix

### Timestamp behavior

| Input state | Output policy |
| --- | --- |
| Truthy `eventTime` | format `eventTime` |
| Falsy `eventTime` | use `createdAt` |
| Falsy effective timestamp | `Unavailable` |
| Invalid date string | preserve original string |
| Valid timestamp | `en-US`, 24-hour `HH:MM:SS` |

### Numeric behavior

| Detector/type | Observed | Threshold |
| --- | --- | --- |
| `spread_spike` | three decimals plus `%` | three decimals plus `%` |
| `price_move` | three decimals plus `%` | three decimals plus `%` |
| `event_lag_spike` | current duration formatter | current duration formatter |
| `stale_data` | current duration formatter | current duration formatter |
| `quote_stuck` | current duration formatter | current duration formatter |
| `trade_burst` | rounded integer plus ` /m` | rounded integer plus ` /m` |
| `depth_sequence_gap` | rounded integer plus ` gap` | rounded integer plus ` limit` |
| unknown detector | `en-US`, maximum three fractional digits | same |

Duration values at or above 1,000 render seconds with one decimal; lower values render milliseconds using generic numeric formatting. Zero and negative values are preserved. Null, undefined, and `NaN` numeric values render `—`.

### Severity behavior

| Accepted tone | Compact badge classes | Observed-value class | Visible label source |
| --- | --- | --- | --- |
| `info` | sky border/background/text | `text-sky-200` | accepted descriptor label |
| `warning` | amber border/background/text | `text-amber-300` | accepted descriptor label |
| `critical` | rose border/background/text | `text-rose-300` | accepted descriptor label |
| fallback | current slate fallback | `text-slate-300` | accepted descriptor label |

## Focused test inventory

The committed test module contains 13 explicit test groups covering:

1. exact desktop wrapper, table label, six headers, column widths, and header classes;
2. supplied order and exact symbol callback;
3. accepted UUID identity and keyed reconciliation rather than array index;
4. row focusability, role, exact accessible label, classes, and click behavior;
5. Enter/Space activation, default prevention, and ignored unrelated keys;
6. event-time display and created-at fallback;
7. invalid timestamp pass-through and falsy timestamp `Unavailable` state;
8. known and unknown accepted detector-label pass-through;
9. info/warning/critical accepted descriptor labels, compact badge classes, and observed tones;
10. percentage, duration, trade-burst, depth-gap, and generic observed/threshold formatting, including zero and negative values;
11. null, undefined, and `NaN` em-dash behavior;
12. no ordering, limiting, mutation, duplicate rejection, or local identity reconstruction;
13. absence of shell, mobile, modal, context, Wave 4, and external behavior ownership.

The source guards are scoped to `rows` ownership and explicit forbidden dependencies; they do not broadly reject unrelated `sort`, `slice`, `filter`, `Set`, or `Map` usage elsewhere.

## Full gates

GitHub Actions CI run `30370164163` (`CI` run `#202`) completed with overall conclusion `success`.

### Frontend job — success

- checkout and Node setup;
- frontend dependency installation;
- `npm run test:run`;
- `npm run typecheck`;
- `npm run lint`;
- `npm run build`;
- `npm run bundle:check`.

### Rust/global job — success

- `cargo fmt --check`;
- generated API contract check;
- OpenAPI 3.0.3 validation;
- `cargo check`;
- clippy with warnings denied;
- full Rust tests;
- replay E2E target discovery;
- base Docker Compose validation;
- app-profile Docker Compose validation;
- demo script syntax check;
- smoke script syntax check.

Pre-publication isolated checks also passed for strict TSX compilation, syntax/transpilation, UTF-8 readability, whitespace hygiene, conflict markers, exact two-path scope, source dependency restrictions, supplied-order ownership, UUID key use, callback behavior, and forbidden-scope ownership.

## MP14, dashboard, and ticker preservation

The final compare contains only the two authorized additions. Additional blob identity proofs:

| Existing path | Assigned-base blob | Final-head blob | Result |
| --- | --- | --- | --- |
| `web/src/features/dashboard/RecentAnomaliesMobileCards.tsx` | `92be317139a738a24e0694f50893ed6756f34bc9` | `92be317139a738a24e0694f50893ed6756f34bc9` | unchanged |
| `web/src/features/dashboard/RecentAnomaliesMobileCards.test.tsx` | `f546ae64c69e4e995ef625fdc18f2fcf30574d4b` | `f546ae64c69e4e995ef625fdc18f2fcf30574d4b` | unchanged |
| `web/src/pages/DashboardPage.tsx` | `deb2eee919938d3b1807e353d309c046b08ba6f5` | `deb2eee919938d3b1807e353d309c046b08ba6f5` | unchanged/unwired |
| `web/src/app/GlobalMarketTicker.tsx` | `727f591706a60327b3b219f3287b153a06d1160d` | `727f591706a60327b3b219f3287b153a06d1160d` | unchanged |

Therefore the integrated MP14 component and tests, the current inline dashboard wiring, and the upper ticker remain byte-identical to the exact assigned base.

## Commit, push, and PR proof

- Exactly one product commit is ahead of the assigned base.
- The commit message is exactly `feat(ui): add recent anomalies desktop table`.
- The only parent is the exact assigned base `93a870010730c458417ccfff392cb97aff23d6c9`.
- The replacement branch ref was updated with a non-force fast-forward operation.
- No amend, rebase, reset, squash, merge, force push, or history rewrite was performed.
- Draft PR `#40` has base `refactor/dashboard-modules`, base SHA `93a870010730c458417ccfff392cb97aff23d6c9`, head `p3/mp13-recent-anomalies-desktop-r1`, head SHA `78aa90f6479397ff8d7907a895969db2a93a5eab`, one commit, two changed files, 725 additions, and zero deletions.
- The PR remains open, draft, and unmerged.

## Final divergence

| Comparison | Status | Ahead | Behind | Commits | Changed paths |
| --- | --- | ---: | ---: | ---: | ---: |
| assigned base → final product head | ahead | 1 | 0 | 1 | 2 |
| `refactor/dashboard-modules` → replacement branch | ahead | 1 | 0 | 1 | 2 |
| assigned base → superseded original branch | diverged | 1 | 1 | 1 unique ahead | 2 shown in original-branch compare |

The replacement merge base remains the exact assigned base `93a870010730c458417ccfff392cb97aff23d6c9`.

## Blockers and unavailable checks

- No product blocker remains.
- A dedicated local focused Vitest run and local full repository gates were unavailable because the isolated worker had no repository checkout, no `gh` executable, and no outbound DNS path for cloning/installing the repository.
- The component and test sources were nevertheless checked in isolation with strict TypeScript/TSX stubs and syntax/transpile audits before publication.
- The authoritative committed test file then executed successfully inside the full repository `npm run test:run` GitHub Actions step.
- The classic combined-status endpoint returned no legacy statuses; GitHub Actions run `30370164163` and both required jobs are the authoritative gate evidence and all concluded `success`.
- Manual browser smoke was not run because wiring `DashboardPage.tsx` was explicitly forbidden; exact DOM/class assertions and the full frontend gates cover this presentation-only lease.
- No merge, dashboard wiring, MP14 edit, P3-MP15 work, or connector-contract modification was performed.
