# P3-CHECKPOINT-2R-RERUN-AFTER-R2 — Independent blocker review

Status: `P3_CHECKPOINT_2R_RERUN_AFTER_R2_BLOCKER_ACCEPTED_R3_REQUIRED`

## Reviewed authority

Blocker report:

- path: `signalguard-rs/phase-3/reports/P3-CHECKPOINT-2R-RERUN-AFTER-R2-BLOCKER/cbf5c543ada8752c273fbb2e91be029c9febc3d3.md`
- publication commit: `974512cb1b9e934c25d5ccfc2c3874767dbf631d`
- blob: `ce613b9d93d5062088d26d735af3fc7919498dbd`
- reported status: `P3_CHECKPOINT_2R_RERUN_AFTER_R2_BLOCKED`

Checkpoint contract:

- path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-RERUN-AFTER-R2-LOCAL.md`
- commit: `cadc9092e9feaaa87d589da112ea5a2f281e6956`
- blob: `1c97fd7f8c1b05b0b53e2c180a05edd92eb49a77`

## Independent product identity check

Product repository: `progeranna/signalguard-rs`

Accepted target:

- branch: `refactor/dashboard-modules`
- commit: `cbf5c543ada8752c273fbb2e91be029c9febc3d3`
- tree: `495d14862b0996766b5376358b99382124df9916`

Independent remote compare after the blocker publication proved the target branch is still identical to the accepted commit: zero commits ahead, zero behind, zero changed files.

No product mutation from the read-only validation worker is present remotely.

## Blocker acceptance

The blocker is accepted as deterministic and product-observable.

The fresh production-browser run reached the real Dashboard successfully and then produced exactly one unexpected browser console error because Chrome automatically requested `/favicon.ico` and the production preview returned HTTP 404.

Recorded fresh-run disposition:

- `/dashboard`: 200
- production CSS/JS: 200
- real Demo dashboard summary: 200
- real Demo BTCUSDT timeline: 200
- `/favicon.ico`: 404
- browser console errors: 1 unexpected
- page errors: 0
- unhandled promise rejections: 0
- transport-level request failures: 0

This directly violates the checkpoint's explicit zero-unexpected-browser-console-error acceptance rule. The validation worker correctly stopped the success matrix at the first deterministic failure and did not substitute unit/component evidence for unexecuted browser cells.

The earlier R1/R2 fixes are not implicated by this failure. The blocker is a separate pre-existing static-document/browser-console defect.

## Independent source inspection

At the exact accepted product commit, `web/index.html` contains no favicon declaration.

Its blob is:

`f9cbef13a7d91bcdd998a5f8082d954a136b8348`

Neither of these files exists at the accepted commit:

- `web/public/favicon.ico`
- `favicon.ico`

Therefore the browser's default `/favicon.ico` request has no repository-provided target and the observed 404 is consistent with the accepted product tree.

## Recovery decision

A diagnostic phase is not required. The causal surface is already exact and independently verified.

Authorize a new narrow recovery micro-phase:

`P3-CHECKPOINT-2R-R3 — Prevent missing favicon browser request`

Exact writable product lease:

- `web/index.html`

No test-file write lease is required. The recovery is validated by exact production build/preview browser evidence plus the unchanged full automated gate suite.

The implementation must declare a network-independent embedded favicon in the document head so Chrome does not issue `/favicon.ico` at all. No favicon asset file is authorized.

Forbidden adjacent scope includes Dashboard/modal logic, R1/R2 behavior, routes, API/resource code, CSS, ticker ownership, dependencies, lockfiles, bundle budgets, Rust/backend/OpenAPI/Docker, and all later semantic work.

## Continuation boundary

Checkpoint 2R remains blocked.

After R3 implementation:

1. independently review the exact worker commit;
2. integrate only after fresh exact-ref PR CI;
3. rerun the full Checkpoint 2R from the new integrated head;
4. only after independent checkpoint acceptance may `P3-W4-BRIDGE01` begin.

No Bridge 01/02, Wave 4, or later work is authorized now.

Terminal status: `P3_CHECKPOINT_2R_RERUN_AFTER_R2_BLOCKER_ACCEPTED_R3_REQUIRED`
