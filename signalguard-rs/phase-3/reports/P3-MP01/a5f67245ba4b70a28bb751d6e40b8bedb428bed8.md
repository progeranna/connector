# P3-MP01-WEB1 Delivery Report

## Task and branch

- Task: `P3-MP01-WEB1` — reusable UI smoke matrix
- Product repository: `progeranna/signalguard-rs`
- Assigned product branch: `p3/mp01-ui-smoke-matrix`
- Required PR base: `refactor/dashboard-modules`
- Immutable contract: `signalguard-rs/phase-3/prompts/P3-MP01-WEB1.md` at connector commit `a7f426388f3ee7f334320a632924cc0d61cf1947`

## Exact product identity

- Exact assigned base SHA: `6b57938d87e05d3b4fa4858f9c34c27739877821`
- Final product head SHA: `a5f67245ba4b70a28bb751d6e40b8bedb428bed8`
- Product commit message: `test(ui): define reusable smoke matrix`
- Product commit count above the assigned base: exactly `1`
- Final comparison against `refactor/dashboard-modules`: ahead `1`, behind `0`

## Draft pull request

- Draft PR: `#24`
- URL: https://github.com/progeranna/signalguard-rs/pull/24
- Head: `p3/mp01-ui-smoke-matrix`
- Base: `refactor/dashboard-modules`
- State at delivery: open, draft, mergeable, not merged
- PR reports one commit and two changed files.

## Exact changed paths and path-lease proof

Only the following authorized product paths changed:

1. `web/src/test/uiSmokeMatrix.ts`
2. `web/src/test/uiSmokeMatrix.test.ts`

Remote compare from the exact assigned base to the final product head reports exactly those two paths, both added, with no other product path changed. `web/src/test/marketFixtures.ts` was not modified. No production, API, route, CSS, configuration, dependency, lockfile, backend, CI, Docker, deployment, documentation, or script path changed.

## Matrix dimensions

The pure TypeScript manifest exports literal vocabularies and readonly scenario collections for:

- public modes: exactly `demo`, `live`;
- canonical markets: exactly `BTCUSDT`, `ETHUSDT`;
- surfaces: `dashboard`, `symbol-detail-route`, `symbol-detail-popup`;
- named responsive targets: `desktop` at `1440` px and `mobile` at `390` px;
- states: `loading`, `error`, `empty`, `unavailable` (the unavailable/non-observed category), and `observed-success`.

The manifest has seven deterministic named groups:

| Group | Scenario count |
|---|---:|
| steady state | 120 |
| mode switch | 24 |
| symbol switch | 16 |
| route/popup parity | 8 |
| late response after mode switch | 24 |
| late response after symbol switch | 16 |
| invalid/missing Live identity | 8 |
| **Total** | **216** |

The groups explicitly represent:

- Demo → Live and Live → Demo;
- BTC → ETH and ETH → BTC;
- route → route and popup → popup;
- route/popup parity;
- late responses after mode and symbol switches, with stale results required to be ignored;
- invalid and missing Live identities, with `permitsDemoFallback: false` and explicit-state requirements.

## Dialog-requirement representation

Wave 5 dialog requirements are represented without claiming incomplete focus behavior currently passes:

- represented as currently covered: Escape close, backdrop close, body scroll lock;
- represented as still required: focus containment, initial focus, focus return.

## Determinism and unique-ID evidence

- Every scenario has a stable string identifier derived only from fixed literal dimensions.
- The executable completeness tests assert that all `216` identifiers are unique.
- The tests assert exact group order, exact group counts, total count, and deterministic first and last identifiers.
- The manifest is pure TypeScript and imports no React, DOM, timer, network, storage, or TanStack Query dependency.
- Import validation uses a Node test environment and verifies that no `window` or `document` setup is required.
- No sleep and no snapshot is used.

## Replay proof

- Public mode vocabulary is exactly `demo`, `live`.
- Executable tests assert that neither the public-mode list nor serialized scenario data contains `replay`.
- Replay was not introduced as a public mode.

## Verification evidence

### Exact-head GitHub Actions CI

GitHub Actions CI run `30352221605` (run number `135`) completed successfully on product head `a5f67245ba4b70a28bb751d6e40b8bedb428bed8`:

https://github.com/progeranna/signalguard-rs/actions/runs/30352221605

The frontend job completed successfully and ran the repository-defined commands from `web`:

- `npm run test:run` — PASS;
- `npm run typecheck` — PASS;
- `npm run lint` — PASS;
- `npm run build` — PASS;
- `npm run bundle:check` — PASS.

The Rust/full-repository CI job also completed successfully; it did not replace or weaken the frontend evidence above.

### Focused smoke-matrix test

A separate focused Vitest command targeting only `uiSmokeMatrix.test.ts` was unavailable in the connector-only worker environment and is not claimed as separately run. The exact reason is that the worker had no product checkout or installed package dependency tree and could not dispatch an arbitrary one-off GitHub Actions command. The full `npm run test:run` suite did execute successfully on the exact product head and includes the newly added `uiSmokeMatrix.test.ts` test file.

### Additional available worker checks

Before product publication, the worker executed and passed:

- isolated strict TypeScript compilation of `uiSmokeMatrix.ts` with an ES2020 strict compiler profile matching the product configuration;
- isolated syntax/type validation of the manifest and test source using minimal declarations for external modules unavailable in the worker;
- deterministic Node semantic assertions covering the `216` scenario total, exact group counts, unique IDs, Replay absence, invalid/missing Live no-Demo-fallback invariants, and dialog coverage split;
- `git diff --check` on the exact reconstructed two-file patch.

These additional checks are supplementary and are not substituted for the exact-head CI gates listed above.

## Forbidden-path and ticker proof

Remote compare confirms that no forbidden product path changed. In particular:

- `web/src/app/GlobalMarketTicker.tsx` base blob SHA: `727f591706a60327b3b219f3287b153a06d1160d`;
- `web/src/app/GlobalMarketTicker.tsx` head blob SHA: `727f591706a60327b3b219f3287b153a06d1160d`.

The upper ticker text, structure, ordering, scrolling, animation, behavior, and CSS therefore remain untouched by this product commit. Production presentation and runtime behavior are unchanged.

## Repository cleanliness and publication evidence

- Initial remote comparison of `refactor/dashboard-modules` to the assigned base SHA was identical: ahead `0`, behind `0`.
- Initial remote comparison of `p3/mp01-ui-smoke-matrix` to the assigned base SHA was identical: ahead `0`, behind `0`.
- The product commit was created atomically from a tree based on the exact assigned base and containing only the two authorized additions.
- The assigned branch ref was advanced normally with a non-force update.
- Final remote comparison is ahead `1`, behind `0`, with exactly one product commit and exactly two authorized changed paths.
- No amend, rebase, reset, squash, force-push, or history rewrite occurred.
- The connector report path did not exist before publication; this commit creates only this report.

## Unavailable checks and limitations

- Separate focused Vitest invocation: unavailable for the exact connector-only reason stated above.
- A conventional local worktree-cleanliness command such as `git status --short`: unavailable because the immutable contract required remote-only GitHub execution and the worker had no product checkout. Remote ref comparisons, atomic tree construction, exact changed-path comparison, and final ahead/behind proof provide the available repository-cleanliness evidence.
- `gh` was not installed; GitHub connector APIs were used for atomic commit publication, non-force ref advancement, PR creation, CI inspection, and report publication.

No unavailable check is claimed as passed.

## Completion statement

The product PR remains an open draft and was not merged. The Phase 3 base branch was not edited directly. No other worker branch was modified. No next microphase or other task was started.
