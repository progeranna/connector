# P3-CHECKPOINT-2R — Independent acceptance review after R4

Status: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_ACCEPTED`

Review date: 2026-08-19

## Review authority and boundary

This report records the dedicated independent GitHub-web checkpoint acceptance review authorized by:

- repository: `progeranna/connector`
- contract path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-RERUN-AFTER-R4-REVIEW.md`
- publication commit: `43fcd00c899d01dbf13f1e4b7f0d69735d6d0fbb`
- publication/current-main blob: `44702b5fee9a34ea25d9372d9b6726ff027fb1cd`
- status: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_REVIEW_AUTHORIZED`

Worker boundary was preserved exactly:

- connected GitHub tools only;
- product write lease: `NONE`;
- connector write lease: this exact review report only;
- no local checkout, shell, Codex CLI, GitHub CLI, filesystem repository copy, browser automation, or unconnected repository data was used;
- no local command, Docker, Chrome, Playwright, localhost, screenshot, or `/tmp` evidence is claimed as independently rerun or reopened by this reviewer.

## Current control authority observed before review publication

`signalguard-rs/phase-3/control/CURRENT_EXECUTION.md` was read completely from current connector `main` and remained:

- blob: `e05312c766f175e085b84f7fe0613bc2d3be22a0`
- status: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_REVIEW_AUTHORIZED`
- exact integrated product: `23656c9b93a24bfc20ba8f417275564bb5b5d240`

`signalguard-rs/phase-3/control/STATUS.md` was read completely from current connector `main` and remained:

- blob: `1585e7a3f75ede1e25c4ffcc767e62b647a32ba0`
- current/terminal state: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_REVIEW_AUTHORIZED`
- expected `CURRENT_EXECUTION.md` blob: `e05312c766f175e085b84f7fe0613bc2d3be22a0`

Immediately before this report write, both control documents still exposed the same authorized review-only state. Neither control file was modified by this reviewer.

## Rerun authority and durable report identity

Completed rerun contract:

- path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-RERUN-AFTER-R4.md`
- publication commit: `e49bb785ff753a78c9c274689365a573d44f695b`
- publication/current-main blob: `cee0cac364ef38f0e52947006392227dbc38a9ec`
- status: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_AUTHORIZED`

Completed rerun report:

- path: `signalguard-rs/phase-3/reports/P3-CHECKPOINT-2R-RERUN-AFTER-R4/23656c9b93a24bfc20ba8f417275564bb5b5d240.md`
- publication commit: `378783556646bc414a0cd16548aa6bda6ae9c503`
- publication blob: `a5a171385f5c902f8128168e763293d13c071109`
- current-main blob immediately before this review write: `a5a171385f5c902f8128168e763293d13c071109`
- status: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_COMPLETE`

The report was read completely. Current connector `main` still resolves the exact report path to the exact publication blob. No report rewrite or identity mismatch was found.

The original checkpoint baseline was also read completely from current connector `main`:

- `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-LOCAL.md`
- blob: `72467c9adae47853586fc4665cbffe93dabfbebd`

## Exact product identity — independently GitHub-verified

Product repository: `progeranna/signalguard-rs`

Target branch: `refactor/dashboard-modules`

Required integrated merge:

- commit: `23656c9b93a24bfc20ba8f417275564bb5b5d240`
- tree: `d8c0289a05b3646b3abc7056bd269b927e61d5c4`
- ordered parent 1: `7dab5647d322339f5bd9d0514e5178522d5181c0`
- ordered parent 2: `79abb161e7a731df7077d49b44481eaaf25bf762`

Connected-GitHub compare from exact commit to current target branch was performed twice, including immediately before publication. Both comparisons returned:

- status: `identical`;
- ahead by: `0`;
- behind by: `0`;
- total commits: `0`;
- merge base: exactly `23656c9b93a24bfc20ba8f417275564bb5b5d240`;
- changed files: empty list.

Therefore the target still resolves to the exact integrated commit and no later product commit has been integrated.

Direct connected-GitHub commit metadata independently verified:

- tree: `d8c0289a05b3646b3abc7056bd269b927e61d5c4`;
- parent order exactly `7dab5647d322339f5bd9d0514e5178522d5181c0`, then `79abb161e7a731df7077d49b44481eaaf25bf762`;
- commit message identifies merge of PR `#74`.

Current PR `#74` metadata independently verified:

- state: closed;
- merged: true;
- draft: false;
- title: `fix(ui): reconcile modal symbol on mode change`;
- base: `refactor/dashboard-modules` at `7dab5647d322339f5bd9d0514e5178522d5181c0`;
- head: `p3/checkpoint2r-selected-symbol-ownership` at `79abb161e7a731df7077d49b44481eaaf25bf762`;
- merge commit: `23656c9b93a24bfc20ba8f417275564bb5b5d240`;
- commits: `1`;
- changed files: `2`;
- additions/deletions: `145 / 14`.

This is consistent with the durable R4 integration report and no target drift exists.

## R4 integration and product-owner acceptance authority

R4 integration report was read completely:

- path: `signalguard-rs/phase-3/reports/P3-CHECKPOINT-2R-R4-INTEGRATION/23656c9b93a24bfc20ba8f417275564bb5b5d240.md`
- publication commit: `f2189063dba447bd0dca4a83ee16120e4f31959e`
- publication/current-main blob: `6a2a94ed57e01635d8fdaa4809170400bb572c65`
- status: `P3_CHECKPOINT_2R_R4_INTEGRATION_COMPLETE`

It records the same final merge commit/tree/ordered parents, PR #74 identity, exact accepted two-file effective diff, successful exact synthetic-merge CI, and deterministic frontend bundle bytes.

Localhost verification contract was read completely:

- path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-R4-LOCALHOST-USER-VERIFICATION.md`
- publication commit: `b68ef92ef152e0c0a3064dbf01932313ab26dfbd`
- publication/current-main blob: `8e3e1ac6e087e61ab9f9f746d84a70e5dd238757`
- status: `P3_CHECKPOINT_2R_R4_LOCALHOST_USER_VERIFICATION_AUTHORIZED`

Product-owner acceptance report was read completely:

- path: `signalguard-rs/phase-3/reports/P3-CHECKPOINT-2R-R4-LOCALHOST-USER-ACCEPTANCE/23656c9b93a24bfc20ba8f417275564bb5b5d240.md`
- publication commit: `74e3fc339036c3fd2c8676adc3f508005c723e0d`
- publication/current-main blob: `0b817619bb820f82b7482bc9b62eb386e6807d4c`
- status: `P3_CHECKPOINT_2R_R4_LOCALHOST_USER_ACCEPTED`

The acceptance semantics are explicitly per-mode selected-symbol persistence and are consistent with the current product source and rerun evidence.

## GitHub-verifiable source/test consistency audit

The following files were independently read at exact integrated commit `23656c9b93a24bfc20ba8f417275564bb5b5d240`.

### `web/src/pages/DashboardPage.tsx`

Blob: `d89a80ba78c27de3bba66d0a7e796cc5e80830eb`.

Repository-backed findings:

- `reconcileSymbolPopupIdentity` first reconciles mode and then reconciles the currently selected symbol while retaining `returnContext`;
- the effect handling open `symbolDetail` / `symbolAnomalyDetail` state replaces incompatible nested state with the reconciled parent Symbol Detail;
- nested symbol-owned anomaly rendering is guarded by exact current mode and selected symbol;
- All Markets opens Dashboard-owned Symbol Detail with `returnContext="symbols"`;
- Back from nested detail restores the parent Symbol Detail with `focusAnomalyId`;
- Back from symbol detail with `returnContext="symbols"` restores All Markets with `focusSymbol`;
- modal ownership remains local component state rather than route-backed popup ownership.

These source facts are consistent with R1, R2 and R4 and expose no contradiction.

### `web/src/pages/DashboardPage.popup.test.tsx`

Blob: `57dbe76426c16e4374502437aedb749755309579`.

Repository-backed regression coverage includes:

- exact desktop-row and mobile-card visible focus restoration with hidden responsive duplicate exclusion;
- complete nested All Markets Back flow restoring exact UUID focus and then exact visible originating market focus;
- preservation of `demo:ETHUSDT:symbols` / symbols return context across nested Back;
- Close, Escape, backdrop, scroll-lock and focus-containment behavior;
- R4 composed regression that exercises Demo BTC → Live BTC → Live ETH → Demo BTC ownership;
- clearing nested UUIDs on mode/symbol replacement;
- exact intermediate `live/BTCUSDT` and `live/ETHUSDT` identities with `returnContext="symbols"`;
- final `demo:BTCUSDT:symbols` identity;
- Demo=`BTCUSDT` and Live=`ETHUSDT` stored selections;
- absence of stale Live ETH content;
- rejection of late stale Live/Demo ETH resources.

The durable rerun report states this file passed all `32` composed tests. This reviewer verified the regression remains present in GitHub but did not independently rerun the test command.

### `web/src/features/dashboard/selectedSymbol.ts`

Blob: `84fd18c12c05f837d021995258bf9252167b17a0`.

Repository-backed findings:

- selected-symbol storage keys are mode-scoped as `signalguard:selected-symbol:<mode>`;
- reads, writes and change events carry the current `UiMode`;
- switching mode reloads the stored selection for that mode.

This matches product-owner acceptance and R4's Demo/Live storage isolation.

### `web/src/features/dashboard/symbolPopup.ts`

Blob: `8fa38f0c2a8573b9bd227505b8cce2c9dda198a8`.

Repository-backed findings:

- popup identity is exactly `{ mode, symbol, returnContext }`;
- replacing symbol preserves mode and return context;
- replacing mode preserves symbol and return context;
- durable identity key is `${mode}:${symbol}:${returnContext}`.

This is consistent with exact identities required by R1/R4.

### `web/index.html`

Blob: `e381a6d1e46b707a86484217e82686cdb6f8c288`.

The favicon is an embedded `data:image/svg+xml` resource. Repository state therefore contains the accepted R3 favicon fix and does not require `/favicon.ico` as its declared icon resource.

No repository-backed evidence in the inspected exact files contradicts R1–R4 or the durable rerun report.

## Durable rerun completeness audit

The following items are accepted as **reported local validator evidence recorded durably in GitHub**, not as local execution independently reproduced by this GitHub-only reviewer.

### Static/frontend gates

The durable rerun report explicitly records:

- bundle-policy: `25/25`, 0 failed;
- frontend test files: `44/44`;
- frontend tests: `615/615`, 0 failed;
- `DashboardPage.popup.test.tsx`: `32/32`, including the composed R4 regression;
- typecheck: PASS;
- lint: PASS with `--max-warnings 0`, zero warnings;
- production build: PASS;
- bundle check: PASS;
- deterministic actual JS bytes: `389559 / 389559 / 389559`;
- unchanged limits: `409600 / 409600 / 414720`.

The deterministic bytes exactly match the R4 integration CI report. No tracked target drift exists that could contradict them.

### Rust/global gates

The durable rerun report explicitly records PASS for:

- `cargo fmt --check`;
- generated API contract `--check`;
- OpenAPI validation (`3.0.3` artifact valid);
- `cargo check`;
- Clippy with warnings denied and zero warnings;
- `cargo test`;
- `cargo test --test replay_e2e`;
- `docker compose config`;
- `docker compose --profile app config`;
- `bash -n scripts/demo-replay.sh`;
- `bash -n scripts/smoke.sh`;
- `git diff --check`.

The report distinguishes designed service-dependent ignores from failures: principal library result `379` passed / `0` failed / `3` ignored; `23` designed service-dependent ignores total across the full test run, including `2` replay E2E cases. It explicitly states that no failure was converted to an ignore.

### Clean execution boundary

The durable report records a fresh detached worktree at exact integrated commit and tree, not reused from previous validation environments. It records:

- exact HEAD `23656c9b93a24bfc20ba8f417275564bb5b5d240`;
- exact tree `d8c0289a05b3646b3abc7056bd269b927e61d5c4`;
- detached HEAD;
- clean status before and after;
- `git diff --check` pass;
- no tracked/untracked/ignored/generated/dependency/build/service/container residue after cleanup;
- byte-identical pre/post hashes for tracked manifests, lockfiles, bundle-policy files, `web/index.html`, and generated API contract;
- isolated services and evidence outside both repositories;
- zero product writes;
- no target drift before or after.

These are local-validator statements. The GitHub-only reviewer did not reopen the reported `/tmp` worktree, but current GitHub target identity independently remains exact.

### Full 8-cell browser matrix

The durable report explicitly records PASS disposition for all eight required cells:

1. Demo / BTCUSDT / desktop 1440×900;
2. Demo / BTCUSDT / mobile 390×844;
3. Demo / ETHUSDT / desktop;
4. Demo / ETHUSDT / mobile;
5. Live / BTCUSDT / desktop;
6. Live / BTCUSDT / mobile;
7. Live / ETHUSDT / desktop;
8. Live / ETHUSDT / mobile.

Across applicable cells it records Dashboard Symbol Detail, exact UUID nested Anomaly Detail, All Markets, All Anomalies, Back, exact visible focus restoration, hidden-responsive-duplicate exclusion, pointer/keyboard/mobile activation, Tab/Shift+Tab containment, Escape, Close, backdrop, body scroll lock, route replacement redirects, ephemeral modal state, Demo/Live isolation and exact symbol identity.

Demo ETHUSDT had a legitimate real-data zero-symbol-anomaly state. The report explicitly states that no record was fabricated; direct/All Markets Symbol Detail still passed and exact Anomaly Detail was exercised from real All Anomalies data and Live ETH cells.

### R1 — All Markets / View-all ownership

Durable report disposition: PASS on desktop and mobile.

It records both intended View-all triggers as visible/enabled/focusable and All Markets activation opening exactly one Dashboard-owned Symbol Detail with `returnContext=symbols`, with no route/popup wrapper ownership. Current exact source/test state independently supports this ownership model.

### R2 — Back focus

Durable report disposition: PASS across the matrix and both composed R4 sequences.

It records nested Back restoring exact visible UUID focus and second Back restoring the exact visible originating All Markets row/card, with hidden responsive duplicates excluded. Totals recorded: `32` exact visible-focus checks and `42` modal focus/lifecycle checks. Current exact tests independently contain explicit desktop/mobile visible-variant assertions.

### R3 — favicon and console cleanliness

Durable report disposition: PASS.

It records `16` fresh browser contexts, `/favicon.ico` request count exactly `0` in every context, and zero console warnings/errors, page errors or unhandled rejections. Current exact `web/index.html` independently declares an embedded data favicon.

### R4 — exact composed mode/symbol ownership

Durable report records independent desktop and mobile PASS for exactly:

`Demo/BTCUSDT → Live/BTCUSDT → Live/ETHUSDT → Demo/BTCUSDT`

It explicitly records all required durable facts:

- nested Demo BTC UUID cleared on Demo→Live;
- exact intermediate parent `live:BTCUSDT:symbols`;
- nested Live BTC UUID cleared on BTC→ETH;
- exact intermediate parent `live:ETHUSDT:symbols`;
- nested Live ETH UUID/detail cleared on Live→Demo;
- final parent exactly `demo:BTCUSDT:symbols`, never `demo:ETHUSDT:symbols`;
- Demo BTC content visible and stale ETH content absent;
- `returnContext=symbols` / All Markets preserved;
- final mode-scoped storage Demo=`BTCUSDT`, Live=`ETHUSDT`;
- Back restores All Markets and exact visible BTCUSDT row/card focus;
- no hard reload;
- stale UUIDs never appear in the newly owned parent interval;
- late stale old-mode/old-symbol resources do not reattach;
- mode-only same-symbol and same-mode BTCUSDT→ETHUSDT replacements are also covered.

Current exact source and composed regression independently support these ownership transitions and expose no GitHub-verifiable contradiction.

### Runtime / transport / navigation audit

The durable report records:

- `16` fresh contexts audited;
- console errors: `0`;
- console warnings: `0`;
- page errors: `0`;
- unhandled rejections: `0`;
- HTTP responses >=400: `0`;
- request failures: `0`;
- unexplained request failures: `0`;
- `/favicon.ico` requests: `0`;
- `22` document requests total, attributable to initial loads plus deliberate direct-route/Back navigation audit;
- modal flows created no document requests;
- each desktop/mobile R4 sequence retained exactly one initial document request / Navigation Timing `navigate` entry, so no hard reload was used.

`net::ERR_ABORTED` disposition is explicit and exact: **none occurred**. The durable report states `net::ERR_ABORTED: none`; therefore there are no aborted URLs to classify. This review does not invent any aborted request.

### Screenshot and structured evidence inventory

The durable report records `58` screenshots, exceeding the required minimum of `18`, all outside both repositories under the reported local evidence root. Every screenshot entry includes filename, mode/symbol/viewport, flow step and SHA-256.

Coverage includes all matrix cells, Symbol Detail, nested Anomaly Detail, All Markets, All Anomalies, multiple post-Back and visible-focus states, and exact final desktop/mobile R4 `demo:BTCUSDT:symbols` states.

The report further records that all screenshot inventory hashes were recomputed after the run and matched.

Structured local evidence is durably inventoried with SHA-256 hashes, including:

- `browser-validation.json`: `653458d039a23945c1447e5851671a79c13a98254e69d365c08076d4cf9e52c3`;
- `screenshot-inventory.json`: `ee59aebd1aff72498bccb3fa746bc7fb1ebdc7e2fc910fafd797a604a33388c1`;
- `browser/proxy-request-log.jsonl`: `f1028c32353d302d548e3f729bc85be39b525ad89df5bdf82129b69cfe8d0015`;
- browser validation helper: `be7e53bb4e3bff999dfa09140348dd5995e0ae69dec8b4da167c74e1628578e1`;
- pass-through proxy helper: `09c857ae931b437343f1470d56d0678fc2c458fd55cd1d1cae1ee76e9b649b9f`.

The report states that the structured browser log contains identities, UUIDs, storage, navigation, request/response data, errors, focus assertions, lifecycle results, R4 mutation trace, MP20R assertions and screenshot hashes.

These screenshot/log bytes live under the validator's reported local `/tmp` evidence root and were **not** independently reopened by this GitHub-only reviewer. Acceptance relies on the complete durable report plus the absence of any GitHub-verifiable contradiction, as the review contract explicitly permits.

## Mutation and continuation audit

This independent reviewer performed:

- zero product writes;
- zero product branch/ref/PR/merge mutations;
- zero product repairs;
- zero connector control-file writes;
- exactly one connector write: publication of this authorized review report.

The durable rerun report records zero product writes and states Bridge01, Bridge02 and semantic Wave 4 did not begin during validation. Current `CURRENT_EXECUTION.md` and `STATUS.md` still make Bridge01/02 and P3-MP21…P3-MP30 unauthorized, and the exact product target remains unchanged at the R4 integrated merge. This reviewer did not begin Bridge01, Bridge02, semantic Wave 4, or any later work.

## Acceptance rationale

Acceptance criteria are satisfied:

1. review contract, rerun contract and rerun report identities/statuses are exact;
2. current connector `main` still resolves the rerun report to the exact publication blob;
3. current control authority remains exactly `P3_CHECKPOINT_2R_RERUN_AFTER_R4_REVIEW_AUTHORIZED`;
4. product target is independently GitHub-verified as the exact integrated commit with exact tree/parents, zero ahead/behind, exact merge base and empty changed-file compare;
5. PR #74 remains merged consistently with that exact identity;
6. required exact product source/test state still contains the accepted R1–R4 implementation/regression and no repository-backed contradiction was found;
7. the durable rerun report completely disposes all required frontend, Rust/global, clean-boundary, 8-cell browser, R1–R4, runtime/transport and evidence-inventory requirements;
8. the report explicitly records that no `net::ERR_ABORTED` occurred;
9. GitHub-verifiable facts are distinguished from local-only validator evidence rather than falsely claiming local re-execution;
10. no unauthorized product or connector-control mutation occurred, and no Bridge/Wave continuation was begun by this reviewer.

Checkpoint 2R rerun after R4 is independently accepted. This acceptance does not itself authorize Bridge01 until the orchestrator verifies this report and performs the next control transition.

Terminal status: `P3_CHECKPOINT_2R_RERUN_AFTER_R4_ACCEPTED`
