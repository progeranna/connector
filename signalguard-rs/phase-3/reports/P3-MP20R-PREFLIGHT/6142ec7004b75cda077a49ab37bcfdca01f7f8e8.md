# P3-MP20R — Preflight and corrected lease

Status: `P3_MP20R_PREFLIGHT_COMPLETE_IMPLEMENTATION_NOT_YET_AUTHORIZED`

## 1. Integrated base

- Product repository: `progeranna/signalguard-rs`
- Target branch: `refactor/dashboard-modules`
- Integrated base commit: `6142ec7004b75cda077a49ab37bcfdca01f7f8e8`
- Integrated base tree: `65c816c76a5f9e31858cdcb29acd523e8a92c122`
- Ordered merge parent 1: `ba31a348dc5055935c45f6be81073688caedd925`
- Ordered merge parent 2: `664daebc9bc63a761ea8db205f9ae345f0d0c622`
- Integrated MP18R PR: `#69`
- Integrated status: `P3_MP18R_INTEGRATED_MP20R_NOT_AUTHORIZED`

Independent read-back confirmed:

- PR #69 is closed and merged;
- target branch is two commits ahead and zero behind the previous base;
- the effective diff remains the accepted twelve MP18R paths;
- Rust and Frontend CI jobs completed successfully against synthetic merge SHA `431ce12ebb413dd436bb101c07a14c443119c029`;
- final merge tree equals the tested synthetic merge tree.

## 2. Preliminary ledger statement requiring correction

The recovered ledger described MP20R as a three-file pure cleanup lease:

- `web/src/features/dashboard/marketViewModel.ts`
- `web/src/features/dashboard/marketAdapters.ts`
- `web/src/features/dashboard/marketAdapters.test.ts`

That lease is not sufficient on the integrated base.

The preliminary ledger remains authoritative for sequencing and intent, but its exact MP20R writable lease is superseded by this evidence-backed preflight.

## 3. Current residue

`marketViewModel.ts` currently defines:

```ts
export type MarketDisplayVariants = Readonly<{
  route: string;
  popup: string;
}>;
```

`MarketAnomalyViewModel.observed` and `.threshold` use this route/popup pair.

`marketAdapters.ts` still contains:

- `formatPopupAnomalyValue`;
- `formatRouteAnomalyValue`;
- `anomalyDisplayVariants`;
- route-only generic number formatting;
- construction of `{ popup, route }` values.

`marketAdapters.test.ts` explicitly preserves and asserts route/popup parity.

## 4. Required consumers outside the preliminary lease

The integrated `SymbolDetailAnomalies.tsx` consumes:

- `anomaly.observed.popup`;
- `anomaly.threshold.popup`.

Its focused test creates `MarketAnomalyViewModel` fixtures containing:

- `observed: { route: "unused", popup: ... }`;
- `threshold: { route: "unused", popup: ... }`;

and asserts the `.popup` members.

Therefore changing the view model from route/popup variants to one modal-owned display string necessarily requires those two files. The original three-file lease would cause deterministic typecheck/test failures or would force preservation of the obsolete popup wrapper.

This is a stale consumer-lease issue, not production scope expansion into controller behavior.

## 5. Corrected exact five-file lease

Production/model:

1. `web/src/features/dashboard/marketViewModel.ts`
2. `web/src/features/dashboard/marketAdapters.ts`
3. `web/src/features/dashboard/SymbolDetailAnomalies.tsx`

Tests:

4. `web/src/features/dashboard/marketAdapters.test.ts`
5. `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`

No other MP18R path is required.

## 6. Exact intended result

- Delete `MarketDisplayVariants`.
- Change `MarketAnomalyViewModel.observed` and `.threshold` to presentation-neutral string fields owned by the modal-only Symbol Detail surface.
- Delete `formatRouteAnomalyValue` and `anomalyDisplayVariants`.
- Rename the surviving type-aware formatter so it is no longer described as popup parity formatting.
- Preserve every currently rendered value exactly:
  - spread/price move percentages;
  - event lag/stale/quote-stuck time units;
  - trade-burst `/m` suffix;
  - depth-sequence gap/limit wording;
  - unavailable marker `—`.
- Update Symbol Detail anomalies to render the direct strings.
- Update focused fixtures and assertions to the direct-string shape.
- Preserve exact UUID activation, keyboard handling, Back/focus behavior, anomaly order and responsive markup.

## 7. Forbidden scope

Do not modify:

- `DashboardPage.tsx` or its tests;
- Symbol Detail header or metrics;
- `symbolPopup.ts` or resource modules;
- routes/router;
- API, DTO, schema or query identity;
- backend or Rust sources;
- visible copy, CSS or responsive layout;
- semantic Wave 4 descriptors;
- ticker ownership;
- packages, lockfiles or bundle budgets.

MP20R is not an opportunity to implement the semantic bridge or Wave 4.

## 8. Validation expectation

Focused tests:

- `marketAdapters.test.ts`;
- `SymbolDetailAnomalies.test.tsx`.

Full validation:

- complete frontend Vitest suite;
- typecheck;
- lint;
- production build;
- unchanged bundle policy;
- repository global gates required by project control;
- exact five-path diff audit;
- residual audit proving no route/popup anomaly display variants remain.

No browser screenshot is required because the result is presentation-neutral and Checkpoint 2R follows after integration. A focused rendered-component regression must prove visible values are unchanged.

## 9. Sequencing

```text
integrated MP18R base
→ P3-MP20R implementation and integration
→ Checkpoint 2R
→ semantic Bridge 01 / Bridge 02
→ P3-MP21…P3-MP30
```

This preflight does not itself authorize product mutation.
