# P3-MP08-WEB1 Delivery Report

## Delivery identity

- Contract: `signalguard-rs/phase-3/prompts/P3-MP08-WEB1.md`
- Immutable contract commit: `ef6854caf331c576d6de98ae21410dc5052b1272`
- Product repository: `progeranna/signalguard-rs`
- Assigned branch: `p3/mp08-recent-anomalies-preview`
- Required PR base: `refactor/dashboard-modules`
- Immutable assigned base: `3587ec9b70b677121aa796467d5bb359ffb4d174`
- Product commit: `b4d47a7797b0c505e37f344d2979dfc1f3612f51`
- Product commit message: `feat(ui): extract recent anomalies preview model`
- Draft PR: `#30` — `https://github.com/progeranna/signalguard-rs/pull/30`
- PR head: `p3/mp08-recent-anomalies-preview`
- PR base: `refactor/dashboard-modules`
- PR state at publication: open, draft, unmerged

## Preflight and publication proof

At preflight, both the assigned branch and phase branch equaled the immutable assigned base and compared `ahead 0 / behind 0` against it.

The product candidate was created as one commit with the immutable assigned base as its sole parent. The assigned branch ref was updated by a normal fast-forward operation with `force=false`; no amend, rebase, reset, squash, force-push, or history rewrite was used.

Final comparison against the immutable assigned base:

- ahead: `1`
- behind: `0`
- total candidate commits: `1`

The phase branch advanced after preflight to `773b110816d10d31e65a36ce2ed76a1b37beca01`. The candidate was preserved unchanged as required. Final comparison against the current phase branch is diverged:

- candidate ahead: `1`
- candidate behind: `1`
- merge base: `3587ec9b70b677121aa796467d5bb359ffb4d174`

No rebase or update-branch action was performed after this moving-base change.

## Exact product paths

Exactly two new paths were added and no existing product path changed:

1. `web/src/features/dashboard/recentAnomaliesPreviewModel.ts`
2. `web/src/features/dashboard/recentAnomaliesPreviewModel.test.ts`

Remote blob proof:

- model blob: `f91a47474e2f63af6bcc6285820e9295d35e97de`
- test blob: `5b44312a36f8217447573b1c8702b60d9c763253`

Immutable-base comparison and PR metadata both report one commit, two changed files, 451 additions, and zero deletions.

## Exported API

The module exports:

- `RECENT_ANOMALIES_PREVIEW_LIMIT` with exact value `7`;
- readonly `RecentAnomaliesPreviewRow`;
- readonly `RecentAnomaliesPreviewResult`;
- `mapDashboardAnomalyToRecentPreviewRow(anomaly)`;
- `buildRecentAnomaliesPreview(anomalies, limit?)`.

The result contains ordered `allRows`, limited `rows`, `limit`, `totalCount`, `hiddenCount`, `hasMore`, and `isEmpty`.

Each row preserves the accepted anomaly fields and adds pure semantic data: `id`, `symbol`, original `anomalyType`, deterministic `detectorLabel`, original severity key, accepted `severityDescriptor`, accepted combined `activeLabel`, `message`, `observedValue`, `thresholdValue`, original `eventTime`, original `createdAt`, and `effectiveTimestampMs` or `null`.

## UUID identity and duplicate-ID behavior

- Row identity is exactly the accepted anomaly UUID `anomaly.id`.
- The original UUID is preserved unchanged.
- No array index, random value, symbol, message, or timestamp is used as identity.
- Duplicate IDs are rejected before ordering with stable `TypeError` text: `Duplicate anomaly id: <ID>`.
- Tests prove stable UUID propagation and deterministic duplicate rejection.

## Ordering, fallback, and tie-break behavior

Input arrays and anomaly objects are not mutated. A new row array is built and sorted by:

1. effective event timestamp descending;
2. created timestamp descending when effective timestamps tie;
3. anomaly UUID ascending as the final deterministic tie-breaker.

Timestamp behavior:

- finite `Date.parse(event_time)` is the preferred effective timestamp;
- otherwise finite `Date.parse(created_at)` is used;
- otherwise the row exposes `effectiveTimestampMs: null` and sorts using negative infinity;
- created-time tie-breaking uses finite `Date.parse(created_at)` or negative infinity;
- input order is never the final tie-breaker.

Tests cover newest-first ordering, created-time ties, UUID ties, invalid-event fallback, both-invalid placement/null exposure, and non-mutation.

## Detector behavior

The model delegates to the accepted pure descriptor functions from `statusDescriptors.ts`.

Known detector identifiers retain the accepted labels:

- `price_move` → `Price Move`
- `spread_spike` → `Spread Spike`
- `stale_data` → `Stale Data`
- `trade_burst` → `Trade Burst`
- `quote_stuck` → `Quote Stuck`
- `event_lag_spike` → `Event Lag Spike`
- `depth_sequence_gap` → `Depth Sequence Gap`

Unknown identifiers use accepted deterministic underscore-segment formatting. For example, `custom_liquidity_gap` becomes `Custom Liquidity Gap`; an empty identifier becomes `Unknown Detector` under the accepted formatter.

## Severity descriptor behavior

The original severity key is preserved. The accepted `getAnomalySeverityDescriptor` result is included unchanged for `info`, `warning`, or `critical`. The accepted active label is produced by `formatActiveAnomalyLabel`, combining the severity label and detector label with ` · `, for example `Critical · Depth Sequence Gap`.

No new severity vocabulary, tone, color, icon, or CSS semantic was introduced.

## Numeric, message, and presentation behavior

- Observed and threshold values are preserved exactly, including `0` and negative values.
- Empty messages are preserved exactly.
- No visible numeric formatting is performed.
- No local-time or visible date string is formatted.
- Original timestamp strings are preserved, with only numeric parse results exposed for deterministic ordering.
- No mode/source is invented; the model remains Demo/Live neutral.

## Preview limit and metadata

- Default limit: `7`.
- Non-finite or non-integer limits throw stable `TypeError`: `limit must be a finite integer`.
- Negative integer limits throw stable `RangeError`: `limit must be non-negative`.
- Limit `0` keeps ordered `allRows`, returns empty `rows`, and preserves metadata.
- `hiddenCount = max(totalCount - rows.length, 0)`.
- `hasMore` is true exactly when `hiddenCount > 0`.
- `isEmpty` is true exactly when `totalCount === 0`.
- Tests cover fewer than, equal to, and more than the default limit, limit zero, invalid limits, and empty input.

## Verification results

Focused deterministic execution:

- isolated focused harness: `22 passed, 0 failed`;
- strict TypeScript compile of the new model/test against compatible accepted type/descriptor stubs: passed;
- equal-input determinism: passed.

Repository CI:

- workflow: `CI`, run `30359693386`, run number `162`;
- conclusion: success;
- frontend full tests: success;
- frontend typecheck: success;
- frontend lint: success;
- frontend build: success;
- frontend bundle-budget check: success;
- Rust formatting, generated API contract, OpenAPI validation, cargo check, clippy, tests, replay target discovery, Docker Compose validation, and script syntax checks: success.

The PR workflow checked synthetic merge commit `82bfda6e56ff9bc05fc1278cfc3a54f7bc20e68f`, combining the unchanged candidate with the phase branch that advanced after preflight. Direct exact-candidate full-repository commands were unavailable in the isolated worker container because no authenticated product checkout or installed repository dependency tree was available. This limitation is not represented as passed; exact candidate source behavior is covered by the focused execution and the PR merge-ref repository CI is recorded separately.

Additional gates:

- `git diff --check` on the exact staged two-file payload: passed;
- exact two-path proof: passed;
- forbidden-path scan: passed;
- identity scan: passed;
- source-purity scan: passed;
- remote one-commit/two-file comparison: passed.

## Forbidden-path and ticker proof

Immutable-base comparison contains only the two authorized new model paths. No page, component, route, CSS, API, resource, adapter, identity, package, backend, OpenAPI, CI, Docker, script, or existing fixture file changed.

Read-only identity proof:

- `web/src/app/GlobalMarketTicker.tsx` base blob: `727f591706a60327b3b219f3287b153a06d1160d`
- `web/src/app/GlobalMarketTicker.tsx` product-head blob: `727f591706a60327b3b219f3287b153a06d1160d`
- `web/src/pages/DashboardPage.tsx` product-head blob: `deb2eee919938d3b1807e353d309c046b08ba6f5`, unchanged from the immutable base.

The model source contains no JSX, React runtime, hooks, browser/DOM access, current time, randomness, locale formatting, network/cache/store access, query hooks, CSS classes, icons, colors, IO, or Replay public-mode concept.

## Blockers and unavailable checks

No implementation or publication blocker remains. The only unavailable check was a direct full-repository exact-candidate run inside the isolated worker container; the reason and the separate successful PR merge-ref CI evidence are recorded above.

The moving phase branch is an integration divergence, not a worker-side blocker. The candidate remains the required one immutable commit above its assigned base for Orchestrator review.

## Completion statement

One product commit, one draft PR, and this one immutable connector report were published. Nothing was merged. No phase branch, worker branch other than the assigned branch, control file, or other path lease was modified. No next microphase was started.
