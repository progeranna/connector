# P3-MP18R — Verified blocker diagnosis

Status: `P3_MP18R_DIAGNOSTIC_VERIFIED_LEASE_EXPANSION_REQUIRED`

## Identity

- Product repository: `progeranna/signalguard-rs`
- Immutable base: `ba31a348dc5055935c45f6be81073688caedd925`
- Base tree: `f629b6ea4339c92d03223c3bd8024cd4cb4571da`
- Branch/worktree: `p3/mp18r-exact-symbol-anomaly-detail` at the immutable base
- Original contract: `signalguard-rs/phase-3/prompts/P3-MP18R.md`
- Original contract commit: `380a6b9a5523f1653adba5cf6a883742de8a1842`
- Original contract blob: `50767715dbd59975fea443d8601952e601654921`
- Diagnostic contract: `signalguard-rs/phase-3/prompts/P3-MP18R-DIAGNOSTIC.md`
- Diagnostic contract commit: `5bb2016654886b7ea5c9fc29d7b50e1d957671ef`
- Diagnostic contract blob: `d076aa17e8347416bc4fc9dcbd26940db41d65a2`

## Primary blocker

`OUT_OF_LEASE_DEPENDENCY`

The implementation changed exactly the ten originally leased files and remained uncommitted at the immutable base. Two stale test-only files outside the original lease require obsolete behavior that the contract explicitly removes.

## First failing gate

Command:

`npm run typecheck`

Exit code: `1`

Failure:

`web/src/features/dashboard/symbolPopupResource.test.tsx(491,7): error TS2345: Argument of type '"anomalies"' is not assignable to parameter of type 'SymbolPopupReturnContext'.`

The immutable-base test calls:

`createSymbolPopupIdentity("live", "BTCUSDT", "anomalies")`.

The failure is expected after strict removal of the unreachable `"anomalies"` return context. It does not fail on the immutable base because the obsolete context is still accepted there.

A temporary leased workaround widened the production input to `string` and normalized unsupported contexts. That workaround is not accepted because it weakens the intended strict type contract and preserves stale test ownership indirectly.

## Full-suite corroboration

Command:

`npm run test:run`

Exit code: `1`

Deterministic result on two runs:

- 43 test files passed;
- 1 test file failed;
- 602 tests passed;
- 1 test failed.

Failing path/test:

`web/src/pages/DashboardPage.test.tsx`

`dashboard retained source contracts > wires the accepted shared sections into popup success presentation`

The immutable-base source-string assertions require:

- `variant="popup"` on `SymbolDetailHeader`;
- `surface="popup"` on `SymbolDetailMetrics`;
- `variant="popup"` on `SymbolDetailAnomalies`;
- `onOpenSymbolDetail={onOpenSymbolDetail}` on `SymbolDetailAnomalies`.

The MP18R contract explicitly requires removing all four obsolete call-site concepts. Reintroducing them in production, comments, or dead source text would violate the result and residual audit.

## Independent GitHub verification

The orchestrator independently fetched both test files at the immutable base and confirmed:

1. `web/src/features/dashboard/symbolPopupResource.test.tsx` contains the exact obsolete `"anomalies"` identity argument.
2. `web/src/pages/DashboardPage.test.tsx` contains exact source-string assertions for all three popup-only props and the anomaly-to-symbol callback.

## Preserved worktree state

- branch: `p3/mp18r-exact-symbol-anomaly-detail`
- HEAD: `ba31a348dc5055935c45f6be81073688caedd925`
- tree: `f629b6ea4339c92d03223c3bd8024cd4cb4571da`
- ahead/behind: `0 / 0`
- implementation changes: present, unstaged and uncommitted
- untracked paths: none
- changed paths: exactly the original ten-file lease
- product commit: none
- remote worker branch: none
- PR: none
- connector implementation report: none

The preserved diff was reported as 10 files, 488 insertions and 95 deletions.

## Required lease correction

Add exactly two test-only files:

- `web/src/features/dashboard/symbolPopupResource.test.tsx`
- `web/src/pages/DashboardPage.test.tsx`

No additional production file is justified.

Required recovery:

- preserve the existing worktree and implementation diff;
- update the two stale tests to the authorized modal-only/prop-free behavior;
- restore strict `SymbolPopupReturnContext` typing in the already leased `symbolPopup.ts`;
- remove the temporary permissive string-normalization workaround;
- rerun all original focused, full, global, bundle and browser gates;
- deliver the same branch and same single required product commit.

Recommendation: `PRESERVE_AND_EXPAND_LEASE`.
