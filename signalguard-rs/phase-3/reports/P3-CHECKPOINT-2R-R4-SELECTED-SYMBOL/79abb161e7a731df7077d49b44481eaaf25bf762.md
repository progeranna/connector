# P3-CHECKPOINT-2R-R4 — Selected-symbol ownership implementation

Status: `P3_CHECKPOINT_2R_R4_SELECTED_SYMBOL_COMPLETE`

Implementation date: 2026-08-19

## Authority and scope

This report records the dedicated local Codex implementation of `P3-CHECKPOINT-2R-R4 — Reconcile open Symbol Detail ownership on mode replacement` using the `$rust-development` correctness-first workflow.

The authoritative contract was fetched through authenticated GitHub access and read completely before product work:

- repository: `progeranna/connector`
- path: `signalguard-rs/phase-3/prompts/P3-CHECKPOINT-2R-R4-SELECTED-SYMBOL.md`
- publication commit: `9ac409bbbd5e2ac8d0cb6bfdc49935b6b7712101`
- verified blob: `e0553a4cf90eeb907eb50bff665174d1917add55`
- verified status: `P3_CHECKPOINT_2R_R4_SELECTED_SYMBOL_IMPLEMENTATION_AUTHORIZED`

The required `CURRENT_EXECUTION.md`, `STATUS.md`, rerun contract, blocker report, blocker-review contract, and independent blocker-review report were read completely. Immediately before this report was written, connector `main` remained at `264a1fad81445734fa02b059958e23ffa2ae8edb`; `CURRENT_EXECUTION.md` remained blob `d4680bde169805a04f34f2b4e8a3c85847022e32`; and both control files still stated `P3_CHECKPOINT_2R_R4_SELECTED_SYMBOL_IMPLEMENTATION_AUTHORIZED`.

## Immutable product base and worker identity

Authenticated fetches before editing and immediately before delivery proved the product target had not drifted:

- repository: `progeranna/signalguard-rs`
- target branch: `refactor/dashboard-modules`
- immutable base commit: `7dab5647d322339f5bd9d0514e5178522d5181c0`
- immutable base tree: `d5ca241f173f2733d6699283084bf7435c0e9259`
- ordered parent 1: `cbf5c543ada8752c273fbb2e91be029c9febc3d3`
- ordered parent 2: `778b23b6a9dbb4e1b652e7a31349a35b707f3373`
- remote target after implementation: still exact base commit/tree

Implementation used the fresh dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p3-checkpoint2r-r4-selected-symbol`

Final worker identity:

- branch: `p3/checkpoint2r-selected-symbol-ownership`
- commit: `79abb161e7a731df7077d49b44481eaaf25bf762`
- tree: `d8c0289a05b3646b3abc7056bd269b927e61d5c4`
- direct parent: `7dab5647d322339f5bd9d0514e5178522d5181c0`
- commit message: `fix(ui): reconcile modal symbol on mode change`
- relation to base: exactly one commit ahead, zero behind
- merge base: exact immutable base
- remote worker ref after push: `79abb161e7a731df7077d49b44481eaaf25bf762`

## Exact diff and behavioral correction

The base-to-worker diff contains exactly the two writable lease paths, with no additions, deletions, or renames:

- `web/src/pages/DashboardPage.tsx`
- `web/src/pages/DashboardPage.popup.test.tsx`

Diff size: 145 insertions and 14 deletions.

`DashboardPage.tsx` now uses one narrow identity-reconciliation function for both the immediately rendered open Symbol Detail identity and the existing modal-state replacement effect. It derives the replacement from the new `selectedUiMode` and the already resolved non-null `selectedSymbol` in the same state transition. The previous `modeChanged` / `!modeChanged` suppression was removed. The existing replacement still preserves `returnContext`; when the parent identity changes, nested symbol anomaly state still collapses to Symbol Detail and therefore clears the stale UUID.

No second selected-symbol source, storage, URL state, resource ownership, delay, remount, reload, or modal-closing workaround was introduced.

`DashboardPage.popup.test.tsx` adds the exact composed regression `reconciles the open parent to the restored per-mode symbol`. It uses distinct Demo BTC, Live BTC, and Live ETH UUIDs and proves:

- separate stored Demo/BTCUSDT and Live/ETHUSDT selection ownership;
- Demo/BTC to Live/BTC nested UUID clearing;
- Live/BTC to Live/ETH nested UUID clearing;
- Live/ETH to Demo atomic restoration to `demo:BTCUSDT:symbols`;
- preserved `symbols` return context;
- absence of the stale Live ETH UUID and all ETHUSDT modal content;
- exact Demo/BTCUSDT popup/resource identity;
- rejection of late Live/ETH and Demo/ETH resources.

Two pre-existing mode-only tests now explicitly seed Live's intended `ETHUSDT` selection so their same-symbol replacement assertions retain their original meaning under the corrected per-mode ownership contract. Existing R1, R2, R3, MP18R, close/focus, and late-resource regressions remain present and passing.

## Focused and complete frontend validation

Focused regression execution:

- command: `npm run test:run -- src/pages/DashboardPage.popup.test.tsx`
- result: PASS — 1/1 file, 32/32 tests
- correctness-first proof: the new test first failed on the immutable behavior with final modal `demo:ETHUSDT:symbols`, then passed after the production correction

Full frontend gates from `web/`:

| Command | Result |
|---|---|
| `node --test scripts/check-bundle-size.test.mjs` | PASS — 25/25 |
| `npm run test:run` | PASS — 44/44 files, 615/615 tests, 0 failures |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS — zero warnings |
| `npm run build` | PASS — 134 modules transformed |
| `npm run bundle:check` | PASS — 25/25 policy tests and budget PASS |

The frontend increased from the accepted 614-test baseline to 615 tests.

Actual production JavaScript measurements and unchanged limits:

- emitted asset: `assets/index-0596aac8.js`
- initial JS: `389559 / 409600` bytes
- largest JS: `389559 / 409600` bytes
- total JS: `389559 / 414720` bytes
- bundle budget files and limits: unchanged

## Complete Rust and global gates

All prescribed root commands passed:

| Command | Result |
|---|---|
| `cargo fmt --check` | PASS |
| `cargo run --quiet --bin export-api-contract -- --check` | PASS |
| `cargo run --quiet --bin export-api-contract -- --validate` | PASS |
| `cargo check` | PASS |
| `cargo clippy --all-targets --all-features -- -D warnings` | PASS — zero warnings |
| `cargo test` | PASS — 379 passed, 0 failed, 3 declared Redis-dependent library tests ignored; declared service-dependent integration targets remained ignored as designed |
| `cargo test --test replay_e2e` | PASS — target discovered; 2 declared service-dependent tests ignored |
| `docker compose config` | PASS |
| `docker compose --profile app config` | PASS |
| `bash -n scripts/demo-replay.sh` | PASS |
| `bash -n scripts/smoke.sh` | PASS |
| `git diff --check` | PASS |

The full `cargo test` run also discovered the declared PostgreSQL, Redis, replay, and runtime-control integration targets; their service-dependent tests remained ignored exactly as designed.

## Focused real-browser recovery proof

The production build and exact worker commit were exercised through Google Chrome `151.0.7922.169` controlled by Playwright Core `1.55.0`. No frontend API resource, selected-symbol storage, mode state, popup resource, navigation, favicon behavior, or market/anomaly data was mocked or intercepted.

Isolated topology:

- PostgreSQL 16 container: `p3c2rr4-postgres`, `127.0.0.1:55446`
- Redis 7 container: `p3c2rr4-redis`, `127.0.0.1:56396`
- worker backend in replay mode: `127.0.0.1:18096`
- worker Vite production preview: `127.0.0.1:15199`
- ordinary pass-through validation proxy: `127.0.0.1:15200`

The backend provided real deterministic Demo resources and real replay-backed Live resources. Live observations were source `live`, BTCUSDT and ETHUSDT both `observed`, with four actual Live anomalies. No Demo fallback was presented as Live.

The exact sequence passed on desktop `1440×900` and mobile `390×844`:

1. `demo:BTCUSDT:symbols`, exact Demo BTC UUID `3d6ce59a-fd8b-4180-9864-08933b9d4168` opened.
2. Demo to Live cleared the nested Demo UUID and produced `live:BTCUSDT:symbols`.
3. Exact Live BTC UUID `902048fe-c430-41df-a846-ae50568eeef4` opened.
4. Live BTC to Live ETH cleared the nested BTC UUID and produced `live:ETHUSDT:symbols`.
5. Exact Live ETH UUID `032179a4-a56a-48f8-bae4-c2271677fb1f` opened.
6. Live ETH to Demo cleared the nested Live ETH UUID and atomically produced `demo:BTCUSDT:symbols`.

Both final viewports proved:

- header mode: Demo;
- header symbol: BTCUSDT;
- stored selections: Demo/BTCUSDT and Live/ETHUSDT;
- open modal identity: `demo:BTCUSDT:symbols`;
- preserved `returnContext`: `symbols`;
- modal heading/content: BTCUSDT;
- ETHUSDT content: absent;
- stale Live ETH UUID: absent;
- late/stale Demo ETH resource request on final transition: absent;
- pathname: `/dashboard`;
- query: only `mode=demo`; no symbol, UUID, or modal identity entered the URL;
- unexpected console errors: 0;
- console warnings: 0;
- page errors: 0;
- unhandled promise rejections: 0;
- unexpected request failures: 0;
- HTTP responses at or above 400: 0;
- transport failures attributable to the recovery: 0;
- `/favicon.ico` request count: 0;
- modal body lock while open and restoration after Close: PASS.

Each viewport made 15 browser requests. The combined proxy log contains 30 requests: 24 backend and 6 preview requests, all HTTP 200, with no logged transport errors and no favicon request.

## External evidence inventory

All focused browser evidence remains outside both repositories at:

`/tmp/p3-checkpoint2r-r4-selected-symbol-20260819`

| Artifact | SHA-256 |
|---|---|
| `focused-browser-proof.json` — structured identity/error/resource log | `a65381fbed504a0c561f271cdd2d0e8a1f1fa48e174ccde107e32ea526f05b50` |
| `request-log.jsonl` — structured request log | `fd9dbe082109705709ce1e31e0b7f4ab5e990d65fe8c77fd81ef5288202e9ebd` |
| `focused-browser-proof.mjs` | `c180d751a4ab3385e3f0aa068cadbd8175ea7c3fc6f2e99f02716efdee52507a` |
| `proxy.mjs` | `c61ea3cfbb5e6b2d65dd0944f19c4c739b5f62386d2885931818d183e6eb9ea2` |
| `screenshots/final-demo-btcusdt-desktop-1440x900.png` | `c9e9c18ddf7efa6f83cc2e076e1b026bf2ce726c058af26e82b13d148613314c` |
| `screenshots/final-demo-btcusdt-mobile-390x844.png` | `e0a2afb6d74cbfb99129691eb8972e8debb8a0f2d80620dd23ff886564658c29` |

Both final screenshots were visually inspected and show the corrected open Demo BTCUSDT Symbol Detail with its All Markets return action on the required viewport.

## Residual audit and delivery boundary

Before commit and again after commit, the effective base-to-worker diff contained exactly the two lease paths. There were no package, lockfile, bundle-budget, script/config, route, CSS, API, backend Rust, favicon/static asset, ticker, popup-helper, popup-resource, AppShell, or selected-symbol-storage changes. No tracked path was added, deleted, or renamed.

After validation, the generated `web/dist` directory and external `web/node_modules` link were removed; isolated processes and containers were stopped; ports `55446`, `56396`, `18096`, `15199`, and `15200` were free; and the worker worktree was clean. The external evidence and Cargo target remain only under `/tmp` outside both repositories.

Only `p3/checkpoint2r-selected-symbol-ownership` was pushed. `refactor/dashboard-modules` remained unmodified at the immutable base. No pull request was opened and no merge was performed. Connector `CURRENT_EXECUTION.md` and `STATUS.md` were not modified. R4 review, integration, Bridge 01/02, semantic Wave 4, and all later work did not begin.

Terminal status: `P3_CHECKPOINT_2R_R4_SELECTED_SYMBOL_COMPLETE`
