# P3-MP10-WEB2 delivery report

Status: `BLOCKED_BEFORE_DRAFT_PR_BY_REQUIRED_CI_GATE`

## Replacement identity

- Contract: `signalguard-rs/phase-3/prompts/P3-MP10-WEB2.md`
- Contract commit: `0439f09bc468c1a391fdf77ebd2269433b62be16`
- Product repository: `progeranna/signalguard-rs`
- Replacement branch: `p3/mp10-timeline-panel-r1`
- Assigned base: `93a870010730c458417ccfff392cb97aff23d6c9`
- Product head: `0ad34843578670d8b313af4f7f53853195c305a9`
- Product commit: `feat(ui): add timeline panel component`
- Product history: exactly one commit ahead of the assigned base; no amend, rebase, reset, force-push, merge, or additional product commit occurred.

## Rejected-head quarantine

The rejected WEB1 evidence remains untouched:

- branch: `p3/mp10-timeline-panel`
- head: `9bddc301d32c51bbb54dc5058d8d33320c144ff7`
- PR: `#36`, closed and unmerged
- CI: `30367741374`; Rust/global succeeded and frontend tests failed
- rejected test blob was not reused

No mutation was made to the rejected branch or head.

## Exact product paths

The assigned-base-to-product-head comparison reports exactly these two added paths and no others:

1. `web/src/features/dashboard/TimelinePanel.tsx`
2. `web/src/features/dashboard/TimelinePanel.test.tsx`

Comparison result against the assigned base:

- status: `ahead`
- ahead: `1`
- behind: `0`
- commits: `1`
- additions: `558` component lines and `436` test lines

## Component API

Exports:

- `TimelinePanel`
- `TimelinePanelProps`

Readonly props:

```ts
export type TimelinePanelProps = Readonly<{
  selectedMarket: DashboardSymbolSummary | null;
  timelinePoints: readonly MarketTimelinePoint[];
  timelineAnomalies: readonly DashboardAnomaly[];
  isSummaryLoading: boolean;
  isTimelineLoading: boolean;
  timelineErrorMessage: string | null;
  onRetryTimeline: () => void;
  emptyAnchorMs: number;
}>;
```

The component owns presentation only. It receives selected-market state, timeline resource state, retry behavior, and the empty-domain anchor through props.

## Render-state matrix

| State | Timeline/chart area | Snapshot area | Precedence/evidence |
|---|---|---|---|
| Summary loading | `h-40` loading skeleton | not rendered | highest precedence |
| No selected market | `Waiting for market data` | `Unknown market`, `No data yet`, no-current-state message | current inline copy preserved |
| Configured | configured-not-active message | configured status and same availability message | timeline error/loading/anomalies suppressed |
| Awaiting | awaiting-first-live-data message | awaiting status and same availability message | timeline error/loading/anomalies suppressed |
| Unavailable | live-data-unavailable message | unavailable status and same availability message | timeline error/loading/anomalies suppressed |
| Observed error | `Market timeline unavailable` with retry | observed snapshot | before loading, empty, and chart |
| Observed loading | `h-[320px]` loading skeleton | observed snapshot | before normalized-empty and chart |
| Observed normalized-empty | `Waiting for market data` | observed snapshot | invalid timestamp/price points are excluded by accepted normalization |
| Observed chart | accepted Recharts area chart, domains, anomaly lines, tooltip | source/status plus Price, Spread, Trades/min, Freshness | final observed state |

The tests also cover Live/Demo source wording, highest-severity anomaly badge, in-domain marker filtering/colors, disabled animation, zero values, inclusive ±15-second tooltip matching, responsive grid/classes, input immutability, and deterministic repeated rendering.

## Accepted-owner usage

The remote component imports and calls the accepted owners:

- `normalizeTimelinePoints` from `./timelineNormalization`
- `buildTimelineDomains` from `./timelineDomains`
- exact call: `buildTimelineDomains(normalizedTimelinePoints, emptyAnchorMs)`

No normalization or timeline-domain builder is reimplemented. No API/query hook, selected-symbol resolution, route, popup, storage, source fallback, browser timer, random value, or current-time owner was introduced. The component contains no `Date.now()` and no zero-argument `new Date()`.

## Corrected domain evidence

The remotely committed test contains:

```ts
const exactPriceDomain = [99.796, 102.204];
expect(102 * 0.002).toBeGreaterThan(2 * 0.08);
expect(exactPriceDomain).toEqual([100 - 102 * 0.002, 102 + 102 * 0.002]);
```

This proves that for prices `100` and `102`:

- range padding: `2 * 0.08 = 0.16`
- magnitude padding: `102 * 0.002 = 0.204`
- magnitude padding wins
- exact accepted domain: `[99.796, 102.204]`

The rejected `[99.84, 102.16]` expectation is absent remotely.

## Exact-SHA remote hardening

Both files were fetched from GitHub by exact product SHA `0ad34843578670d8b313af4f7f53853195c305a9` as complete UTF-8 TSX from first through final line.

Remote blobs:

- `web/src/features/dashboard/TimelinePanel.tsx`: `ba89c4f707a76b1abe531011c293bc61dee3a606`
- `web/src/features/dashboard/TimelinePanel.test.tsx`: `f72c6e59266d43f18797db89de471661be435a4c`

Additional unchanged-file evidence:

- `web/src/pages/DashboardPage.tsx` at the product head remains blob `deb2eee919938d3b1807e353d309c046b08ba6f5`, identical to the assigned base.

## Verification and gates

### Completed

- immutable contract read at its exact connector commit
- assigned base verified
- replacement branch verified at the assigned base before fast-forward
- exactly one normal product commit created
- non-force fast-forward to the replacement branch
- exact-SHA UTF-8 read-back of both committed files
- both remote blob SHAs recorded
- corrected-domain presence and rejected-domain absence verified
- exact assigned-base diff verified as only the two authorized paths
- local TypeScript `transpileModule` syntax check of `TimelinePanel.test.tsx`: zero diagnostics
- current presentation/ownership/forbidden-boundary assertions included in the focused suite

### Unavailable or blocked

The isolated worker environment had no usable GitHub CLI or repository checkout. Network/DNS prevented cloning, and frontend dependencies could not be installed from the available registry/cache. Therefore these local commands could not be executed honestly:

- focused Vitest command
- full frontend test suite
- frontend typecheck
- frontend lint
- frontend build
- bundle budget
- Rust/global gates
- literal `git diff --check`

The GitHub connector can retrieve pull-request-triggered workflow runs for a commit but does not expose repository-wide or push-triggered run listing. For the exact product head it returned no PR-triggered run, and the commit-status endpoint returned no legacy statuses. Consequently, a complete successful exact-head/current-base Actions run could not be identified or verified before PR creation.

No draft PR was opened, because the immutable contract explicitly requires complete green CI before opening the one draft PR. Opening it to manufacture the first visible run would violate the required ordering.

## CI state

- required replacement CI run: unavailable/unverified
- frontend tests: unavailable/unverified
- typecheck: unavailable/unverified
- lint: unavailable/unverified
- build: unavailable/unverified
- bundle budget: unavailable/unverified
- Rust/global job: unavailable/unverified
- draft PR: not opened due the preceding required gate

The rejected run `30367741374` was inspected only as immutable evidence and is not claimed for the replacement head.

## Final divergence

The assigned base remains the product commit parent. The moving PR base advanced during execution:

- current `refactor/dashboard-modules`: `144ca95ae0338cfcf5ae00bd1cccd8317dbbc0b0`
- merge base: `93a870010730c458417ccfff392cb97aff23d6c9`
- replacement versus current base: `ahead 1`, `behind 1`, status `diverged`

No rebase, merge, reset, or history rewrite was performed because the contract and current orchestration status prohibit it.

## Forbidden-path and ticker proof

The exact assigned-base diff contains only the two authorized TimelinePanel paths. Therefore all of the following remain untouched by the product commit:

- `DashboardPage.tsx` and caller wiring
- upper ticker and ticker CSS/behavior
- accepted normalization/domain files and tests
- other Wave 2 component leases
- pages, routes, popup/modal composition, CSS/global styles
- API/query/resource/adapter/identity files
- package, lock, and configuration files
- backend, OpenAPI, CI, Docker, deployment, and scripts
- P3-MP15

The unchanged `DashboardPage.tsx` blob and two-path compare provide remote proof; no ticker path appears in the commit.

## Blocker disposition

Product implementation, one-commit publication, exact-SHA remote hardening, and scope verification are complete. Publication is intentionally stopped before the draft PR because required green GitHub Actions CI could not be observed or verified through the available interfaces, and the moving base has diverged without authorization to rewrite the worker branch.
