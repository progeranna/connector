# Phase 3 Checkpoint 0 — ACCEPT

## Identity

- Phase branch: `refactor/dashboard-modules`
- Phase 3 starting SHA / final Phase 2 UI: `6b57938d87e05d3b4fa4858f9c34c27739877821`
- Accepted Wave 0 SHA: `3587ec9b70b677121aa796467d5bb359ffb4d174`
- Integrated microphases: P3-MP01, P3-MP02, P3-MP03, P3-MP04
- Verdict: `ACCEPT`

## Combined Wave 0 tree

Comparison from the Phase 3 starting SHA to the accepted Wave 0 SHA is ahead `4`, behind `0` and contains exactly eight added files:

- `web/src/test/uiSmokeMatrix.ts`
- `web/src/test/uiSmokeMatrix.test.ts`
- `web/src/shared/components/Tooltip.tsx`
- `web/src/shared/components/Tooltip.test.tsx`
- `web/src/features/dashboard/statusDescriptors.ts`
- `web/src/features/dashboard/statusDescriptors.test.ts`
- `web/src/test/statusDescriptorFixtures.ts`
- `web/src/test/statusDescriptorFixtures.test.ts`

No existing product file changed.

## Visual-equivalence evidence

Checkpoint 0 requires stable localhost to remain visually identical to the final Phase 2 UI.

Visual equivalence is established by construction on this Wave 0 tree:

- no page, route, AppShell, dashboard compositor, symbol route, popup, shared existing component, CSS, package, build configuration, API/query/resource/adapter/identity file, backend, or OpenAPI file changed;
- the new smoke matrix and semantic fixtures are test-only files;
- the new tooltip primitive has no caller migration;
- the new status descriptor model has no JSX or caller wiring;
- no existing production import was modified to reference either new production module;
- therefore the rendered production graph, visible copy, layout, interactions, routes, responsive surfaces, and runtime data behavior remain identical to final Phase 2.

No screenshot or manual browser run is represented as performed for this checkpoint. Because the existing production render graph is byte-identical and all Wave 0 production additions are unreferenced foundations, a browser run cannot expose a Wave 0 visual delta. This structural proof is stronger than a subjective screenshot comparison and satisfies the visual-identity gate without claiming an unavailable manual run.

## Ticker proof

The upper ticker remained byte-identical throughout Wave 0.

- Starting and accepted Wave 0 blob: `727f591706a60327b3b219f3287b153a06d1160d`
- Path: `web/src/app/GlobalMarketTicker.tsx`

Ticker text, data access, order, structure, scrolling, animation, behavior, and styling are unchanged.

## Verification evidence

- P3-MP01 exact-head CI: `30352221605` — success.
- P3-MP02 current-base combined-tree CI: `30352898480` — success.
- P3-MP03 refreshed combined-tree CI after MP01+MP02: `30353706127` — success.
- P3-MP04 exact current-base integration-tree CI: `30355416018` — success.

The final run validates the full accepted Wave 0 base plus P3-MP04 and passed:

- frontend full tests;
- frontend typecheck;
- frontend lint;
- production build;
- bundle budget;
- Rust formatting;
- generated API contract check;
- OpenAPI validation;
- cargo check;
- clippy with warnings denied;
- Rust tests;
- replay target discovery;
- Docker Compose validation;
- script syntax checks.

## Result

Checkpoint 0 is closed.

Wave 1 is authorized from exact Phase 3 SHA:

`3587ec9b70b677121aa796467d5bb359ffb4d174`

The authorized parallel fan-out is:

- P3-MP05 — timeline normalization;
- P3-MP07 — Market Health preview model;
- P3-MP08 — Recent Anomalies preview model;
- P3-MP09 — dashboard resource-state mapping.

P3-MP06 remains blocked until P3-MP05 is accepted and integrated.