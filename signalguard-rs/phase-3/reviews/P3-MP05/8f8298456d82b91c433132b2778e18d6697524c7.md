# P3-MP05 Review — `8f8298456d82b91c433132b2778e18d6697524c7`

## Verdict

`ACCEPT`

Guarded squash integration is authorized only for exact product head:

`8f8298456d82b91c433132b2778e18d6697524c7`

## Identity

- product repository: `progeranna/signalguard-rs`
- assigned base: `3587ec9b70b677121aa796467d5bb359ffb4d174`
- candidate branch: `p3/mp05-timeline-normalization`
- candidate head: `8f8298456d82b91c433132b2778e18d6697524c7`
- PR: `#28`
- commit message: `feat(ui): extract timeline normalization`
- divergence from assigned base: ahead `1`, behind `0`, exactly one commit

## Exact authorized diff

Only two new files:

- `web/src/features/dashboard/timelineNormalization.ts`
- `web/src/features/dashboard/timelineNormalization.test.ts`

No existing product file changed. `DashboardPage.tsx`, domain paths, API/data boundaries, package/configuration files, backend, OpenAPI, CI, scripts, CSS, and upper ticker are unchanged.

## Semantic review

The production module satisfies the immutable P3-MP05 contract:

- exports readonly `NormalizedTimelinePoint`;
- exports one-point and collection normalizers;
- parses timestamps only with `Date.parse` and rejects non-finite results;
- preserves the original timestamp string;
- rejects empty/whitespace and non-finite prices;
- accepts finite zero and negative prices;
- preserves the exact source price string as `priceLabel`;
- preserves finite spread including zero and negatives;
- maps invalid spread, trades/minute, and event-age values independently to `null`;
- requires trades/minute and event age to be finite and non-negative;
- filters unusable points without sorting or deduplication;
- preserves valid input order and duplicate timestamps/prices;
- returns a new array and does not mutate input;
- contains no chart-domain policy, JSX, React/Recharts runtime, browser/current-time/locale/network/query/presentation dependency, or Replay concept.

Focused tests cover the required semantics, including exact string preservation, zero/negative price, invalid timestamps/prices, optional-field handling, duplicate/order preservation, immutability, determinism, and purity.

## Verification

Exact-head GitHub Actions run `30358841104` completed successfully:

- full frontend tests;
- typecheck;
- lint;
- production build;
- bundle budget;
- Rust formatting/check/clippy/tests;
- generated contract and OpenAPI checks;
- replay target, Docker Compose, and script syntax checks.

A separate focused-only Vitest command was unavailable in the remote-only worker environment and was not falsely claimed. The focused test file passed within the full frontend suite.

## Delivery evidence

- delivery report: `signalguard-rs/phase-3/reports/P3-MP05/8f8298456d82b91c433132b2778e18d6697524c7.md`
- report commit: `5a04a9a1e37a2293cc3e89af5f103949b3965d96`
- upper ticker blob remains `727f591706a60327b3b219f3287b153a06d1160d`

## Authorization

The Orchestrator may mark PR #28 ready and squash-merge it into `refactor/dashboard-modules` using `expected_head_sha=8f8298456d82b91c433132b2778e18d6697524c7`.

Any head movement invalidates this authorization.
