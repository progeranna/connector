# P3-MP12-WEB2 Independent Review

Verdict: `REJECT`

## Identity

- Product repository: `progeranna/signalguard-rs`
- Candidate branch: `p3/mp12-market-health-mobile-r1`
- Candidate head: `ae19b372943f890c9f0ec18bc85143e366dadef1`
- Assigned base: `93a870010730c458417ccfff392cb97aff23d6c9`
- Draft PR: `#39` — closed, unmerged
- Product commit: `feat(ui): add market health mobile cards`
- Connector report: `signalguard-rs/phase-3/reports/P3-MP12/ae19b372943f890c9f0ec18bc85143e366dadef1.md`

## Scope review

The immutable-base diff contains exactly the two authorized additions:

1. `web/src/features/dashboard/MarketHealthMobileCards.tsx`
2. `web/src/features/dashboard/MarketHealthMobileCards.test.tsx`

No existing product file, `DashboardPage.tsx`, accepted model, CSS, package/configuration, backend, CI, or upper ticker file changed.

## Required-gate result

GitHub Actions run `30369207184` failed:

- Rust/global job: success;
- frontend tests: success;
- frontend typecheck: failure;
- frontend lint: skipped;
- frontend build: skipped;
- frontend bundle budget: skipped.

A red required gate and skipped downstream required gates prohibit acceptance.

## Confirmed defect

The committed test blob `1ed9583f89f329f097db8d4285a37a954645a642` contains:

```ts
previewRow({ key: "live:INFO", symbol: "INFO", healthStatus: "info" })
```

The accepted model derives `healthStatus` from `DashboardHealthSummary["status"]`, and the authoritative schema permits only:

```text
healthy | degraded | unhealthy
```

with `null` allowed by `MarketHealthPreviewRow`. `"info"` is an anomaly-severity value, not a market-health status. This invalid fixture causes the repository typecheck failure. The unknown-status presentation must be tested with `healthStatus: null`, not an out-of-domain literal.

## Semantic note

The replacement corrected the prior score-tone precedence regression and its frontend tests passed. This does not override the failed typecheck or authorize repair on the immutable one-commit branch.

## Disposition

- Candidate head: `REJECTED_AND_QUARANTINED`.
- PR `#39`: closed without merge.
- Do not amend, reset, rebase, force-push, reopen, merge, or add a corrective commit to the candidate branch.
- Recovery requires a new branch and immutable WEB3 contract from the current accepted Phase 3 SHA.
