# P3.5-MP06B-R1 — Current-base type-contract recovery report

## Product identity

- Repository: `progeranna/signalguard-rs`
- Exact current base and phase head: `bb3df51339ad4a7a3524fe4782fdc24069944160`
- Immutable MP06B worker head: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`
- Recovery branch: `p35/mp06b-r1-symbol-detail-test-guards`
- Recovery merge commit: `1893a0dc193b837a8be26274d00fcb6cf83401da`
- Commit message: `test(ui): repair MP06B type contracts`
- First parent: `bb3df51339ad4a7a3524fe4782fdc24069944160`
- Second parent: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`

The recovery branch was created from the exact current phase head. The immutable worker was merged with `git merge --no-ff --no-commit`, and exactly one normal merge commit was created after validation. The immutable worker commit remains unchanged and is the second parent.

## Validation context

The failed PR was `#64`, with merge ref `e923defbeeeebf7cf6bf7457b77be28898286c82` and CI run `30712501382`. Its runtime tests passed, but TypeScript failed on six diagnostics in two MP06B-leased files.

The six diagnostics were:

1. `web/src/pages/DashboardPage.popup.test.tsx`: the adapter recorder stored the optional second parameter inferred from `Parameters<typeof adaptMarketResourceToViewModel>` in a required identity field.
2. `web/src/pages/DashboardPage.popup.test.tsx`: the refetch mock for the loading identity inferred an untyped `vi.fn` rather than `() => Promise<unknown>`.
3. `web/src/pages/DashboardPage.popup.test.tsx`: the refetch mock for the error identity inferred an untyped `vi.fn` rather than `() => Promise<unknown>`.
4. `web/src/pages/DashboardPage.popup.test.tsx`: the refetch mock for the unavailable identity inferred an untyped `vi.fn` rather than `() => Promise<unknown>`.
5. `web/src/pages/DashboardPage.popup.test.tsx`: the refetch mock for the success/current identity inferred an untyped `vi.fn` rather than `() => Promise<unknown>`.
6. `web/src/pages/SymbolDetailPage.test.tsx`: the observed dashboard-state fixture supplied numeric `best_ask_price`, `best_bid_price`, and `last_trade_price` values where the current `DashboardStateSummary` contract requires `string | null`.

## Exact repair diff

Only the two authorized recovery files were edited beyond the immutable worker merge:

```diff
diff --git a/web/src/pages/DashboardPage.popup.test.tsx b/web/src/pages/DashboardPage.popup.test.tsx
@@
 import type {
   DashboardAnomaly,
   DashboardSummary,
   DashboardSymbolSummary,
   UiMode,
 } from "@/features/dashboard/types";
 
+type PopupResourceRefetch = SymbolPopupResourceState["refetch"];
+
 const testState = vi.hoisted(() => ({
@@
-  resourceRefetchByIdentity: new Map<string, ReturnType<typeof vi.fn>>(),
+  resourceRefetchByIdentity: new Map<string, PopupResourceRefetch>(),
@@
     adaptMarketResourceToViewModel: (
       ...args: Parameters<typeof actual.adaptMarketResourceToViewModel>
     ) => {
       const viewModel = actual.adaptMarketResourceToViewModel(...args);
+      const identity = args[1];
+      if (!identity) {
+        throw new Error("Expected market resource adapter identity");
+      }
       testState.adapterCalls.push({
-        identity: args[1],
+        identity,
         resource: args[0],
         viewModel,
@@
     const status = testState.resourceStatusByIdentity.get(key) ?? "success";
     let refetch = testState.resourceRefetchByIdentity.get(key);
     if (!refetch) {
-      refetch = vi.fn(async () => undefined);
+      const createdRefetch: PopupResourceRefetch = vi.fn(
+        async (): Promise<unknown> => undefined,
+      );
+      refetch = createdRefetch;
       testState.resourceRefetchByIdentity.set(key, refetch);
     }
diff --git a/web/src/pages/SymbolDetailPage.test.tsx b/web/src/pages/SymbolDetailPage.test.tsx
@@
     state: availability === "observed"
       ? {
-          best_ask_price: 101,
-          best_bid_price: 100,
+          best_ask_price: "101",
+          best_bid_price: "100",
           depth_sequence_gap_count: 0,
           last_event_age_ms: 100,
           last_event_time: "2026-07-20T10:00:00.000Z",
-          last_trade_price: 100.5,
+          last_trade_price: "100.5",
```

The adapter identity assertions, canonical `(uiMode, symbol)` identity, per-identity retry isolation, promise-returning mocks, route behavior, and all accepted MP06B contracts remain intact. No diagnostic suppression, `any`, or production change was used.

## Validation results

### Frontend

- `npm ci`: pass.
- Focused command covering all six MP06B files: 6 files passed, 60 tests passed.
- Full `npm run test:run`: 43 files passed, 640 tests passed.
- `npm run typecheck`: pass.
- `npm run lint`: pass with zero warnings.
- `npm run build`: pass; 714 modules transformed.
- `npm run bundle:check`: pass.
- Raw production JS: `757217` bytes.
- Vite gzip display: `219.99 kB`.
- Direct `gzip -c` measurement: `219504` bytes.
- Largest JS budget: `761856` bytes, unchanged.
- Total JS budget: `761856` bytes, unchanged.
- Raw headroom: `4639` bytes.

The focused and full suites preserved the route/popup visual, behavior, accessibility, identity, ordering, immutability, state precedence, modal lifecycle, no-fallback, and selected-symbol isolation contracts. React Router future-flag messages and existing Recharts DOM warnings were emitted by tests but did not produce failures or lint warnings.

### Rust, repository, and scripts

- `cargo fmt --check`: pass.
- `cargo run --quiet --bin export-api-contract -- --check`: pass.
- `cargo run --quiet --bin export-api-contract -- --validate`: pass.
- `cargo check`: pass.
- `cargo clippy --all-targets --all-features -- -D warnings`: pass.
- `cargo test`: pass; 379 unit tests passed and service-backed tests were explicitly ignored as documented.
- `cargo test --test replay_e2e`: pass; both service-backed replay tests were explicitly ignored as documented.
- `docker compose config`: pass.
- `docker compose --profile app config`: pass.
- `bash -n scripts/demo-replay.sh`: pass.
- `bash -n scripts/smoke.sh`: pass.

No ignored service-backed test was claimed as a live integration test.

## Scope and history verification

- The final diff from `bb3df51339ad4a7a3524fe4782fdc24069944160` contains exactly these five MP06B worker test files:
  - `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`
  - `web/src/features/dashboard/SymbolDetailHeader.test.tsx`
  - `web/src/features/dashboard/selectedSymbolConsumers.test.ts`
  - `web/src/pages/DashboardPage.popup.test.tsx`
  - `web/src/pages/SymbolDetailPage.test.tsx`
- Only `DashboardPage.popup.test.tsx` and `SymbolDetailPage.test.tsx` contain changes beyond the immutable worker merge.
- `web/src/features/dashboard/SymbolDetailMetrics.test.tsx` remains byte-identical to both the current base and immutable worker blob `b67df30b29438b8ff055e9f830e74dbce9edd26c`.
- No production, MP06A, package, lockfile, configuration, budget, route, generated-contract, backend, documentation, Wave 3, or Phase 4 file changed.
- The recovery merge has exactly two parents in the required order.
- The branch is one first-parent commit beyond the exact current base and contains the immutable worker commit as an ancestor.
- Product branch publication was verified at `1893a0dc193b837a8be26274d00fcb6cf83401da`.

No pull request was opened and no merge was performed by the recovery worker.

## Terminal disposition

`P3_5_MP06B_R1_IMPLEMENTATION_COMPLETE`
