# P3.5-MP06B-R1 — Integration record

## Product identity

- Repository: `progeranna/signalguard-rs`
- Previous integrated phase head: `bb3df51339ad4a7a3524fe4782fdc24069944160`
- Immutable MP06B worker head: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`
- Recovery branch: `p35/mp06b-r1-symbol-detail-test-guards`
- Recovery merge commit: `1893a0dc193b837a8be26274d00fcb6cf83401da`
- Recovery commit message: `test(ui): repair MP06B type contracts`
- Recovery first parent: `bb3df51339ad4a7a3524fe4782fdc24069944160`
- Recovery second parent: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`
- Recovery report: `signalguard-rs/phase-3.5/reports/P3.5-MP06B-R1/1893a0dc193b837a8be26274d00fcb6cf83401da.md`
- Recovery report commit: `12d12b67d3ad96ae6faa19ffecd4b1799fc1371c`

The immutable worker commit remains preserved in history. The recovery used one normal merge commit and did not rebase, cherry-pick, amend, reset, squash, force-push, or rewrite either history.

## Accepted scope

The final diff from the previous integrated phase head contains exactly these five MP06B-leased test files:

1. `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`
2. `web/src/features/dashboard/SymbolDetailHeader.test.tsx`
3. `web/src/features/dashboard/selectedSymbolConsumers.test.ts`
4. `web/src/pages/DashboardPage.popup.test.tsx`
5. `web/src/pages/SymbolDetailPage.test.tsx`

The first three files are byte-identical to the immutable MP06B worker versions. Only `DashboardPage.popup.test.tsx` and `SymbolDetailPage.test.tsx` contain the recovery-authorized TypeScript repairs.

`web/src/features/dashboard/SymbolDetailMetrics.test.tsx` remained unchanged. No production, MP06A, package, lockfile, dependency, configuration, budget, route, generated-contract, backend, visual, product-copy, Wave 3, or Phase 4 file changed.

## Exact merge-ref validation

- Integration PR: `#65`
- PR head: `1893a0dc193b837a8be26274d00fcb6cf83401da`
- Tested PR merge ref: `162fcb9f497aaacfc794163ee4c429a5c44394b7`
- Authoritative CI run: `30713464533`
- Merge method: normal merge commit
- Actual merge commit and new phase head: `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`

GitHub Actions checked out `refs/remotes/pull/65/merge` at `162fcb9f497aaacfc794163ee4c429a5c44394b7`.

Validation result:

- Rust job: success;
- frontend job: success;
- frontend test files: `43` passed;
- frontend tests: `640` passed;
- TypeScript: pass;
- ESLint with zero warnings: pass;
- production build: pass;
- transformed modules: `714`;
- bundle check: pass;
- raw production JS: `757217` bytes;
- Vite gzip display: `219.99 kB`;
- largest JS budget: `761856` bytes, unchanged;
- total JS budget: `761856` bytes, unchanged;
- raw headroom: `4639` bytes.

The actual merge commit tree was directly compared with the exact green PR merge-ref tree. The comparison returned zero changed files.

The phase branch `refactor/dashboard-modules` was then compared with `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328` and was identical.

## Wave 2 disposition

MP04, MP05, MP06A, and MP06B are now integrated. Wave 2 is complete.

Phase 3.5 remains open because the minimum target is `8192` bytes of total-JS headroom and current headroom is `4639` bytes. The remaining required reduction is `3553` bytes.

Wave 3 may proceed only with measured local MP07 and MP08 experiments against exact phase head `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`. Production implementation is not authorized until those measurements are reviewed and exact contracts/path leases are published.

## Terminal disposition

`P3_5_MP06B_INTEGRATION_COMPLETE`
