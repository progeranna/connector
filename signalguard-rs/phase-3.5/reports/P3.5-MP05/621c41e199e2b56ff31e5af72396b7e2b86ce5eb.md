# P3.5-MP05 — Confirmed dead exports and declarations

## Terminal disposition

`P3_5_MP05_IMPLEMENTATION_COMPLETE`

Product commit and connector report were published and verified. No pull request was opened and no merge was performed.

## Product identity

- Repository: `progeranna/signalguard-rs`
- Exact base: `0027679f7a5b2cc783098b1d4a625e1638bee67d`
- Branch: `p35/mp05-confirmed-dead-code`
- Product worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp05`
- Product commit: `621c41e199e2b56ff31e5af72396b7e2b86ce5eb`
- Commit message: `refactor(ui): remove confirmed dead exports and declarations`

The branch was created from the exact base, was zero commits behind and one commit ahead at publication, and was pushed without force, rebase, amend, squash, reset, or history rewrite.

## Recovery history

The initial implementation reached the terminal state `P3_5_MP05_BLOCKED_BY_VALIDATION` before a product commit. The original modifier-only removals failed executable validation for:

- `web/src/test/deferredFetch.ts`: `installControlledFetch`
- `web/src/test/uiSmokeMatrix.ts`: `UiSmokeDialogRequirement`

The initial `npm run lint` failed with `@typescript-eslint/no-unused-vars` for both declarations after their `export` modifiers were removed. The initial one-shot `tsc --noUnusedLocals --noUnusedParameters` check failed with TS6133 for `installControlledFetch` and TS6196 for `UiSmokeDialogRequirement`. No artificial use, suppression, test weakening, commit, push, or report was created in that blocked attempt.

Recovery addendum:

`signalguard-rs/phase-3.5/prompts/P3.5-MP05-R1.md`

Authoritative connector ref: `90700533e6e3b9ca7e74c052bddcd688bdf00a79` (`docs(phase-3.5): authorize MP05 validation recovery`).

The recovery restored and retained the `export` modifier on exactly those two declarations without changing either body. All other valid MP05 changes remained in place. `requestIdentityKey` remained private and locally reachable through `formatIdentity`; the other fixture-internal UI smoke types remained private.

## Exact changed paths and symbol disposition

Only the nine leased product paths changed:

1. `web/src/features/dashboard/MarketHealthMobileCards.tsx`
   - Removed `export` from `EmptyBlock` only.
2. `web/src/features/dashboard/marketOrder.ts`
   - Deleted `buildCoveredDashboardSymbols`.
   - Deleted its unreachable private helper `coverCanonicalMarketEntries`.
   - Retained `DEMO_MARKETS`, `orderMarkets`, `orderMarketEntries`, `isDashboardSymbolPlaceholder`, and all semantics.
3. `web/src/features/dashboard/marketCatalog.ts`
   - Deleted `isMarketCatalogSymbolList` only.
4. `web/src/features/dashboard/api.ts`
   - Removed `export` from `fetchDashboardSummary`, `fetchMarketState`, `fetchMarketHealth`, `fetchMarketTimeline`, and `fetchMarketAnomalies`.
5. `web/src/test/marketFixtures.ts`
   - Removed `export` from `matrixObservedSymbol` only.
6. `web/src/test/deferredFetch.ts`
   - Restored and retained `export` on `installControlledFetch` per MP05-R1.
   - Removed `export` from `requestIdentityKey` only.
7. `web/src/shared/api/client.ts`
   - Removed `export` from `getApiBaseUrl`, `buildApiUrl`, and `parseWithSchema`.
8. `web/src/test/statusDescriptorFixtures.ts`
   - Removed `export` from `SemanticDescriptorExpectation` only.
9. `web/src/test/uiSmokeMatrix.ts`
   - Removed `export` from `UiSmokeMode`, `UiSmokeMarket`, `UiSmokeSurface`, `UiSmokeViewport`, `UiSmokeViewportName`, `UiSmokeState`, `UiSmokeScenarioGroup`, and `UiSmokeNavigationContinuity`.
   - Restored and retained `export` on `UiSmokeDialogRequirement` per MP05-R1.

No declaration body was changed for either recovery symbol. No file was deleted.

## Pre-write and post-edit reference evidence

Exact-word scans covered `web/src`, `web/index.html`, `package.json` scripts, Vite/Vitest/TypeScript/ESLint/Tailwind/PostCSS configuration, repository scripts, production dynamic imports, and public entries.

- Before editing, the five API fetch functions and the three API client helpers had same-module callers only.
- `matrixObservedSymbol`, `SemanticDescriptorExpectation`, and the private UI smoke types had only same-file use.
- `requestIdentityKey` retained its same-file caller `formatIdentity`.
- `installControlledFetch` and `UiSmokeDialogRequirement` were restored as exports because executable lint/no-unused evidence proved modifier-only privatization invalid.
- `buildCoveredDashboardSymbols` had no caller; after editing it appears only in the two negative source assertions in `marketCatalogConsumers.test.ts`.
- `coverCanonicalMarketEntries` and `isMarketCatalogSymbolList` have no remaining declarations or references.
- Production dynamic-import search found no production dynamic import; only the existing test-only `vi.importActual("react-router-dom")` matched.
- `index.html` remains the sole public `/src/main.tsx` entry. Routes, `DEMO_MARKETS`, `replaceSymbolPopupSymbol`, `normalizeSelectedSymbol`, and `useSymbolPopupResource` were retained.

## Frontend validation

Post-recovery `npm ci` passed with the existing lockfile and no tracked mutation.

- Focused frontend tests: PASS, 10 files and 127 tests.
- Full frontend tests: PASS, 43 files and 629 tests.
- `npm run typecheck`: PASS.
- `npm run lint`: PASS with zero warnings.
- `npm run build`: PASS, 714 modules transformed.
- `npm run bundle:check`: PASS.

Production bundle result:

| Metric | Baseline | Candidate | Delta | Budget/headroom |
|---|---:|---:|---:|---:|
| Raw production JS | 757217 bytes | 757217 bytes | 0 bytes | 761856 budget / 4639 headroom |
| Vite gzip display | 219.99 kB | 219.99 kB | 0.00 kB display | existing budget unchanged |

The existing largest-JS and total-JS budgets remain `761856` bytes. Production JS is not greater than `757217` raw bytes.

## Static analysis

- Both one-shot TypeScript projects passed with `--noUnusedLocals --noUnusedParameters`, using build-info paths outside the repository; no diagnostics.
- Knip export inventory reported only retained compatibility/public/verification-required candidates, including `DEMO_MARKETS`, runtime/schema/type surfaces, `installControlledFetch`, and `UiSmokeDialogRequirement`. The Knip files/dependencies/devDependencies/unlisted/unresolved report was `{"issues":[]}`. No dependency deletion proposal appeared.
- `npm ls --depth=0 --json` passed with all 24 direct packages present and no extraneous package.
- Madge production check: 51 files, no circular dependency.
- Madge tooling check: 2 files, no circular dependency.
- Madge production orphan inventory remained the known baseline set: `dashboardResourceState.ts`, `main.tsx`, `shared/components/Tooltip.tsx`, and `vite-env.d.ts`; none was changed.

## Repository-level validation

All required commands passed:

```text
cargo fmt --check
cargo run --quiet --bin export-api-contract -- --check
cargo run --quiet --bin export-api-contract -- --validate
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --test replay_e2e
docker compose config
docker compose --profile app config
bash -n scripts/demo-replay.sh
bash -n scripts/smoke.sh
```

Rust unit/integration test result: 379 passed, 3 ignored; replay E2E: 2 ignored because PostgreSQL/Redis services were not available. No test failed.

## Warnings and scope proof

Existing non-gating warnings remain unchanged: npm reported dependency deprecation/audit notices during `npm ci`; Vitest reported existing React Router future-flag and Recharts/jsdom renderer warnings; Vite reported the existing advisory that the single JS asset exceeds 500 KiB. ESLint produced zero warnings, and no warning or diagnostic was suppressed.

The final diff was checked with `git diff --check`. Exactly the nine leased paths changed. No test assertion file, package manifest, lockfile, configuration, budget, route, generated contract, backend, product copy, API behavior, UI behavior, or Phase 4 file changed. No probable, verification-required, intentional, or false-positive candidate was modified. No dependency was deleted or added.

No pull request was opened and no merge was performed.
