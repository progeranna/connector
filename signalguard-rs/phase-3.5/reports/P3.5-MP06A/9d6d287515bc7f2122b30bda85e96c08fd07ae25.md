# P3.5-MP06A Implementation Report

## Outcome

`P3.5-MP06A — Market-health and timeline test-guard modernization` was implemented as a GitHub web worker against the exact immutable phase head. The product change is test-only. No production file, package file, lockfile, configuration file, generated contract, backend file, Wave 3 file, Phase 4 file, or MP06B-leased file changed.

Executable npm, TypeScript, ESLint, build, and bundle gates were not run by this web-only worker and are explicitly deferred to the Orchestrator merge-ref CI.

## Exact product identity

- Repository: `progeranna/signalguard-rs`
- Immutable base/current phase head: `21fec6d357b365a3710c8e7118a467564247dee6`
- Assigned branch: `p35/mp06a-market-health-test-guards`
- Product commit: `9d6d287515bc7f2122b30bda85e96c08fd07ae25`
- Product commit message: `test(ui): modernize market health and timeline guards`
- Product commit parent: `21fec6d357b365a3710c8e7118a467564247dee6`

## Exact changed paths

1. `web/src/features/dashboard/TimelinePanel.test.tsx`
2. `web/src/features/dashboard/timelineDomains.test.ts`
3. `web/src/features/dashboard/timelineNormalization.test.ts`
4. `web/src/features/dashboard/marketHealthPreviewModel.test.ts`
5. `web/src/features/dashboard/MarketHealthDesktopTable.test.tsx`
6. `web/src/features/dashboard/MarketHealthMobileCards.test.tsx`
7. `web/src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx`
8. `web/src/features/dashboard/RecentAnomaliesMobileCards.test.tsx`
9. `web/src/features/dashboard/dashboardResourceState.test.ts`
10. `web/src/features/dashboard/marketCatalogConsumers.test.ts`
11. `web/src/pages/DashboardPage.test.tsx`

No other path changed. In particular, `web/src/pages/DashboardPage.popup.test.tsx` was not modified.

## MP00D guard dispositions

### Timeline

- `GUARD-002` / `R-01`: replaced exact import formatting and exact call-text ownership with public-module delegation evidence. `TimelinePanel.test.tsx` dynamically mocks the normalization and domain modules, verifies the accepted input and explicit empty anchor, and verifies the chart consumes the sentinel normalized data and domains. Pure helper tests parse imports and enforce exact allowed runtime dependency sets.
- `GUARD-003`: removed only the redundant Timeline source-copy loop. Existing rendered label, gradient, stroke, chart, anomaly-marker, tooltip, snapshot, state, and layout assertions remain.
- `GUARD-004` / `R-02`: replaced broad hook/path/vocabulary scans with parsed forbidden import categories and explicit bans for React Query, API/query ownership, router/popup/resource ownership, browser/network APIs, storage, timers, randomness, and direct current time. Presentation-only behavior remains covered through rendered DOM and chart stubs.
- `GUARD-005` / `R-03`: replaced the exact Dashboard import/JSX-name requirement with Dashboard recorder mocks proving timeline query identity, observed gating, loading/error/empty/success state flow, retry ownership, selected-market identity, and deterministic `dataUpdatedAt` anchor forwarding.

### Dashboard compositor

- `GUARD-006` / `R-04`: replaced exact accepted-component import/render loops with focused recorder mocks proving Dashboard delegates accepted preview rows and callbacks to desktop/mobile market-health and anomaly surfaces and delegates timeline state to its presentation boundary.
- `GUARD-007` / `R-05`: replaced exact preview-builder import strings/call counts with behavior proving builders receive the raw summary collections, preview surfaces receive only accepted preview rows, and full raw collections remain available to modal workflows.
- `GUARD-008` / `R-06`: replaced exact multiline hook/prop substrings with table-driven behavior for `(uiMode, symbol)` query identity, observed gating, loading/error/empty/success precedence, retry, identity transition, stale-data detachment, and finite empty-anchor behavior.
- `GUARD-010` / `R-07`: replaced exact JSX collection-flow regexes with behavior proving preview children receive bounded rows and full market/anomaly collections remain present in `All markets` and `All anomalies` modal flows without mutation.

### Recent anomalies desktop

- `GUARD-014`: removed only the positive `key={row.id}` source regex. Rerender node-identity proof and explicit rejection of index/unstable-field keys remain.
- `GUARD-016` / `R-09`: replaced exact import count/helper-path assertions with parsed import-specifier checks that allow presentation dependencies and explicitly reject query/API/router/popup/resource/network/time/storage ownership.
- `GUARD-017` / `R-10`: replaced generic hidden-field vocabulary bans with sentinel values for `message`, `activeLabel`, `effectiveTimestampMs`, and severity description plus rendered DOM absence assertions. Shell, heading, modal, title, and `View all` ownership are tested through DOM absence.

### Recent anomalies mobile

- `GUARD-020`: removed only the positive `key={row.id}` source regex. Rerender node-identity proof and explicit rejection of index/unstable-field keys remain.
- `GUARD-021` / `R-11`: narrowed collection-operation scans to the `rows` input and retained frozen-order, no-limit, no-deduplication, duplicate-ID, and no-mutation behavior.
- `GUARD-022` / `R-12`: replaced exact import arrays/helper locations with parsed presentation-boundary import checks and explicit ownership exclusions.
- `GUARD-023` / `R-13`: retained explicit query/API/router/popup/network/time/storage bans while replacing generic UI-word bans with DOM absence checks for table, dialog, shell heading, section title, and `View all`, plus hidden-row-field sentinel assertions.

### Market health desktop/mobile

- `GUARD-026` / `R-14`: replaced exact source type/import regexes with TypeScript `satisfies` contracts proving public props accept readonly `MarketHealthPreviewRow[]`; retained frozen input and rows-qualified mutation/ordering guards.
- `GUARD-027` / `R-15`: replaced broad path-fragment and generic vocabulary bans with parsed explicit forbidden ownership imports, explicit API/time/storage checks, and rendered absence of dialog, heading, `View all`, and loading ownership. Generic `resource`/`adapter` path text is not rejected by itself.
- `GUARD-030` / `R-16`: retained explicit forbidden import/API/time/storage ownership and replaced broad shell/modal/text scans with rendered absence of table, dialog, heading, `View all`, loading, and empty-shell ownership.

### Catalog and pure helpers

- `GUARD-038` / `R-21`: replaced function-order slicing/exact raw/catalog source topology with public-hook behavior under a controlled `useQuery` result. The raw hook and catalog wrapper each issue exactly one accepted query and return the exact server summary/query result without Demo fallback. The narrow ticker raw-hook source boundary remains.
- `GUARD-040` / `R-23`: retained state precedence, frozen input, reference identity, source/availability preservation, and deterministic output; replaced vocabulary bans with exact type-only import and explicit runtime/network/browser/current-time exclusions.
- `GUARD-041` / `R-24`: retained exact domain formulas, error messages, finite ascending domains, order independence, frozen inputs, deterministic anchor behavior, and no-current-time proof; replaced import-count/visual-word scans with exact type-only pure-model dependency and zero runtime imports.
- `GUARD-042` / `R-25`: proves ordering ownership by comparing preview output with the public `orderMarketEntries` contract and retains a targeted ban on local sorting of the input collection.
- `GUARD-043` / `R-26`: retained frozen input, deterministic output, `(source, symbol)` identity, no fallback/synthesis, limits, metadata, and non-observed metric suppression; added an exact public row-shape `satisfies` contract and exact allowed runtime dependency set.
- `GUARD-044` / `R-27`: retained invalid-value matrices, order/duplicate preservation, frozen input, and determinism; replaced import-format/visual-word scans with exact type-only pure-model dependency and zero runtime imports plus explicit browser/network/time exclusions.

## `KEEP_EXACT` confirmation

The following authoritative guards were retained unchanged or equivalently strict, with their original protected contracts preserved:

- `GUARD-001`
- `GUARD-009`
- `GUARD-012`
- `GUARD-013`
- `GUARD-015`
- `GUARD-018`
- `GUARD-019`
- `GUARD-024`
- `GUARD-025`
- `GUARD-028`
- `GUARD-029`
- `GUARD-037`
- `GUARD-045`
- `GUARD-046`
- `GUARD-047`

This includes direct-current-time protection, ticker raw-hook ownership, presentation input order/immutability, no sorting/limiting/mutation/deduplication, explicit external ownership exclusions, keyboard/click/focus accessibility, state precedence, exact responsive classes, accepted column widths, severity/status colors, score classes, typography, and focus-ring tokens.

## Behavioral, type, and boundary evidence added

- Timeline normalizer/domain delegation via focused module recorder mocks and sentinel chart output.
- Dashboard hook/component recorder mocks for source/mode/symbol identity, observed gating, state transitions, retry, preview/full-collection flow, callbacks, and popup identity.
- TypeScript `satisfies` contracts for readonly market-health props and exact preview row shape.
- Public `orderMarketEntries` equivalence for market-health ordering ownership.
- Public raw/catalog hook tests with one controlled query and exact server-result identity.
- Parsed import specifier checks and exact runtime dependency sets for pure helpers.
- DOM absence assertions replacing broad UI vocabulary scans.
- Hidden-field sentinel rendering checks replacing generic property-name bans.
- Frozen input/reference/order assertions retained across presentation and pure-model tests.

## Redundant assertions removed

Only the contract-authorized redundant assertions were removed:

- `GUARD-003` Timeline source-copy loop.
- `GUARD-014` desktop recent-anomaly positive `key={row.id}` source regex.
- `GUARD-020` mobile recent-anomaly positive `key={row.id}` source regex.

Their stronger rendered behavior and rerender identity proofs remain.

## Production and scope confirmation

- Production files changed: none.
- MP06B-leased files changed: none.
- `web/src/pages/DashboardPage.popup.test.tsx` changed: no.
- Package/lock/configuration/budget/route/generated/backend files changed: none.
- Pull request opened: no.
- Merge performed: no.
- Rebase, amend, reset, force-push, squash, or history rewrite performed: no.

## Web-worker verification

Verified through connected GitHub data:

- The assigned branch was created from exact base `21fec6d357b365a3710c8e7118a467564247dee6`.
- Product commit message is exactly `test(ui): modernize market health and timeline guards`.
- Product commit parent is exactly the immutable base.
- Base-to-branch comparison reports `ahead_by: 1`, `behind_by: 0`, `total_commits: 1`.
- Merge base is exactly `21fec6d357b365a3710c8e7118a467564247dee6`.
- The comparison contains exactly the eleven leased test paths listed above.
- No production path appears in the diff.
- No combined status checks or commit workflow runs were available at verification time; no executable gate is claimed as run.

## Deferred executable commands

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/TimelinePanel.test.tsx src/features/dashboard/timelineDomains.test.ts src/features/dashboard/timelineNormalization.test.ts src/features/dashboard/marketHealthPreviewModel.test.ts src/features/dashboard/MarketHealthDesktopTable.test.tsx src/features/dashboard/MarketHealthMobileCards.test.tsx src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx src/features/dashboard/RecentAnomaliesMobileCards.test.tsx src/features/dashboard/dashboardResourceState.test.ts src/features/dashboard/marketCatalogConsumers.test.ts src/pages/DashboardPage.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

These commands are deferred to the Orchestrator merge-ref CI under the exact current base.