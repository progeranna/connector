# P3.5-MP02 — Timeline market-health presentation reuse implementation report

Terminal status: `P3_5_MP02_IMPLEMENTATION_COMPLETE`

## Identity

- Exact product base SHA: `c06082a97254bfa2f6ebd7e29a1ad753c4acc798`
- Product branch: `p35/mp02-timeline-presentation-reuse`
- Product head SHA: `e86d46161e8b99fed2b38f6e05240af0abe0f74b`
- Product commit message: `refactor(ui): reuse market health presentation in timeline`
- Commit count ahead of base: `1`
- Product branch comparison: `ahead`, ahead `1`, behind `0`

## Changed files

- `web/src/features/dashboard/TimelinePanel.tsx`

No other product file changed.

## Lease compliance

Result: `COMPLIANT`.

The only changed product path is explicitly writable under the P3.5-MP02 lease. No DashboardPage, MarketHealth desktop/mobile, RecentAnomalies, SymbolDetail, adapter, view-model, shared formatter/status, router, package/configuration, bundle-budget, backend, deployment, Docker, migration, or Rust path changed.

The diff contains no Recharts import or chart-component change, no router or route-loading change, no dynamic import, no lazy boundary, and no Suspense change.

## Canonical helpers reused

`TimelinePanel.tsx` now imports the accepted canonical implementations from `marketHealthPresentation.ts`:

- `formatTickerPrice`
- `formatTickerPercent`
- `formatOptionalCompact`
- `formatOptionalAge`
- `statusLabel`
- `availabilityMessage`

The corresponding Timeline-local implementations were removed. The direct Timeline imports of generic `formatAgeMs` and `formatCompactNumber` were removed so the market-health-specific fallback semantics remain owned by the canonical presentation module.

## `marketStatusLabel` decision

A new canonical `marketStatusLabel` export was **not added**.

Exact inspection confirmed the same branch wording appears in Timeline, MarketHealth desktop/mobile, and Dashboard callers. However, MP02 does not require a new cross-caller API to migrate the six already accepted canonical helpers, and the other callers are outside this worker's lease. Keeping the Timeline wrapper local avoids introducing a new structural contract and partial cross-wave API surface solely for one migrated caller. The local wrapper now delegates observed-status capitalization to canonical `statusLabel`.

## Timeline-specific helpers deliberately retained

The following remain local because they own Timeline-only semantics rather than duplicated market-health presentation behavior:

- `formatTimelineTick`
- `formatTimelinePriceTick`
- `formatTimelineTooltipTimestamp`
- `marketStatusLabel`
- timeline normalization and domain calls
- visible anomaly filtering and the inclusive chart-domain window
- anomaly severity selection, marker colors, and badge classes
- anomaly type formatting
- tooltip anomaly matching within `±15,000 ms`
- chart, axes, tooltip composition, state precedence, dimensions, and responsive behavior

## Tests changed

No test file changed.

The existing focused tests were inspected and remain applicable without requiring the removed local helper definitions. They already cover:

- zero and missing price;
- zero and missing percent;
- zero and missing compact values;
- zero and missing age;
- configured, awaiting, unavailable, and observed status wording;
- the availability-message matrix through rendered states;
- snapshot summary values;
- tooltip values;
- Timeline-specific timestamp behavior;
- loading/error/configured/awaiting/unavailable/normalized-empty/success precedence;
- deterministic time ownership;
- no query/router/network ownership transfer.

No broader MP06A source-guard modernization was performed.

## Checks actually performed by the web worker

- Fetched and read the complete authoritative P3.5-MP02 contract.
- Read the required Phase 3.5 plan, control status, consolidated inventory, MP00B duplicate audit, and MP00D test-guard audit.
- Verified `refactor/dashboard-modules` resolved to exact base `c06082a97254bfa2f6ebd7e29a1ad753c4acc798` before product writes.
- Verified the assigned branch did not exist, then created it from the exact base.
- Fetched every conditionally writable product file from the assigned branch before updating.
- Compared the Timeline-local implementations with `marketHealthPresentation.ts` at the exact base.
- Inspected the repeated market-status mapping in Timeline, MarketHealth desktop/mobile, and Dashboard at the exact base.
- Inspected the complete product commit diff.
- Compared base to branch and verified exactly one commit ahead, zero behind, and exactly one changed leased file.
- Verified the product commit SHA and exact required commit message.
- Queried GitHub workflow runs and combined commit statuses for the product head; none were present.

## Checks not run by the web worker

No local checkout or shell was used. No tests, typecheck, lint, build, bundle check, or emitted-byte measurement were run. No such result is claimed.

The Orchestrator must run:

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/TimelinePanel.test.tsx src/features/dashboard/marketHealthPresentation.test.ts
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

## Bundle claim

Not measured. No production raw/gzip or emitted-byte reduction is claimed by this web worker. Production total JS must be checked against the exact candidate base by the Orchestrator.

## Publication and integration boundaries

- Product commit created and pushed normally on `p35/mp02-timeline-presentation-reuse`.
- No pull request was opened.
- No merge was performed.
- No rebase, amend, reset, force-push, or history rewrite was performed.
