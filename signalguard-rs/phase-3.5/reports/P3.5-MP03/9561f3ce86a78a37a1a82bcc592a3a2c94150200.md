# P3.5-MP03 — Dashboard anomaly presentation consolidation

## Implementation identity

- Product repository: `progeranna/signalguard-rs`
- Exact product base SHA: `c06082a97254bfa2f6ebd7e29a1ad753c4acc798`
- Product branch: `p35/mp03-dashboard-anomaly-presentation`
- Product head SHA: `9561f3ce86a78a37a1a82bcc592a3a2c94150200`
- Product commit message: `refactor(ui): consolidate dashboard anomaly presentation`
- Product commits ahead of base: `1`
- Coordination repository: `progeranna/connector`
- Worker mode: connected GitHub web implementation; no local checkout or shell

## Exact changed files

1. `web/src/pages/DashboardPage.tsx`
2. `web/src/pages/DashboardPage.test.tsx`
3. `web/src/features/dashboard/recentAnomaliesPresentation.ts`
4. `web/src/features/dashboard/recentAnomaliesPresentation.test.ts`

No product path outside the exclusive MP03 writable lease changed.

## Implementation summary

`DashboardPage.tsx` now imports the existing pure anomaly-presentation owner and retains only Dashboard orchestration and JSX ownership. The canonical presentation module remains independent of page state, modal state, resources, queries, navigation, popup identity, and callbacks.

### Canonical helpers reused

- `severityBadgeClass`
- `anomalyValueClass`
- `formatAnomalyTime`
- `formatAnomalyValue`
- the existing private duration, integer, and numeric formatters reached through `formatAnomalyValue`

### Canonical helper added

- `formatAnomalyType`
  - preserves the Dashboard fallback `Unknown`
  - preserves underscore-segment filtering and title capitalization
  - remains a pure presentation-only function

### Dashboard-local duplicate helpers removed

- `severityBadgeClass`
- `anomalyValueClass`
- `formatAnomalyType`
- `formatAnomalyTime`
- `formatAnomalyValue`
- `formatDurationValue`
- `formatIntegerValue`
- `formatNumericValue`

The Dashboard-local `SeverityBadge` renderer was deliberately retained because its compact/full DOM and visual presentation remain Dashboard-owned.

## Preserved contracts and intentional differences

- Desktop anomaly modal column wording remains `Detected at`.
- Mobile anomaly modal metric wording remains `Detected`.
- Timestamp selection remains exactly `anomaly.event_time || anomaly.created_at` in both Dashboard modal presentations.
- Existing missing/invalid timestamp behavior is unchanged.
- Type-specific observed and threshold value formatting is unchanged.
- Duration precision, integer rounding, rate, gap, and limit wording are unchanged.
- Exact severity badge class tokens and anomaly-value class tokens are unchanged.
- Full raw `summary.recent_anomalies` remains the all-anomalies modal input; preview-limited rows are not reused.
- Desktop anomaly rows retain click, Enter, Space, `preventDefault`, `role="button"`, `tabIndex={0}`, aria labels, and focus-visible classes.
- Mobile anomaly entries remain native buttons.
- Return context, popup identity, modal lifecycle, resource identity, query ownership, back/close behavior, and shared symbol-detail wiring are unchanged.
- Route/popup formatting and interaction owners were not modified.
- Market-health presentation and helpers were not modified.
- Product copy, styling, columns, modal structure, navigation, routes, Recharts ownership, lazy boundaries, and ticker ownership were not modified.

## Tests changed

### `recentAnomaliesPresentation.test.ts`

Expanded the pure owner matrix to cover:

- anomaly/detector type labels and `Unknown` fallback
- event and fallback timestamp formatting
- missing and invalid timestamps
- severity badge classes, including neutral fallback
- anomaly value classes, including neutral fallback
- null, undefined, NaN, zero, and accepted non-finite behavior
- percentage values
- duration values for event lag, stale data, and quote stuck
- integer/rate values
- depth gap and threshold limit values
- generic numeric values

### `DashboardPage.test.tsx`

Added a narrow ownership guard that:

- requires the canonical anomaly-presentation import
- requires the Dashboard `SeverityBadge` renderer to remain local
- prohibits the eight removed page-local helper definitions
- proves both modal presentations retain `anomaly.event_time || anomaly.created_at`

All pre-existing guards for raw collections, preview/full-list separation, popup lifecycle, shared symbol-detail wiring, deterministic time, no direct Recharts, no duplicate preview ownership, and ticker ownership were retained. `DashboardPage.popup.test.tsx` was not modified.

## Checks actually performed by the web worker

- Read the complete authoritative MP03 contract and required Phase 3.5 plan, status, and inventories from `progeranna/connector@main`.
- Verified `refactor/dashboard-modules` matched the exact immutable base before writing.
- Verified the assigned branch contained no prior worker or unrelated commit, then created it from the exact base.
- Fetched every writable product file from the exact assigned branch before preparing changes.
- Compared each Dashboard-local anomaly helper with the exact pure-owner implementation and migrated only proven-equivalent semantics.
- Created one atomic product commit through Git blobs/tree/commit and advanced the assigned branch without force.
- Inspected the complete product commit diff.
- Verified the branch is exactly one commit ahead and zero commits behind the base.
- Verified the exact changed-file set is the four leased paths above.
- Re-fetched committed production and test content from the assigned branch.
- Inspected GitHub workflow runs and combined commit statuses for the product head; no workflow run or status was present.

## Checks not run by the web worker

The web worker did not run or claim results for:

- dependency installation
- focused tests
- full tests
- typecheck
- lint
- production build
- bundle-budget check

### Exact later validation commands

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/recentAnomaliesPresentation.test.ts src/pages/DashboardPage.test.tsx src/pages/DashboardPage.popup.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

## Bundle claim

Not measured. No emitted-byte or production-JS reduction claim is made by this web worker. The later `npm run bundle:check` gate must confirm that production total JS does not increase against the exact candidate base.

## Lease and publication disposition

- Writable lease compliance: **PASS**
- Product commit count: **PASS — exactly one commit ahead of base**
- Product branch history rewrite: **none**
- Force push: **none**
- Pull request created: **no**
- Merge performed: **no**

`P3_5_MP03_IMPLEMENTATION_COMPLETE`
