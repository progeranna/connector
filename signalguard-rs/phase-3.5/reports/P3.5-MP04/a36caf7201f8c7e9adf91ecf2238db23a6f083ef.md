# P3.5-MP04 — Dashboard market-health presentation consolidation

## Product identity

- Repository: `progeranna/signalguard-rs`
- Immutable base: `4b90a9fe9e4ddda9b0b9411857e5d8b2c3685c69`
- Product branch: `p35/mp04-dashboard-market-health-presentation`
- Product commit: `a36caf7201f8c7e9adf91ecf2238db23a6f083ef`
- Product commit message: `refactor(ui): consolidate dashboard market health presentation`
- Assigned worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp04`

## Exact changed paths

- `web/src/pages/DashboardPage.tsx`
- `web/src/pages/DashboardPage.test.tsx`
- `web/src/features/dashboard/marketHealthPresentation.ts`
- `web/src/features/dashboard/marketHealthPresentation.test.ts`

## Implementation

Dashboard now imports and reuses the canonical `HealthScore` owner in both full-market modal surfaces: the desktop table row and mobile card. Regular layout and all score/status inputs remain unchanged; no `compact` prop was added.

Removed Dashboard-local owners:

- `HealthScore`
- `healthScoreTone`
- `healthScoreTextClass`
- `healthScoreBarClass`
- `formatTickerPrice`
- `formatTickerPercent`
- `formatOptionalCompact`
- `formatOptionalAge`
- `statusLabel`
- `availabilityMessage`
- `marketStatusLabel`

Canonical owners reused:

- `HealthScore` from `@/features/dashboard/HealthScore`;
- `formatTickerPrice`;
- `formatTickerPercent`;
- `formatOptionalCompact`;
- `formatOptionalAge`;
- `statusLabel`;
- `availabilityMessage` from `marketHealthPresentation.ts`.

Added canonical helper contract:

```ts
marketStatusLabel(availability, healthStatus): string
```

It returns `Configured`, `Awaiting data`, `Unavailable`, or, for observed markets, `statusLabel(healthStatus)`; missing observed status remains `Unknown`. Coverage includes configured, awaiting, unavailable, healthy, degraded, unhealthy, null/undefined, and delegated capitalization behavior.

Preserved behavior and intentional non-changes:

- null, zero, missing, NaN, precision, fallback, status precedence, bar color, minimum width, classes, and inline score width;
- exact modal copy, columns, labels, DOM structure, responsive boundary, keyboard/click/accessibility behavior, popup lifecycle, selected-symbol persistence, resource/query identity, Demo/Live isolation, timeline, anomalies, ticker, routes, and visual presentation;
- no changes to `HealthScore.tsx`, desktop/mobile preview components, Timeline, anomaly modules, routes, configuration, budgets, or Rust code;
- no MP05, MP06, route splitting, Recharts splitting, or Phase 4 work.

## Validation

Baseline at the clean immutable base:

- Frontend: 43 test files, 627 tests;
- typecheck: pass;
- lint: pass;
- production build: pass;
- bundle check: pass;
- emitted JS: `dist/assets/index-ff7e0f60.js` — 758875 raw bytes;
- Vite gzip display: 220.36 kB;
- existing largest/total JS budget: 761856 bytes.

Candidate:

- Focused command: 7 files, 113 tests passed;
- Full frontend: 43 test files, 629 tests passed;
- typecheck: pass;
- lint with zero warnings/errors: pass;
- production build: pass;
- bundle check: pass;
- emitted JS: `dist/assets/index-a4d88fe2.js` — 757217 raw bytes;
- raw JS delta: -1658 bytes;
- Vite gzip display: 219.99 kB;
- existing largest/total JS budget: 761856 bytes, unchanged;
- candidate headroom: 4639 bytes.

Repository gates:

- `cargo fmt --check`: pass;
- `cargo run --quiet --bin export-api-contract -- --check`: pass;
- `cargo run --quiet --bin export-api-contract -- --validate`: pass;
- `cargo check`: pass;
- `cargo clippy --all-targets --all-features -- -D warnings`: pass;
- `cargo test`: 379 passed, 3 ignored, 0 failed;
- `cargo test --test replay_e2e`: 2 ignored, 0 failed;
- `docker compose config`: pass;
- `docker compose --profile app config`: pass;
- `bash -n scripts/demo-replay.sh`: pass;
- `bash -n scripts/smoke.sh`: pass.

Warning disposition:

- Existing Recharts mock SVG DOM warnings and React Router future-flag notices remain in tests;
- the existing Vite single-chunk advisory remains;
- `npm ci` reported existing dependency deprecation/audit notices, including 13 audit findings;
- no new warning class or warning suppression was introduced;
- Rust clippy is clean under `-D warnings`.

## Lease and publication verification

- Product base-to-branch comparison contains only the four exact leased paths;
- canonical read-only files and all forbidden paths are unchanged;
- product branch was pushed and resolves to `a36caf7201f8c7e9adf91ecf2238db23a6f083ef`;
- exactly one normal product commit was created;
- no pull request was opened;
- no merge occurred;
- no rebase, amend, reset, force-push, or history rewriting occurred;
- connector report is published as the single commit with message `docs(phase-3.5): publish MP04 implementation report`.
