# P3.5-MP06B — Current-base validation block

## Identity

- Product repository: `progeranna/signalguard-rs`
- Current integrated phase head after MP06A: `bb3df51339ad4a7a3524fe4782fdc24069944160`
- Immutable MP06B worker branch: `p35/mp06b-symbol-detail-test-guards`
- Immutable MP06B worker head: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`
- Original shared worker base: `21fec6d357b365a3710c8e7118a467564247dee6`
- Current-base validation PR: `#64`
- Tested merge ref: `e923defbeeeebf7cf6bf7457b77be28898286c82`
- CI run: `30712501382`
- PR disposition: closed without merge

The immutable MP06B branch was not rebased, amended, reset, force-pushed, squashed, or rewritten. GitHub generated the current-base merge ref by combining the integrated MP06A tree with the immutable MP06B worker commit.

## Scope verification

The PR contained exactly five MP06B-leased test files. `web/src/features/dashboard/SymbolDetailMetrics.test.tsx` remained unchanged. No production file, MP06A-leased test, package file, lockfile, configuration, budget, route, generated contract, backend file, visual presentation, or product copy changed.

## Executable result

- Rust job: success;
- frontend dependency installation: success;
- frontend test files: `43` passed;
- frontend tests: `640` passed;
- TypeScript: failed;
- lint, production build, and bundle check: skipped after the typecheck failure.

No runtime test failed. The block consists only of compile-time test-fixture/mock typing defects in two MP06B-leased files.

## Exact TypeScript diagnostics

### `web/src/pages/DashboardPage.popup.test.tsx`

1. Adapter recorder identity at line 97 accepted an optional second parameter from `Parameters<typeof adaptMarketResourceToViewModel>` but stored it in a required identity field.
2. Four `vi.fn` refetch values at lines 165, 172, 178, and 195 inferred `Mock<any[], unknown>` while `SymbolPopupResourceState.refetch` requires `() => Promise<unknown>`.

Required recovery boundary:

- narrow or assert the adapter identity through the public function contract without weakening the identity assertion;
- type the refetch map and newly created mocks as the public async `refetch` contract;
- do not change resource behavior, statuses, retry behavior, identity, or production code.

### `web/src/pages/SymbolDetailPage.test.tsx`

The observed dashboard-state fixture at line 173 used numeric values for:

- `last_trade_price`;
- `best_bid_price`;
- `best_ask_price`.

The current Zod/API-derived `DashboardStateSummary` contract requires `string | null` for these price fields. The recovery must replace only those fixture values with valid decimal strings while preserving the represented values and all route behavior assertions.

## Terminal disposition

`P3_5_MP06B_BLOCKED_BY_CURRENT_BASE_TYPECHECK`
