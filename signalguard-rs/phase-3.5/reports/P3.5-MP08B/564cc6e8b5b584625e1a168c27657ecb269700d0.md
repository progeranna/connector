# P3.5-MP08B implementation report

## Identity and history

- Immutable base: `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`.
- Product branch: `p35/mp08b-native-svg-timeline`.
- Product commit: `564cc6e8b5b584625e1a168c27657ecb269700d0`.
- Commit message: `refactor(ui): replace Recharts timeline with native SVG`.
- History relation at publication: exactly one commit ahead and zero behind the base; the product commit's sole parent is the base.

## Changed paths and ownership

The product commit changes exactly this strict subset of the writable lease:

- `web/package-lock.json`
- `web/package.json`
- `web/src/features/dashboard/TimelineChartRenderer.test.tsx`
- `web/src/features/dashboard/TimelineChartRenderer.tsx`
- `web/src/features/dashboard/TimelinePanel.test.tsx`
- `web/src/features/dashboard/TimelinePanel.tsx`
- `web/src/features/dashboard/timelineChartGeometry.test.ts`
- `web/src/features/dashboard/timelineChartGeometry.ts`

`TimelinePanel` remains eager and retains shell/state precedence, market-query ownership, normalization, domains, visible-anomaly filtering, selected-market behavior, and Demo/Live and symbol isolation. `TimelineChartRenderer` is a dependency-free native SVG presentation component. It owns only chart rendering and local pointer/keyboard tooltip interaction. No router, page owner, query owner, popup owner, backend, generated contract, configuration, script, budget, route, or product-copy file changed.

The pure `timelineChartGeometry` helper has no React or browser dependency. It defines fixed view-box geometry and deterministically projects normalized time/price points, produces axis ticks, and serializes line/fill paths from the domains supplied by `TimelinePanel`. Tests cover endpoint projection, degenerate domains, ticks, and path output.

## Dependency removal

`recharts` was removed from `web/package.json`; the npm-generated lock update removed Recharts and its now-unused measured production graph. No replacement chart package and no new runtime dependency were introduced. A clean `npm ci` succeeded after removal, installing 424 packages (425 audited), compared with 463 installed (464 audited) at the base. The vulnerability report remained unchanged at 13 findings.

Complete dependency and production-chunk inspection found no Recharts package/module and no surviving measured Recharts-only graph, including `victory-vendor`, `decimal.js-light`, `eventemitter3`, the Recharts-local Redux/React Redux/use-sync-external-store copies, and the removed Recharts-only `react-is` entry. `npm ls --all recharts victory-vendor decimal.js-light eventemitter3 react-is` returned an empty relevant graph. Lockfile and built-asset searches were also empty. Temporary Rollup/Vite instrumentation found zero duplicate emitted module IDs.

## Frontend validation

Focused tests passed: 3 files, 26 tests (`TimelinePanel`, `TimelineChartRenderer`, and `timelineChartGeometry`). The renderer coverage includes native SVG presentation and dimensions, marker rendering, inclusive 15-second matching, zero/missing values, first/middle/last keyboard traversal, Escape dismissal, pointer interaction, focus-visible information, and node stability across rerender.

The required sequential frontend run passed:

1. `npm ci`
2. `npm run test:run`: 45 files, 653 tests passed (floor: 44 files and 643 tests)
3. `npm run typecheck`
4. `npm run lint`
5. `npm run build`
6. `npm run bundle:check`

The production build transformed 140 modules and emitted one initial JavaScript asset, `assets/index-241ec209.js`. Temporary Vite/Rollup instrumentation measured 107 surviving modules, no async JavaScript chunk, no static/dynamic JavaScript import relocation, and zero duplicate module IDs.

| Measurement | Exact base | Candidate | Reduction |
| --- | ---: | ---: | ---: |
| Total/initial raw JS | 757,217 bytes | 395,700 bytes | 361,517 bytes |
| Total/initial direct-gzip JS | 219,504 bytes | 113,042 bytes | 106,462 bytes |

The candidate's sole and largest JavaScript asset is 395,700 raw bytes. It is 4,300 bytes below the 400,000-byte implementation ceiling; direct gzip is 1,958 bytes below 115,000. The unchanged largest and total budgets are 761,856 bytes, yielding exact total headroom of 366,156 bytes. `bundle:check` passed. The 76-byte raw difference from the 395,624-byte measurement reference comes from the finalized renderer implementation and remains within the strict acceptance ceiling; direct gzip matches the reference exactly.

## Repository gates

All required repository gates passed sequentially:

- `cargo fmt --check`
- API contract export `--check`
- API contract export `--validate`
- `cargo check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test`: 379 passed, 0 failed, 3 ignored unit tests; environment-dependent integration suites remained explicitly ignored as configured
- `cargo test --test replay_e2e`: 0 failed, 2 ignored as configured
- `docker compose config`
- `docker compose --profile app config`
- `bash -n scripts/demo-replay.sh`
- `bash -n scripts/smoke.sh`

## Browser/runtime matrix

Exact-base and candidate production previews were exercised with installed Chromium automation, deterministic schema-valid mock API data, and frozen time at desktop and mobile viewports. The matrix covered `/`, `/dashboard?mode=demo`, `/dashboard?mode=live`, UI-driven Demo/Live and BTCUSDT/ETHUSDT transitions, summary loading, configured/unavailable market, timeline loading, injected timeline error and Retry, normalized-empty data, observed data, anomaly-free data, all three anomaly severities, first/middle/last tooltips, inclusive `-15s` and `+15s` anomaly matches, keyboard left/right traversal, focus tooltip, Escape dismissal, and responsive resizing.

- Each settled `(uiMode, symbol)` identity issued exactly one summary and one timeline request. The UI transition run observed one each for demo/BTCUSDT, live/BTCUSDT, and live/ETHUSDT, with no duplicate settled-identity request.
- The retry scenario intentionally injected four HTTP 503 timeline responses consumed by the configured query retry policy, then one successful user Retry request: five timeline requests total. Retry recovered successfully.
- Stable parent rerender and responsive resize retained the same chart node and recorded one renderer initialization. Identity transitions passed through their expected loading states and produced three initializations for three observed identities; no repeated initialization or remount loop occurred.
- The chart stayed 285 px high during stable rerender and resize. Desktop and mobile chart regions did not collapse or shift; the mobile Time label remained fully inside its measured bounds.
- Keyboard traversal reached first, middle, and last points; focus exposed tooltip content; inclusive boundary anomalies appeared; Escape dismissed the tooltip.
- Normal scenarios produced no console errors, page errors, or unhandled promise rejections. The injected retry run produced only the four expected browser network-resource 503 console events, with no candidate-attributable error, page error, or unhandled rejection.
- Configured/unavailable states made one summary and zero timeline requests; normalized-empty and anomaly-free scenarios retained their expected request and rendering behavior.

## Screenshot comparison

The reproducible chart-region comparison used per-pixel changed fraction, mean absolute channel difference, and RGB RMSE:

| Viewport/chart region | Changed fraction | Mean absolute channel difference | RGB RMSE |
| --- | ---: | ---: | ---: |
| Desktop `(32,122,1148,463)` | 0.3549044030 | 3.0613637240 | 12.5711629094 |
| Mobile `(16,254,374,595)` | 0.3606464719 | 4.7309780086 | 17.6782533792 |

Manual inspection of all four exact-base/candidate captures confirmed that the shell, card, chart dimensions, markers, tooltip semantics, and responsive layout remained aligned. Every visible difference was attributable to the three accepted categories: a straight native-SVG polyline instead of monotone interpolation, slight native-SVG tick-position/value differences, and CSS-sized mobile labels. No additional material visual difference was observed. Screenshots and browser artifacts were deleted and were not committed.

## Diff, cleanup, and publication constraints

Before commit, `git status --short`, `git diff --check`, `git diff --stat`, and the complete `git diff` were inspected. The changed paths were exactly the eight-path lease subset above, and `git diff --check` was clean. All temporary build output, package experiments, instrumentation, preview files, screenshots, browser data, and analysis artifacts were removed. The committed product worktree is clean.

Exactly one normal product commit was created and pushed only to `p35/mp08b-native-svg-timeline`. No product rebase, amend, reset, squash, force-push, merge, pull request, tag, or additional product commit occurred.

## Remaining merge-ref risks

The Orchestrator should repeat merge-ref frontend/build/bundle validation because another integration may change package resolution, asset hashing, byte totals, or surrounding dashboard CSS. Browser validation should also be repeated on the final merge ref to detect interactions with concurrent UI changes. The candidate has 4,300 raw bytes and 1,958 direct-gzip bytes of implementation-ceiling margin; the unchanged product budget has 366,156 bytes of total headroom.
