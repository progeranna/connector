# P3.5-MP09-R1 — Integration record

## Product identity

- Product repository: `progeranna/signalguard-rs`
- Previous integrated phase head: `38a1cc440a264d9e04fad3c699386fd45778797f`
- Immutable worker branch: `p35/mp09-bundle-policy-refinement`
- Worker head: `b1e574705dea0ae2e3fb682cbd187ce4e3d717ff`
- Worker commit message: `chore(ui): refine bundle policy reporting`
- Worker report: `signalguard-rs/phase-3.5/reports/P3.5-MP09-R1/b1e574705dea0ae2e3fb682cbd187ce4e3d717ff.md`
- Worker report commit: `0d7ad3294e513f0403a6bcf9688c5dbfe5b3ae15`

The worker commit was exactly one commit ahead and zero behind the previous phase head.

## Accepted scope

The final diff contains exactly the six-path MP09-R1 lease:

1. `web/bundle-size-budget.json`
2. `web/package.json`
3. `web/scripts/check-bundle-size.mjs`
4. `web/scripts/check-bundle-size.test.mjs`
5. `web/vite.config.ts`
6. `web/vitest.config.ts`

No file under `web/src/`, no lockfile, CI workflow, backend, generated contract, route, component, CSS, product copy, API, or query-owner file changed. No dependency was added.

The recovery retains all Vitest default exclusions and adds only `scripts/check-bundle-size.test.mjs`, preserving the protected `vitest run --threads` command. The Node policy suite remains a Node built-in suite and runs explicitly before the real bundle check.

## Exact merge-ref validation

- Integration PR: `#67`
- PR base: `refactor/dashboard-modules` at `38a1cc440a264d9e04fad3c699386fd45778797f`
- PR head: `b1e574705dea0ae2e3fb682cbd187ce4e3d717ff`
- Tested PR merge ref: `67ec5488b8747ceeb8f09b3fb4c921af24c05eff`
- Authoritative CI run: `30721976267`
- Frontend job: `91427128390` — success
- Rust job: `91427128400` — success

GitHub Actions fetched `+67ec5488b8747ceeb8f09b3fb4c921af24c05eff:refs/remotes/pull/67/merge`, checked out `refs/remotes/pull/67/merge`, and verified HEAD exactly at that SHA.

Validation result:

- frontend dependency installation: success;
- Vitest: `45` files and `653` tests passed;
- Node bundle-policy suite: `25` tests passed, `0` failed;
- TypeScript: pass;
- ESLint with zero warnings: pass;
- production build: pass;
- transformed modules: `140`;
- emitted JavaScript assets: `1` initial, `0` async;
- initial JS: `395700 / 409600` bytes, headroom `13900`;
- largest JS: `395700 / 409600` bytes, headroom `13900`;
- total JS: `395700 / 414720` bytes, headroom `19020`;
- bundle policy: pass;
- Rust formatting, API-contract check/validation, check, Clippy, tests, replay target discovery, Docker Compose, and script syntax gates: pass.

The worker report records direct gzip `113042` bytes. Manifest generation did not change the application JavaScript asset hash or size.

## Merge and tree verification

- Merge method: normal merge commit
- Actual merge commit and new phase head: `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

The exact green merge-ref tree and actual merge tree were directly compared. The comparison returned zero changed files. The phase branch `refactor/dashboard-modules` was then compared with the actual merge commit and was identical.

The connected workflow-run lookup exposes pull-request-triggered runs and does not expose a separate push-run for the actual merge SHA. Checkpoint 3.5 must independently query GitHub for any push run and must rerun the full exact-head gates locally; no separate push-run ID is claimed here.

## Final bundle policy

- deterministic Vite manifest: `dist/.vite/manifest.json`;
- initial JS: union of JavaScript entries and recursive static-import closure only;
- largest JS: largest emitted `.js`, `.mjs`, or `.cjs` asset;
- total JS: every emitted `.js`, `.mjs`, and `.cjs` under `dist/assets`, including dynamic and unreferenced assets;
- budgets: `409600` initial, `409600` largest, `414720` total;
- failure modes and deterministic ordering covered by 25 Node built-in tests.

## Wave 3 disposition

- MP07 route splitting: measurement accepted, implementation rejected because total JS increased.
- MP08 lazy Recharts boundary: measurement accepted, implementation rejected because total JS increased.
- MP08B native SVG renderer: integrated.
- MP09-R1 bundle policy: integrated.
- Wave 3 implementation is complete.
- Checkpoint 3.5 remains required before Phase 4.

## Terminal disposition

`P3_5_MP09_R1_INTEGRATION_COMPLETE`
