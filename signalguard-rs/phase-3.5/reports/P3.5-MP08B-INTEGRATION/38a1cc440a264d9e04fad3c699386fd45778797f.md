# P3.5-MP08B — Integration record

## Product identity

- Product repository: `progeranna/signalguard-rs`
- Previous integrated phase head: `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`
- Immutable worker branch: `p35/mp08b-native-svg-timeline`
- Immutable worker head: `564cc6e8b5b584625e1a168c27657ecb269700d0`
- Worker commit message: `refactor(ui): replace Recharts timeline with native SVG`
- Worker report: `signalguard-rs/phase-3.5/reports/P3.5-MP08B/564cc6e8b5b584625e1a168c27657ecb269700d0.md`
- Worker report commit: `bae5ba91f7d1e8d3e3d280cf773a116dabd42156`

The worker commit is exactly one commit ahead and zero behind the previous phase head.

## Accepted scope

The final diff contains exactly eight paths, all within the ten-path writable lease:

1. `web/package-lock.json`
2. `web/package.json`
3. `web/src/features/dashboard/TimelineChartRenderer.test.tsx`
4. `web/src/features/dashboard/TimelineChartRenderer.tsx`
5. `web/src/features/dashboard/TimelinePanel.test.tsx`
6. `web/src/features/dashboard/TimelinePanel.tsx`
7. `web/src/features/dashboard/timelineChartGeometry.test.ts`
8. `web/src/features/dashboard/timelineChartGeometry.ts`

`TimelinePanel` remains eager and retains shell, state precedence, normalization, domains, visible-anomaly derivation, selected-market identity, and query ownership. `TimelineChartRenderer` owns only dependency-free native SVG presentation and local pointer/keyboard tooltip interaction. `timelineChartGeometry` is a pure projection/path helper.

`recharts` and its no-longer-used dependency graph were removed. No replacement runtime dependency was introduced. No router, page owner, API/query owner, backend, generated contract, bundle-budget, CI, product-copy, or unrelated visual file changed.

## Exact merge-ref validation

- Integration PR: `#66`
- PR base: `refactor/dashboard-modules` at `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`
- PR head: `564cc6e8b5b584625e1a168c27657ecb269700d0`
- Tested PR merge ref: `5a342a9bdd2a7330ef84fb83832f7b60158b7456`
- Authoritative CI run: `30721050915`
- Rust job: `91424818626` — success
- Frontend job: `91424818632` — success

GitHub Actions fetched `+5a342a9bdd2a7330ef84fb83832f7b60158b7456:refs/remotes/pull/66/merge`, checked out `refs/remotes/pull/66/merge`, and verified HEAD exactly at that SHA.

Validation result:

- frontend dependency installation: success;
- frontend test files: `45` passed;
- frontend tests: `653` passed;
- TypeScript: pass;
- ESLint with zero warnings: pass;
- production build: pass;
- transformed modules: `140`;
- emitted JS assets: `1`;
- total and largest raw JS: `395700` bytes;
- Vite gzip display: `113.11 kB`;
- largest JS budget: `761856` bytes, unchanged;
- total JS budget: `761856` bytes, unchanged;
- bundle check: pass;
- Rust formatting, API-contract, check, clippy, tests, replay target discovery, Docker Compose, and script syntax gates: pass.

The worker report additionally records direct gzip `113042` bytes and a deterministic desktop/mobile production-preview browser matrix covering request counts, retry, keyboard tooltips, responsive resize, console/rejection checks, and remount/initialization checks.

## Merge and tree verification

- Merge method: normal merge commit
- Actual merge commit and new phase head: `38a1cc440a264d9e04fad3c699386fd45778797f`

The exact green merge-ref tree and actual merge tree were directly compared. The comparison returned zero changed files. The phase branch `refactor/dashboard-modules` was then compared with `38a1cc440a264d9e04fad3c699386fd45778797f` and was identical.

## Quantitative result

Compared with the previous integrated phase head:

- raw JS: `757217 -> 395700` bytes;
- raw reduction: `361517` bytes;
- direct gzip: `219504 -> 113042` bytes;
- direct-gzip reduction: `106462` bytes;
- total raw headroom under the unchanged `761856`-byte budget: `366156` bytes.

The Phase 3.5 minimum `8192`-byte headroom target and preferred `16384`-byte target are both exceeded without raising any limit.

## Wave 3 disposition

- MP07 route splitting remains rejected because it increased total JS.
- MP08 lazy Recharts splitting remains rejected because it increased total JS.
- MP08B native SVG replacement is integrated and supplies the required true total-JS reduction.
- MP09 bundle-policy refinement is now unblocked.
- Phase 4 remains blocked until MP09 and Checkpoint 3.5 are complete.

## Terminal disposition

`P3_5_MP08B_INTEGRATION_COMPLETE`
