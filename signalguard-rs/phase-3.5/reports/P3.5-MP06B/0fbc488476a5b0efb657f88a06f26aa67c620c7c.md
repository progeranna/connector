# P3.5-MP06B — Implementation report

## Product identity

- Product repository: `progeranna/signalguard-rs`
- Exact immutable base and phase head: `21fec6d357b365a3710c8e7118a467564247dee6`
- Base branch verified before branch creation: `refactor/dashboard-modules`
- Assigned product branch: `p35/mp06b-symbol-detail-test-guards`
- Product commit: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`
- Product commit message: `test(ui): modernize anomaly and symbol detail guards`

The assigned branch was created from the exact immutable base and was updated by a normal non-force fast-forward to the single product commit.

## Exact changed product paths

Exactly five leased test files changed:

1. `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`
2. `web/src/features/dashboard/SymbolDetailHeader.test.tsx`
3. `web/src/features/dashboard/selectedSymbolConsumers.test.ts`
4. `web/src/pages/SymbolDetailPage.test.tsx`
5. `web/src/pages/DashboardPage.popup.test.tsx`

`web/src/features/dashboard/SymbolDetailMetrics.test.tsx` was audited and intentionally left unchanged because the exact-base test already satisfies the focused `GUARD-034` supplied-view-model boundary. Its branch blob remains byte-identical to the base at `b67df30b29438b8ff055e9f830e74dbce9edd26c`.

No production file, package file, lockfile, configuration, route, budget, generated contract, backend file, documentation file in the product repository, MP06A-leased test, `web/src/pages/DashboardPage.test.tsx`, or `marketNavigationIsolation.test.tsx` changed.

## MP00D guard dispositions

### `GUARD-011` / `R-08` — Dashboard popup modernization

Final mechanism: public `DashboardPage` rendering with controlled `(uiMode, symbol)` popup-resource states and recorder wrappers around the public adapter and shared presentation sections.

Evidence added or retained:

- table-driven `loading`, `error`, `unavailable`, and `success` popup states;
- exact canonical popup identity including return context;
- one shared adapted `MarketDetailViewModel` reference passed to popup metrics and anomaly sections;
- raw selected symbol passed to header and anomaly presentation;
- current-resource retry isolation;
- old-symbol and old-mode late-resolution isolation;
- close button, Escape, backdrop, back navigation, return-context preservation, focus, native-button, and click behavior;
- explicit configured-Live empty state with no Demo content fallback.

No exact internal popup helper name, exact popup-success JSX block, modal lifecycle source string, or function-order assertion remains.

### `GUARD-032` / `R-17` — Anomaly desktop/mobile identity

Final mechanism: behaviorally capture desktop rows and mobile items for two frozen anomalies with distinct IDs, symbols, and content; rerender in reversed order; prove each original DOM node follows its anomaly identity in both route and popup variants.

The test no longer names `AnomalyDesktopTable`, `AnomalyDesktopRow`, `AnomalyMobileItem`, or an exact `key={...}` expression.

### `GUARD-033` / `R-18` — Anomaly ownership narrowing

Final mechanism: frozen multi-row behavior proves complete input preservation, order, no limiting, no deduplication, and no mutation. Source boundaries are narrowed to anomaly-qualified collection operations, parsed import specifiers, and explicit forbidden APIs/hooks for router, query, popup/resource, storage, network, browser, timers, and current time.

The generic word `mode` and unrelated `.slice(` calls are not banned.

### `GUARD-035` / `R-19` — Header ownership narrowing

Final mechanism: parsed import-specifier checks and explicit calls/hooks reject router, query, selected-symbol, popup/resource-hook, storage, browser, network, timers, and current-time ownership. Generic type-name words such as `Resource` and `Controller` are no longer globally banned.

DOM behavior still proves the header contains no links, buttons, form controls, or focusable descendants.

### `GUARD-036` / `R-20` — Symbol route composition

Final mechanism: controlled catalog data, `useSymbolMarketResource`, adapter output, and shared presentation-section mocks table-drive invalid symbol, loading, error, unavailable, and success states.

Evidence added:

- resource identity remains `(uiMode, canonical symbol)`;
- heading receives the normalized selected route symbol;
- route strip and route state receive the same view-model object reference;
- route anomalies receive the same view-model anomaly array;
- route presentation receives no popup callback;
- unavailable Live identity stays explicit, reports `Live`, and does not synthesize Demo presentation;
- catalog-loading precedence over unavailable is preserved;
- route links update only the active mode selected-symbol key;
- a narrow direct-ownership boundary rejects duplicated table ownership and known direct presentation-owner imports.

All Dashboard source slicing, Dashboard internal function ordering, exact shared-section JSX strings, and local helper-name assertions were removed.

### `GUARD-039` / `R-22` — Selected-symbol mode isolation

Final mechanism: public selected-symbol hooks and storage APIs are exercised with distinct Demo and Live values. Mode switches, hook writes, route-link writes, and popup writes prove only the active mode key changes while the inactive mode selection remains intact.

A narrow source check remains only for the legacy one-argument `storeSelectedSymbol(...)` API and the legacy storage constant. Exact call formatting, dependency-array text, and internal helper placement are no longer asserted.

### Exact guards retained

- `GUARD-031` exact route/popup anomaly visual distinctions remain strict.
- `GUARD-034` remains unchanged at the exact-base blob.

## Exact route/popup visual contracts confirmed unchanged

The modernization retains exact or equivalently strict assertions for:

- route anomaly timestamp class: `mt-1 text-xs uppercase tracking-[0.14em] text-slate-500`;
- route timestamp remaining non-semibold;
- popup metric-label class: `text-xs font-semibold uppercase tracking-[0.14em] text-slate-500`;
- intentional route/popup typography distinction;
- responsive desktop/mobile wrapper tokens, including `hidden`, `lg:block`, and `lg:hidden` behavior;
- route severity text classes: `text-rose-100`, `text-amber-100`, `text-sky-100`;
- popup severity border classes: `border-rose-400/35`, `border-amber-400/35`, `border-sky-400/35`;
- popup supplied anomaly value classes;
- route non-interactive anomaly presentation;
- popup mobile anomaly items as focusable native `button` elements with `type="button"` and preserved callbacks;
- exact route and popup titles, subtitles, column labels, field selection, empty-state copy, and presentation hierarchy.

## Verification

Product commit-object verification before publication:

- parent/merge base: `21fec6d357b365a3710c8e7118a467564247dee6`;
- commit: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`;
- commit message: `test(ui): modernize anomaly and symbol detail guards`;
- status versus base: `ahead`;
- ahead by: `1`;
- behind by: `0`;
- total commits: `1`;
- changed files: exactly the five leased paths listed above;
- production changes: `0`;
- MP06A-leased changes: `0`.

Published branch blob verification:

- `SymbolDetailAnomalies.test.tsx`: `94150e913f515e773f959dedb8ed71fb17c80f72`;
- `SymbolDetailHeader.test.tsx`: `4b29883ab9ffae684b3cc9174d1843ca3ba6f389`;
- `selectedSymbolConsumers.test.ts`: `002957245fa72602ea938da4e2fcea316eded3a0`;
- `SymbolDetailPage.test.tsx`: `a057769a4e2ab79a491a8b70ebb8b050215c6128`;
- `DashboardPage.popup.test.tsx`: `e09d22f2efefcb41b0aea98a92487e96e60c235d`;
- unchanged `SymbolDetailMetrics.test.tsx`: `b67df30b29438b8ff055e9f830e74dbce9edd26c`.

Available status inspection at publication time:

- combined commit statuses: none reported;
- pull-request-triggered workflow runs for the product commit: none reported.

No executable npm, Vitest, TypeScript, ESLint, build, bundle, or Rust gate was claimed by this web worker. Those gates are deferred to Orchestrator merge-ref CI.

## Deferred executable commands

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/SymbolDetailAnomalies.test.tsx src/features/dashboard/SymbolDetailHeader.test.tsx src/features/dashboard/SymbolDetailMetrics.test.tsx src/features/dashboard/selectedSymbolConsumers.test.ts src/pages/SymbolDetailPage.test.tsx src/pages/DashboardPage.popup.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

## Publication disposition

- Product branch pushed: `p35/mp06b-symbol-detail-test-guards`.
- Product history rewritten: no.
- Force push: no.
- Pull request opened: no.
- Merge performed: no.

## Terminal disposition

`P3_5_MP06B_IMPLEMENTATION_COMPLETE`
