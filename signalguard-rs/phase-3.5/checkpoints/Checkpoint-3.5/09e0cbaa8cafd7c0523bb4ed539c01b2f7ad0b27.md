# Checkpoint 3.5 evidence — `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

## Disposition

`P3_5_CHECKPOINT_EVIDENCE_COMPLETE`

The exact final Phase 3.5 product tree passed the required local, CI, browser, visual, archive, and cleanup gates. Product history was not changed.

## Product identity and ancestry

- Product repository: `progeranna/signalguard-rs`
- Phase branch: `refactor/dashboard-modules`
- Immutable Phase 3.5 start: `c06082a97254bfa2f6ebd7e29a1ad753c4acc798`
- Final integrated head: `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`
- Final MP09-R1 worker head: `b1e574705dea0ae2e3fb682cbd187ce4e3d717ff`
- Tested PR merge-ref: `67ec5488b8747ceeb8f09b3fb4c921af24c05eff`

Authenticated fetch and `git ls-remote` both resolved `refs/heads/refactor/dashboard-modules` exactly to the final head. The final commit is a normal merge with parents `38a1cc440a264d9e04fad3c699386fd45778797f` and `b1e574705dea0ae2e3fb682cbd187ce4e3d717ff`; the MP09-R1 worker head is an ancestor. `git diff --name-status 67ec5488b8747ceeb8f09b3fb4c921af24c05eff 09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27` was empty, and both commits resolve to tree `0ad193d7aca4b668ba51232b7bf8362ddd066e10`.

The validation worktree was detached at the exact final SHA. Before validation, `git status --short`, `git diff`, and `git diff --check` were empty.

## GitHub Actions evidence

Authenticated `gh` inspection established:

- PR `#67` is merged from `p35/mp09-bundle-policy-refinement` into `refactor/dashboard-modules` with merge commit `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`.
- Authoritative PR run `30721976267` completed successfully.
- Frontend job `91427128390` and Rust job `91427128400` both completed successfully.
- Both job logs fetched `67ec5488b8747ceeb8f09b3fb4c921af24c05eff` into `refs/remotes/pull/67/merge`, checked out that ref, and printed the exact merge-ref SHA as HEAD.
- The workflow declares `push` and `pull_request` triggers; it does not declare `workflow_dispatch`.
- The complete final-SHA run query returned one run: push-triggered run `30722022839`, branch `refactor/dashboard-modules`, exact `headSha` `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`, conclusion `success`.
- Final-SHA frontend job `91427246502` and Rust job `91427246542` both completed successfully. Their checkout logs printed the exact final SHA.

No dispatch was necessary because successful push-triggered final-SHA CI exists.

## Exact-head frontend gates

The required sequence ran from `web` after a clean `npm ci`:

| Gate | Result |
|---|---|
| `node --test scripts/check-bundle-size.test.mjs` | 25 passed, 0 failed |
| `npm run test:run` | 45 files, 653 tests passed |
| `npm run typecheck` | passed |
| `npm run lint` | passed with zero warnings |
| `npm run build` | passed; 140 transformed modules |
| `npm run bundle:check` | passed, including the repeated 25-test policy suite |

The deterministic asset was `assets/index-241ec209.js`. The Vite manifest was present and internally consistent. It described one JavaScript entry, one initial JavaScript asset, and zero async JavaScript assets.

| Bundle metric | Actual | Budget | Headroom |
|---|---:|---:|---:|
| Initial raw JS | 395700 | 409600 | 13900 |
| Largest raw JS | 395700 | 409600 | 13900 |
| Total raw JS | 395700 | 414720 | 19020 |

Direct `gzip -c` size was `113042` bytes. Temporary in-memory Vite/Rollup instrumentation transformed 140 modules, recorded 107 surviving module IDs in one chunk, and found zero duplicate emitted module IDs. Package, lockfile, and built-asset inspection found no Recharts or measured Recharts-only production graph.

## Exact-head repository gates

All required commands passed sequentially:

- `cargo fmt --check`
- API contract `--check` and `--validate`
- `cargo check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test`: 379 passed, 0 failed, 23 intentionally ignored, 0 measured, 0 filtered out across all targets
- `cargo test --test replay_e2e`: 0 passed, 0 failed, 2 intentionally ignored, 0 measured, 0 filtered out; both require controlled PostgreSQL and Redis
- `docker compose config`
- `docker compose --profile app config`
- `bash -n scripts/demo-replay.sh`
- `bash -n scripts/smoke.sh`

The ignored integration tests explicitly require external PostgreSQL/Redis services; no test failed.

## Production-preview browser and visual acceptance

Installed Chromium automation exercised production builds with frozen time and deterministic schema-valid Demo/Live fixtures at desktop and mobile widths.

Coverage passed for `/`, `/dashboard`, `/symbols/BTCUSDT`, `/symbols/ETHUSDT`, and `/anomalies`, including direct navigation and refresh. It also passed Demo-to-Live isolation, BTC-to-ETH UI transitions, direct route versus dashboard popup presentation, ticker content, responsive resize, and desktop/mobile layouts.

Timeline coverage passed for summary/timeline loading, configured/unavailable, injected error and user Retry, normalized empty, observed, anomaly-free, and info/warning/critical marker states. Pointer and keyboard traversal covered first, middle, and last points, focus exposure, ArrowLeft/ArrowRight behavior, and Escape dismissal. Anomalies at exact `-15s` and `+15s` boundaries both appeared in the middle-point tooltip.

Each settled `(uiMode, symbol)` transition issued exactly one summary and one timeline request. The configured/unavailable case issued one summary and zero timeline requests. The injected error case issued four HTTP 503 timeline attempts from the query retry policy followed by exactly one successful user-Retry request. Three observed identities produced exactly three renderer initializations; stable resize retained the same renderer node and produced zero remounts. Normal scenarios recorded zero console errors, zero page errors, and zero unhandled rejections. The error scenario recorded only the four intentionally injected 503 resource events.

Fresh screenshots compared the immutable starting SHA and final SHA with identical data and viewports. Chart-region metrics were:

| Viewport and region | Changed fraction | Mean absolute channel difference | RGB RMSE |
|---|---:|---:|---:|
| Desktop 1440×1000, `(32,122,1148,463)` | 0.3539268859 | 3.2971214048 | 11.0299003227 |
| Mobile 390×1000 viewport, `(16,254,374,595)` | 0.3577384951 | 4.3426224763 | 13.4440916795 |

Manual inspection accepted only the authorized differences: the straight native-SVG polyline in place of Recharts monotone interpolation, slight native-SVG tick-position/value differences, and CSS-sized mobile labels. Shells, card and chart dimensions, surrounding layout, copy, routes, popup behavior, ticker, markers, badges, metrics, and accessibility behavior remained materially aligned. This is not a pixel-parity claim. Temporary screenshots, profiles, mock data, and browser scripts were removed.

## Phase 3.5 quantitative ledger

- Frontend: 42 files / 607 tests → 45 files / 653 tests.
- Raw JS: 761856 → 395700 bytes; reduction 366156 bytes.
- Direct gzip: 220163 → 113042 bytes; reduction 107121 bytes.
- Starting largest/total budgets: 761856 / 761856 bytes.
- Final initial/largest/total budgets: 409600 / 409600 / 414720 bytes.
- Final headrooms: 13900 / 13900 / 19020 bytes.
- Final graph: one initial JS asset, zero async JS assets, no Recharts production graph, no duplicate emitted module IDs.
- No budget increased; both the 8192-byte minimum and 16384-byte preferred total-headroom targets were exceeded.

MP07 route splitting and MP08 lazy-Recharts splitting were rejected because each increased total JS. MP08B removed Recharts and its transitive production graph and supplied a true total-JS reduction.

## Snapshot verification

- Archive: `signalguard-rs/phase-3.5/checkpoints/Checkpoint-3.5/signalguard-rs-refactor-dashboard-modules-09e0cbaa8c.zip`
- SHA-256: `8ef66046b09fcbdac424ac6ad1a338993bb4bd1212beec25b4d69e25dee1377b`
- Root prefix: `signalguard-rs/`

The archive was created by `git archive` from the exact final commit. `unzip -t` passed. A sorted archive/extraction inventory matched all 214 paths from `git ls-tree -r --name-only`; every archived file independently hashed to the expected Git blob; there were zero unexpected roots, zero missing/extra files, and zero blob mismatches. The archive contained no `.git`, `node_modules`, `dist`, worktree metadata, browser artifacts, screenshots, reports, prompts, or connector controls. `shasum -a 256` and OpenSSL independently produced the checksum above, and checksum-file verification passed.

## Worktree cleanup and repository state

Before removal, every registered Phase 3.5 worktree was clean, its HEAD was an ancestor of the final merge, and its HEAD was preserved by at least one remote ref. The following worktrees were removed with `git worktree remove`, then metadata was pruned:

- `p35-mp00c-r1`
- `p35-mp04`
- `p35-mp05`
- `p35-mp06b-r1`
- `p35-mp07-measure`
- `p35-mp08-measure`
- `p35-mp08b`
- `p35-mp08b-measure`
- `p35-mp09`
- `p35-checkpoint-baseline`
- `p35-checkpoint`

No Phase 3.5 worktree was retained. Unrelated retained product worktrees were: the primary product checkout, `data-boundaries`, `p2-mp01`, `p2-mp03-r2`, `p2-mp04`, `p2-mp05`, `p2-mp05-r2`, `p2-mp06`, `p2-mp07`, `p3-mp15-r6`, `p3-mp15-r7`, `p3-mp16`, `p3-mp17`, `p3-mp18`, and `p3-wave3-validation`; they were outside this checkpoint's cleanup scope.

The primary product checkout remained clean and unchanged at the immutable starting commit; its local branch was intentionally not rewritten. The authoritative remote phase branch remained exactly at the final SHA. No product branch, commit, PR, merge, tag, or history change was created. Connector `STATUS.md` was not modified.
