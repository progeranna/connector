# P3.5-MP07-MEASURE — Route-level code-splitting measurement

## 1. Worker class and authority

This is a **local Codex read-only measurement task** for `progeranna/signalguard-rs`.

Use the `$rust-development` skill.

The worker may make temporary, uncommitted edits only inside its dedicated disposable product worktree in order to build and measure prototypes. It must leave the product worktree clean before publishing the report.

The worker must not create or push a product branch, create a product commit, open a pull request, merge, modify the phase branch, or publish any product artifact.

Only the required connector inventory report may be committed.

## 2. Exact product identity

- Product repository: `progeranna/signalguard-rs`
- Phase branch: `refactor/dashboard-modules`
- Exact immutable measurement base and current integrated phase head: `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`
- Baseline frontend tests: `43` files, `640` tests
- Baseline production JS assets: `1`
- Baseline total raw JS: `757217` bytes
- Baseline Vite gzip display: `219.99 kB`
- Baseline direct gzip measurement: `219504` bytes
- Largest-JS budget: `761856` bytes
- Total-JS budget: `761856` bytes
- Current raw headroom: `4639` bytes

Before any experiment, verify that the phase branch and dedicated worktree resolve exactly to the base SHA above.

## 3. Required local paths

- Product repository: `/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`
- Dedicated disposable product worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp07-measure`
- Isolated connector checkout/worktree: `/Users/anion/Desktop/work/connector-p35-mp07-measure`

Do not reuse another task's writable worktree.

## 4. Authoritative source scope

Inspect at minimum:

- `web/src/app/router.tsx`
- `web/src/app/router.test.tsx`
- `web/src/app/CanonicalSymbolRoute.tsx`
- `web/src/pages/AnomaliesPage.tsx`
- `web/src/pages/SymbolDetailPage.tsx`
- `web/src/app/ConsoleLayout.tsx`
- `web/src/app/AppShell.tsx`
- `web/src/app/GlobalMarketTicker.tsx`
- `web/src/main.tsx`
- `web/vite.config.ts`
- `web/package.json`
- `web/package-lock.json`
- `web/bundle-size-budget.json`
- `web/scripts/check-bundle-size.mjs`
- `.github/workflows/ci.yml`
- the accepted MP00A-WEB inventory
- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`

Production files are read-only outside temporary uncommitted prototypes.

## 5. Measurement question

Determine, with executable evidence, whether route-level lazy loading can reduce the initial application entry cost without increasing total production JS, breaking route/deep-link behavior, weakening loading boundaries, or changing shared root-layout behavior.

Static source inspection alone is insufficient.

## 6. Required baseline build

From the exact clean base:

```bash
cd web
npm ci
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Record for every emitted JavaScript asset:

- file name;
- raw bytes;
- direct `gzip -c` bytes;
- whether it is an entry chunk or async chunk;
- static imports;
- dynamic imports;
- facade module ID when available;
- included source modules when available.

Record:

- total raw JS;
- total direct-gzip JS;
- largest asset raw/gzip;
- initial entry closure raw/gzip;
- async-only raw/gzip;
- asset count;
- transformed module count;
- current budget result.

Use a temporary Vite/Rollup `generateBundle` analysis hook or equivalent local script if necessary. Do not commit the hook.

## 7. Mandatory prototypes

Build each prototype from a fresh restoration of the exact base. Do not stack prototypes unless the prototype explicitly requires a combination.

### Prototype R1 — lazy `/anomalies` only

Test the smallest contract-safe dynamic boundary for `AnomaliesPage`.

Requirements:

- preserve `/anomalies` direct navigation and deep-link rendering;
- preserve the root `ConsoleLayout` and `AppShell` behavior;
- use a deterministic loading boundary;
- do not lazy-load `/` or `/dashboard`;
- do not change product copy or visual design except the temporary loading boundary needed to measure the candidate.

### Prototype R2 — lazy `/symbols/:symbol` only

Test the smallest contract-safe dynamic boundary for `CanonicalSymbolRoute` and/or `SymbolDetailPage`.

Measure at least these two alternatives when both build cleanly:

1. lazy `CanonicalSymbolRoute` from the router;
2. keep `CanonicalSymbolRoute` eager but lazy its `SymbolDetailPage` child.

Requirements:

- preserve canonical route validation;
- preserve direct deep links for valid and invalid symbols;
- preserve mode-scoped selected-symbol behavior;
- preserve no-fallback and `(uiMode, symbol)` resource identity;
- use deterministic loading UI;
- do not change popup behavior.

### Prototype R3 — combined accepted route boundaries

Combine the best R1 and R2 variants exactly as they would coexist.

Do not include Timeline/Recharts splitting. MP08 owns that experiment.

## 8. Required validation per prototype

For every prototype that compiles:

```bash
cd web
npm run test:run -- src/app/router.test.tsx src/pages/SymbolDetailPage.test.tsx src/app/marketNavigationIsolation.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Also verify with a local production preview or equivalent browser-capable check:

- `/` renders the dashboard;
- `/dashboard` renders the dashboard;
- `/anomalies` renders after its loading boundary;
- `/symbols/BTCUSDT` renders correctly;
- `/symbols/ETHUSDT` renders correctly;
- an invalid symbol route preserves canonical invalid-state behavior;
- browser refresh on each route works;
- back/forward navigation works;
- no duplicate dashboard or symbol-detail query is introduced;
- no console error or unhandled dynamic-import rejection occurs.

If browser automation is unavailable, record the limitation and provide the strongest available production-preview evidence. Do not claim a browser check that did not run.

## 9. Quantitative decision rules

For each prototype report:

- delta in total raw JS from `757217`;
- delta in total direct-gzip JS from `219504`;
- delta in initial entry closure raw/gzip;
- largest entry or async asset;
- number of initial and async assets;
- whether total-JS budget remains unchanged and passing;
- whether the current Phase 3.5 total-headroom target improves, stays unchanged, or regresses.

Classify each prototype:

- `IMPLEMENTATION_CANDIDATE` only if all required gates pass, initial cost improves materially, and total raw JS does not exceed `757217` bytes;
- `INITIAL_ONLY_BENEFIT` if initial cost improves but total raw JS increases;
- `NO_MEANINGFUL_BENEFIT` if the initial improvement is trivial or shared eager imports eliminate most benefit;
- `CONTRACT_REJECTED` if route, deep-link, loading, query, identity, or visual contracts fail;
- `BUILD_REJECTED` if required executable gates fail.

Do not recommend raising either bundle budget.

## 10. Required final recommendation

The report must state:

1. which exact route boundary, if any, should be implemented;
2. exact proposed writable paths for a future MP07 implementation contract;
3. exact tests that must be added or changed;
4. whether `/anomalies` and `/symbols/:symbol` should be implemented together or sequentially;
5. measured total and initial byte effects;
6. known loading/UX tradeoffs;
7. whether MP07 helps the remaining `3553`-byte total-headroom requirement;
8. whether any result depends on a later MP09 policy change.

Do not implement the recommendation in this task.

## 11. Cleanup and proof

Before reporting:

```bash
git status --short
git diff --check
git diff --stat
git diff
```

Restore every temporary product edit and generated analysis file. Remove temporary build-only instrumentation. Re-run:

```bash
git status --short
```

The final product worktree must be clean and still resolve to `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`.

No product branch, commit, push, PR, merge, tag, budget change, package change, or lockfile change is permitted.

## 12. Connector report

Publish exactly one report:

`signalguard-rs/phase-3.5/inventory/P3.5-MP07-MEASURE/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`

Connector commit message:

`docs(phase-3.5): publish MP07 route-splitting measurements`

The report must contain commands, exact outputs or summarized measurements, prototype diffs, asset tables, validation results, cleanup proof, limitations, and the final classification.

Verify the report and connector commit after publication.

## 13. Terminal status

Return exactly:

`P3_5_MP07_MEASUREMENT_COMPLETE`

only after the report is committed and verified and the product worktree is clean.

If the task cannot complete, return a precise blocked status and do not publish unsupported conclusions.
