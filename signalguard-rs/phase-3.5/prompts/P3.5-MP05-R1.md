# P3.5-MP05-R1 — Validation recovery addendum

## 1. Purpose

This addendum resolves the exact validation block encountered while executing:

`signalguard-rs/phase-3.5/prompts/P3.5-MP05.md`

It does not replace or broaden the original MP05 contract except for the two exact binding dispositions below. All other identity, lease, validation, commit, publication, and prohibition requirements remain authoritative.

Use the $rust-development skill.

## 2. Immutable product identity

Product repository:

`progeranna/signalguard-rs`

Exact base and current integrated phase head:

`0027679f7a5b2cc783098b1d4a625e1638bee67d`

Assigned branch:

`p35/mp05-confirmed-dead-code`

Existing dedicated worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp05`

Required product commit message:

`refactor(ui): remove confirmed dead exports and declarations`

Continue the existing local branch and worktree. Do not recreate, reset, rebase, clean, discard, or overwrite the valid uncommitted changes. No product commit, push, connector report, PR, or merge existed when this recovery was authorized.

## 3. Verified blocked state

The original pre-write reference checks passed.

The current uncommitted candidate remained within the original nine-path lease.

The following validation passed:

- focused frontend tests: 10 files, 127 tests;
- full frontend tests: 43 files, 629 tests;
- TypeScript project typecheck.

Lint and one-shot post-edit no-unused validation failed only because removing `export` made these two declarations unused locally:

- `web/src/test/deferredFetch.ts` — `installControlledFetch`;
- `web/src/test/uiSmokeMatrix.ts` — `UiSmokeDialogRequirement`.

This is validation evidence that the original modifier-only action was invalid. It is not authorization to add artificial uses, suppress diagnostics, weaken checks, or delete a larger transitive helper cluster.

## 4. Exact recovery changes

### 4.1 `installControlledFetch`

Restore and retain the declaration exactly as exported:

```ts
export function installControlledFetch(...)
```

Do not delete, rename, inline, restructure, or modify its body.

Keep the original MP05 change that removes only the `export` modifier from:

- `requestIdentityKey`.

The latter remains locally reachable through `formatIdentity` and `installControlledFetch`.

### 4.2 `UiSmokeDialogRequirement`

Restore and retain the type alias exactly as exported:

```ts
export type UiSmokeDialogRequirement = ...
```

Do not delete or modify `UI_SMOKE_DIALOG_REQUIREMENTS` or the type body.

Keep the original MP05 modifier removals for the other authorized fixture-internal UI smoke types:

- `UiSmokeMode`;
- `UiSmokeMarket`;
- `UiSmokeSurface`;
- `UiSmokeViewport`;
- `UiSmokeViewportName`;
- `UiSmokeState`;
- `UiSmokeScenarioGroup`;
- `UiSmokeNavigationContinuity`.

## 5. Unchanged original implementation scope

Retain all other valid uncommitted MP05 changes required by the original contract, including:

- remove the `EmptyBlock` export modifier;
- delete `buildCoveredDashboardSymbols`;
- delete its now-unreachable private helper `coverCanonicalMarketEntries`;
- delete `isMarketCatalogSymbolList`;
- remove export modifiers from the five same-module API fetch functions;
- remove the `matrixObservedSymbol` export modifier;
- remove export modifiers from `getApiBaseUrl`, `buildApiUrl`, and `parseWithSchema`;
- remove the `SemanticDescriptorExpectation` export modifier.

Do not modify any probable, verification-required, intentional, or false-positive finding.

The writable lease remains the same nine paths from the original MP05 contract. No test assertion file, package file, lockfile, config, budget, route, backend, generated contract, or other product file is writable.

## 6. Required validation after recovery

After restoring only the two exact exports, rerun all original required gates. Previously passing output is not a substitute for a post-recovery run.

At minimum rerun:

```bash
cd web
npm run test:run -- \
  src/features/dashboard/MarketHealthMobileCards.test.tsx \
  src/features/dashboard/marketCatalog.test.ts \
  src/features/dashboard/marketCatalogConsumers.test.ts \
  src/features/dashboard/api.test.ts \
  src/features/dashboard/api-contract.test.ts \
  src/features/dashboard/apiCancellation.test.ts \
  src/features/dashboard/marketDataIsolation.test.tsx \
  src/shared/api/client.test.ts \
  src/test/statusDescriptorFixtures.test.ts \
  src/test/uiSmokeMatrix.test.ts
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Also rerun the original one-shot no-unused checks for both TypeScript projects, exact importer/reference searches, Knip checks, Madge checks, all repository-level Rust gates, Docker Compose validation, script syntax checks, and final diff/lease proof.

Required results:

- lint passes with zero warnings;
- no-unused checks pass with no diagnostics;
- no artificial use or suppression exists;
- no new dead local declaration is introduced;
- no import cycle, unresolved dependency, or package deletion proposal appears;
- total production JS remains `<= 757217` raw bytes;
- the `761856`-byte budgets remain unchanged;
- only the original nine leased paths, or a strict subset, differ from base.

## 7. Commit and report

After every required gate passes, create exactly one normal product commit with the original required message and push only the assigned branch.

Publish the original required connector report at:

`signalguard-rs/phase-3.5/reports/P3.5-MP05/<PRODUCT_COMMIT_SHA>.md`

The report must additionally record:

- the initial terminal state `P3_5_MP05_BLOCKED_BY_VALIDATION`;
- the two exact failed declarations;
- this recovery addendum path and connector commit;
- that `installControlledFetch` and `UiSmokeDialogRequirement` remain exported based on executable lint/no-unused evidence;
- that no body deletion, artificial use, suppression, test weakening, scope expansion, reset, or history rewrite occurred.

Required connector commit message remains:

`docs(phase-3.5): publish MP05 implementation report`

Do not open a PR or merge.

## 8. Terminal status

Success:

`P3_5_MP05_IMPLEMENTATION_COMPLETE`

Any remaining required-gate failure:

`P3_5_MP05_BLOCKED_BY_VALIDATION`

This addendum does not authorize MP06, Wave 3, Phase 4, or any product change outside the original MP05 lease.