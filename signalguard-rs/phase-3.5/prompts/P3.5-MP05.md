# P3.5-MP05 — Confirmed dead exports and declarations

## 1. Worker class

This is a local Codex implementation task.

Use the $rust-development skill.

Use a dedicated isolated Git worktree. Local Git, shell, Node/npm, TypeScript, Cargo, Docker Compose configuration validation, and the existing repository tooling are required.

Do not execute this contract as a GitHub web-only worker. The task must re-run importer analysis and authoritative executable gates before publication.

## 2. Repositories and local roots

Product repository:

`progeranna/signalguard-rs`

Expected local product checkout:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Dedicated product worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp05`

Coordination repository:

`progeranna/connector`

Recommended isolated coordination checkout/worktree:

`/Users/anion/Desktop/work/connector-p35-mp05`

Authoritative documents:

- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00C-R1/0027679f7a5b2cc783098b1d4a625e1638bee67d.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP04-INTEGRATION/0027679f7a5b2cc783098b1d4a625e1638bee67d.md`

Read all four before any product write.

## 3. Immutable identity

Exact product base and current phase head:

`0027679f7a5b2cc783098b1d4a625e1638bee67d`

Base branch:

`refactor/dashboard-modules`

Assigned product branch:

`p35/mp05-confirmed-dead-code`

Required single product commit message:

`refactor(ui): remove confirmed dead exports and declarations`

Before creating the worktree:

1. fetch normally;
2. verify `origin/refactor/dashboard-modules` resolves exactly to the assigned base;
3. verify the assigned product branch does not already exist locally or remotely;
4. verify the main product checkout is clean;
5. create the assigned branch and dedicated worktree from the exact SHA.

Do not start from `main`, an older Phase 3.5 SHA, an integration branch, or another worker branch. Stop with the blocked terminal status if identity or cleanliness differs.

## 4. Accepted audit basis

The refreshed MP00C-R1 audit was executed locally against the exact assigned base with:

- clean TypeScript no-unused checks;
- Knip export/importer and dependency analysis;
- Madge cycle/orphan analysis;
- manual importer, dynamic-reference, entry-point, configuration, script, and compatibility inspection;
- full frontend build and bundle validation.

Accepted baseline:

- frontend: 43 test files, 629 tests;
- production JS raw: `757217` bytes;
- Vite gzip display: `219.99 kB`;
- largest/total JS budget: `761856` bytes;
- headroom: `4639` bytes;
- no direct package or lockfile deletion candidate;
- no credible import cycle.

Only findings `R1-C-001` through `R1-C-013` are authorized. Every probable, verification-required, intentional, and false-positive item remains forbidden.

## 5. Exact writable lease

Only these product paths may change:

1. `web/src/features/dashboard/MarketHealthMobileCards.tsx`
2. `web/src/features/dashboard/marketOrder.ts`
3. `web/src/features/dashboard/marketCatalog.ts`
4. `web/src/features/dashboard/api.ts`
5. `web/src/test/marketFixtures.ts`
6. `web/src/test/deferredFetch.ts`
7. `web/src/shared/api/client.ts`
8. `web/src/test/statusDescriptorFixtures.ts`
9. `web/src/test/uiSmokeMatrix.ts`

No other product file is writable. In particular, no `*.test.ts` or `*.test.tsx` assertion file is leased.

## 6. Required exact implementation

Perform only the following changes.

### 6.1 `MarketHealthMobileCards.tsx`

Remove only the `export` modifier from the locally used `EmptyBlock` declaration.

Do not delete or move the component. Do not change its props, DOM, classes, copy, rendering, or callers.

### 6.2 `marketOrder.ts`

Delete the confirmed unreachable declaration:

- `buildCoveredDashboardSymbols`

Delete its now-unreachable private implementation helper:

- `coverCanonicalMarketEntries`

Retain unchanged:

- the `DEMO_MARKETS` compatibility re-export;
- `orderMarkets`;
- `orderMarketEntries`;
- `isDashboardSymbolPlaceholder`;
- ordering, canonicalization, availability, and placeholder semantics.

### 6.3 `marketCatalog.ts`

Delete only the confirmed unreachable declaration:

- `isMarketCatalogSymbolList`

Retain every other catalog type, constant, helper, branch, and behavior unchanged.

### 6.4 `api.ts`

Remove only the `export` modifiers from these same-module fetch functions:

- `fetchDashboardSummary`
- `fetchMarketState`
- `fetchMarketHealth`
- `fetchMarketTimeline`
- `fetchMarketAnomalies`

Do not rename, inline, reorder, delete, or alter their implementations. Do not modify hooks, query keys, schemas, cancellation, URL construction, mode/symbol identity, or error behavior.

### 6.5 `test/marketFixtures.ts`

Remove only the `export` modifier from:

- `matrixObservedSymbol`

Do not alter fixture values or matrix behavior.

### 6.6 `test/deferredFetch.ts`

Remove only the `export` modifiers from:

- `installControlledFetch`
- `requestIdentityKey`

Do not delete, rename, inline, or change either body. Other exported test helpers remain unchanged.

### 6.7 `shared/api/client.ts`

Remove only the `export` modifiers from:

- `getApiBaseUrl`
- `buildApiUrl`
- `parseWithSchema`

Do not alter `fetchJson`, URL semantics, schema validation, error translation, abort handling, headers, or request behavior.

### 6.8 `test/statusDescriptorFixtures.ts`

Remove only the export modifier from the fixture-internal type:

- `SemanticDescriptorExpectation`

Do not alter fixture data or descriptor expectations.

### 6.9 `test/uiSmokeMatrix.ts`

Remove only the export modifiers from these fixture-internal types:

- `UiSmokeMode`
- `UiSmokeMarket`
- `UiSmokeSurface`
- `UiSmokeViewport`
- `UiSmokeViewportName`
- `UiSmokeState`
- `UiSmokeDialogRequirement`
- `UiSmokeScenarioGroup`
- `UiSmokeNavigationContinuity`

Do not alter scenario values, ordering, coverage, navigation continuity, or any runtime/test behavior.

## 7. Explicitly forbidden changes

Do not modify, remove, privatize, or reinterpret any of the following:

- `dashboardResourceState.ts` or its tests;
- `shared/components/Tooltip.tsx` or its tests;
- `replaceSymbolPopupSymbol`;
- the `DEMO_MARKETS` re-export from `marketOrder.ts`;
- runtime-mode query/API/schema/type exports;
- query-key compatibility re-exports;
- `normalizeSelectedSymbol`;
- `useSymbolPopupResource` or popup resource types;
- broad schema/type exports listed as verification-required;
- defensive availability, status, severity, resource, error, or fallback branches;
- any package manifest, lockfile, dependency, TypeScript config, Vite/Vitest config, ESLint config, CI workflow, script, generated API contract, budget, route, page, Dashboard presentation, Timeline, Recharts, visual presentation, product copy, backend, or Phase 4 file.

Do not introduce an analysis dependency. Temporary `npm exec --yes --package=...` use is permitted only when it does not modify tracked manifests or lockfiles.

Do not weaken, delete, or rewrite tests to make the cleanup pass.

## 8. Pre-write revalidation

Before editing, independently re-run exact-word importer and reference searches for every authorized symbol across:

- `web/src`;
- `web/index.html`;
- package scripts;
- Vite/Vitest/TypeScript/ESLint/Tailwind/PostCSS configuration;
- repository scripts;
- production dynamic imports and public entry points.

Confirm:

- every export-only candidate has only same-file use and no external importer;
- the two dead declarations have no caller;
- `coverCanonicalMarketEntries` is used only by `buildCoveredDashboardSymbols`;
- no out-of-scope candidate is needed for the authorized deletions.

If any importer, dynamic reference, public entry, generated-contract dependency, configuration dependency, or compatibility consumer appears, stop without product commit and return the blocked terminal status with evidence.

## 9. Required validation

### 9.1 Focused frontend tests

From `web` run at minimum:

```bash
npm ci
npm run test:run -- \
  src/features/dashboard/MarketHealthMobileCards.test.tsx \
  src/features/dashboard/marketCatalog.test.ts \
  src/features/dashboard/marketCatalogConsumers.test.ts \
  src/features/dashboard/api.test.ts \
  src/features/dashboard/api-contract.test.ts \
  src/features/dashboard/apiCancellation.test.ts \
  src/features/dashboard/marketDataIsolation.test.tsx \
  src/shared/api/client.test.ts \
  src/test/statusDescriptorFixtures.test.ts \
  src/test/uiSmokeMatrix.test.ts
```

### 9.2 Full frontend gates

```bash
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Required result:

- all gates pass;
- no new warning class is introduced or suppressed;
- the existing `761856`-byte budgets remain unchanged;
- total production JS must be `<= 757217` raw bytes;
- record exact raw and Vite gzip results and delta from `757217`;
- do not invent an expected byte reduction.

### 9.3 Post-edit unused/importer/cycle proof

Re-run:

- TypeScript one-shot `noUnusedLocals` and `noUnusedParameters` checks for both committed TypeScript projects, using temporary build-info paths outside the repository;
- exact importer/reference searches for every changed symbol;
- Knip export/file/dependency checks using temporary execution only;
- Madge production/tooling cycle checks using temporary execution only.

Required result:

- no new unused diagnostic;
- no unresolved or unlisted dependency;
- no dependency deletion proposal;
- no import cycle;
- no remaining declaration for the two authorized dead implementations;
- all intended local-only declarations remain reachable within their modules.

### 9.4 Repository-level gates

From the product repository root:

```bash
cargo fmt --check
cargo run --quiet --bin export-api-contract -- --check
cargo run --quiet --bin export-api-contract -- --validate
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --test replay_e2e
docker compose config
docker compose --profile app config
bash -n scripts/demo-replay.sh
bash -n scripts/smoke.sh
```

### 9.5 Diff and lease proof

Before commit run:

```bash
git status --short
git diff --check
git diff --stat
git diff
```

Verify:

- exactly the nine leased paths changed, or a strict subset when no edit is needed in a leased path;
- no test assertion file changed;
- no package, lockfile, config, budget, route, backend, or generated file changed;
- there is zero runtime behavior change;
- branch is exactly one logical commit ahead of the assigned base and zero behind.

No commit may be created after a failed required gate.

## 10. Commit and publication

Create exactly one normal product commit with message:

`refactor(ui): remove confirmed dead exports and declarations`

Push only:

`p35/mp05-confirmed-dead-code`

Do not rebase, amend, reset, force-push, squash, or rewrite history.

Do not open a pull request. Do not merge.

Publish one connector report at:

`signalguard-rs/phase-3.5/reports/P3.5-MP05/<PRODUCT_COMMIT_SHA>.md`

Required connector commit message:

`docs(phase-3.5): publish MP05 implementation report`

The report must include:

- exact product base, branch, commit, and commit message;
- exact changed paths and per-symbol disposition;
- revalidation commands and importer evidence;
- focused/full frontend results;
- no-unused, Knip, and Madge results;
- repository-level gate results;
- raw/gzip baseline, candidate, delta, budget, and headroom;
- warning disposition;
- lease proof;
- explicit confirmation that probable and verification-required candidates were not changed;
- explicit confirmation that no package/lockfile/config/budget change occurred;
- explicit confirmation that no PR or merge occurred.

Verify both the pushed product branch and the connector report at their exact commits before returning.

## 11. Terminal statuses

Success:

`P3_5_MP05_IMPLEMENTATION_COMPLETE`

Blocked identity or cleanliness:

`P3_5_MP05_BLOCKED_IDENTITY_OR_CLEANLINESS`

Blocked by importer or compatibility evidence:

`P3_5_MP05_BLOCKED_BY_REFERENCE_EVIDENCE`

Blocked by lease or scope:

`P3_5_MP05_BLOCKED_BY_SCOPE`

Blocked by validation failure:

`P3_5_MP05_BLOCKED_BY_VALIDATION`

Do not claim completion unless the exact product commit and connector report are both published and verified.