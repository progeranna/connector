# P3.5-MP06A — Market-health and timeline test-guard modernization

## 1. Worker class

This is a GitHub web implementation-worker task.

Use the connected GitHub tools only. Do not use a local checkout, local shell, Codex CLI, or an unconnected repository copy.

The task is test-only. Production code is read-only.

## 2. Repositories

Product repository:

`progeranna/signalguard-rs`

Coordination repository:

`progeranna/connector`

Authoritative documents to read before any write:

- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00D/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP05-INTEGRATION/21fec6d357b365a3710c8e7118a467564247dee6.md`

Read the complete MP00D replacement specifications, especially `R-01` through `R-16`, `R-21`, and `R-23` through `R-27`.

## 3. Immutable identity

Exact product base and current phase head:

`21fec6d357b365a3710c8e7118a467564247dee6`

Base branch:

`refactor/dashboard-modules`

Assigned product branch:

`p35/mp06a-market-health-test-guards`

Required single product commit message:

`test(ui): modernize market health and timeline guards`

Create the assigned branch from the exact base. If the branch already exists at any other SHA, or the phase branch no longer resolves to the exact base before branch creation, stop and return the blocked identity status. Do not write to any other product branch.

Do not rebase, amend, reset, force-push, squash, or rewrite history.

## 4. Exact writable lease

Only these product test files may change:

1. `web/src/features/dashboard/TimelinePanel.test.tsx`
2. `web/src/features/dashboard/timelineDomains.test.ts`
3. `web/src/features/dashboard/timelineNormalization.test.ts`
4. `web/src/features/dashboard/marketHealthPreviewModel.test.ts`
5. `web/src/features/dashboard/MarketHealthDesktopTable.test.tsx`
6. `web/src/features/dashboard/MarketHealthMobileCards.test.tsx`
7. `web/src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx`
8. `web/src/features/dashboard/RecentAnomaliesMobileCards.test.tsx`
9. `web/src/features/dashboard/dashboardResourceState.test.ts`
10. `web/src/features/dashboard/marketCatalogConsumers.test.ts`
11. `web/src/pages/DashboardPage.test.tsx`

A strict subset is allowed when an audited replacement is already satisfied at the exact base.

No other product file is writable.

`web/src/pages/DashboardPage.test.tsx` is exclusively leased to MP06A. MP06B must not edit it.

## 5. Global purpose

Modernize brittle implementation-shape guards without weakening architecture, behavior, accessibility, identity, ordering, state, or visual contracts.

The task must:

- replace exact import counts, exact helper locations, exact JSX names, exact call substrings, and internal-function-name slicing where MP00D classified them as brittle;
- narrow overbroad whole-source word and method bans to explicit forbidden modules/APIs, import-specifier allowlists, collection-qualified checks, DOM absence assertions, behavior tests, or TypeScript contracts;
- remove only source assertions proven redundant by stronger existing behavioral tests;
- preserve every `KEEP_EXACT` guard and every documented visual/responsive/accessibility token;
- preserve all existing tests not directly targeted by the accepted MP00D replacement design;
- change no production behavior because production code is read-only.

Do not mechanically delete source guards. Replace them with the exact behavioral, type-level, or narrowed boundary proof specified below.

## 6. Non-negotiable contracts

Retain unchanged or equivalently strict proof for:

- `(uiMode, symbol)` resource identity;
- no cross-mode or cross-symbol contamination;
- no Live-to-Demo fallback;
- ticker ownership: `GlobalMarketTicker` remains on the raw dashboard-summary hook;
- presentation input order and immutability;
- no sorting, limiting, mutation, or deduplication inside presentation components;
- query/network/router/popup/resource/storage/current-time ownership boundaries;
- keyboard accessibility, click callbacks, row/card identity, and focus behavior;
- loading, error, empty, unavailable, and success precedence;
- exact route/popup differences owned by other tests;
- exact responsive tokens, accepted column widths, severity/status colors, score classes, focus-ring tokens, and cited typography contracts;
- direct Recharts ownership prohibition outside the accepted Timeline owner;
- deterministic time ownership and bans on direct current-time access where already accepted.

## 7. Required guard disposition

### 7.1 Keep exact

Do not weaken or remove these MP00D guards:

- `GUARD-001`
- `GUARD-009`
- `GUARD-012`
- `GUARD-013`
- `GUARD-015`
- `GUARD-018`
- `GUARD-019`
- `GUARD-024`
- `GUARD-025`
- `GUARD-028`
- `GUARD-029`
- `GUARD-037`
- `GUARD-045`
- `GUARD-046`
- `GUARD-047`

The direct-current-time checks, ticker boundary, input-order/mutation checks, explicit forbidden external ownership checks, exact responsive/visual tokens, and focus/accessibility contracts remain authoritative.

### 7.2 Remove only redundant source assertions

Apply exactly these removals while retaining the stronger behavior proof in the same test file:

- `GUARD-003`: remove the Timeline source-copy loop for labels/gradient/stroke values already asserted through rendered chart stubs and DOM behavior;
- `GUARD-014`: remove the desktop recent-anomaly `key={row.id}` source regex, retaining rerender node-identity proof;
- `GUARD-020`: remove the mobile recent-anomaly `key={row.id}` source regex, retaining rerender node-identity proof.

Do not remove the behavioral identity tests.

### 7.3 Timeline replacements and narrowing

Implement the accepted designs:

- `GUARD-002` / `R-01`: replace exact import formatting and exact `buildTimelineDomains(...)` call substring with an approved pure-owner/import-category boundary. Allow aliases and helper movement within the accepted Timeline pure modules. Continue to reject duplicate local normalization/domain ownership.
- `GUARD-004` / `R-02`: replace broad `/use[A-Z]/`, generic `replay`, and path-fragment bans with explicit forbidden imports and APIs for React Query hooks, API clients, router ownership, browser storage, timers, randomness, and direct current time. Keep the component presentation-only.
- `GUARD-005` / `R-03`: replace the exact Dashboard import path and `<TimelinePanel` source-name requirement with behavior proving the Dashboard timeline boundary delegates rendering and preserves timeline query/state behavior. Do not require a specific component identifier or import formatting.

### 7.4 Dashboard compositor replacements

In `DashboardPage.test.tsx`:

- `GUARD-006` / `R-04`: replace the exact accepted-component import/render loop with compositor behavior or focused module mocks proving that Dashboard delegates to accepted market-health, anomaly, and timeline presentation surfaces. Do not assert exact local component names, exact import formatting, or exact JSX spelling.
- `GUARD-007` / `R-05`: replace exact preview-builder import strings and call counts with behavior proving previews receive bounded/derived rows while full raw collections remain available to modal/detail flows. The real contract is no duplicate transformation and no child-owned slicing—not one exact textual call.
- `GUARD-008` / `R-06`: replace exact timeline-hook multiline call and every-prop source substring with table-driven behavior for query identity, observed gating, loading/error/empty/success precedence, retry, and stable empty anchor. Do not depend on local variable names or wrapper extraction.
- `GUARD-010` / `R-07`: replace exact JSX regexes for preview/raw collection flow with behavior proving preview children receive the preview rows/callbacks and modal/detail flows receive the complete raw collection. Preserve no accidental limiting.

Retain `GUARD-009` and `GUARD-012` exactly or equivalently strict.

Do not edit `DashboardPage.popup.test.tsx`; it belongs to MP06B.

### 7.5 Recent anomalies desktop narrowing

In `RecentAnomaliesDesktopTable.test.tsx`:

- `GUARD-016` / `R-09`: replace exact import count and exact helper paths with parsed import-specifier/category allowlisting. Permit pure dashboard presentation helpers and shared presentation primitives. Explicitly reject API/query/router/popup/resource/network/time/storage ownership.
- `GUARD-017` / `R-10`: replace broad bans on generic words such as `message`, `activeLabel`, and `effectiveTimestampMs` with property-qualified hidden-field checks and rendered DOM absence assertions proving the preview does not expose hidden context or own loading/modal/shell controls.
- Correct any malformed import regex only to a quote-style-safe equivalent while preserving `GUARD-018`.

Keep exact columns, widths, wrapper classes, severity/focus classes, frozen-input behavior, order, and rows-qualified mutation/limit bans.

### 7.6 Recent anomalies mobile narrowing

In `RecentAnomaliesMobileCards.test.tsx`:

- `GUARD-021` / `R-11`: replace the global `.sort/.slice/.splice/.reverse` source ban with collection-qualified checks targeting the `rows` input or with stronger frozen-order behavior. Do not ban unrelated string formatting methods.
- `GUARD-022` / `R-12`: replace exact three-path import arrays/helper locations with the same approved pure-presentation import category boundary as the desktop counterpart.
- `GUARD-023` / `R-13`: retain explicit API/network/router/popup/time/storage bans. Replace generic UI vocabulary bans with DOM absence assertions for table/dialog/shell heading/section title/“View all”/loading/empty ownership and property-qualified hidden-row-field checks.

Keep exact mobile wrapper, native button, focus, market/detector/severity classes, input order, and identity behavior.

### 7.7 Market-health desktop modernization

In `MarketHealthDesktopTable.test.tsx`:

- `GUARD-026` / `R-14`: replace exact source type/import regexes with a TypeScript contract in the test using `satisfies` or assignments that prove the public props accept readonly `MarketHealthPreviewRow[]`. Do not require exact import formatting. Keep frozen-array runtime proof.
- `GUARD-027` / `R-15`: parse import specifiers and explicitly reject known ownership modules: API/query keys, router, popup/resource hooks, selected-symbol storage, browser/network/time ownership. Do not reject paths merely because they contain generic substrings such as `resource` or `adapter`. Replace generic words such as `icon`, `mobile`, and `loading` with rendered DOM absence assertions or explicit imported-symbol bans.

Keep exact wrapper/column/typography/status tokens and rows-qualified ordering/mutation checks.

### 7.8 Market-health mobile modernization

In `MarketHealthMobileCards.test.tsx`:

- `GUARD-030` / `R-16`: retain explicit forbidden imports/APIs/time/storage ownership. Replace broad shell/modal/text/dependency word bans with DOM absence assertions proving there is no table, dialog, shell heading, or view-all/loading/empty owner in the component.

Keep exact mobile-only wrapper, native button, status/metric/focus classes, order, immutability, no fallback, and no transformation contracts.

### 7.9 Catalog ownership modernization

In `marketCatalogConsumers.test.ts`:

- retain `GUARD-037` exactly: ticker remains on the raw summary hook;
- `GUARD-038` / `R-21`: replace function-order slicing and exact source topology for raw/catalog hooks with behavior using public hooks and controlled/mocked raw query results where feasible. Prove the raw hook returns server summary unchanged and catalog-aware consumers apply only the accepted catalog transformation without Demo fallback. A narrow explicit ticker raw-hook source guard remains required.

Do not edit `GlobalMarketTicker.tsx`, API production files, or catalog production files.

### 7.10 Pure helper boundary narrowing

Apply these accepted designs without changing production:

- `dashboardResourceState.test.ts`, `GUARD-040` / `R-23`: retain frozen-input/reference/determinism/state-precedence behavior. Replace generic vocabulary bans with import-specifier bans for React, React Query, browser/network clients, and direct current-time APIs.
- `timelineDomains.test.ts`, `GUARD-041` / `R-24`: retain exact numeric-domain behavior, frozen inputs, determinism, and no-current-time proof. Permit type-only imports from pure dashboard model modules; reject runtime React/Recharts/query/network/browser/time imports. Remove exact import count and generic visual/API vocabulary bans.
- `marketHealthPreviewModel.test.ts`, `GUARD-042` / `R-25`: prove ordering ownership through the public `orderMarketEntries` contract or equivalent sentinel behavior. Do not require exactly one call or exact import text. Retain a targeted ban against local sorting of the input collection.
- same file, `GUARD-043` / `R-26`: keep frozen input, deterministic output, `(source, symbol)` identity, no fallback, limits, and metric suppression. Replace generic vocabulary/serialized-output word bans with explicit forbidden runtime ownership and direct public row-shape assertions using `toEqual`/`satisfies`.
- `timelineNormalization.test.ts`, `GUARD-044` / `R-27`: retain invalid-value matrix, order/duplicates, frozen input, and deterministic output. Permit type-only pure-model imports; explicitly reject runtime React/Recharts/query/network/browser/time imports. Remove exact import formatting and generic visual-word bans.

## 8. Test quality requirements

- Do not reduce the number of meaningful protected contracts.
- Do not replace one brittle exact string with another equivalent brittle exact string.
- Prefer behavior, accessible DOM queries, public module contracts, typed assignments, parsed import specifiers, and explicitly scoped source boundaries.
- New source parsing helpers must remain local to leased test files unless an existing leased test helper already owns the exact behavior. Do not create a new shared production or test utility file.
- Do not use snapshots as a substitute for explicit contract assertions.
- Do not add network access, timers, or nondeterminism to tests.
- Keep fixtures frozen where immutability is part of the contract.
- Preserve existing visual-contract-critical Tailwind assertions.

## 9. Explicitly forbidden changes

Do not modify any production file, including but not limited to:

- `TimelinePanel.tsx`
- timeline helper production modules
- market-health preview/model production modules
- market-health desktop/mobile production components
- recent-anomaly desktop/mobile production components
- `dashboardResourceState.ts`
- `api.ts`
- `GlobalMarketTicker.tsx`
- `DashboardPage.tsx`

Do not modify:

- `DashboardPage.popup.test.tsx`;
- symbol-detail test files leased to MP06B;
- package files or lockfiles;
- Vite, Vitest, TypeScript, ESLint, Tailwind, PostCSS, CI, scripts, budgets, routes, generated contracts, Rust/backend files, documentation, or Phase 4 files.

Do not add dependencies or suppress warnings.

## 10. Verification available to the web worker

Before commit:

1. fetch every leased file from the exact base;
2. inspect every full changed file after editing;
3. inspect the complete base-to-branch diff;
4. verify only leased test paths changed;
5. verify no production file changed;
6. verify exactly one logical commit is created;
7. verify the branch is one commit ahead and zero behind the exact base;
8. inspect any available commit workflow/status records without claiming executable gates that did not run.

Executable npm/TypeScript/ESLint/build/Rust gates are deferred to the Orchestrator merge-ref CI. The worker must not claim they ran under web-only restrictions.

Record exact deferred commands:

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/TimelinePanel.test.tsx src/features/dashboard/timelineDomains.test.ts src/features/dashboard/timelineNormalization.test.ts src/features/dashboard/marketHealthPreviewModel.test.ts src/features/dashboard/MarketHealthDesktopTable.test.tsx src/features/dashboard/MarketHealthMobileCards.test.tsx src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx src/features/dashboard/RecentAnomaliesMobileCards.test.tsx src/features/dashboard/dashboardResourceState.test.ts src/features/dashboard/marketCatalogConsumers.test.ts src/pages/DashboardPage.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

The Orchestrator will also run repository-level CI through the exact current-base PR merge ref.

## 11. Commit and publication

Create exactly one normal product commit with message:

`test(ui): modernize market health and timeline guards`

Push only:

`p35/mp06a-market-health-test-guards`

Do not open a pull request. Do not merge.

Publish one connector report at:

`signalguard-rs/phase-3.5/reports/P3.5-MP06A/<PRODUCT_COMMIT_SHA>.md`

Required connector commit message:

`docs(phase-3.5): publish MP06A implementation report`

The report must include:

- exact base, branch, product commit, and commit message;
- exact changed paths;
- each MP00D guard ID changed and its final mechanism;
- every `KEEP_EXACT` guard confirmed unchanged;
- behavioral/type/import-boundary evidence added;
- redundant assertions removed;
- explicit confirmation that no production file changed;
- deferred executable commands;
- exact branch/commit/diff verification;
- explicit confirmation that no PR or merge occurred.

Verify both the pushed product branch and connector report before returning.

## 12. Terminal statuses

Success:

`P3_5_MP06A_IMPLEMENTATION_COMPLETE`

Blocked identity:

`P3_5_MP06A_BLOCKED_IDENTITY`

Blocked by scope or conflicting contract:

`P3_5_MP06A_BLOCKED_BY_SCOPE`

Do not claim completion unless both product commit and connector report are published and verified.
