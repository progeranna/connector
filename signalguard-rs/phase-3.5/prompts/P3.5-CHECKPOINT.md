# P3.5-CHECKPOINT — Final Phase 3.5 evidence and snapshot

## 1. Worker class and authority

Execute this task as a **local Codex checkpoint worker** for `progeranna/signalguard-rs`.

Use the `$rust-development` skill.

This is a read-only product validation and archival task. The worker may create temporary detached worktrees, build output, browser artifacts, and deterministic mock-server artifacts, but must remove them before completion. It must not create or push a product branch, product commit, PR, merge, tag, or history rewrite.

The worker may publish the required checkpoint report, archive, and checksum to `progeranna/connector` only after all evidence is complete.

The Orchestrator alone closes Checkpoint 3.5, updates the final status ledger, and authorizes Phase 4.

## 2. Exact identities

- Product repository: `progeranna/signalguard-rs`
- Phase branch: `refactor/dashboard-modules`
- Immutable Phase 3.5 starting SHA: `c06082a97254bfa2f6ebd7e29a1ad753c4acc798`
- Exact final integrated Phase 3.5 head: `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`
- Final MP09-R1 worker head: `b1e574705dea0ae2e3fb682cbd187ce4e3d717ff`
- Exact tested final PR merge ref: `67ec5488b8747ceeb8f09b3fb4c921af24c05eff`
- Authoritative final PR CI run: `30721976267`

The final PR merge-ref tree and final integrated merge tree were already directly compared and returned zero changed files. Reverify this independently.

## 3. Local paths

- Primary product repository: `/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`
- Temporary final-head worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-checkpoint`
- Temporary baseline worktree when required for visual comparison: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-checkpoint-baseline`
- Isolated connector checkout/worktree: `/Users/anion/Desktop/work/connector-p35-checkpoint`

Do not reuse another task's writable worktree.

## 4. Authoritative materials

Read completely before validation:

- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP08B-INTEGRATION/38a1cc440a264d9e04fad3c699386fd45778797f.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP09-R1-INTEGRATION/09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27.md`
- all accepted Wave 3 measurement reviews;
- the product repository's `AGENTS.md`, project rules, CI workflow, package scripts, and bundle policy.

## 5. Initial identity and cleanliness proof

From the primary product repository:

1. fetch the exact phase branch and final SHA without rewriting any branch;
2. verify `refactor/dashboard-modules` resolves exactly to `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`;
3. verify the final merge commit has the expected MP09 worker ancestry;
4. compare `67ec5488b8747ceeb8f09b3fb4c921af24c05eff` with `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27` and require an empty file diff;
5. create the temporary final-head worktree detached at the exact final SHA;
6. require empty `git status --short`, empty `git diff`, and empty `git diff --check` before validation.

Do not proceed from a different SHA or a dirty worktree.

## 6. GitHub CI evidence

Use authenticated `gh` commands to inspect GitHub Actions directly, not only local git state.

Required checks:

1. verify PR `#67`, merge ref `67ec5488b8747ceeb8f09b3fb4c921af24c05eff`, run `30721976267`, and both successful jobs;
2. verify from logs that Actions checked out `refs/remotes/pull/67/merge` at the exact merge-ref SHA;
3. query all workflow runs associated with final merge SHA `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`, including `push` events;
4. inspect the CI workflow triggers.

If a successful push-triggered CI run exists on the final SHA, record its run and job IDs.

If no final-SHA push run exists and the workflow supports `workflow_dispatch`, dispatch the CI workflow on `refactor/dashboard-modules`, wait for completion, and verify that the run's `headSha` is exactly the final SHA.

If the workflow has neither a final-SHA push run nor a usable dispatch trigger, do not fabricate post-merge CI. Record this exact workflow limitation. Continue with the exact-tree equivalence proof and mandatory exact-head local gates, and return an evidence-complete status for Orchestrator disposition.

Any existing or dispatched final-SHA CI failure blocks the checkpoint.

## 7. Exact-head frontend validation

Run sequentially from the final-head worktree's `web` directory:

```bash
npm ci
node --test scripts/check-bundle-size.test.mjs
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Required results:

- Node bundle-policy tests: `25` passed, `0` failed;
- Vitest: exactly `45` files and `653` tests passed;
- TypeScript: pass;
- ESLint: pass with zero warnings;
- build: pass;
- transformed modules: `140`;
- one initial JavaScript asset and zero async JavaScript assets;
- exact production asset: `assets/index-241ec209.js`, unless a reproducible toolchain-only hash change is explained while bytes and content remain equivalent;
- initial raw JS: `395700` bytes;
- largest raw JS: `395700` bytes;
- total raw JS: `395700` bytes;
- direct gzip: `113042` bytes;
- initial budget: `409600`, headroom `13900`;
- largest budget: `409600`, headroom `13900`;
- total budget: `414720`, headroom `19020`;
- Vite manifest present and internally consistent;
- Recharts and its measured transitive production graph absent;
- no duplicate emitted module IDs when measured with temporary instrumentation.

Do not retain build output or instrumentation after evidence capture.

## 8. Exact-head repository validation

Run sequentially from the final-head worktree:

```bash
cargo fmt --check
cargo run --quiet --bin export-api-contract -- --check
cargo run --quiet --bin export-api-contract -- --validate
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --test replay_e2e
docker compose config
docker compose --profile app config
bash -n scripts/demo-replay.sh
bash -n scripts/smoke.sh
```

Record passed, failed, and intentionally ignored counts exactly. Any required failure blocks the checkpoint.

## 9. Final browser and visual acceptance

Use deterministic schema-valid mock data, frozen time, production builds, and installed browser automation.

Validate the final exact-head UI at desktop and mobile widths, including:

- `/` and `/dashboard`;
- `/symbols/BTCUSDT` and `/symbols/ETHUSDT`;
- `/anomalies`;
- direct navigation and refresh;
- Demo/Live isolation;
- BTCUSDT/ETHUSDT transitions;
- popup and route differences;
- timeline loading, unavailable, error/retry, normalized-empty, and observed states;
- anomaly-free and info/warning/critical marker states;
- first/middle/last pointer and keyboard tooltip behavior;
- inclusive `-15s` and `+15s` anomaly matching;
- responsive resize;
- upper ticker behavior;
- request counts, renderer initialization/remount counts, browser console errors, page errors, and unhandled rejections.

Each settled `(uiMode, symbol)` identity must issue exactly one summary and one timeline request. Retry-policy requests must be identified separately from duplicate settled-identity requests.

Produce fresh temporary screenshots for accepted desktop and mobile surfaces. Compare against the immutable Phase 3.5 starting SHA where practical using the same deterministic data and viewport. The only visual changes that may be accepted are the already authorized MP08B differences:

1. straight native-SVG polyline instead of Recharts monotone interpolation;
2. slight native-SVG tick-position/value differences;
3. CSS-sized mobile labels.

No other material visual, copy, layout, route, popup, ticker, or accessibility difference is accepted.

Record reproducible comparison metrics and manual inspection findings. Do not claim pixel parity. Remove all screenshots, browser profiles, mock-server files, and temporary build output after recording evidence.

## 10. Bundle and phase ledger

Record the complete Phase 3.5 quantitative movement:

- starting frontend: `42` files / `607` tests;
- final frontend: `45` files / `653` tests;
- starting raw JS: `761856` bytes;
- final raw JS: `395700` bytes;
- raw reduction: `366156` bytes;
- starting direct gzip: `220163` bytes;
- final direct gzip: `113042` bytes;
- direct-gzip reduction: `107121` bytes;
- starting budgets: `761856` largest and total;
- final budgets: `409600` initial, `409600` largest, `414720` total;
- final headrooms: `13900`, `13900`, and `19020` bytes;
- final asset graph: one initial JS asset, zero async JS assets;
- no budget increase;
- minimum and preferred headroom targets exceeded.

Explain that MP07 route splitting and MP08 lazy-Recharts splitting were rejected because they increased total JS, while MP08B removed the Recharts graph and supplied true total-JS reduction.

## 11. Snapshot

Create a fresh archive from the exact final product commit using `git archive`, not a filesystem zip:

```bash
git -C /Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs archive \
  --format=zip \
  --prefix=signalguard-rs/ \
  --output=<CHECKPOINT_ARCHIVE_PATH> \
  09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27
```

Required connector archive path:

`signalguard-rs/phase-3.5/checkpoints/Checkpoint-3.5/signalguard-rs-refactor-dashboard-modules-09e0cbaa8c.zip`

Required checksum path:

`signalguard-rs/phase-3.5/checkpoints/Checkpoint-3.5/signalguard-rs-refactor-dashboard-modules-09e0cbaa8c.zip.sha256`

Compute SHA-256 with `shasum -a 256` or an equivalent cryptographic implementation. The checksum file must contain the lowercase 64-character digest and archive filename.

Verify:

- archive opens successfully;
- every tracked file at the exact commit is represented under the `signalguard-rs/` prefix;
- archive contains no `.git`, `node_modules`, `dist`, worktree metadata, browser artifacts, screenshots, reports, prompts, or connector control files;
- a fresh extraction has a deterministic file-list comparison with `git ls-tree -r --name-only` at the final SHA.

## 12. Worktree and repository cleanup

Inventory all registered product worktrees.

For every obsolete Phase 3.5 worktree:

- verify it is clean;
- verify no unpushed commit or unique branch head would be lost;
- remove it with `git worktree remove` only after those checks;
- prune stale worktree metadata.

Do not delete a dirty worktree or a unique unpushed branch. Such a worktree blocks checkpoint closure and must be reported exactly.

After all evidence and snapshot creation:

- remove temporary `p35-checkpoint-baseline` and `p35-checkpoint` worktrees from the primary product repository;
- remove temporary build/browser/test artifacts;
- verify the primary product repository is clean;
- verify the phase branch still resolves to the exact final SHA;
- verify no obsolete verified-clean Phase 3.5 worktree remains.

The connector checkout must also be clean after publication.

## 13. Connector publication

Publish exactly these checkpoint artifacts to `progeranna/connector`:

1. archive:
   `signalguard-rs/phase-3.5/checkpoints/Checkpoint-3.5/signalguard-rs-refactor-dashboard-modules-09e0cbaa8c.zip`
2. checksum:
   `signalguard-rs/phase-3.5/checkpoints/Checkpoint-3.5/signalguard-rs-refactor-dashboard-modules-09e0cbaa8c.zip.sha256`
3. evidence report:
   `signalguard-rs/phase-3.5/checkpoints/Checkpoint-3.5/09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27.md`

Use one connector commit with message:

`docs(phase-3.5): publish Checkpoint 3.5`

The report must include all identities, CI evidence, exact-head gates, visual acceptance, bundle ledger, archive verification, SHA-256, worktree cleanup, repository cleanliness, limitations, and terminal status.

Push to connector `main`, then fetch and verify all three artifacts from `main`, verify the connector commit SHA, and independently recompute the downloaded archive checksum.

Do not modify the product repository or the connector `STATUS.md`; the Orchestrator performs the final status update after independent review.

## 14. Terminal status

Return:

`P3_5_CHECKPOINT_EVIDENCE_COMPLETE`

only when:

- all required exact-head local gates pass;
- visual acceptance passes;
- snapshot and checksum are published and verified;
- worktree cleanup and repository cleanliness pass;
- no final-SHA CI failure exists;
- any absent post-merge push CI is reported accurately with workflow-trigger evidence.

Include:

- connector checkpoint commit SHA;
- final product SHA;
- final-SHA CI run ID or exact workflow-trigger limitation;
- final test counts;
- bundle metrics and headrooms;
- archive path and SHA-256;
- visual acceptance result;
- removed and retained worktree lists;
- clean repository confirmations.

Return:

`P3_5_CHECKPOINT_BLOCKED`

if any required gate, visual acceptance, archive verification, cleanup requirement, or available final-SHA CI run fails. Preserve all recoverable evidence and report the exact blocker without changing product history.
