# P3.5-MP06B-R1 — Current-base type-contract recovery

## 1. Worker class

This is a local Codex implementation-recovery task.

Use the `$rust-development` skill.

The recovery is test-only. Production code is strictly read-only.

## 2. Authoritative inputs

Product repository:

`progeranna/signalguard-rs`

Coordination repository:

`progeranna/connector`

Read completely before any product write:

- `signalguard-rs/phase-3.5/prompts/P3.5-MP06B.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP06B/0fbc488476a5b0efb657f88a06f26aa67c620c7c.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP06A-INTEGRATION/bb3df51339ad4a7a3524fe4782fdc24069944160.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP06B-VALIDATION/e923defbeeeebf7cf6bf7457b77be28898286c82.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`

The original MP06B contract remains authoritative except where this recovery contract explicitly changes branch, history construction, and the two type repairs.

## 3. Exact identities

Current integrated product base and phase head:

`bb3df51339ad4a7a3524fe4782fdc24069944160`

Immutable accepted MP06B worker commit:

`0fbc488476a5b0efb657f88a06f26aa67c620c7c`

The immutable worker commit must remain unchanged and must be preserved as the second parent of the recovery merge commit.

Assigned recovery branch:

`p35/mp06b-r1-symbol-detail-test-guards`

Required recovery commit message:

`test(ui): repair MP06B type contracts`

Required product worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp06b-r1`

Required coordination checkout or worktree:

`/Users/anion/Desktop/work/connector-p35-mp06b-r1`

If the phase branch does not resolve exactly to `bb3df51339ad4a7a3524fe4782fdc24069944160`, the immutable worker branch does not resolve exactly to `0fbc488476a5b0efb657f88a06f26aa67c620c7c`, or the assigned recovery branch exists at an unexpected SHA, stop without writing.

## 4. Required history construction

Create the recovery branch from exact current phase head:

`bb3df51339ad4a7a3524fe4782fdc24069944160`

In the dedicated recovery worktree, merge the immutable MP06B worker commit using a normal non-fast-forward, no-commit merge:

```text
git merge --no-ff --no-commit 0fbc488476a5b0efb657f88a06f26aa67c620c7c
```

Do not cherry-pick, rebase, squash, amend, reset, force-push, or rewrite either history.

The merge must apply without production-file conflicts. Before repair edits, verify that the pending merge differs from the current phase head in exactly the five MP06B worker test files and no others.

After the two authorized repairs and all required gates pass, create exactly one new normal merge commit with:

- first parent: `bb3df51339ad4a7a3524fe4782fdc24069944160`;
- second parent: `0fbc488476a5b0efb657f88a06f26aa67c620c7c`;
- message: `test(ui): repair MP06B type contracts`.

## 5. Exact repair write lease

Only these two product files may be edited during recovery:

1. `web/src/pages/DashboardPage.popup.test.tsx`
2. `web/src/pages/SymbolDetailPage.test.tsx`

The immutable worker merge also introduces its already accepted changes in:

- `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`;
- `web/src/features/dashboard/SymbolDetailHeader.test.tsx`;
- `web/src/features/dashboard/selectedSymbolConsumers.test.ts`.

Do not edit those three files during recovery.

`web/src/features/dashboard/SymbolDetailMetrics.test.tsx` must remain byte-identical to the current phase head and original worker base.

No other product path is writable.

## 6. Exact authorized repairs

### 6.1 Dashboard popup adapter identity

In `DashboardPage.popup.test.tsx`, repair the adapter recorder type error without weakening identity coverage.

The second argument inferred through `Parameters<typeof adaptMarketResourceToViewModel>` is optional at the tuple type level. Before storing it in the required recorder identity field, use a type-safe assertion or explicit fail-fast narrowing that proves it is present for the exercised call.

Requirements:

- retain the exact recorded `(uiMode, canonical symbol)` identity assertions;
- do not make the recorder identity optional;
- do not use `as any`, `@ts-ignore`, `@ts-expect-error`, or diagnostic suppression;
- do not change production adapter signatures or behavior.

### 6.2 Dashboard popup async refetch contract

In the same file, type `resourceRefetchByIdentity` and every created refetch mock to satisfy the public `SymbolPopupResourceState["refetch"]` contract:

`() => Promise<unknown>`

Requirements:

- preserve per-identity retry isolation and all existing call assertions;
- mocks must return promises;
- do not widen the production resource state or cast through `any`/`unknown` merely to silence TypeScript;
- prefer a named local test-only function type derived from the public resource contract or an equivalently exact explicit async function type.

### 6.3 Symbol route price fixture contract

In `SymbolDetailPage.test.tsx`, change only the three observed dashboard-state fixture price values to valid decimal strings:

- `best_ask_price` represents `101`;
- `best_bid_price` represents `100`;
- `last_trade_price` represents `100.5`.

The values must comply with the current Zod/API-derived `DashboardStateSummary` contract (`string | null`) while preserving the represented numeric values and all route behavior assertions.

Do not change percentage, count, age, timestamp, availability, source, health, or symbol fixture fields.

## 7. Contracts that must remain preserved

All original MP06B contracts remain mandatory, including:

- exact route/popup visual distinctions from `GUARD-031`;
- unchanged `GUARD-034` and unchanged `SymbolDetailMetrics.test.tsx`;
- anomaly ID-based desktop/mobile node identity;
- frozen input, order, no sorting, no limiting, no deduplication, and no mutation;
- route non-interactive and popup native-button behavior;
- keyboard, focus, click, close, back, Escape, backdrop, and return-context behavior;
- canonical `(uiMode, symbol)` resource identity;
- selected-symbol mode isolation;
- old/late mode and symbol response isolation;
- no Live-to-Demo fallback;
- loading, error, unavailable, and success precedence;
- same shared view-model reference passed to accepted detail sections;
- test-only scope and zero production behavior change.

Do not remove or weaken a test to make validation pass.

## 8. Required validation

Run from the isolated recovery worktree.

### Frontend install and focused validation

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/SymbolDetailAnomalies.test.tsx src/features/dashboard/SymbolDetailHeader.test.tsx src/features/dashboard/SymbolDetailMetrics.test.tsx src/features/dashboard/selectedSymbolConsumers.test.ts src/pages/SymbolDetailPage.test.tsx src/pages/DashboardPage.popup.test.tsx
```

### Full frontend gates

```text
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Required result:

- all frontend test files pass;
- all frontend tests pass;
- TypeScript passes;
- ESLint passes with zero warnings;
- production build passes;
- raw production JS is no greater than `757217` bytes;
- existing largest and total JS budgets remain exactly `761856` bytes.

### Repository gates

From repository root:

```text
cargo fmt --check
cargo run --quiet --bin export-api-contract -- --check
cargo run --quiet --bin export-api-contract -- --validate
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --test replay_e2e
docker compose config
docker compose --profile app config
bash -n scripts/demo-replay.sh
bash -n scripts/smoke.sh
```

Do not claim ignored service-backed tests executed as live integration tests.

## 9. Final verification

Before commit and again after commit, verify:

- the diff from `bb3df51339ad4a7a3524fe4782fdc24069944160` contains exactly the five original MP06B changed test files;
- only the two exact recovery files contain changes beyond the immutable worker blobs;
- no production, MP06A, package, lockfile, configuration, budget, generated-contract, route, backend, documentation, Wave 3, or Phase 4 file changed;
- `SymbolDetailMetrics.test.tsx` remains byte-identical;
- the recovery commit has exactly the two required parents in the required order;
- the recovery branch contains the immutable worker commit and exactly one new recovery merge commit;
- no history rewrite occurred.

Push only:

`p35/mp06b-r1-symbol-detail-test-guards`

Do not open a pull request and do not merge.

## 10. Connector report

Publish:

`signalguard-rs/phase-3.5/reports/P3.5-MP06B-R1/<RECOVERY_MERGE_COMMIT_SHA>.md`

Use connector commit message:

`docs(phase-3.5): publish MP06B recovery report`

The report must include:

- exact current base, immutable worker head, recovery branch, and recovery merge commit;
- both commit parents in order;
- the failed PR `#64`, merge ref `e923defbeeeebf7cf6bf7457b77be28898286c82`, and CI run `30712501382`;
- the six exact TypeScript diagnostics summarized by file and cause;
- the exact two-file repair diff;
- focused and full frontend results;
- TypeScript, lint, build, bundle, Rust, repository, and scope results;
- final raw/gzip bundle measurements and unchanged budgets;
- proof that no PR or merge was performed by the recovery worker.

Verify both product and connector writes at their exact SHAs.

## 11. Terminal statuses

Success:

`P3_5_MP06B_R1_IMPLEMENTATION_COMPLETE`

Identity block:

`P3_5_MP06B_R1_BLOCKED_BY_IDENTITY`

Merge/scope block:

`P3_5_MP06B_R1_BLOCKED_BY_SCOPE`

Validation block:

`P3_5_MP06B_R1_BLOCKED_BY_VALIDATION`
