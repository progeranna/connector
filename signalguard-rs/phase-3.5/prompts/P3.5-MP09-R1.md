# P3.5-MP09-R1 — Node policy-suite discovery recovery

## 1. Worker class and authority

Continue the existing local Codex MP09 implementation work in the same dedicated worktree.

Use the `$rust-development` skill.

This is a narrow recovery addendum to `P3.5-MP09.md`. The original MP09 contract remains authoritative except where this addendum explicitly overrides its writable lease, report path, and terminal status.

The worker must not discard or recreate already validated MP09 changes unless required to fix a demonstrated defect. No product commit currently exists; the final result must still be exactly one product commit from the original MP09 base.

## 2. Exact product identity

- Product repository: `progeranna/signalguard-rs`
- Phase branch: `refactor/dashboard-modules`
- Exact immutable implementation base and current integrated phase head: `38a1cc440a264d9e04fad3c699386fd45778797f`
- Assigned product branch: `p35/mp09-bundle-policy-refinement`
- Required single product commit message: `chore(ui): refine bundle policy reporting`

Required local paths remain:

- product worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp09`
- connector checkout/worktree: `/Users/anion/Desktop/work/connector-p35-mp09`

Before editing, verify:

- `git rev-parse HEAD` equals the exact base;
- no product commit has been created;
- the existing uncommitted MP09 changes are confined to the five original lease paths;
- generated `dist` output is absent.

## 3. Accepted blocker

The mandated Node built-in policy suite:

`web/scripts/check-bundle-size.test.mjs`

is correctly executed by Node and already passed `25` tests. However, the unchanged Vitest discovery also matches `*.test.mjs` and treated it as a 46th frontend suite, failing with `No test suite found` because `node:test` registrations are not Vitest registrations.

Do not solve this by:

- changing `npm run test:run`;
- changing the protected `vitest run --threads` command;
- renaming the mandated Node suite;
- importing Vitest into the Node policy suite;
- duplicating the policy tests in a Vitest wrapper;
- weakening frontend test discovery generally.

## 4. Writable lease override

The final writable lease is exactly the original five MP09 paths plus one recovery path:

1. `web/vite.config.ts`
2. `web/vitest.config.ts`
3. `web/bundle-size-budget.json`
4. `web/scripts/check-bundle-size.mjs`
5. `web/scripts/check-bundle-size.test.mjs`
6. `web/package.json`

Every other product path remains read-only, including:

- all files under `web/src/`;
- `web/package-lock.json`;
- `.github/workflows/ci.yml`;
- backend and generated-contract files.

## 5. Exact recovery change

Modify `web/vitest.config.ts` only to preserve Vitest's default exclusions and exclude the Node policy suite from Vitest discovery.

Required implementation shape:

- import `configDefaults` alongside `defineConfig` from `vitest/config`;
- add an explicit `test.exclude` array containing:
  - every existing `configDefaults.exclude` entry;
  - exactly `scripts/check-bundle-size.test.mjs` as the additional project exclusion.

Equivalent formatting is allowed, but the semantic result must be:

```ts
import { configDefaults, defineConfig } from "vitest/config";

// ...

test: {
  exclude: [...configDefaults.exclude, "scripts/check-bundle-size.test.mjs"],
  // all existing test settings unchanged
}
```

Do not add broad patterns such as `scripts/**`, `**/*.mjs`, or `**/check-bundle-size*`.

Do not remove or alter the existing environment, setup files, or mock-reset settings.

## 6. Required command ownership

The Node policy suite must remain a Node built-in test suite and must be executed explicitly by Node.

`npm run bundle:check` must execute, in deterministic order:

1. the Node policy suite;
2. the real built-output bundle check.

The protected frontend test command must remain unchanged and must no longer discover the Node policy file.

## 7. Required recovery validation

Run sequentially from `web`:

```bash
npm ci
node --test scripts/check-bundle-size.test.mjs
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Required results:

- direct Node policy suite: at least `25` tests passed, zero failed;
- Vitest: exactly `45` files passed and `653` tests passed;
- `scripts/check-bundle-size.test.mjs` absent from Vitest's discovered suite list;
- TypeScript: pass;
- ESLint: pass with zero warnings;
- build: pass;
- one initial JS asset and no async JS assets;
- initial raw JS: `395700` bytes;
- largest raw JS: `395700` bytes;
- total raw JS: `395700` bytes;
- direct gzip: `113042` bytes;
- initial budget `409600`: pass;
- largest budget `409600`: pass;
- total budget `414720`: pass.

Run the repository gates from the original MP09 contract:

```bash
cargo fmt --check
cargo run --quiet --bin export-api-contract -- --check
cargo run --quiet --bin export-api-contract -- --validate
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --test replay_e2e
docker compose config
docker compose --profile app config
bash -n scripts/demo-replay.sh
bash -n scripts/smoke.sh
```

No candidate may be committed after a failed required gate.

## 8. Diff and history requirements

Before committing, verify:

```bash
git status --short
git diff --check
git diff --stat
git diff
git rev-parse HEAD
```

Required final pre-commit state:

- HEAD remains `38a1cc440a264d9e04fad3c699386fd45778797f`;
- changed paths are exactly the six-path recovery lease, or a strict subset only if one original path proves materially unnecessary;
- no `web/src/` or lockfile change;
- no generated `dist` or temporary artifact.

Create exactly one normal product commit with:

`chore(ui): refine bundle policy reporting`

After commit, verify exactly one commit ahead and zero behind the base.

Push only:

`p35/mp09-bundle-policy-refinement`

Do not open a pull request. Do not merge. Do not rebase, amend, reset, squash, tag, force-push, or rewrite history.

## 9. Connector report

Publish exactly one UTF-8 Markdown implementation report at:

`signalguard-rs/phase-3.5/reports/P3.5-MP09-R1/<PRODUCT_COMMIT_SHA>.md`

Use connector commit message:

`docs(phase-3.5): publish MP09 recovery report`

The report must include:

- original blocker and why it was valid;
- exact `vitest.config.ts` exclusion and proof that default exclusions were retained;
- Node policy-suite results;
- exact Vitest file/test counts;
- exact bundle metrics and all three policy headrooms;
- frontend and repository gates;
- final changed paths;
- exact product commit and parent;
- clean product worktree proof;
- confirmation that no PR or merge occurred.

Verify the report from connector `main` and verify the connector commit SHA.

## 10. Terminal status

Return:

`P3_5_MP09_R1_IMPLEMENTATION_COMPLETE`

only after every required gate passes and both product and connector writes are published and verified.

If validation still fails, do not create a product commit. Return:

`P3_5_MP09_R1_BLOCKED_BY_VALIDATION`

with exact diagnostics and retained worktree state.
