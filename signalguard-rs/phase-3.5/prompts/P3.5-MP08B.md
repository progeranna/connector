# P3.5-MP08B — Replace Recharts Timeline with native SVG renderer

## 1. Worker class and authority

Execute this task as a **local Codex implementation worker** for `progeranna/signalguard-rs`.

Use the `$rust-development` skill.

The worker may modify only the exact writable lease in this contract, create one product commit on the assigned branch, push only that branch, and publish the required connector implementation report.

The worker must not open a pull request, merge, modify the phase branch directly, rebase, amend, reset, squash, force-push, or rewrite history.

The Orchestrator alone performs independent source review, creates the validation PR, accepts merge-ref CI, and merges.

## 2. Exact product identity

- Product repository: `progeranna/signalguard-rs`
- Phase branch: `refactor/dashboard-modules`
- Exact immutable implementation base and current integrated phase head: `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`
- Assigned product branch: `p35/mp08b-native-svg-timeline`
- Required single product commit message: `refactor(ui): replace Recharts timeline with native SVG`
- Baseline frontend: `43` test files, `640` tests
- Baseline total raw JS: `757217` bytes
- Baseline direct gzip: `219504` bytes
- Existing largest and total JS budgets: `761856` bytes
- Baseline raw headroom: `4639` bytes

Before editing, verify that the phase branch and dedicated worktree resolve exactly to the base SHA above.

## 3. Required local paths

- Product repository: `/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`
- Dedicated product worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp08b`
- Isolated connector checkout/worktree: `/Users/anion/Desktop/work/connector-p35-mp08b`

Do not reuse another task's writable worktree.

## 4. Authoritative evidence

Read completely before implementation:

- `signalguard-rs/phase-3.5/prompts/P3.5-MP08B-MEASURE.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP08B-MEASURE/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP08B-MEASUREMENT-REVIEW/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP08-MEASURE/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP08-MEASUREMENT-REVIEW/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP07-MEASURE/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP07-MEASUREMENT-REVIEW/9c8aed635e57272fc834c1ddcdbc3dbf33cf4328.md`
- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`

The accepted implementation design is `S2 — native SVG Timeline renderer plus pure geometry helper`.

## 5. Exact writable lease

Modify exactly the following paths, or a strict subset when a path does not require a material change:

1. `web/src/features/dashboard/TimelinePanel.tsx`
2. `web/src/features/dashboard/TimelineChartRenderer.tsx`
3. `web/src/features/dashboard/timelineChartGeometry.ts`
4. `web/src/features/dashboard/TimelinePanel.test.tsx`
5. `web/src/features/dashboard/TimelineChartRenderer.test.tsx`
6. `web/src/features/dashboard/timelineChartGeometry.test.ts`
7. `web/src/pages/DashboardPage.test.tsx`
8. `web/src/features/dashboard/marketDataIsolation.test.tsx`
9. `web/package.json`
10. `web/package-lock.json`

New files are allowed only at the exact new renderer/helper/test paths listed above.

Every other product path is read-only.

## 6. Strictly forbidden product paths and changes

Do not modify:

- `web/src/pages/DashboardPage.tsx`;
- `web/src/app/router.tsx` or any route file;
- dashboard API/query hooks;
- selected-symbol, popup, mode, or resource-identity production owners;
- timeline normalization or domain owner files;
- generated contracts;
- backend code;
- Vite, TypeScript, ESLint, CI, Docker, or script configuration;
- `web/bundle-size-budget.json`;
- bundle-check implementation;
- product copy outside the chart's existing labels and tooltip semantics;
- route-level or lazy chart splitting;
- Wave 4 or Phase 4 work.

Do not introduce another chart library, D3, Canvas, WebGL, a runtime dependency, or an analysis dependency.

Do not raise or weaken any bundle limit, warning, test, lint rule, type rule, or CI gate.

## 7. Required production implementation

### 7.1 Timeline ownership

Keep `TimelinePanel` eager. It must remain the owner of:

- shell wrappers and stable chart-card dimensions;
- selected market, source, status, and snapshot presentation;
- summary-loading precedence;
- configured/unavailable handling;
- timeline loading;
- error and retry;
- normalized-empty handling;
- timeline normalization;
- exact `buildTimelineDomains` output;
- visible anomaly filtering;
- selected-market identity;
- all query ownership.

Do not introduce a second timeline query or fetch owner.

### 7.2 Native renderer

Create `TimelineChartRenderer.tsx` using React, DOM, and native SVG primitives only.

It owns only:

- SVG geometry and rendering;
- green line and gradient fill;
- horizontal dashed grid;
- Time and Price axes, labels, and tick formatting;
- severity-colored vertical anomaly markers;
- pointer/focus tooltip interaction and positioning;
- chart-specific formatting;
- responsive chart-area behavior;
- chart accessibility.

Use the measured stable chart area and responsive correction. Preserve mobile readability with explicit responsive behavior and `preserveAspectRatio="none"` or an evidence-equivalent implementation. Tick labels may use the accepted HTML overlay design.

### 7.3 Pure geometry helper

Create `timelineChartGeometry.ts` as a pure helper. It may own only:

- stable view-box constants;
- time/price domain projection;
- evenly spaced ticks;
- line-path construction;
- closed fill-path construction;
- small geometry types/constants required by those operations.

It must not import React, access the browser, own state, fetch data, read time, own queries, mutate inputs, or depend on dashboard stores.

### 7.4 Dependency removal

Remove direct dependency `recharts` from `web/package.json` and clean its unused graph from `web/package-lock.json` using normal npm tooling.

Do not hand-edit unrelated lockfile entries.

After the dependency removal, verify that source, tests, manifest, lockfile, and production chunks contain no surviving requirement for:

- `recharts`;
- `victory-vendor`;
- the Recharts-only D3 graph;
- Recharts-only Redux graph;
- other measured Recharts-only transitive modules.

Do not remove packages that remain required by another direct or transitive owner.

## 8. Permanent behavior and presentation contracts

Preserve all of the following:

- observed/non-observed gating;
- summary, unavailable, error, retry, loading, normalized-empty, and success precedence;
- normalized point ordering and accepted duplicate handling;
- exact time and price domains from `buildTimelineDomains`;
- the same green stroke/fill visual language;
- horizontal grid treatment;
- Time and Price labels;
- existing price/time formatting;
- inclusive anomaly matching at both `-15000 ms` and `+15000 ms` boundaries;
- severity-colored anomaly marker lines;
- tooltip timestamp, price, anomaly list, status/source, zero-value, missing-value, and fallback semantics;
- stable chart height and surrounding layout;
- desktop and mobile responsiveness;
- Demo/Live isolation;
- BTCUSDT/ETHUSDT identity isolation;
- no duplicate summary or timeline requests;
- no remount or reinitialization loop;
- no animation regression;
- no new product copy;
- no route or popup behavior change.

The accepted visual differences are limited to:

1. a straight dependency-free polyline instead of Recharts monotone interpolation;
2. slight native-SVG tick-position differences;
3. CSS-sized labels used to preserve mobile readability.

No other visual difference is authorized.

## 9. Accessibility requirements

The chart must provide at least the measured S2 accessibility contract:

- `role="img"` or a semantically stronger equivalent;
- a meaningful accessible chart label;
- keyboard focus support;
- left/right point navigation;
- Escape tooltip dismissal;
- focus-driven tooltip content;
- visible focus treatment compatible with the existing UI;
- no inaccessible pointer-only information.

Tests must validate the keyboard and focus behavior behaviorally, not through broad source snapshots.

## 10. Required tests

Add or update focused tests for:

- geometry projection at domain boundaries;
- tick endpoints and stable ordering;
- line-path and fill-path construction;
- empty geometry;
- input immutability;
- state precedence in `TimelinePanel`;
- retry/error/loading/unavailable/normalized-empty states;
- observed chart rendering;
- exact domain propagation;
- visible marker filtering;
- inclusive `±15000 ms` tooltip matching;
- zero and missing values;
- source/status tooltip semantics;
- first, middle, and last point navigation;
- keyboard focus, arrow navigation, and Escape dismissal;
- responsive wrappers and stable chart height;
- Demo/Live and BTCUSDT/ETHUSDT transitions;
- sole query ownership;
- one summary and one timeline request per settled identity;
- no duplicate requests;
- no renderer initialization/remount loop.

Avoid brittle full-source, exact-complete-JSX, generated-hash, or broad generic-word assertions.

Expected final frontend floor is at least:

- `44` test files;
- `643` tests.

A higher legitimate count is allowed. A lower count requires explicit proof that no accepted assertion was removed and is not expected.

## 11. Browser/runtime and visual validation

Run exact-base and candidate production previews with deterministic schema-valid mock API responses and frozen time.

Use installed Chromium/Chrome/Playwright/Puppeteer or an equivalent browser harness.

Verify at desktop and mobile widths:

- `/`;
- `/dashboard?mode=demo`;
- `/dashboard?mode=live`;
- BTCUSDT and ETHUSDT selection/identity transitions;
- summary loading;
- unavailable/configured market;
- timeline loading;
- timeline error and retry;
- normalized-empty state;
- observed chart;
- anomaly-free chart;
- info/warning/critical markers;
- tooltip at first, middle, and last points;
- inclusive `±15`-second tooltip matching;
- keyboard tooltip traversal and Escape dismissal;
- responsive resize;
- browser console events;
- unhandled promise rejections;
- summary/timeline request counts;
- renderer initialization/remount counts.

Required runtime results:

- exactly one summary request and one timeline request per settled `(uiMode, symbol)` identity;
- no repeated renderer initialization caused by stable parent rerenders;
- no console error attributable to the candidate;
- no unhandled promise rejection;
- no layout collapse or chart-area shift.

Capture deterministic baseline and candidate chart-region screenshots at both viewports. Report the same reproducible pixel-difference metrics used in measurement and inspect manually.

Do not require pixel parity. Confirm that every difference falls within the three explicitly accepted visual differences. Any additional material difference blocks the commit.

No screenshot or browser artifact may be committed to the product repository or connector report.

## 12. Bundle and dependency acceptance

Run temporary Rollup/Vite analysis without retaining configuration changes.

The committed candidate must satisfy all of the following:

- total raw production JS `<= 400000` bytes;
- total direct gzip `<= 115000` bytes;
- one initial JS asset unless an existing build-system behavior independently produces another asset;
- no hidden total regression through chunk relocation;
- Recharts absent from production chunks;
- measured Recharts-only transitive graph absent from production chunks;
- no duplicate emitted module IDs;
- unchanged largest and total budgets of `761856` bytes;
- bundle check passes;
- no new runtime dependency.

The measured reference is `395624` raw / `113042` direct gzip. A small deterministic variance is allowed only within the strict ceilings above and must be explained.

If the implementation exceeds either ceiling, do not commit.

## 13. Required frontend gates

Run sequentially after `npm ci`:

```bash
cd web
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Do not run bundle analysis concurrently with a build that uses the same `dist` directory.

## 14. Required repository gates

Run:

```bash
cargo fmt --check
cargo run --quiet --bin export-api-contract -- --check
cargo run --quiet --bin export-api-contract -- --validate
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --test replay_e2e
docker compose config
docker compose --profile app config
bash -n scripts/demo-replay.sh
bash -n scripts/smoke.sh
```

Record ignored tests accurately. No failure is acceptable.

## 15. Static and lease gates

Before commit, run and record:

```bash
git status --short
git diff --check
git diff --stat
git diff
```

Verify:

- changed paths are exactly the writable lease or a strict subset;
- no forbidden file changed;
- no retained build, analysis, screenshot, browser, or temporary artifact exists;
- no `recharts` importer remains;
- no package/configuration/budget change exists beyond the leased manifest and lockfile removal;
- no source or test references the removed renderer API incorrectly.

## 16. Product history and publication

Create exactly one normal product commit on:

`p35/mp08b-native-svg-timeline`

Required commit message:

`refactor(ui): replace Recharts timeline with native SVG`

The commit must be exactly one commit ahead and zero behind base `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`.

Push only the assigned branch.

Do not open a PR. Do not merge.

## 17. Required implementation report

Publish exactly one UTF-8 Markdown report at:

`signalguard-rs/phase-3.5/reports/P3.5-MP08B/<PRODUCT_COMMIT_SHA>.md`

Use connector commit message:

`docs(phase-3.5): publish MP08B implementation report`

The report must include:

1. exact base, branch, product commit, message, and history relation;
2. exact changed paths;
3. production ownership summary;
4. geometry-helper contract;
5. dependency and lockfile removal evidence;
6. focused and full frontend test counts;
7. TypeScript, lint, build, and bundle results;
8. exact raw, direct-gzip, headroom, asset, and module measurements;
9. proof that Recharts and its measured transitive graph are absent;
10. repository gate results;
11. browser/runtime matrix;
12. console, request-count, remount/init-count results;
13. screenshot comparison metrics and manual visual conclusion;
14. accepted visual differences and confirmation that no additional material difference exists;
15. diff/lease/cleanup proof;
16. confirmation that no PR or merge occurred;
17. remaining risks for Orchestrator merge-ref validation.

The connector commit must modify only the required report path.

Verify the product branch, product commit, report path, report blob, and connector commit after publication.

## 18. Terminal status

Return:

`P3_5_MP08B_IMPLEMENTATION_COMPLETE`

only after:

- every required gate passes;
- the product commit and branch are published and verified;
- the connector report and commit are published and verified;
- no PR or merge occurred.

If any required gate fails, do not commit. Return:

`P3_5_MP08B_BLOCKED_BY_VALIDATION`

with the exact diagnostics and retained local worktree state.
