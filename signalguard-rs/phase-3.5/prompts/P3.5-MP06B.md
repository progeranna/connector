# P3.5-MP06B — Anomaly and symbol-detail test-guard modernization

## 1. Worker class

This is a GitHub web implementation-worker task.

Use the connected GitHub tools only. Do not use a local checkout, local shell, Codex CLI, or an unconnected repository copy.

The task is test-only. Production code is read-only.

## 2. Repositories

Product repository:

`progeranna/signalguard-rs`

Coordination repository:

`progeranna/connector`

Authoritative documents to read before any write:

- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00D/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`
- `signalguard-rs/phase-3.5/reports/P3.5-MP05-INTEGRATION/21fec6d357b365a3710c8e7118a467564247dee6.md`

Read the complete MP00D replacement specifications, especially `R-08`, `R-17` through `R-20`, and `R-22`.

## 3. Immutable identity

Exact product base and current phase head:

`21fec6d357b365a3710c8e7118a467564247dee6`

Base branch:

`refactor/dashboard-modules`

Assigned product branch:

`p35/mp06b-symbol-detail-test-guards`

Required single product commit message:

`test(ui): modernize anomaly and symbol detail guards`

Create the assigned branch from the exact base. If the branch already exists at another SHA, or the phase branch no longer resolves to the exact base before branch creation, stop with the blocked identity status. Do not write to any other product branch.

Do not rebase, amend, reset, force-push, squash, or rewrite history.

## 4. Exact writable lease

Only these product test files may change:

1. `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`
2. `web/src/features/dashboard/SymbolDetailHeader.test.tsx`
3. `web/src/features/dashboard/SymbolDetailMetrics.test.tsx`
4. `web/src/features/dashboard/selectedSymbolConsumers.test.ts`
5. `web/src/pages/SymbolDetailPage.test.tsx`
6. `web/src/pages/DashboardPage.popup.test.tsx`

A strict subset is allowed when an audited replacement is already satisfied at the exact base.

No other product file is writable.

`web/src/pages/DashboardPage.popup.test.tsx` is exclusively leased to MP06B. MP06A must not edit it.

## 5. Global purpose

Replace brittle internal-name, exact-JSX, function-slicing, generic-word, and broad-method source guards with behavior, public-contract, identity, accessibility, or explicitly scoped architecture boundaries.

The task must preserve:

- route/popup visual and behavioral distinctions;
- raw anomaly ordering and identity;
- desktop/mobile identity;
- route no-callback behavior and popup native-button behavior;
- click, keyboard, focus, and accessibility contracts;
- `(uiMode, canonical symbol)` resource identity;
- no cross-mode or cross-symbol contamination;
- no Live-to-Demo fallback;
- selected-symbol persistence isolation by mode;
- loading, error, unavailable, and success state precedence;
- supplied-view-model ownership for pure presentation components;
- exact visual-contract classes repaired in PR #56.

Production code is read-only. Do not alter behavior to make tests easier.

## 6. Non-negotiable contracts

Retain exact or equivalently strict proof for:

- route anomaly timestamp class:
  `mt-1 text-xs uppercase tracking-[0.14em] text-slate-500`;
- popup metric-label typography and its intentional distinction from the route;
- route/popup responsive wrappers and presentation differences;
- anomaly severity/value/status classes where currently accepted;
- native popup buttons and route non-interactive rows where applicable;
- row/card identity keyed by `anomaly.id`, demonstrated behaviorally rather than by internal component names;
- frozen input, no mutation, no sorting, no limiting, no deduplication;
- presentation-only components importing no API/query/router/popup/resource/storage/network/current-time ownership;
- canonical symbol normalization and resource identity;
- selected-symbol mode-scoped storage behavior;
- no Demo fallback for unavailable Live identities;
- same view model passed to all route detail sections;
- no popup callback passed by the route page;
- popup modal lifecycle, return context, close/back behavior, and resource isolation.

## 7. Required guard disposition

### 7.1 Keep exact

Do not weaken:

- `GUARD-031`: exact route/popup visual distinctions in `SymbolDetailAnomalies.test.tsx`;
- `GUARD-034`: `SymbolDetailMetrics` supplied-view-model presentation boundary;
- all existing focused behavior for route/popup differences, keyboard access, callbacks, resource identity, no Demo fallback, and state precedence.

Keep exact visual-contract-critical Tailwind tokens. Do not replace them with generic visibility assertions.

### 7.2 Dashboard popup modernization

In `DashboardPage.popup.test.tsx`, implement `GUARD-011` / `R-08`:

- replace exact internal function names, exact popup-success JSX blocks, and exact modal-lifecycle source strings with behavior through public Dashboard rendering and controlled resource mocks;
- table-drive popup loading, error, unavailable, and success states;
- prove canonical `(uiMode, symbol)` request identity;
- prove old/late mode or symbol responses cannot contaminate the active popup;
- prove the raw selected symbol and shared view model are passed to popup sections;
- prove close, back, return-context, and native interaction behavior remains correct;
- preserve no Live-to-Demo fallback;
- do not assert exact names of internal popup/render helper functions or source ordering.

Source guards may remain only for explicit forbidden ownership that behavior cannot reliably exercise, such as direct route ownership or duplicate local production implementations. They must target concrete imports/APIs, not generic words or helper names.

### 7.3 Symbol anomaly identity modernization

In `SymbolDetailAnomalies.test.tsx`:

- `GUARD-032` / `R-17`: remove assertions requiring internal identifiers such as `AnomalyDesktopTable`, `AnomalyDesktopRow`, `AnomalyMobileItem`, or exact key expressions;
- render at least two frozen anomalies with unique IDs/content;
- capture desktop row and mobile item nodes by accessible role/name or unique content;
- rerender in reversed order and prove each node follows its anomaly ID rather than array position;
- exercise route and popup variants;
- keep route rows non-interactive and popup items as native buttons with preserved callbacks;
- keep exact route/popup class distinctions from `GUARD-031`.

For duplicate-ID handling, do not normalize, sort, limit, or mutate the input. A React duplicate-key warning does not authorize production changes.

### 7.4 Symbol anomaly ownership narrowing

Still in `SymbolDetailAnomalies.test.tsx`:

- `GUARD-033` / `R-18`: replace the broad string loop banning generic `filter`, `sort`, `.slice`, `mode`, hooks, and routing words;
- use anomalies/rows-qualified collection-operation checks and explicit forbidden import/API checks;
- explicitly reject API/query/router/popup-resource/network/storage/time ownership;
- do not ban the generic word `mode`;
- do not ban all `.slice(` occurrences;
- retain behavior proving order, no mutation, no limiting, no deduplication, route no-callback/no-button behavior, popup native-button behavior, and exact classes.

### 7.5 Symbol header boundary narrowing

In `SymbolDetailHeader.test.tsx`:

- `GUARD-035` / `R-19`: replace generic whole-source bans for words such as `Resource` and `Controller` with parsed import-specifier and explicit API/hook checks;
- reject router, query, selected-symbol storage, popup/resource-hook, browser/network, and current-time ownership;
- reject explicit calls such as `useQuery`, `useNavigate`, and `useSymbol*Resource` rather than generic type-name substrings;
- preserve DOM proof that the header contains no links, buttons, controls, or focusable descendants;
- preserve all visible heading/status/market presentation assertions.

### 7.6 Symbol metrics exact boundary

In `SymbolDetailMetrics.test.tsx`:

- preserve `GUARD-034` and its focused architectural restrictions;
- do not broaden generic source bans beyond concrete forbidden imports/APIs;
- retain frozen input, supplied-view-model rendering, route/popup formatting distinctions, and no query/router/storage/network ownership;
- only edit this file when needed to align a malformed or overbroad guard with the exact `GUARD-034` boundary. Do not create unrelated churn.

### 7.7 Symbol route composition modernization

In `SymbolDetailPage.test.tsx`, implement `GUARD-036` / `R-20`:

- remove source slicing of `DashboardPage.tsx` between internal function names;
- remove exact shared-section JSX strings and exact local helper-name requirements;
- mock or control `useSymbolMarketResource`, catalog data, and shared presentation sections;
- table-drive invalid route symbol, loading, error, unavailable, and success states;
- prove the resource key remains `(uiMode, canonical symbol)`;
- prove heading receives the normalized selected symbol;
- prove route detail sections receive the same view model object/reference where contractually expected;
- prove the route does not pass popup click callbacks;
- prove unavailable Live identity remains explicit and does not synthesize Demo data;
- retain a narrow source boundary against direct duplicated route table/metric implementations only when expressed as forbidden owners/imports, not internal helper names.

Popup parity assertions belong in `DashboardPage.popup.test.tsx`; do not make `SymbolDetailPage.test.tsx` inspect Dashboard internal function ordering.

### 7.8 Selected-symbol mode isolation modernization

In `selectedSymbolConsumers.test.ts` and, where needed, `DashboardPage.popup.test.tsx`, implement `GUARD-039` / `R-22`:

- seed distinct selected symbols for Demo and Live storage keys;
- exercise public selected-symbol consumers under both modes;
- switch mode and symbol through route/table/popup interactions represented by the leased tests;
- prove only the active mode key is updated;
- prove the inactive mode selection remains intact;
- resolve old deferred responses and prove no cross-mode or cross-symbol UI contamination;
- retain a narrow source ban against the legacy one-argument selected-symbol storage API only if TypeScript does not make it unrepresentable;
- do not assert exact internal storage helper placement, exact source formatting, or exact component names.

Do not modify `marketNavigationIsolation.test.tsx`; it is outside this lease. Preserve its already accepted contracts by avoiding incompatible assumptions.

## 8. Test quality requirements

- Do not reduce meaningful contract coverage.
- Do not replace one exact implementation string with another equivalent brittle exact string.
- Prefer public rendering, controlled hooks/resources, accessible DOM queries, rerender identity, frozen inputs, typed fixtures, and explicit import-specifier boundaries.
- New helper functions must remain local to the six leased files. Do not create a new shared utility file.
- Do not use snapshots as a substitute for explicit route/popup contract assertions.
- Do not introduce real network access, uncontrolled timers, nondeterministic clocks, or random values.
- Keep exact visual-contract assertions documented by PR #56.
- Keep source guards where needed for forbidden architectural ownership, but target specific modules/APIs and avoid generic vocabulary.

## 9. Explicitly forbidden changes

Do not modify any production file, including:

- `SymbolDetailAnomalies.tsx`
- `SymbolDetailHeader.tsx`
- `SymbolDetailMetrics.tsx`
- `selectedSymbol.ts`
- `symbolPopup.ts`
- `symbolPopupResource.ts`
- `symbolMarketResource.ts`
- `marketAdapters.ts`
- `SymbolDetailPage.tsx`
- `DashboardPage.tsx`

Do not modify:

- `DashboardPage.test.tsx` or any MP06A-leased test;
- `marketNavigationIsolation.test.tsx`;
- package files or lockfiles;
- Vite, Vitest, TypeScript, ESLint, Tailwind, PostCSS, CI, scripts, budgets, routes, generated contracts, Rust/backend files, documentation, or Phase 4 files.

Do not add dependencies, suppress warnings, weaken exact visual contracts, or change product behavior.

## 10. Verification available to the web worker

Before commit:

1. fetch every leased file from the exact base;
2. inspect every full changed file after editing;
3. inspect the complete base-to-branch diff;
4. verify only leased test paths changed;
5. verify no production file changed;
6. verify exactly one logical commit;
7. verify the branch is one commit ahead and zero behind the exact base;
8. inspect available workflow/status records without claiming executable gates that did not run.

Executable gates are deferred to Orchestrator merge-ref CI. The web worker must not claim npm, TypeScript, ESLint, build, bundle, or Rust execution.

Record exact deferred commands:

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/SymbolDetailAnomalies.test.tsx src/features/dashboard/SymbolDetailHeader.test.tsx src/features/dashboard/SymbolDetailMetrics.test.tsx src/features/dashboard/selectedSymbolConsumers.test.ts src/pages/SymbolDetailPage.test.tsx src/pages/DashboardPage.popup.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

The Orchestrator will run repository-level CI through the exact current-base PR merge ref.

## 11. Commit and publication

Create exactly one normal product commit with message:

`test(ui): modernize anomaly and symbol detail guards`

Push only:

`p35/mp06b-symbol-detail-test-guards`

Do not open a pull request. Do not merge.

Publish one connector report at:

`signalguard-rs/phase-3.5/reports/P3.5-MP06B/<PRODUCT_COMMIT_SHA>.md`

Required connector commit message:

`docs(phase-3.5): publish MP06B implementation report`

The report must include:

- exact base, branch, product commit, and commit message;
- exact changed paths;
- each MP00D guard ID changed and its final mechanism;
- every exact route/popup visual contract confirmed unchanged;
- behavioral, identity, state, storage-isolation, and import-boundary evidence added;
- brittle internal-name/source-slicing assertions removed;
- explicit confirmation that no production or MP06A file changed;
- deferred executable commands;
- exact branch/commit/diff verification;
- explicit confirmation that no PR or merge occurred.

Verify both the pushed product branch and connector report before returning.

## 12. Terminal statuses

Success:

`P3_5_MP06B_IMPLEMENTATION_COMPLETE`

Blocked identity:

`P3_5_MP06B_BLOCKED_IDENTITY`

Blocked by scope or conflicting contract:

`P3_5_MP06B_BLOCKED_BY_SCOPE`

Do not claim completion unless both product commit and connector report are published and verified.
