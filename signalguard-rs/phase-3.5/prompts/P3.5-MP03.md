# P3.5-MP03 — Dashboard anomaly presentation consolidation

You are the GitHub web implementation worker for:

`P3.5-MP03 — Dashboard anomaly presentation consolidation`

This is a GitHub web-worker task, not a local Codex task.

Use the connected GitHub tools. Do not use a local checkout or shell. Do not claim that tests, typecheck, lint, build, or bundle checks ran unless a GitHub workflow actually ran and you inspected it.

## 1. Authoritative repositories and documents

Product repository:

`progeranna/signalguard-rs`

Coordination repository:

`progeranna/connector`

Read before any product write:

- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00B/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00D/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`

The consolidated MP00 inventory is authoritative when another document is less specific.

## 2. Immutable identity

Exact product base SHA:

`c06082a97254bfa2f6ebd7e29a1ad753c4acc798`

Phase branch at authorization:

`refactor/dashboard-modules`

Assigned implementation branch:

`p35/mp03-dashboard-anomaly-presentation`

Required single product commit message:

`refactor(ui): consolidate dashboard anomaly presentation`

Before editing:

1. Verify `refactor/dashboard-modules` still resolves to the exact base SHA.
2. Verify the assigned branch does not contain unrelated work.
3. Create the assigned branch from the exact base SHA, or use it only if it still points exactly to the base with no worker commit.
4. Fetch every writable product file from the exact assigned branch before updating it.

If the phase branch no longer equals the immutable base, or the assigned branch contains unknown commits, stop with `P3_5_MP03_BLOCKED`. Do not rebase, reset, amend, force-push, or rewrite history.

## 3. Objective

Consolidate Dashboard anomaly-modal presentation semantics into the existing pure owner:

`web/src/features/dashboard/recentAnomaliesPresentation.ts`

Remove page-local duplicate anomaly presentation helpers from `DashboardPage.tsx` where the semantics are exactly or deliberately canonically equivalent.

Dashboard must retain orchestration ownership. This mini-phase does not consolidate route/popup behavior, market-health presentation, resource logic, query logic, navigation, or modal lifecycle.

## 4. Exclusive writable path lease

You may modify only:

- `web/src/pages/DashboardPage.tsx`
- `web/src/pages/DashboardPage.test.tsx`
- `web/src/features/dashboard/recentAnomaliesPresentation.ts`
- `web/src/features/dashboard/recentAnomaliesPresentation.test.ts`

No other product path is writable.

`DashboardPage.tsx` and `DashboardPage.test.tsx` are exclusively leased to this worker while MP03 is active.

## 5. Forbidden paths and changes

Do not modify:

- any MarketHealth desktop/mobile production or test file
- `TimelinePanel.tsx` or its tests
- `marketHealthPresentation.ts` or its tests
- `marketAdapters.ts`, `marketViewModel.ts`, or their tests
- any SymbolDetail production or test module
- `DashboardPage.popup.test.tsx`
- shared components or shared libraries
- API, query-key, resource, popup-resource, ticker, mode, router, or route modules
- package, lockfile, TypeScript, ESLint, Vite, Vitest, CI, build, or bundle-budget files
- backend, deployment, Docker, migrations, or Rust code

Do not modify market-health helpers inside Dashboard in this mini-phase. They belong to later MP04.

Do not change Recharts, routes, dynamic imports, lazy boundaries, product copy, styling, columns, modal structure, or navigation behavior.

## 6. Canonical anomaly presentation ownership

Inspect the exact base implementations in:

- `DashboardPage.tsx`
- `recentAnomaliesPresentation.ts`
- their focused tests

The pure presentation module should own the canonical reusable semantics for Dashboard anomaly modal and preview-compatible formatting, including where proven equivalent:

- anomaly/detector type label formatting
- anomaly timestamp formatting
- anomaly value formatting
- duration formatting
- integer formatting
- numeric formatting
- severity badge class selection
- anomaly value class selection

Use the current exported canonical helpers where they already exist. Add or extend a pure helper only when needed to remove a proven duplicate and when the contract remains presentation-only.

Do not import Dashboard page types, modal state, resource state, router state, query types, popup state, or page callbacks into `recentAnomaliesPresentation.ts`.

Do not move route/popup variant assembly into this owner.

## 7. Intentional differences that must remain explicit

Preserve all accepted surface differences.

### 7.1 Desktop versus mobile modal copy

Preserve:

- desktop modal header/column wording `Detected at`
- mobile modal metric label `Detected`
- all current modal headings, labels, columns, context, and copy

No copy normalization is authorized.

### 7.2 Event timestamp fallback

Preserve the current event-time/created-at fallback semantics exactly.

Do not change which timestamp is selected, how null/undefined values are handled, or how the result is formatted.

### 7.3 Values and classes

Preserve:

- anomaly-type-specific value formatting
- null, undefined, and non-finite value behavior
- duration units and precision
- integer/rate/gap/limit semantics where currently used by the Dashboard modal
- exact severity class tokens
- exact anomaly-value class tokens
- all existing capitalization, punctuation, fallback text, and typography

Do not replace anomaly severity pills with `StatusBadge`; their DOM and visual semantics differ.

### 7.4 Route and popup boundaries

Do not migrate or normalize:

- route anomaly value formatting
- popup anomaly value formatting
- route timestamp formatting
- popup time-only formatting
- route versus popup counts
- route versus popup columns
- route versus popup interaction
- popup mobile-card navigation
- route non-navigation rows/cards

`marketAdapters.ts`, SymbolDetail modules, and popup tests are forbidden.

## 8. Dashboard ownership that must remain unchanged

Dashboard retains:

- modal open/close state
- selected anomaly/symbol state
- full raw anomaly collection ownership
- preview versus full-list distinction
- return context
- popup identity and lifecycle
- resource identity
- query ownership
- desktop interactive row behavior
- mobile native-button behavior
- back/close behavior

Preserve desktop anomaly-row interaction exactly:

- click callback
- Enter activation
- Space activation
- `preventDefault`
- role
- tabIndex
- aria label
- focus-visible classes

Preserve mobile native buttons; do not replace them with generic clickable containers.

Preserve full raw collections for modal/detail flows. Do not accidentally reuse preview-limited rows.

## 9. Implementation requirements

1. Enumerate the Dashboard-local anomaly helper copies at the exact base.
2. Compare each with the pure presentation owner and accept only proven semantic equivalence.
3. Import canonical helpers into `DashboardPage.tsx`.
4. Remove page-local duplicate helper implementations.
5. Extend `recentAnomaliesPresentation.ts` only with minimal pure functions needed for accepted semantics.
6. Keep Dashboard orchestration and JSX ownership in Dashboard.
7. Keep route/popup assembly in existing forbidden owners.
8. Avoid new shared/global abstractions, barrels, render-prop primitives, or generic modal components.
9. Do not touch Dashboard market-health duplication.
10. Keep the product output visually and behaviorally identical.

## 10. Test requirements

Update or extend `recentAnomaliesPresentation.test.ts` to prove the pure canonical contract.

At minimum cover the currently relevant matrix for:

- anomaly type/detector labels
- event timestamp and fallback timestamp
- time formatting
- null and undefined values
- numeric zero
- NaN/non-finite values when accepted by current inputs
- duration values
- integer values
- numeric/percentage/rate values used by Dashboard
- severity classes
- anomaly value classes

Update `DashboardPage.test.tsx` only where helper ownership/import shape changed and where behavioral coverage is needed.

Preserve strong guards for:

- full raw anomaly collection in modal flows
- preview/full-list distinction
- modal headings and columns
- `Detected at` versus `Detected`
- desktop click/Enter/Space/`preventDefault`
- mobile native-button interaction
- aria labels, role, tabIndex, and focus behavior
- return context
- popup lifecycle and shared symbol-detail wiring
- resource identity and no cross-symbol/cross-mode contamination
- no Live fallback to Demo
- ticker ownership
- deterministic time behavior
- no direct Recharts or duplicate preview ownership in Dashboard

Do not perform broad MP06A/MP06B guard modernization. Narrow only assertions directly made stale by the accepted anomaly-helper ownership change.

Do not edit `DashboardPage.popup.test.tsx`.

## 11. Static review and unrun gates

Because this is a GitHub web worker:

- inspect the complete branch diff after the commit
- confirm only leased files changed
- confirm one logical product commit exists
- do not claim local test/build results
- list the exact focused and full commands the Orchestrator must run later

Required later local/CI commands conceptually include:

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/recentAnomaliesPresentation.test.ts src/pages/DashboardPage.test.tsx src/pages/DashboardPage.popup.test.tsx
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Production total JS must not increase against the exact candidate base. This worker cannot prove emitted-byte reduction and must not invent one.

## 12. Commit and publication

Create exactly one normal product commit on:

`p35/mp03-dashboard-anomaly-presentation`

Commit message:

`refactor(ui): consolidate dashboard anomaly presentation`

Push normally.

Do not open a pull request. Do not merge. Do not rebase. Do not amend. Do not force-push.

After the product commit exists, publish one worker report to `progeranna/connector` at:

`signalguard-rs/phase-3.5/reports/P3.5-MP03/<PRODUCT_HEAD_SHA>.md`

Use connector commit message:

`docs(phase-3.5): publish MP03 implementation report`

The report must include:

- exact base SHA
- product branch
- product head SHA
- product commit message
- exact changed files
- lease compliance
- canonical helpers reused or added
- Dashboard-local duplicate helpers removed
- intentional route/popup and desktop/mobile differences retained
- tests changed
- checks not run by the web worker
- exact later validation commands
- bundle claim: not measured
- confirmation of no PR and no merge

Verify both the product branch head and connector report after publication.

## 13. Final response

Return:

- exact base SHA
- product branch
- product head SHA
- changed files
- commit count ahead of base
- canonical helpers reused/added
- duplicate Dashboard helpers removed
- preserved intentional differences
- lease-compliance result
- checks actually performed
- checks deferred to local/CI
- report path
- connector report commit SHA
- confirmation that no PR or merge was created

Return exactly one terminal status:

`P3_5_MP03_IMPLEMENTATION_COMPLETE`

Use it only when the product commit and verified connector report both exist.

Otherwise return:

`P3_5_MP03_BLOCKED`

Explain the exact blocker and do not make out-of-contract repairs.