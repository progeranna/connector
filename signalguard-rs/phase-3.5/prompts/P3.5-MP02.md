# P3.5-MP02 — Timeline market-health presentation reuse

You are the GitHub web implementation worker for:

`P3.5-MP02 — Timeline market-health presentation reuse`

This is a GitHub web-worker task, not a local Codex task.

Use the connected GitHub tools. Do not use a local checkout or shell. Do not claim that tests, typecheck, lint, build, or bundle checks ran unless a GitHub workflow actually ran and you inspected it.

## 1. Authoritative repositories and documents

Product repository:

`progeranna/signalguard-rs`

Coordination repository:

`progeranna/connector`

Read before any product write:

- `signalguard-rs/phase-3.5/PLAN.md`
- `signalguard-rs/phase-3.5/control/STATUS.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00B/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`
- `signalguard-rs/phase-3.5/inventory/P3.5-MP00D/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`

The consolidated MP00 inventory is authoritative when another document is less specific.

## 2. Immutable identity

Exact product base SHA:

`c06082a97254bfa2f6ebd7e29a1ad753c4acc798`

Phase branch at authorization:

`refactor/dashboard-modules`

Assigned implementation branch:

`p35/mp02-timeline-presentation-reuse`

Required single product commit message:

`refactor(ui): reuse market health presentation in timeline`

Before editing:

1. Verify `refactor/dashboard-modules` still resolves to the exact base SHA.
2. Verify the assigned branch does not contain unrelated work.
3. Create the assigned branch from the exact base SHA, or use it only if it still points exactly to the base with no worker commit.
4. Fetch every writable product file from the exact assigned branch before updating it.

If the phase branch no longer equals the immutable base, or the assigned branch contains unknown commits, stop with `P3_5_MP02_BLOCKED`. Do not rebase, reset, amend, force-push, or rewrite history.

## 3. Objective

Remove Timeline-local copies of market-health presentation helpers where their semantics are identical to the accepted canonical owner:

`web/src/features/dashboard/marketHealthPresentation.ts`

Migrate `TimelinePanel.tsx` to reuse the canonical market-health functions while keeping all Timeline-specific chart/date/domain/tooltip/anomaly behavior local.

This mini-phase is presentation deduplication only. It is not the Recharts/code-splitting phase.

## 4. Writable path lease

You may modify only:

- `web/src/features/dashboard/TimelinePanel.tsx`
- `web/src/features/dashboard/TimelinePanel.test.tsx`
- `web/src/features/dashboard/marketHealthPresentation.ts` only if a minimal, general-purpose `marketStatusLabel` contract is proven necessary
- `web/src/features/dashboard/marketHealthPresentation.test.ts` only for that minimal contract or focused regression coverage

No other product path is writable.

## 5. Forbidden paths and changes

Do not modify:

- `web/src/pages/DashboardPage.tsx` or its tests
- any MarketHealth desktop/mobile production or test file
- any RecentAnomalies production or test file
- any SymbolDetail production or test file
- `marketAdapters.ts`
- `marketViewModel.ts`
- shared formatter or status modules
- API, query-key, resource, popup, ticker, mode, router, or route modules
- package, lockfile, TypeScript, ESLint, Vite, Vitest, CI, build, or bundle-budget files
- backend, deployment, Docker, migrations, or Rust code

Do not change Recharts imports, chart components, router imports, dynamic imports, lazy boundaries, Suspense, or route loading behavior.

Do not perform broad source-test modernization. Only repair assertions directly made stale by the accepted helper-ownership change.

## 6. Canonical functions to reuse

Inspect the exact base implementations in `marketHealthPresentation.ts` and the Timeline-local copies before editing.

Where semantics are identical, reuse the canonical owners for:

- ticker price formatting
- ticker percent formatting
- optional compact-number formatting
- optional age formatting
- health-status wording
- availability messaging

The audit identified the current canonical names:

- `formatTickerPrice`
- `formatTickerPercent`
- `formatOptionalCompact`
- `formatOptionalAge`
- `statusLabel`
- `availabilityMessage`

Do not rename these merely for stylistic consistency.

### 6.1 Optional `marketStatusLabel` owner

Timeline also contains a repeated market-availability/status label mapping. Add a new canonical `marketStatusLabel` export only if exact inspection proves that:

- the Timeline mapping is semantically identical to other accepted market-health mappings
- the function can accept a minimal general-purpose structural input
- no page-specific or Timeline-specific type must be imported into the presentation owner
- introducing the export removes a real local duplicate

If the minimal contract is not clearly proven, leave that helper local and explain why in the report. Do not over-generalize.

## 7. Behavior that must remain unchanged

Preserve exact formatting and fallback semantics, including:

- missing price/percent/compact values
- `—` versus `n/a`
- `Unavailable` age fallback
- numeric zero formatting
- configured wording
- awaiting-data wording
- unavailable wording
- observed health-status wording
- capitalization and punctuation

Do not replace market-health presentation wrappers directly with generic shared formatters when their missing-value copy differs.

Preserve all Timeline-specific behavior and ownership:

- `formatTimelineTick`
- `formatTimelinePriceTick`
- timeline tooltip timestamp including its current date/time semantics
- timeline normalization
- price/time domains
- chart axes
- anomaly marker colors
- anomaly matching window
- tooltip composition
- loading/error/configured/awaiting/unavailable/normalized-empty/success precedence
- query identity and query ownership
- chart dimensions and responsive behavior
- exact DOM, classes, copy, aria behavior, and visual output

Conditional chart rendering must remain unchanged. Do not introduce a code-splitting boundary.

## 8. Implementation requirements

1. Fetch and compare the local Timeline helper implementations with `marketHealthPresentation.ts` at the exact base.
2. Import canonical helpers only where semantic equivalence is proven.
3. Remove the corresponding Timeline-local helper implementations.
4. Keep Timeline-only helpers local.
5. Avoid type widening beyond what the canonical functions actually need.
6. Do not import page-level models into `marketHealthPresentation.ts`.
7. Do not create a barrel file or move the owner into `shared`.
8. Keep production output structurally and visually identical.

## 9. Test requirements

Update focused tests so they protect behavior and accepted ownership without requiring obsolete local helper definitions.

At minimum preserve or add coverage for:

- price zero and missing price
- percent zero and missing percent
- compact-number zero and missing value
- age zero and missing age
- configured, awaiting, unavailable, and observed status labels
- availability message matrix
- snapshot summary values
- tooltip values
- Timeline-specific date/timestamp behavior
- chart state precedence
- deterministic time ownership
- no query/router/network ownership transfer

Keep exact visual-contract assertions and deterministic-time guards.

A source-level ownership guard may require canonical imports and prohibit duplicate local formatter definitions, but it must not depend on formatting, aliases, exact import count, or exact JSX internal names more than necessary.

Do not perform the broader MP06A test-guard modernization.

## 10. Static review and unrun gates

Because this is a GitHub web worker:

- inspect the complete branch diff after the commit
- confirm only leased files changed
- confirm one logical product commit exists
- do not claim local test/build results
- list the exact focused and full commands the Orchestrator must run later

Required later local/CI commands conceptually include:

```text
cd web
npm ci
npm run test:run -- src/features/dashboard/TimelinePanel.test.tsx src/features/dashboard/marketHealthPresentation.test.ts
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

Production total JS must not increase against the exact candidate base. This worker cannot prove emitted-byte reduction and must not invent one.

## 11. Commit and publication

Create exactly one normal product commit on:

`p35/mp02-timeline-presentation-reuse`

Commit message:

`refactor(ui): reuse market health presentation in timeline`

Push normally.

Do not open a pull request. Do not merge. Do not rebase. Do not amend. Do not force-push.

After the product commit exists, publish one worker report to `progeranna/connector` at:

`signalguard-rs/phase-3.5/reports/P3.5-MP02/<PRODUCT_HEAD_SHA>.md`

Use connector commit message:

`docs(phase-3.5): publish MP02 implementation report`

The report must include:

- exact base SHA
- product branch
- product head SHA
- product commit message
- exact changed files
- lease compliance
- which helpers became canonical imports
- whether `marketStatusLabel` was added, with justification
- Timeline-specific helpers deliberately retained
- tests changed
- checks not run by the web worker
- exact later validation commands
- bundle claim: not measured
- confirmation of no PR and no merge

Verify both the product branch head and connector report after publication.

## 12. Final response

Return:

- exact base SHA
- product branch
- product head SHA
- changed files
- commit count ahead of base
- canonical helpers reused
- local helpers retained and why
- lease-compliance result
- checks actually performed
- checks deferred to local/CI
- report path
- connector report commit SHA
- confirmation that no PR or merge was created

Return exactly one terminal status:

`P3_5_MP02_IMPLEMENTATION_COMPLETE`

Use it only when the product commit and verified connector report both exist.

Otherwise return:

`P3_5_MP02_BLOCKED`

Explain the exact blocker and do not make out-of-contract repairs.