# P3.5-MP08-MEASURE — Timeline/Recharts lazy-boundary measurement

## Identity

- Contract: `progeranna/connector` `main`, `signalguard-rs/phase-3.5/prompts/P3.5-MP08-MEASURE.md`.
- Contract commit fetched/read: `5ead8616fe1057d09218d8c80444e8f1ecf70303`.
- Product base: `9c8aed635e57272fc834c1ddcdbc3dbf33cf4328`.
- Product worktree: `/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp08-measure`.
- No product branch, commit, PR, package, dependency, lockfile, budget, or route change was retained.

## Baseline

Before experiments, the dedicated worktree was created detached at the exact base and the mandated source scope was inspected. Commands:

```text
git fetch --all --tags
git worktree add --detach /Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp08-measure 9c8aed635e57272fc834c1ddcdbc3dbf33cf4328
cd web
npm ci
npm run test:run
npm run typecheck
npm run lint
npm run build
npm run bundle:check
```

`npm ci`, typecheck, lint, build, and the sequential bundle check passed. The first bundle check was launched concurrently with the build and saw the expected shared-`dist` race; it was rerun after the build and passed.

| Gate | Result |
| --- | --- |
| tests | PASS — 43 files, 640 tests |
| typecheck | PASS |
| lint | PASS |
| build | PASS |
| bundle budget | PASS |

| Asset | Kind | Raw | Direct gzip |
| --- | --- | ---: | ---: |
| `assets/index-a4d88fe2.js` | initial entry | 757,217 | 219,504 |
| Total | 1 initial / 0 async | 757,217 | 219,504 |

Budget: largest and total limits were both 761,856 bytes; baseline raw headroom was 4,639 bytes. The build transformed 714 modules; the Rollup analysis recorded 458 surviving bundled modules.

## Instrumentation and Recharts chain

A temporary Vite `generateBundle` hook, enabled only with `MP08_BUNDLE_ANALYSIS=1`, recorded chunk metadata, static/dynamic edges, facade IDs, module IDs, rendered lengths, rendered exports, and removed exports. A local `gzip -c` loop recorded direct gzip bytes. The hook and generated JSON were not retained.

Baseline had 183 surviving `recharts` modules with 677,585 bytes of per-module `renderedLength` attribution. The measured Recharts/transitive-module chain attribution was 944,104 rendered bytes across Recharts and its surviving graph members: `@reduxjs/toolkit`, `react-redux`, `redux`, `redux-thunk`, `reselect`, `immer`, `use-sync-external-store`, `victory-vendor`, `decimal.js-light`, `eventemitter3`, `internmap`, and the D3 packages `d3-array`, `d3-color`, `d3-format`, `d3-interpolate`, `d3-path`, `d3-scale`, `d3-shape`, `d3-time`, and `d3-time-format`.

Rendered attribution is a Rollup module attribution total, not an additive asset-size total; final deltas below use emitted file lengths and direct gzip.

## C1 — lazy entire `TimelinePanel`

Temporary change: one `React.lazy` edge from `DashboardPage` to `TimelinePanel`, with an `h-40` Suspense fallback. No route split.

Focused tests (50), full tests (43 files/640 tests), typecheck, lint, build, and bundle budget all passed.

| Asset | Kind | Raw | Direct gzip |
| --- | --- | ---: | ---: |
| `assets/index-c25994ab.js` | initial entry | 385,210 | 110,470 |
| `assets/TimelinePanel-b080a5b9.js` | async TimelinePanel | 372,580 | 109,100 |
| Total | 1 initial / 1 async | 757,790 | 219,570 |

- Total delta: `+573` raw, `+66` direct gzip.
- Initial closure delta: `−372,007` raw, `−109,034` gzip.
- Largest initial/async assets: 385,210 / 372,580 raw.
- Recharts chain moved async; attribution remained 944,104 rendered bytes; duplicate module IDs: 0.
- Budget passed; remaining raw headroom: 4,066 bytes.

Classification: `CONTRACT_REJECTED`. Although it reduces initial JS, it defers the Timeline shell, summary/timeline loading, unavailable, error/retry, empty, snapshot, and shell-dimension behavior. The whole panel is not the implementation boundary.

## C2 — lazy only Recharts renderer, normalized API

Temporary `web/src/features/dashboard/TimelineChartRenderer.tsx` owned only Recharts JSX, chart formatting, axes, gradient, markers, tooltip, and responsive wrapper. `TimelinePanel` retained shell, states, normalization, domains, visible anomalies, and query ownership. A stable 285px chart-area fallback was used.

The first run exposed four synchronous chart assertions; temporary tests were changed to use `waitFor`/`findByTestId` for the lazy chart. After that adaptation all required gates passed: focused 50, full 43/640, typecheck, lint, build, and bundle budget.

| Asset | Kind | Raw | Direct gzip |
| --- | --- | ---: | ---: |
| `assets/index-0a837210.js` | initial entry | 390,828 | 111,734 |
| `assets/TimelineChartRenderer-2bbb914f.js` | async renderer | 367,241 | 107,405 |
| Total | 1 initial / 1 async | 758,069 | 219,139 |

- Total delta: `+852` raw, `−365` direct gzip.
- Initial closure delta: `−366,389` raw, `−107,770` gzip.
- Recharts chain entirely async; attribution 944,104 rendered bytes; duplicate module IDs: 0.
- Largest initial/async assets: 390,828 / 367,241 raw.
- Budget passed; remaining raw headroom: 3,787 bytes.

Classification: `INITIAL_ONLY_BENEFIT`. It removes Recharts from the initial graph and materially reduces initial JS, but total raw JS increases, so it is not an `IMPLEMENTATION_CANDIDATE` under the total-JS non-regression rule.

## C3 — raw-input renderer API alternative

Same renderer boundary as C2, but the async module received raw `timelinePoints`, anomalies, and `emptyAnchorMs`, and owned normalization, domain derivation, and visible-anomaly derivation. Temporary tests awaited renderer-owned normalization/domain spies and the normalized-empty state.

All required gates passed: focused 50, full 43/640, typecheck, lint, build, and bundle budget.

| Asset | Kind | Raw | Direct gzip |
| --- | --- | ---: | ---: |
| `assets/index-2fdf80f4.js` | initial entry | 389,103 | 111,085 |
| `assets/TimelineChartRenderer-73055c9f.js` | async renderer | 368,998 | 108,047 |
| Total | 1 initial / 1 async | 758,101 | 219,132 |

- Total delta: `+884` raw, `−372` direct gzip.
- Initial closure delta: `−368,114` raw, `−108,419` gzip.
- Recharts chain entirely async; attribution 944,104 rendered bytes; duplicate module IDs: 0.
- Largest initial/async assets: 389,103 / 368,998 raw.
- Budget passed; remaining raw headroom: 3,755 bytes.

Classification: `INITIAL_ONLY_BENEFIT`. It saves 1,725 more initial raw bytes than C2 but adds 1,757 async raw bytes and regresses total raw by 32 bytes relative to C2. It also moves normalized-empty ownership behind the boundary. C2 is preferred.

## C4 — controlled inert-renderer delta

Only the normalized renderer API was retained; the Recharts renderer was replaced with an inert API-compatible renderer. This intentionally does not claim chart/tooltip/visual parity.

Typecheck, lint, build, and bundle budget passed. Focused/full chart tests failed against the inert placeholder, as expected: the placeholder does not provide real Recharts chart, tooltip, or clock behavior. This prototype is an upper-bound measurement only.

| Asset | Kind | Raw | Direct gzip |
| --- | --- | ---: | ---: |
| `assets/index-6fd680b3.js` | initial entry | 390,797 | 111,719 |
| `assets/TimelineChartRenderer-2854ce77.js` | async inert renderer | 1,865 | 941 |
| Total | 1 initial / 1 async | 392,662 | 112,660 |

- Total delta: `−364,555` raw, `−106,844` direct gzip.
- Initial closure delta: `−366,420` raw, `−107,785` gzip.
- One dynamic renderer edge; duplicate module IDs: 0.
- This is not pure Recharts-only size; it includes the retained renderer API/wrapper/inert code.

Classification: `CONTRACT_REJECTED`; it is the requested complete chart-renderer/Recharts upper bound, not a recommendation.

## Runtime and visual validation

For C1–C3, focused/full tests preserved the tested summary loading, configured/unavailable, timeline loading, error/retry, normalized-empty, observed chart, snapshot, Demo/Live, symbol transitions, query isolation, normalization, domains, markers, and tooltip contracts. The production graph showed one dynamic renderer edge and no duplicated module IDs for renderer-only splits.

A Vite production preview was started for C4. `curl` verified the entry HTML and `/assets/TimelineChartRenderer-2854ce77.js` returned HTTP 200, and the HTML referenced the entry asset.

No Chromium/Chrome/Playwright/Puppeteer/browser automation executable was installed. Therefore desktop/mobile screenshot comparison, actual rendered visual parity, browser console cleanliness, runtime duplicate-request count, runtime dynamic-import count, and interactive retry/transition/remount checks could not be verified. No visual-parity claim is made. This limitation is recorded rather than inferred away.

Behavioral finding matrix:

| Finding | Evidence/result | Scope and limitation |
| --- | --- | --- |
| Duplicate timeline/data requests | PASS in the existing focused isolation/ownership coverage: query keys include mode and symbol, old-identity data is detached, and the ownership tests exercise Demo/Live and symbol transitions without an extra timeline owner. The emitted renderer-only graphs also had one dynamic edge and zero duplicated module IDs. | Browser-level network counting was unavailable, so this is test/static-graph evidence rather than an instrumented production-preview request count. |
| Suspense/remount loops | No loop was observed in the C1–C3 focused/full test runs; each prototype used one stable lazy boundary, and the ownership tests preserved state transitions, retry, and old-identity detachment. | An automated browser render-loop probe was unavailable; interactive runtime verification remains unclaimed. |
| Dynamic renderer request multiplicity | PASS statically: each renderer-only prototype emitted one renderer async chunk and one dynamic renderer edge. | Runtime import-request count could not be measured without browser automation. |

## Recommendation

Preferred boundary: lazy-load only a new `TimelineChartRenderer`; keep the entire `TimelinePanel` eager. `DashboardPage` remains the sole query owner.

Preferred C2 ownership:

- Eager `TimelinePanel`: shell wrappers; symbol/source/status; snapshots; availability; summary loading; timeline loading/error/retry; normalized-empty; normalization; domains; visible anomalies; stable fallback.
- Lazy renderer: normalized points, visible anomalies, all anomalies for tooltip semantics, and derived domains as props; Recharts JSX, axes, gradient, markers, tooltip, and chart-specific formatting.

Exact future implementation paths:

```text
web/src/features/dashboard/TimelinePanel.tsx
web/src/features/dashboard/TimelineChartRenderer.tsx
web/src/features/dashboard/TimelinePanel.test.tsx
web/src/features/dashboard/TimelineChartRenderer.test.tsx
web/src/pages/DashboardPage.test.tsx
web/src/features/dashboard/marketDataIsolation.test.tsx
```

Tests must cover state precedence, stable fallback dimensions, retry, normalized-empty, observed points/domains, ±15-second markers/tooltip filtering, zero/unavailable snapshots, accessibility, responsive wrappers, Demo/Live and symbol transitions, query ownership, no duplicate requests, no Suspense/remount loops, and one dynamic import request. Browser preview checks must cover desktop and mobile.

C2 reduces initial JS by 366,389 raw / 107,770 gzip but adds 852 total raw bytes. C3 reduces initial JS by 368,114 raw / 108,419 gzip but adds 884 total raw bytes. Neither meets the contract's total raw `<=757217` implementation-candidate rule. C4's inert upper bound is not product behavior.

C2/C3 do not improve total raw headroom toward the remaining 3,553-byte requirement; they consume 852/884 of the 4,639-byte baseline headroom. Any acceptance of the initial-only benefit depends on MP09 distinguishing initial-entry JS from total JS while retaining total-JS non-regression. MP08 can execute independently of MP07; no route splitting was performed.

No budget increase or weakened total-JS gate is recommended.

## Cleanup proof

Before cleanup, the required `git status --short`, `git diff --check`, `git diff --stat`, and `git diff` commands were run. They showed only temporary Timeline/Vite/renderer/analysis changes. All were restored or removed.

Final product proof:

```text
git status --short
git diff --check
git diff --stat
git diff
git rev-parse HEAD
```

Status/diff output was empty and the final proof was:

```text
9c8aed635e57272fc834c1ddcdbc3dbf33cf4328
PRODUCT_BASE_SHA_OK
```

The product worktree is clean, detached, and exactly at the immutable base.
