# P3.5-MP00 — Consolidated Wave 0 inventory and Wave 1 authorization

Status: `ACCEPTED`

## 1. Authoritative identity

Product repository:

`progeranna/signalguard-rs`

Phase branch:

`refactor/dashboard-modules`

Immutable Phase 3.5 base and current phase-branch head:

`c06082a97254bfa2f6ebd7e29a1ad753c4acc798`

Branch comparison at consolidation time:

- status: `identical`;
- ahead: `0`;
- behind: `0`.

Checkpoint 2 CI:

`30548108769` — success.

Accepted frontend baseline:

- 42 test files;
- 607 tests;
- typecheck pass;
- lint pass with zero warnings/errors;
- production JS raw: `761856` bytes;
- production JS gzip: `220163` bytes;
- largest-JS budget: `761856` bytes;
- total-JS budget: `761856` bytes;
- headroom: `0` bytes.

The bundle budget must not be increased during Phase 3.5.

## 2. Accepted Wave 0 reports

### MP00A-WEB — static bundle dependency graph

Report:

`signalguard-rs/phase-3.5/inventory/P3.5-MP00A-WEB/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`

Connector commit:

`3ba9dc726de4f9e46048ef2b0236ffc74bd2a77e`

Disposition:

`ACCEPTED_STATIC_GRAPH`

Accepted conclusions:

1. `/`, `/dashboard`, `/symbols/:symbol`, and `/anomalies` all enter the initial static dependency graph.
2. No route-level or chart-level dynamic import boundary exists.
3. Recharts is statically reachable through `index.html -> main.tsx -> App.tsx -> router.tsx -> DashboardPage.tsx -> TimelinePanel.tsx -> recharts`.
4. Conditional chart JSX does not provide code splitting.
5. `/anomalies` is directly eager; `/symbols/:symbol` is indirectly eager through `CanonicalSymbolRoute -> SymbolDetailPage`.
6. Route splitting alone cannot defer Recharts within `/dashboard`; a separate Timeline/chart boundary is required.
7. The current bundle checker counts every emitted JS asset toward total JS, including lazy chunks.

The report does not prove emitted chunk composition, Recharts byte contribution, tree-shaking effectiveness, raw/gzip savings, or prototype correctness. Those measurements are deferred to isolated no-commit local experiments before Wave 3.

### MP00B — duplicate implementation audit

Report:

`signalguard-rs/phase-3.5/inventory/P3.5-MP00B/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`

Connector commit:

`c115742575c2f47808473fe2f2c8a378e5d2f26d`

Disposition:

`ACCEPTED`

Accepted result:

- 13 duplicate or near-duplicate findings;
- Priority A/B/C: `2 / 6 / 5`;
- 8 rejected similarities.

Leading accepted candidates:

1. exact desktop/mobile `HealthScore` consolidation;
2. Timeline reuse of `marketHealthPresentation.ts`;
3. Dashboard anomaly modal reuse of `recentAnomaliesPresentation.ts` under an exclusive Dashboard lease.

### MP00C-WEB — dead code, exports, dependencies, and cycles

Report:

`signalguard-rs/phase-3.5/inventory/P3.5-MP00C-WEB/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`

Connector commit:

`5ffc52ca5ee5d3f25b27fb5ea96a4d23900145c1`

Disposition:

`ACCEPTED_WEB_INVENTORY`

Accepted result:

- 51 production files inspected;
- 245 module export bindings inventoried;
- 182 runtime-capable exports, including re-exports;
- 63 type-only exports;
- 3 confirmed findings, including 2 immediate cleanup candidates;
- 4 probable candidates;
- 2 groups requiring local verification;
- 0 direct dependency deletion candidates;
- 0 credible import cycles.

Confirmed cleanup candidates:

1. remove only the `export` modifier from the local `HealthScore` in `MarketHealthDesktopTable.tsx`, unless MP01 removes or moves the implementation first;
2. remove only the `export` modifier from `EmptyBlock` in `MarketHealthMobileCards.tsx`, unless MP01 changes the module first.

These findings are intentionally deferred. MP01 owns the same files, and MP05 must remain after MP04 with a refreshed importer/export inventory.

Probable or verification-required candidates retained for later review:

- test-only `replaceSymbolPopupSymbol` export;
- `DEMO_MARKETS` compatibility re-export from `marketOrder.ts`;
- `dashboardResourceState.ts` probable test-only/orphan owner;
- `Tooltip.tsx` probable test-only/orphan component;
- runtime-control public-contract exports requiring local proof.

No package or lockfile change is justified by MP00C-WEB.

### MP00D — test-guard maintainability audit

Report:

`signalguard-rs/phase-3.5/inventory/P3.5-MP00D/c06082a97254bfa2f6ebd7e29a1ad753c4acc798.md`

Connector commit:

`73408022752cc63dc77aeff7c897ad1c6a1dd889`

Disposition:

`ACCEPTED`

Accepted result:

- 42 test files inspected;
- 47 guard findings;
- `KEEP_EXACT`: 17;
- `KEEP_BUT_NARROW`: 18;
- `REPLACE_WITH_BEHAVIOR`: 8;
- `REPLACE_WITH_TYPE_OR_API_CONTRACT`: 1;
- `REMOVE_REDUNDANT`: 3;
- `INVESTIGATE`: 0.

Exact visual-parity assertions from PR #56 remain exact. Resource identity, Demo/Live isolation, ticker ownership, order, immutability, callbacks, accessibility, route/popup differences, and complete state coverage remain non-negotiable.

MP06A and MP06B remain blocked until their Wave 2 dependency gate.

## 3. Accepted findings and rejected false positives

### Accepted now

- one health-score owner for desktop/mobile market-health preview callers;
- one market-health formatter/status owner for Timeline reuse;
- one anomaly formatter/severity owner for Dashboard anomaly modal callers;
- source-test modernization is valid later, but only after production ownership stabilizes;
- dead-export/file/package cleanup must be evidence-backed and sequenced after MP04;
- route/chart code splitting is justified for local measurement in Wave 3.

### Rejected or deferred

The following are not authorized as Wave 1 cleanup:

- generic clickable table-row abstraction;
- replacing native mobile buttons with generic containers;
- direct replacement of market-health wrappers by generic shared formatters with different missing-value copy;
- replacing anomaly severity pills with `StatusBadge` where DOM and visual semantics differ;
- merging route and popup anomaly value/timestamp/interaction semantics;
- unifying preview, full modal, route, and popup column/count behavior;
- extracting metric cards or empty-state fragments without measured benefit;
- deleting `Tooltip.tsx`, `dashboardResourceState.ts`, compatibility exports, or any package before local proof;
- changing the bundle budget;
- changing product copy or visual design.

## 4. Wave 1 execution model

Wave 1 contains three independent product candidates. They may be implemented concurrently from the same immutable base because their writable path leases do not overlap.

Worker class for this execution wave:

`GitHub web implementation worker`

Each worker must:

- verify the exact base SHA before any write;
- create or use only its assigned product branch;
- make one logical normal commit;
- push normally;
- not open or merge a PR unless explicitly instructed by the Orchestrator;
- not edit files outside its lease;
- not raise the bundle budget;
- not change visual presentation, copy, resource identity, query ownership, or route/popup behavior;
- report exact changed files, commit SHA, focused checks available to it, and any checks requiring local/CI verification.

No worker merges its own work.

Implementation may run in parallel. Integration is sequential and Orchestrator-controlled.

## 5. P3.5-MP01 — Health-score presentation consolidation

Status:

`EXECUTION_AUTHORIZED`

Base SHA:

`c06082a97254bfa2f6ebd7e29a1ad753c4acc798`

Assigned product branch:

`p35/mp01-health-score-presentation`

Required commit message:

`refactor(ui): consolidate health score presentation`

### Writable paths

- new `web/src/features/dashboard/HealthScore.tsx`;
- new `web/src/features/dashboard/HealthScore.test.tsx`;
- `web/src/features/dashboard/MarketHealthDesktopTable.tsx`;
- `web/src/features/dashboard/MarketHealthDesktopTable.test.tsx`;
- `web/src/features/dashboard/MarketHealthMobileCards.tsx`;
- `web/src/features/dashboard/MarketHealthMobileCards.test.tsx`.

### Forbidden paths

- `web/src/pages/DashboardPage.tsx` and tests;
- `web/src/features/dashboard/TimelinePanel.tsx` and tests;
- `web/src/features/dashboard/marketHealthPresentation.ts` and tests;
- all recent-anomaly production/test modules;
- all symbol-detail production/test modules;
- `web/src/shared/lib/status.ts`;
- router, package, lockfile, TypeScript, lint, Vite, Vitest, CI, and bundle-budget files;
- backend and deployment paths.

### Required result

- exactly one `HealthScore` implementation for desktop/mobile preview callers;
- score tone, text class, bar class, minimum width, compact layout, and regular layout owned by the new focused module;
- local duplicate implementations removed from both callers;
- ordered score/status precedence preserved;
- status wording remains separately status-derived;
- null score remains `—` with neutral 0% bar;
- numeric zero remains `0` with unhealthy 4% minimum bar;
- exact compact and regular classes preserved;
- caller DOM, identity, callbacks, keyboard behavior, and responsive presentation unchanged.

### Bundle expectation

Qualitative expected reduction: `medium`.

No exact byte claim is accepted before build evidence. Production total JS must not increase against the exact base.

## 6. P3.5-MP02 — Timeline market-health formatter reuse

Status:

`EXECUTION_AUTHORIZED`

Base SHA:

`c06082a97254bfa2f6ebd7e29a1ad753c4acc798`

Assigned product branch:

`p35/mp02-timeline-presentation-reuse`

Required commit message:

`refactor(ui): reuse market health presentation in timeline`

### Writable paths

- `web/src/features/dashboard/TimelinePanel.tsx`;
- `web/src/features/dashboard/TimelinePanel.test.tsx`;
- `web/src/features/dashboard/marketHealthPresentation.ts` only if a minimal general-purpose `marketStatusLabel` contract is proven necessary;
- `web/src/features/dashboard/marketHealthPresentation.test.ts` only for that minimal contract or focused regression coverage.

### Forbidden paths

- `web/src/pages/DashboardPage.tsx` and tests;
- all MarketHealth desktop/mobile production/test files;
- all RecentAnomalies production/test files;
- all SymbolDetail production/test files;
- `marketAdapters.ts`, `marketViewModel.ts`, shared formatter/status modules;
- router, package, lockfile, TypeScript, lint, Vite, Vitest, CI, and bundle-budget files;
- backend and deployment paths.

### Required result

- remove Timeline-local copies of ticker-price, percent, compact-number, age, status-label, and availability formatting where semantics are identical;
- import the accepted canonical functions from `marketHealthPresentation.ts`;
- keep Timeline-specific tick, domain, normalization, tooltip timestamp, anomaly marker, matching window, and chart behavior local;
- preserve exact missing fallbacks, zero formatting, availability wording, observed status wording, DOM, classes, and state precedence;
- no Recharts, router, dynamic import, or lazy-boundary change in MP02.

### Bundle expectation

Qualitative expected reduction: `medium`.

No exact byte claim is accepted before build evidence. Production total JS must not increase against the exact base.

## 7. P3.5-MP03 — Dashboard anomaly presentation consolidation

Status:

`EXECUTION_AUTHORIZED_HIGH_CONFLICT`

Base SHA:

`c06082a97254bfa2f6ebd7e29a1ad753c4acc798`

Assigned product branch:

`p35/mp03-dashboard-anomaly-presentation`

Required commit message:

`refactor(ui): consolidate dashboard anomaly presentation`

### Exclusive writable paths

- `web/src/pages/DashboardPage.tsx`;
- `web/src/pages/DashboardPage.test.tsx`;
- `web/src/features/dashboard/recentAnomaliesPresentation.ts`;
- `web/src/features/dashboard/recentAnomaliesPresentation.test.ts`.

No other worker may edit `DashboardPage.tsx` or `DashboardPage.test.tsx` while MP03 is active.

### Forbidden paths

- all MarketHealth desktop/mobile files and tests;
- `TimelinePanel.tsx` and tests;
- `marketHealthPresentation.ts` and tests;
- `marketAdapters.ts`, `marketViewModel.ts`, and tests;
- all SymbolDetail production/test files;
- shared components and libraries;
- router, package, lockfile, TypeScript, lint, Vite, Vitest, CI, and bundle-budget files;
- backend and deployment paths.

### Required result

- Dashboard anomaly modal callers reuse canonical anomaly type, time, value, severity-class, and anomaly-value-class semantics;
- `recentAnomaliesPresentation.ts` remains the pure presentation owner;
- Dashboard retains modal state, full raw collection ownership, return context, desktop row interaction, mobile native-button interaction, and popup lifecycle;
- route/popup formatting and interaction are not migrated;
- exact modal headings, columns, labels, copy, classes, `Detected at`/`Detected` distinction, event-time fallback, click/Enter/Space behavior, aria labels, role, tab index, and return context are preserved;
- no resource, query, popup identity, back/close, or modal-ownership change.

### Bundle expectation

Qualitative expected reduction: `medium`.

No exact byte claim is accepted before build evidence. Production total JS must not increase against the exact base.

## 8. Parallel safety and integration order

Implementation safety:

- MP01 and MP02 have disjoint production and test leases;
- MP03 has an exclusive Dashboard lease and does not overlap MP01 or MP02;
- all three may start from the same exact base without rebasing.

Recommended integration order:

1. MP01;
2. MP02;
3. MP03.

For each candidate, the Orchestrator must:

1. verify exact remote head and one logical commit;
2. inspect the complete diff and confirm lease compliance;
3. obtain focused test evidence;
4. run or require full frontend tests, typecheck, lint, build, and bundle check;
5. compare production JS against the candidate base;
6. create a draft PR only after independent candidate acceptance;
7. require merge-ref CI against the current `refactor/dashboard-modules` head;
8. merge with a normal merge commit only after review;
9. verify post-merge CI before integrating the next candidate.

Branches from the same immutable base must not be rebased. Current-base merge-ref CI is authoritative.

## 9. Reserved high-conflict paths

Until Wave 1 closes:

- `DashboardPage.tsx` and its main test are reserved exclusively for MP03;
- `marketHealthPresentation.ts` is reserved for MP02 only under its narrow conditional lease;
- router and route modules remain read-only;
- package, lockfile, build, TypeScript, lint, test-runner, CI, and budget files remain read-only;
- ticker files and ticker presentation remain read-only;
- symbol resource, popup resource, API, query-key, adapter, and view-model ownership remain unchanged.

## 10. Later-wave decisions

### MP04

MP04 remains blocked until MP01, MP02, and MP03 are accepted and integrated. It receives the exclusive Dashboard lease for market-health consolidation on the then-current integrated base.

### MP05

MP05 remains after MP04. Before authorization, MP00C must be refreshed against the integrated tree using local unused/export/file/dependency/cycle evidence. The two confirmed export-modifier candidates may disappear naturally through MP01.

### MP06A and MP06B

Both remain blocked. They are test-only and may later run in parallel with disjoint ownership:

- `DashboardPage.test.tsx` belongs to MP06A;
- `DashboardPage.popup.test.tsx` belongs to MP06B.

### MP07 and MP08

Before authorization, run isolated no-commit local experiments in this order:

1. fine-grained Timeline/Recharts renderer boundary;
2. lazy `/anomalies`;
3. lazy `/symbols/:symbol`.

Required evidence includes emitted assets, largest JS, total JS, initial-entry raw/gzip, direct deep links, loading states, query identity, tests, typecheck, lint, build, and unchanged budget.

## 11. Final gate disposition

Wave 0 is complete.

Authorized now:

- P3.5-MP01 implementation;
- P3.5-MP02 implementation;
- P3.5-MP03 implementation;
- three isolated branches from exact SHA `c06082a97254bfa2f6ebd7e29a1ad753c4acc798`;
- independent worker reports and Orchestrator review.

Not authorized:

- merging by workers;
- bundle-budget increase;
- MP04–MP09 implementation;
- Phase 4;
- product redesign, copy change, visual redesign, backend/API change, or ownership changes outside exact leases.

Terminal status:

`P3_5_MP00_COMPLETE_WAVE_1_AUTHORIZED`
