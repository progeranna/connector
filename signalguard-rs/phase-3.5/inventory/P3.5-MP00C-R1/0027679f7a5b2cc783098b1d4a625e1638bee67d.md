# P3.5-MP00C-R1 — Refreshed dead-code, export, dependency, and cycle audit

## 1. Product identity and cleanliness

- Product: progeranna/signalguard-rs
- Phase branch: refactor/dashboard-modules
- Assigned integrated SHA: 0027679f7a5b2cc783098b1d4a625e1638bee67d
- Product checkout: /Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs
- Analysis worktree: /Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp00c-r1
- git fetch origin completed normally.
- origin/refactor/dashboard-modules resolved exactly to 0027679f7a5b2cc783098b1d4a625e1638bee67d.
- Main product checkout was clean before analysis. The detached analysis worktree was created at the assigned SHA and was clean before and after analysis.
- Final analysis-worktree status showed only HEAD (no branch); git diff --stat was empty.
- No product file, package file, lockfile, budget, configuration, source, branch, commit, PR, or merge was changed.

## 2. Tools, versions, and exact commands

Versions captured locally:

- Node v20.20.2; npm 10.8.2
- TypeScript 5.9.3; Vite 4.5.14
- ESLint 8.57.1; Vitest 0.34.6
- Knip 6.31.0; Madge 8.0.0

Identity/worktree commands:

    git fetch origin
    git status --short --branch
    git rev-parse --verify origin/refactor/dashboard-modules
    git worktree add --detach /Users/anion/Desktop/work/git-signalguard-rs/worktrees/p35-mp00c-r1 0027679f7a5b2cc783098b1d4a625e1638bee67d

Results: fetch exit 0; remote SHA exact; product checkout clean; worktree created at exact SHA and clean.

Baseline commands, all from web:

    npm ci
    npm run test:run
    npm run typecheck
    npm run lint
    npm run build
    npm run bundle:check

Results: every command exit 0. npm ci added 463 packages without tracked mutation; tests passed 43 files/629 tests.

Unused checks:

    ./node_modules/.bin/tsc --project tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters --tsBuildInfoFile /tmp/signalguard-p35-mp00c-r1-unused.tsbuildinfo --pretty false
    ./node_modules/.bin/tsc --project tsconfig.node.json --noEmit --noUnusedLocals --noUnusedParameters --tsBuildInfoFile /tmp/signalguard-p35-mp00c-r1-unused-node.tsbuildinfo --pretty false

Both exited 0 with no diagnostics and used only temporary files outside the product repository.

Export/dependency commands:

    npm exec --yes --package=knip -- knip --version
    npm exec --yes --package=knip -- knip --reporter json
    npm exec --yes --package=knip -- knip --include files,dependencies,devDependencies,unlisted,unresolved --reporter json
    npm ls --depth=0 --json

Knip version was 6.31.0. The export report exited 1 with JSON export/type candidates. The include-files/dependencies report exited 0 with {"issues":[]}. npm ls exited 0 with all 24 direct packages present and no extraneous package.

Cycle/orphan commands:

    npm exec --yes --package=madge -- madge --extensions ts,tsx --ts-config tsconfig.json --exclude '(^|/)(test|__tests__)/|\.test\.(ts|tsx)$' --circular src/app src/pages src/features/dashboard src/shared
    npm exec --yes --package=madge -- madge --extensions ts,tsx --ts-config tsconfig.node.json --circular vite.config.ts vitest.config.ts
    npm exec --yes --package=madge -- madge --orphans --extensions ts,tsx --ts-config tsconfig.json --exclude '(^|/)(test|__tests__)/|\.test\.(ts|tsx)$' src

Madge exited 0: 51 production files, no cycle; 2 tooling files, no cycle. Production orphans were dashboardResourceState.ts, main.tsx, Tooltip.tsx, and vite-env.d.ts.

Manual importer evidence used rg over src, index.html, package scripts, configs, and scripts. Dynamic-import search found only test-only vi.importActual("react-router-dom"); no production dynamic candidate reference exists.

## 3. Executable baseline verification

- Typecheck: PASS.
- Lint: PASS with zero warnings/errors.
- Test suite: PASS, 43 files and 629 tests.
- Build: PASS; 714 modules transformed.
- Bundle check: PASS.

Build assets and budget:

- assets/index-a4d88fe2.js: 757217 raw bytes, 739.47 KiB.
- Vite gzip display: 219.99 kB.
- One JS asset; largest JS and total JS are both 757217 bytes.
- Existing largest/total budget: 761856 bytes.
- Headroom: 4639 bytes.

These values match the MP04 integrated evidence at the assigned SHA. The Vite 500 KiB advisory is unchanged and the existing budget was not modified.

## 4. Compiler unused diagnostics

One-shot noUnusedLocals and noUnusedParameters checks passed for both committed TypeScript projects with no diagnostics. No unused local or parameter candidate was found in production, test, script, or configuration TypeScript.

The compiler does not prove unused export modifiers, so export candidates below require the independent Knip, importer, dynamic-reference, and public-entry evidence.

## 5. Exported-symbol inventory

The current export surface was inventoried with rg -n ^export over frontend TypeScript/TSX. Knip candidates were checked individually with exact-word searches. The canonical HealthScore owner introduced by MP01 is imported by MarketHealthDesktopTable.tsx, MarketHealthMobileCards.tsx, and DashboardPage.tsx and is directly tested.

Current candidate groups:

- EmptyBlock at features/dashboard/MarketHealthMobileCards.tsx:120 is locally called only and has no importer.
- fetchDashboardSummary, fetchMarketState, fetchMarketHealth, fetchMarketTimeline, and fetchMarketAnomalies in features/dashboard/api.ts are only called by same-module React Query hooks.
- getApiBaseUrl, buildApiUrl, and parseWithSchema in shared/api/client.ts are only called by same-module fetchJson.
- buildCoveredDashboardSymbols at marketOrder.ts:61 and isMarketCatalogSymbolList at marketCatalog.ts:99 have no caller/importer or dynamic/public reference.
- matrixObservedSymbol is used only within test/marketFixtures.ts.
- installControlledFetch and requestIdentityKey are not imported by any other file; deferredFetch.ts itself is imported for other helpers.
- SemanticDescriptorExpectation and the listed UI smoke types are used only within their fixture module.
- DEMO_MARKETS from marketOrder.ts is a compatibility re-export with no current importer of that path.
- replaceSymbolPopupSymbol is imported and tested by symbolPopup.test.ts but not production code.
- Runtime-mode query/API/schema/type exports remain API/backend/test contract candidates.
- SymbolPopupResourceData, SymbolDetailMetricsSurface, KnownDetectorKey, TimeFactLabel, and ApiErrorResponse remain type/public-contract candidates.

False positives and intentional surfaces:

- MobileSymbolMetric is imported by RecentAnomaliesMobileCards.tsx.
- normalizeSelectedSymbol is imported by AppShell.tsx and DashboardPage.tsx.
- appRoutes is consumed by the runtime router and router.test.tsx.
- useSymbolPopupResource remains used by DashboardPage.tsx and resource/isolation tests.
- The api.ts query-key re-export bridge is retained as an intentional compatibility surface.

## 6. No-importer file inventory

| Path | Evidence | Classification |
|---|---|---|
| src/main.tsx | index.html loads /src/main.tsx; main side-effect imports globals.css | FALSE_POSITIVE entry |
| src/features/dashboard/dashboardResourceState.ts | no production importer; dashboardResourceState.test.ts direct-imports and raw-reads it | PROBABLE test-only/orphan |
| src/shared/components/Tooltip.tsx | no production importer; Tooltip.test.tsx direct-imports it; Recharts Tooltip is different | PROBABLE test-only/orphan |
| src/vite-env.d.ts | Vite ambient declaration contract | INTENTIONAL configuration |
| src/shared/styles/globals.css | side-effect import from main.tsx | FALSE_POSITIVE side-effect entry |
| route page modules | imported by app/router.tsx | FALSE_POSITIVE route entries |

Test helpers were also checked: deferredFetch.ts, marketFixtures.ts, uiSmokeMatrix.ts, and statusDescriptorFixtures.ts have test importers. No generated production file or barrel/index entry was found.

## 7. Dependency inventory

web/package.json contains six runtime and eighteen development dependencies. Knip dependency/file scan returned {"issues":[]}; npm ls showed all 24 direct entries and no extraneous package. Direct inspection found:

| Package group | Evidence/role |
|---|---|
| @tanstack/react-query, react, react-dom, react-router-dom, recharts, zod | providers/API, React runtime/root, router/pages, TimelinePanel chart, schemas/client/errors |
| @testing-library/jest-dom, @testing-library/react, jsdom, vitest | test setup, component/hook tests, browser test environment, test runner |
| @types/node, @types/react, @types/react-dom | Node configs/scripts, TSX/React types, ReactDOM client typing |
| @typescript-eslint/eslint-plugin, @typescript-eslint/parser, eslint, eslint-plugin-react-hooks, eslint-plugin-react-refresh | .eslintrc.cjs parser/plugins/rules and package lint script |
| @vitejs/plugin-react, vite, typescript | Vite/Vitest configs, dev/build scripts, compiler/build |
| autoprefixer, postcss, tailwindcss | postcss.config.cjs, Tailwind config, globals.css processing |

The root Cargo.toml has Rust dependencies only and no relevant JavaScript manifest. No direct dependency deletion candidate is accepted. No package or lockfile change is justified.

## 8. Cycle inventory

Madge 8.0.0 found no production frontend cycle and no Vite/Vitest tooling cycle. Representative graph chains are:

    main -> App -> providers/router -> layout/pages
    DashboardPage -> API/preview/tables/popup/resource/detail owners
    TimelinePanel -> timelineDomains -> timelineNormalization -> types
    symbolPopupResource -> symbolMarketResource -> API/marketOrder/symbolId/types

No runtime, type-only, test-only, or compatibility cycle is asserted. A cycle alone would not prove dead code.

## 9. Reachability and compatibility findings

| Area | Evidence | Classification/action |
|---|---|---|
| Demo/Live UiMode | URL/storage resolution, queries, rendering, tests | INTENTIONAL; retain |
| Availability | observed/configured/awaiting/unavailable schemas and surface rendering | INTENTIONAL; retain all branches |
| Resource states | loading/error/unavailable/success and dashboard resolver empty are emitted/rendered/tested | INTENTIONAL where production-used; dashboard resolver remains PROBABLE |
| Health status | healthy/degraded/unhealthy schema and HealthScore/dashboard tests | INTENTIONAL; retain |
| Anomaly severity | info/warning/critical schemas and presentation classes | INTENTIONAL; retain defensive fallbacks |
| Popup return context | dashboard/symbols/anomalies identities and back/close tests | INTENTIONAL; retain |
| API errors | typed/validation/unknown fallback paths | INTENTIONAL; retain |
| Runtime-mode statuses | active API/schema/cancellation tests but no production UI caller | NEEDS_LOCAL_VERIFICATION |
| buildCoveredDashboardSymbols | no call; only negative source guard remains | CONFIRMED dead declaration |
| isMarketCatalogSymbolList | declaration only | CONFIRMED dead declaration |

MP04 did not wire dashboardResourceState.ts. MP01's new HealthScore owner is reachable and tested. No branch-level deletion is authorized for defensive status, availability, resource, error, or compatibility fallbacks.

## 10. Original MP00C-WEB delta

| Original ID | Current result |
|---|---|
| DEAD-001 HealthScore export | Removed/moved by MP01; new HealthScore owner is production-used. FALSE_POSITIVE/currently superseded. |
| DEAD-002 EmptyBlock | Still present and locally called only. CONFIRMED unchanged. |
| DEAD-003 dashboardResourceState.ts | Still a production orphan with a direct test. PROBABLE unchanged. |
| DEAD-004 Tooltip.tsx | Still a production orphan with a direct test. PROBABLE unchanged. |
| DEAD-005 replaceSymbolPopupSymbol | Still test-only. PROBABLE unchanged. |
| DEAD-006 marketOrder DEMO_MARKETS bridge | Still no bridge importer. PROBABLE unchanged. |
| DEAD-007 runtime-control group | API/schema/query/cancellation contracts remain; no production UI caller. NEEDS_LOCAL_VERIFICATION. |
| DEAD-008 query-key re-exports | Compatibility bridge retained; evidence narrowed for three roots. INTENTIONAL. |
| DEAD-009 normalizeSelectedSymbol | Still production-imported. INTENTIONAL. |
| DEAD-010 popup-resource bridge | Still production-used/tested. INTENTIONAL. |
| DEAD-011 broad type/schema exports | Knip subset exists, but public/API ownership remains uncertain. NEEDS_LOCAL_VERIFICATION. |
| DEAD-012 missing committed unused gates | Both configs still omit flags; one-shot checks pass. CONFIRMED audit fact; no config edit. |
| DEAD-013 MobileSymbolMetric | Direct production importer remains. FALSE_POSITIVE. |
| DEAD-014 entry chain/pages/CSS | HTML/router/side-effect entry remains. FALSE_POSITIVE. |
| DEAD-015 appRoutes | Runtime and route-test consumer remain. INTENTIONAL. |

New local-only candidates are the export-only helpers in api.ts, shared/api/client.ts, test fixtures, marketCatalog.ts, and marketOrder.ts; they were not authoritative candidates in the original web-only audit because local export analysis was not run.

## 11. Complete classified finding table

Each row is one stable finding record. Grouped rows list every affected symbol. CONFIRMED requires tool output plus independent importer/public-entry evidence.

| ID | Exact path/symbol | Classification | Evidence and disposition |
|---|---|---|---|
| R1-C-001 | MarketHealthMobileCards.tsx:120 EmptyBlock | CONFIRMED | same-module calls only; remove export modifier or whole declaration after focused proof |
| R1-C-002 | marketOrder.ts:61 buildCoveredDashboardSymbols | CONFIRMED | no importer/dynamic reference; remove declaration with focused catalog/order validation |
| R1-C-003 | marketCatalog.ts:99 isMarketCatalogSymbolList | CONFIRMED | declaration only; remove with catalog validation |
| R1-C-004 | api.ts:56 fetchDashboardSummary | CONFIRMED | same-module hook only; remove export modifier |
| R1-C-005 | api.ts:69 fetchMarketState | CONFIRMED | same-module hook only; remove export modifier |
| R1-C-006 | api.ts:81 fetchMarketHealth | CONFIRMED | same-module hook only; remove export modifier |
| R1-C-007 | api.ts:93 fetchMarketTimeline | CONFIRMED | same-module hook only; remove export modifier |
| R1-C-008 | api.ts:112 fetchMarketAnomalies | CONFIRMED | same-module hook only; remove export modifier |
| R1-C-009 | test/marketFixtures.ts:53 matrixObservedSymbol | CONFIRMED | same-file use only; remove export modifier |
| R1-C-010 | test/deferredFetch.ts:44,96 installControlledFetch, requestIdentityKey | CONFIRMED | no external importer for these names; remove modifiers; body deletion separately gated |
| R1-C-011 | shared/api/client.ts:20,30,109 getApiBaseUrl, buildApiUrl, parseWithSchema | CONFIRMED | same-file fetchJson calls only; remove modifiers |
| R1-C-012 | test/statusDescriptorFixtures.ts:12 SemanticDescriptorExpectation | CONFIRMED | same-file fixture type only; remove modifier |
| R1-C-013 | test/uiSmokeMatrix.ts:2,5,12,18,19,28,62,65,96 UiSmokeMode, UiSmokeMarket, UiSmokeSurface, UiSmokeViewport, UiSmokeViewportName, UiSmokeState, UiSmokeDialogRequirement, UiSmokeScenarioGroup, UiSmokeNavigationContinuity | CONFIRMED | fixture-internal types; remove modifiers |
| R1-P-001 | symbolPopup.ts:25 replaceSymbolPopupSymbol | PROBABLE | direct test contract; retain pending migration/owner decision |
| R1-P-002 | marketOrder.ts:8 DEMO_MARKETS re-export | PROBABLE | compatibility bridge with no current importer; retain pending confirmation |
| R1-P-003 | dashboardResourceState.ts | PROBABLE | production orphan but direct semantic test/owner uncertainty; retain |
| R1-P-004 | shared/components/Tooltip.tsx | PROBABLE | production orphan but accessibility test/owner uncertainty; retain |
| R1-V-001 | runtimeModeQueryKey, fetchRuntimeMode, useRuntimeModeQuery, runtimeModeSchema, runtimeModeStatusSchema, runtimeModeSourceSchema, RuntimeMode, RuntimeModeStatus, RuntimeModeSource, RuntimeModeResponse | NEEDS_LOCAL_VERIFICATION | backend/API/test contract; no production UI caller; retain |
| R1-V-002 | types.ts:7,12,14,23,35,37,58,60,72,128,174-176 schema/type group | NEEDS_LOCAL_VERIFICATION | API/type contract ownership not proven private; retain |
| R1-V-003 | symbolPopupResource.ts:11 SymbolPopupResourceData | NEEDS_LOCAL_VERIFICATION | exported popup bridge type; retain |
| R1-V-004 | SymbolDetailMetrics.tsx:22 SymbolDetailMetricsSurface | NEEDS_LOCAL_VERIFICATION | route/popup surface type; retain |
| R1-V-005 | statusDescriptors.ts:173 KnownDetectorKey | NEEDS_LOCAL_VERIFICATION | descriptor type predicate contract; retain |
| R1-V-006 | statusDescriptors.ts:182 TimeFactLabel | NEEDS_LOCAL_VERIFICATION | descriptor label contract; retain |
| R1-V-007 | shared/api/errors.ts:8 ApiErrorResponse | NEEDS_LOCAL_VERIFICATION | API/generated error contract; retain |
| R1-I-001 | api.ts:35-43 query-key root re-exports | INTENTIONAL | deliberate compatibility bridge; retain |
| R1-I-002 | app/router.tsx:8 appRoutes | INTENTIONAL | runtime router and router.test.tsx use; retain |
| R1-I-003 | selectedSymbol.ts:6 normalizeSelectedSymbol | INTENTIONAL | AppShell and DashboardPage production use; retain |
| R1-I-004 | symbolPopupResource.ts/useSymbolPopupResource | INTENTIONAL | DashboardPage and isolation/resource tests use; retain |
| R1-F-001 | old MarketHealthDesktopTable.tsx HealthScore export | FALSE_POSITIVE | superseded by MP01 canonical owner; reject |
| R1-F-002 | MarketHealthMobileCards.tsx:106 MobileSymbolMetric | FALSE_POSITIVE | RecentAnomaliesMobileCards.tsx imports it; retain |
| R1-F-003 | index.html/main.tsx/routes/globals.css | FALSE_POSITIVE | entry, route, or CSS side effect; retain |

Finding-record totals: CONFIRMED 13, PROBABLE 4, NEEDS_LOCAL_VERIFICATION 7, INTENTIONAL 4, FALSE_POSITIVE 3. Confirmed rows cover 22 grouped export bindings plus 2 dead declarations; totals count stable records.

## 12. MP05 confirmed candidate set

Only R1-C-001 through R1-C-013 are eligible for a future MP05 lease. MP05 is not authorized by this report.

Exact proposed paths and actions:

- MarketHealthMobileCards.tsx: remove EmptyBlock export modifier; body only after focused proof.
- marketOrder.ts: remove buildCoveredDashboardSymbols and private dead helper only after catalog/order tests and source-guard review.
- marketCatalog.ts: remove isMarketCatalogSymbolList.
- api.ts: remove export modifiers from the five internal fetch functions.
- test/marketFixtures.ts: remove matrixObservedSymbol export modifier.
- test/deferredFetch.ts: remove the two unused export modifiers; consider body removal only with focused proof.
- shared/api/client.ts: remove export modifiers from getApiBaseUrl, buildApiUrl, parseWithSchema.
- test/statusDescriptorFixtures.ts and test/uiSmokeMatrix.ts: remove listed test-only export modifiers.

Required focused tests are the corresponding market catalog/order, dashboard/API, client, isolation, and fixture suites. Full validation is npm ci, npm run test:run, npm run typecheck, npm run lint, npm run build, and npm run bundle:check. Re-run importer/dynamic/public-entry checks before any write. Candidate-level raw-JS effect is unmeasured; no byte estimate is invented.

## 13. Rejected candidates and false positives

Reject the old HealthScore deletion, MobileSymbolMetric, appRoutes, route pages, main.tsx, globals.css, all 24 package deletions, all cycle-based deletions, all defensive availability/resource/status/severity/API fallbacks, normalizeSelectedSymbol, the popup-resource bridge, runtime-control/schema/type deletion, dashboardResourceState whole-file deletion, Tooltip whole-file deletion, replaceSymbolPopupSymbol deletion, and the DEMO_MARKETS bridge deletion.

No product file was deleted or modified.

## 14. Proposed exact MP05 writable lease

No lease is authorized by this worker. If separately authorized by the Orchestrator, restrict the lease to the exact confirmed paths listed in section 12. Exclude all probable/verification-required items, manifests, lockfiles, configs, budgets, routes, production behavior, Phase 4, and MP06/Wave 3 work. Revalidate against the then-current head first.

## 15. Validation matrix for MP05

| Candidate | Focused validation | Full validation |
|---|---|---|
| export-only modifiers | exact importer scan, compiler, owner tests | test:run, typecheck, lint, build, bundle:check |
| dead market declarations | catalog/order tests and negative source-guard review | same full frontend gates and raw/gzip comparison |
| test fixtures | corresponding fixture/isolation tests | same full frontend gates |
| package/lockfile proposal | separate dependency justification | not part of this lease |

The existing 761856-byte budget must remain unchanged and production raw total must not increase.

## 16. Unresolved uncertainties

1. Whether dashboardResourceState.ts is a future owner or a test-only orphan.
2. Whether Tooltip.tsx is a retained accessibility primitive or a test-only orphan.
3. Whether replaceSymbolPopupSymbol is an intentional testable identity API.
4. Whether the marketOrder DEMO_MARKETS bridge has an external/internal compatibility consumer.
5. Whether runtime-mode exports are supported backend/public contract surfaces despite no production UI caller.
6. Whether out-of-repository consumers exist; no package exports or dynamic references were found locally.
7. Candidate-level raw-JS effects are unmeasured.

These are explicitly excluded from the confirmed candidate set.

## 17. Terminal status

P3_5_MP00C_R1_AUDIT_COMPLETE

Summary:

- Exact product SHA: 0027679f7a5b2cc783098b1d4a625e1638bee67d
- Findings by classification: CONFIRMED 13; PROBABLE 4; NEEDS_LOCAL_VERIFICATION 7; FALSE_POSITIVE 3; INTENTIONAL 4
- Confirmed MP05 candidates: 13 finding records
- Package/lockfile changes justified: no
- Baseline: 757217 raw JS; 219.99 kB Vite gzip display; 761856 budget; 4639 headroom
- Report path: signalguard-rs/phase-3.5/inventory/P3.5-MP00C-R1/0027679f7a5b2cc783098b1d4a625e1638bee67d.md
- Connector commit SHA: verified after publication and returned in the worker terminal response; it is not embedded in the content-addressed report itself.
- No product write, branch, commit, PR, or merge occurred

This report does not implement MP05, authorize MP06/Wave 3/Phase 4, or modify STATUS.md.
