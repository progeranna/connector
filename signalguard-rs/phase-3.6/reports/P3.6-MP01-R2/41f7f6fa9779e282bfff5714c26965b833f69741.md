# P3.6-MP01-R2 implementation report

Status: `P3_6_MP01_R2_IMPLEMENTATION_COMPLETE`

## Contract identity

- Original `P3.6-MP01`: commit `7936a232ed9e9c7ce15547d8272b8f73de6c5c15`; blob `fcdae3da63bc20db2b79b52733443e1d5a939c0e`.
- Recovery `P3.6-MP01-R1`: commit `f579d0aaaef64d2e3466bc71df13f2f503c319c7`; blob `c338abb0540e03b8ef71de840cb261b105e0499f`.
- Recovery `P3.6-MP01-R2`: commit `68e95962f648c186a8bcb10840f2e964f7e9e839`; blob `5018e644450f9d881ef01ac5114831ef79d80cda`.
- R2 authorization status was verified as `P3_6_MP01_R2_IMPLEMENTATION_AUTHORIZED`.

## Recovery history and preserved state

The original MP01 implementation reached a lease blocker because three collection-time consumer guards still read or asserted the deleted standalone Symbol Detail page, but those test paths were outside the original lease. R1 authorized `marketNavigationIsolation.test.tsx`, `selectedSymbolConsumers.test.ts`, and `marketCatalogConsumers.test.ts`; their obsolete standalone-route/page reads were replaced while the selected-symbol, catalog, raw-summary, query-ownership, no-fallback, and mode-isolation guards remained strong.

R1 then exposed the second validated lease blocker: the complete suite reached `590 passed / 1 failed`, where `DashboardPage.popup.test.tsx` still expected an All Anomalies row to create a Symbol Detail identity and return from Symbol Detail. That file was outside the R1 lease. R2 authorized exactly that additional test path, and the obsolete popup test was replaced with exact anomaly-UUID detail and Back-stack coverage.

Before the R2 edit, the retained product worktree was verified without resetting or reconstructing it:

- HEAD `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`;
- tree `0ad193d7aca4b668ba51232b7bf8362ddd066e10`;
- branch `fix/modal-only-detail-navigation`;
- ahead/behind `0 / 0`;
- existing uncommitted implementation preserved;
- `git diff --check` passed and every existing changed path was already in the complete R2 lease.

Only the retained worktree was used. No reset, restore, clean, stash-drop, worktree removal, implementation reconstruction, rebase, amend, squash, force-push, or history rewrite was performed.

## Product identity and route result

- Immutable parent: `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`.
- Product commit: `41f7f6fa9779e282bfff5714c26965b833f69741`.
- Product tree: `f629b6ea4339c92d03223c3bd8024cd4cb4571da`.
- Commit subject: `fix(ui): restore modal-only detail navigation`.
- Ahead/behind immutable parent: `1 / 0`.
- The commit has one parent and is not a merge commit.

Route inventory changed from:

- `/` -> Dashboard;
- `/dashboard` -> Dashboard;
- `/symbols/:symbol` -> standalone/canonical Symbol Detail visual route;
- `/anomalies` -> standalone Anomalies visual page;

to:

- `/` -> Dashboard;
- `/dashboard` -> Dashboard;
- `/symbols/:symbol` -> replacement redirect to `/dashboard`;
- `/anomalies` -> replacement redirect to `/dashboard`.

Dashboard is now the only visual console page. Symbol Detail, All Anomalies, and Anomaly Detail are Dashboard-owned dialogs. Header symbol selection updates mode-scoped Dashboard selection without navigating or synchronizing modal state into the URL.

## Exact changed and deleted paths

All 26 paths are within the complete R2 lease: 22 original Section 6 paths, the three R1 test paths, and the one R2 popup-test path. No package, lockfile, Rust, API-contract, Docker, script, CI, Vite, Vitest, CSS, Timeline, ticker, or query/resource-ownership path changed.

Modified:

- `web/src/app/AppShell.tsx`
- `web/src/app/marketNavigationIsolation.test.tsx` (R1)
- `web/src/app/router.test.tsx`
- `web/src/app/router.tsx`
- `web/src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx`
- `web/src/features/dashboard/RecentAnomaliesDesktopTable.tsx`
- `web/src/features/dashboard/RecentAnomaliesMobileCards.test.tsx`
- `web/src/features/dashboard/RecentAnomaliesMobileCards.tsx`
- `web/src/features/dashboard/SymbolDetailAnomalies.test.tsx`
- `web/src/features/dashboard/SymbolDetailAnomalies.tsx`
- `web/src/features/dashboard/SymbolDetailHeader.test.tsx`
- `web/src/features/dashboard/SymbolDetailHeader.tsx`
- `web/src/features/dashboard/SymbolDetailMetrics.test.tsx`
- `web/src/features/dashboard/SymbolDetailMetrics.tsx`
- `web/src/features/dashboard/marketCatalogConsumers.test.ts` (R1)
- `web/src/features/dashboard/selectedSymbolConsumers.test.ts` (R1)
- `web/src/pages/DashboardPage.popup.test.tsx` (R2)
- `web/src/pages/DashboardPage.test.tsx`
- `web/src/pages/DashboardPage.tsx`
- `web/src/test/uiSmokeMatrix.test.ts`
- `web/src/test/uiSmokeMatrix.ts`

Deleted:

- `web/src/app/CanonicalSymbolRoute.tsx`
- `web/src/app/symbolRoutePath.ts`
- `web/src/pages/AnomaliesPage.tsx`
- `web/src/pages/SymbolDetailPage.test.tsx`
- `web/src/pages/SymbolDetailPage.tsx`

`git diff --check`, the complete diff review, name-status audit, and lease comparison passed before the commit. The committed product worktree is clean.

## Implementation and test recovery

Anomaly activation now passes an exact accepted UUID and resolves the active object from the current mode's Dashboard summary. The detail dialog shows only symbol, anomaly type, severity, message, observed value, threshold value, event time, created time, and UUID; zero is preserved and null uses the established em dash. Two BTC anomalies with different UUIDs are independently selectable. Anomaly activation does not create a `SymbolPopupIdentity`.

All Anomalies rows/cards open Anomaly Detail. Back restores All Anomalies and its exact row focus; Close closes the stack. Mode changes cannot retain stale anomaly detail. The existing accessible Dashboard dialog supplies Close, Escape, backdrop, body scroll lock, initial focus, focus containment, and focus return. Deterministic browser validation found that responsive controls hidden at the active breakpoint could still be considered by the modal's raw selector; the focusable set now filters elements without client rectangles, preventing focus from dropping to the document body while retaining the existing dialog boundary.

Focused R2 command result:

- 6 files passed;
- 62 tests passed;
- 0 failed.

The focused set covered `DashboardPage.popup.test.tsx`, `DashboardPage.test.tsx`, `router.test.tsx`, `marketNavigationIsolation.test.tsx`, and both Recent Anomalies desktop/mobile suites.

Final Vitest result:

- baseline: 45 files / 653 tests;
- final: 44 files / 591 tests;
- exact delta: -1 file / -62 tests;
- all 44 files and all 591 tests passed.

The removed test file was the deleted standalone `SymbolDetailPage.test.tsx`. Route-only Symbol Detail branches, canonicalization/presentation, route-to-route, route-popup parity, route surfaces, and standalone Anomalies expectations were also removed from leased suites. Their replacements cover Dashboard-only routing, replacement redirects, header selection without navigation, popup-only shared components, exact anomaly UUID activation, same-symbol UUID independence, accepted detail fields and null/zero semantics, mouse/Enter/Space/mobile activation, dialog lifecycle, All Anomalies -> Anomaly Detail -> Back, stale-mode closure, popup identity, and retained Timeline/ticker/market/data-isolation behavior. The suite was not padded to preserve the old numeric count.

## Frontend and bundle gates

The required sequence passed after the final production focus correction:

- `npm ci`;
- `node --test scripts/check-bundle-size.test.mjs`: 25/25 passed;
- `npm run test:run`: 44 files / 591 tests passed;
- `npm run typecheck`;
- `npm run lint` with zero warnings;
- `npm run build`;
- `npm run bundle:check`, including its repeated 25/25 policy tests.

Final production bundle:

- transformed modules: 134;
- initial JS assets: 1;
- async JS assets: 0;
- asset: `assets/index-8f56a1c6.js`;
- initial raw JS: 387,239 bytes;
- largest raw JS: 387,239 bytes;
- total raw JS: 387,239 bytes;
- direct gzip (`gzip -9 -c`): 110,799 bytes;
- strict 395,700-byte raw limit headroom: 8,461 bytes;
- 113,042-byte gzip limit headroom: 2,243 bytes;
- fixed budgets remained `409600 / 409600 / 414720`, with headroom `22361 / 22361 / 27481` bytes.

The manifest has one unique initial module asset and no duplicate emitted asset/module identifier. There is no production Recharts import, no new dependency, and no package or lockfile change.

## Repository gates

All required repository commands passed:

- `cargo fmt --check`;
- `cargo run --quiet --bin export-api-contract -- --check`;
- `cargo run --quiet --bin export-api-contract -- --validate`;
- `cargo check`;
- `cargo clippy --all-targets --all-features -- -D warnings`;
- `cargo test`: 379 passed, 0 failed, 3 ignored in the library target; service-backed integration cases remained explicitly ignored as designed;
- `cargo test --test replay_e2e`: 0 failed, 2 explicitly ignored because isolated PostgreSQL/Redis were not started;
- `docker compose config`;
- `docker compose --profile app config`;
- `bash -n scripts/demo-replay.sh`;
- `bash -n scripts/smoke.sh`.

The backend `/anomalies` API and every Rust/API/Docker/script file remained unchanged. The manual-QA environment and its ports `5173`, `8080`, `5432`, and `6379` were not used or stopped.

## Browser and screenshot evidence

An isolated deterministic production-build server ran at `127.0.0.1:18136` with accepted mock API payloads. Google Chrome `150.0.7871.188` was driven at 1440x1100 desktop and 390x844 mobile viewports. The matrix passed:

- Dashboard without overlay;
- exact market -> Symbol Detail by click, Enter, and Space;
- Recent Anomalies -> exact Anomaly Detail;
- View All -> All Anomalies;
- All Anomalies -> exact Anomaly Detail -> Back;
- two same-symbol anomalies distinguished by UUID;
- no Symbol Detail identity from anomaly activation;
- Close, Escape, backdrop, initial focus, containment, and trigger/row focus return;
- Demo -> Live stale-detail closure and Demo restoration;
- header BTC/ETH transition without navigation;
- `/symbols/BTCUSDT`, `/symbols/ETHUSDT`, mixed-case `/symbols/bTcUsDt`, and `/anomalies` replacement redirects to `/dashboard` without obsolete visual content;
- zero unexpected browser-console errors, page errors, or unhandled rejections.

Observed API request counts were 6 Demo summary requests, 1 Live summary request, 6 Demo BTC timeline requests, 1 Live BTC timeline request, and 2 Demo ETH timeline requests. Static navigation counts were one request per tested legacy symbol path and one `/anomalies` path request. No stale-content assertion failed.

Eight fresh screenshots were captured, hashed, and then removed with the temporary browser harness before committing:

- desktop Dashboard: 177,658 bytes; SHA-256 `8b66cbd77d7e56e9bcc95547b26086e7106bf439199852ed0914d42e8703546b`;
- desktop Symbol Detail: 137,416 bytes; SHA-256 `d6b4629e0a4bff8ca02b7a50b1a63ce6cd5c712945c4bc996e8fa7992975a312`;
- desktop All Anomalies: 170,312 bytes; SHA-256 `f753a18a91e4e92e5eee2332fd90e6003c18f606d1b45058794f4de1d6f309df`;
- desktop Anomaly Detail: 108,257 bytes; SHA-256 `44f6adfc098916dc74eb32deb079efc1c3a7e4a30733851cdb82f771a63b4a9c`;
- mobile Dashboard: 39,048 bytes; SHA-256 `61e76a186ff5015c7a6572f0531462718cf5d9695150baa7599bd994d70cc66f`;
- mobile Symbol Detail: 39,175 bytes; SHA-256 `2861c4839fbe28676c5b1b8bd941129ce678b7fe7a4f934bac5983f485f473b6`;
- mobile All Anomalies: 46,477 bytes; SHA-256 `756db7e435cce3f71bf05f12a14ecd282efebaba63c7666e1a45be5a4f95b51f`;
- mobile Anomaly Detail: 39,491 bytes; SHA-256 `7e6491a7404d769809a727d043440cd71eb7d55baa72ffccb2afb80df01aa96e`.

## Residual, publication, and process audit

The active `web/src` production and test tree has no residual occurrence of:

- `SymbolDetailPage`;
- `AnomaliesPage`;
- `CanonicalSymbolRoute`;
- `canonicalSymbolRoutePath`;
- `symbol-detail-route`;
- `route-popup-parity`;
- `route-to-route`;
- `View sample symbol route`.

Compatibility route strings remain only in the router implementation/redirect tests and the purpose-built navigation-isolation assertion. Historical connector records and the Phase 3.5 archive were not modified.

Remote product branch `fix/modal-only-detail-navigation` was verified at `41f7f6fa9779e282bfff5714c26965b833f69741`, with exact parent `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27` and the exact 26-path lease-compliant change set above. The final product worktree is clean. No pull request, merge, tag, or force-push was created.
