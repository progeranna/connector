# P3.6-MP01-R2 — Popup-test lease recovery for modal-only navigation

Status: `P3_6_MP01_R2_IMPLEMENTATION_AUTHORIZED`

## 1. Purpose and authority

This recovery addendum resolves a second validated writable-lease conflict in:

- `signalguard-rs/phase-3.6/prompts/P3.6-MP01.md`;
- `signalguard-rs/phase-3.6/prompts/P3.6-MP01-R1.md`.

Original contract commit:

`7936a232ed9e9c7ce15547d8272b8f73de6c5c15`

Original contract blob:

`fcdae3da63bc20db2b79b52733443e1d5a939c0e`

R1 recovery commit:

`f579d0aaaef64d2e3466bc71df13f2f503c319c7`

R1 recovery blob:

`c338abb0540e03b8ef71de840cb261b105e0499f`

The worker correctly stopped with:

`P3_6_MP01_R1_BLOCKED_BY_VALIDATION`

because one active popup test outside the R1-expanded lease still requires the obsolete All Anomalies to Symbol Detail interaction.

This R2 addendum is authoritative only for:

- one additional writable test path;
- the required replacement coverage in that test;
- preservation of the current uncommitted implementation state;
- the recovery report path and terminal status.

Every other product invariant, deletion, prohibition, validation command, browser requirement, Git rule, branch identity, product base, commit message, and delivery rule from P3.6-MP01 and P3.6-MP01-R1 remains authoritative.

## 2. Validated blocker

At exact immutable product base:

`09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

with tree:

`0ad193d7aca4b668ba51232b7bf8362ddd066e10`

baseline file:

`web/src/pages/DashboardPage.popup.test.tsx`

contains a test named:

`returns from a BTC popup to All anomalies`

The test:

1. opens `All anomalies`;
2. activates a control labeled `Open BTCUSDT market detail`;
3. expects a Symbol Detail popup identity with:
   - mode `demo`;
   - symbol `BTCUSDT`;
   - returnContext `anomalies`;
4. expects `Back to all anomalies` from Symbol Detail.

That behavior is obsolete and conflicts with the binding product-owner correction:

- anomaly activation opens exact UUID-keyed Anomaly Detail;
- anomaly activation must not automatically open Symbol Detail;
- Back from Anomaly Detail opened from All Anomalies restores All Anomalies.

The worker's reported validation state is accepted as the recovery starting point:

- required focused R1 suite: 5 files / 45 tests passed;
- full suite: 43 files passed, 1 failed;
- tests: 590 passed, 1 failed;
- sole failure: `DashboardPage.popup.test.tsx` obsolete All Anomalies to Symbol Detail assertion;
- typecheck: passed;
- `git diff --check`: passed;
- forbidden residual audit: clean;
- no product commit, push, connector report, PR, or merge created.

This is a second contract lease defect, not an implementation defect.

## 3. Retained implementation state

Product repository:

`progeranna/signalguard-rs`

Local repository:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Assigned branch:

`fix/modal-only-detail-navigation`

Assigned worktree:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p36-mp01`

Exact immutable base and current HEAD:

`09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

Expected HEAD tree:

`0ad193d7aca4b668ba51232b7bf8362ddd066e10`

Required product commit remains:

`fix(ui): restore modal-only detail navigation`

The current implementation is intentionally uncommitted. Do not reset, restore, clean, checkout over, stash-and-drop, recreate, or remove the worktree. Preserve all current in-lease modifications.

Before continuing, verify:

- HEAD remains the exact immutable base;
- ahead/behind remains `0 / 0`;
- there are uncommitted changes only in the R2-complete writable lease;
- `git diff --check` remains clean.

If any changed path lies outside the complete lease, stop and report it. Do not discard it automatically.

## 4. Complete writable product lease

The complete writable lease is:

1. every path in Section 6 of `P3.6-MP01.md`;
2. the three paths added by `P3.6-MP01-R1.md`;
3. exactly one additional path:

`web/src/pages/DashboardPage.popup.test.tsx`

No other path is added.

A strict subset remains permitted when a leased path requires no change.

This R2 recovery adds no production scope and authorizes no backend, API, query, package, lockfile, CSS, configuration, CI, Docker, script, Timeline, ticker, bundle-policy, or Phase 4 modification.

## 5. Required treatment of DashboardPage.popup.test.tsx

Preserve all still-valid Symbol Detail popup tests and popup identity tests.

Replace only obsolete anomaly-to-symbol expectations with modal-only anomaly-detail behavior.

At minimum, the updated test coverage must prove:

1. opening All Anomalies still renders the `All anomalies` dialog;
2. activating an anomaly row/card uses the exact accepted anomaly UUID;
3. activation opens Anomaly Detail, not Symbol Detail;
4. accessible activation text describes anomaly detail, not market detail;
5. the opened detail corresponds to the exact UUID even when another anomaly has the same symbol;
6. the Anomaly Detail dialog displays accepted fields for that exact anomaly;
7. no new SymbolPopupIdentity is created by anomaly activation;
8. a Back action from Anomaly Detail restores the All Anomalies dialog;
9. closing after returning preserves expected focus behavior already owned by the Dashboard dialog boundary;
10. no assertion requires `returnContext: "anomalies"` on Symbol Detail.

Use the current implementation's exact accessible names where they satisfy the original contracts. Do not weaken accessibility assertions merely to pass.

Prefer a test name equivalent to:

`returns from exact anomaly detail to All anomalies`

The test must identify the anomaly by UUID, not only by symbol, label position, array index, message, or timestamp.

If the existing fixtures contain only one anomaly for the symbol, add or adapt test fixture data within this leased test file so two anomalies can share a symbol while retaining different UUIDs. Do not change production fixtures or unleased test helpers.

Do not delete valid Symbol Detail coverage for:

- direct Dashboard market activation;
- All Markets to Symbol Detail;
- Symbol Detail loading/error/unavailable/success states;
- exact mode/symbol popup identity;
- Back to All Markets.

## 6. Focused validation

Run the updated popup test together with direct owners before the full suite:

```bash
cd /Users/anion/Desktop/work/git-signalguard-rs/worktrees/p36-mp01/web

npm run test:run -- \
  src/pages/DashboardPage.popup.test.tsx \
  src/pages/DashboardPage.test.tsx \
  src/app/router.test.tsx \
  src/app/marketNavigationIsolation.test.tsx \
  src/features/dashboard/RecentAnomaliesDesktopTable.test.tsx \
  src/features/dashboard/RecentAnomaliesMobileCards.test.tsx
```

If `test:run` does not forward file arguments, invoke the existing Vitest binary with the repository's protected thread settings and exact files. Do not modify `package.json`.

Then rerun every complete validation requirement from P3.6-MP01 and P3.6-MP01-R1, including:

- Node bundle-policy tests;
- full Vitest suite;
- typecheck;
- lint;
- production build;
- bundle check and direct gzip;
- all Rust, API-contract, Docker and shell gates;
- deterministic browser matrix;
- fresh desktop/mobile screenshots;
- forbidden residual audit;
- exact lease audit;
- clean final worktree after commit.

The prior partial test result is not final evidence. All required gates must be rerun after this test correction.

## 7. Git and delivery

Continue using the existing uncommitted worktree and branch.

Create exactly one product commit only after every required validation passes:

`fix(ui): restore modal-only detail navigation`

The commit must have exact parent:

`09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

The branch must then be exactly one commit ahead and zero behind the immutable base.

Push only:

`fix/modal-only-detail-navigation`

Do not open a pull request. Do not merge. Do not amend, rebase, reset, squash, force-push, tag, or rewrite history.

Publish the recovery report at:

`signalguard-rs/phase-3.6/reports/P3.6-MP01-R2/<PRODUCT_COMMIT_SHA>.md`

Required connector report commit message:

`docs(phase-3.6): publish MP01 R2 recovery report`

The report must include:

- original, R1, and R2 contract commit/blob identities;
- both validated lease blockers;
- retained uncommitted-state verification;
- exact changed/deleted path list;
- proof that the complete R2 lease was not exceeded;
- focused popup/recovery test results;
- final full Vitest file/test count and exact delta from `45 / 653`;
- explanation of obsolete route/popup assertions removed and modal-only assertions added;
- frontend, bundle, Rust, API, Docker, script, browser and screenshot evidence;
- forbidden residual audit;
- product branch/parent/ahead-behind verification;
- clean worktree state;
- no-PR/no-merge confirmation.

## 8. Terminal statuses

Return:

`P3_6_MP01_R2_IMPLEMENTATION_COMPLETE`

only after implementation, all gates, the single product commit, product push, connector report publication, and remote verification succeed.

Return:

`P3_6_MP01_R2_BLOCKED_BY_VALIDATION`

when another required validation fails or another genuine out-of-lease dependency prevents completion. Do not create a product commit after a failed required gate.
