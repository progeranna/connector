# P3.6-MP01-R1 — Test-lease recovery for modal-only navigation

Status: `P3_6_MP01_R1_IMPLEMENTATION_AUTHORIZED`

## 1. Purpose and authority

This recovery addendum resolves a validated writable-lease conflict in the authoritative contract:

`signalguard-rs/phase-3.6/prompts/P3.6-MP01.md`

Original contract commit:

`7936a232ed9e9c7ce15547d8272b8f73de6c5c15`

Original contract blob:

`fcdae3da63bc20db2b79b52733443e1d5a939c0e`

The original worker correctly stopped with:

`P3_6_MP01_BLOCKED_BY_VALIDATION`

because three active baseline tests outside the original lease depend on standalone symbol-route implementation that the contract requires deleting.

This addendum is authoritative only for:

- the writable product lease;
- the required treatment of the three newly leased test files;
- the recovery report path and terminal status.

Every other requirement, invariant, prohibition, exact product identity, validation command, browser requirement, Git rule, and delivery rule in `P3.6-MP01.md` remains authoritative.

## 2. Validated blocker

At exact immutable base:

`09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

with tree:

`0ad193d7aca4b668ba51232b7bf8362ddd066e10`

the following active tests conflict with the required deletion of standalone symbol pages:

1. `web/src/app/marketNavigationIsolation.test.tsx`
   - imports `CanonicalSymbolRoute`;
   - creates `/symbols/:symbol` routes;
   - tests route-to-route symbol navigation, route heading, route normalization, and explicit invalid-route presentation.

2. `web/src/features/dashboard/selectedSymbolConsumers.test.ts`
   - reads `src/pages/SymbolDetailPage.tsx` during module collection;
   - includes that deleted page in source-level storage API guards.

3. `web/src/features/dashboard/marketCatalogConsumers.test.ts`
   - reads `src/pages/SymbolDetailPage.tsx` during module collection;
   - includes that deleted page in catalog-hook and no-fallback source guards.

Keeping those dependencies would violate the binding modal-only UX, mandatory deletions, and forbidden-residual audit. Editing them was impossible under the original exact lease. The blocker is accepted as a contract defect, not an implementation defect.

## 3. Retained implementation identity

Product repository:

`progeranna/signalguard-rs`

Local product repository:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Exact immutable base:

`09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

Expected base tree:

`0ad193d7aca4b668ba51232b7bf8362ddd066e10`

Assigned branch remains:

`fix/modal-only-detail-navigation`

Retained worktree remains:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p36-mp01`

Required single product commit remains:

`fix(ui): restore modal-only detail navigation`

The branch must remain exactly one commit ahead and zero behind the immutable base after implementation.

Do not create a second product branch or worktree. Continue in the retained clean worktree only after independently proving it is still clean, at the exact base, and zero commits ahead/behind.

## 4. Expanded writable product lease

The complete writable lease is the original Section 6 lease plus exactly these three test files:

- `web/src/app/marketNavigationIsolation.test.tsx`
- `web/src/features/dashboard/selectedSymbolConsumers.test.ts`
- `web/src/features/dashboard/marketCatalogConsumers.test.ts`

No other path is added.

A strict subset remains permitted when a leased path requires no change.

All production scope remains exactly as defined by the original contract. This recovery does not authorize additional production files, backend changes, query changes, package changes, CSS changes, CI changes, or Phase 4 work.

## 5. Required treatment of newly leased tests

### 5.1 `marketNavigationIsolation.test.tsx`

Remove all ownership and expectations for standalone symbol-detail routes, including:

- importing `CanonicalSymbolRoute`;
- registering `/symbols/:symbol` as a visual detail route;
- route-to-route BTC/ETH detail navigation;
- mixed-case route canonicalization into another symbol route;
- invalid-route standalone market presentation;
- route headings and route-detail data assertions.

Replace them with behavior-level coverage that remains meaningful under the modal-only product model. At minimum prove:

- AppShell header symbol selection changes the active Dashboard selection without navigating to `/symbols/*`;
- the pathname remains `/` or `/dashboard` after symbol selection;
- Demo/Live selected-symbol storage remains mode-scoped;
- no standalone detail heading or route-only content is rendered;
- legacy symbol paths are covered only by the router redirect tests owned by `router.test.tsx`, not recreated as a second route harness here.

Do not duplicate the complete router redirect matrix already required in the original contract.

### 5.2 `selectedSymbolConsumers.test.ts`

Remove the deleted `SymbolDetailPage.tsx` source read and every assertion that requires it as an active selected-symbol consumer.

Retain meaningful guards for the actual remaining consumers:

- `AppShell.tsx`;
- `DashboardPage.tsx`;
- active selected-symbol storage helpers and mode isolation.

The test must still prevent reintroduction of the legacy one-argument storage API in active consumers. Do not weaken the storage guard merely to make the suite pass.

### 5.3 `marketCatalogConsumers.test.ts`

Remove the deleted `SymbolDetailPage.tsx` source read and every assertion that treats it as an active catalog consumer.

Retain meaningful guards for the active consumers:

- `AppShell.tsx`;
- `DashboardPage.tsx`;
- `GlobalMarketTicker.tsx`;
- `api.ts`.

Continue to prove:

- the ticker owns the raw dashboard summary hook;
- mode-aware Dashboard consumers use the catalog summary hook;
- no client-side Demo coverage or fallback is reintroduced;
- raw and catalog hooks preserve accepted server summary identity.

Do not weaken no-fallback or query-ownership assertions.

## 6. Validation requirements

All original `P3.6-MP01` validation requirements remain mandatory.

Additionally, before the full suite, run focused tests covering all newly leased files and the directly related route/Dashboard files. Use the repository's normal Vitest command and exact file paths, for example:

```bash
npm run test:run -- \
  src/app/marketNavigationIsolation.test.tsx \
  src/app/router.test.tsx \
  src/features/dashboard/selectedSymbolConsumers.test.ts \
  src/features/dashboard/marketCatalogConsumers.test.ts \
  src/pages/DashboardPage.test.tsx
```

If the protected `test:run` script does not forward path arguments in this repository, invoke the existing Vitest binary with the same protected thread settings and exact files; do not change `package.json`.

Then run the complete original frontend, bundle, Rust, API-contract, Docker, shell, browser, screenshot, residual-audit, scope, Git, publication, and cleanup gates.

The final active source must not import or read deleted standalone page files. The residual audit must include the three recovered tests.

Baseline remains:

- `45` Vitest files;
- `653` Vitest tests;
- all passing.

The final file/test count may decrease because obsolete standalone-route suites are removed and may increase because modal-only behavior is added. The recovery report must provide an exact count delta and explain it. Meaningless count padding is forbidden.

## 7. Delivery and report

Use the same product branch and required product commit message from the original contract.

Do not open a pull request. Do not merge. Do not amend, rebase, reset, squash, force-push, tag, or rewrite history.

Publish the recovery implementation report at:

`signalguard-rs/phase-3.6/reports/P3.6-MP01-R1/<PRODUCT_COMMIT_SHA>.md`

Required connector report commit message:

`docs(phase-3.6): publish MP01 recovery report`

The report must include:

- original blocker disposition;
- this recovery contract commit and blob;
- exact product parent/base and resulting commit;
- complete changed/deleted path list;
- proof that only the expanded lease was used;
- focused recovered-test results;
- full final Vitest file/test count and delta from `45 / 653`;
- all original validation and browser evidence;
- bundle metrics;
- residual audit;
- clean worktree and no-PR/no-merge confirmation.

## 8. Terminal statuses

Return:

`P3_6_MP01_R1_IMPLEMENTATION_COMPLETE`

only after the implementation, all original gates, recovered tests, single product commit, product push, connector recovery report, and independent write verification succeed.

Return:

`P3_6_MP01_R1_BLOCKED_BY_VALIDATION`

when any required gate fails or another genuine out-of-lease dependency prevents completion. Do not create the product commit after a failed required validation.
