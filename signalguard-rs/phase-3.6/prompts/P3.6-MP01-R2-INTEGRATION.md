# P3.6-MP01-R2 — Modal-only navigation integration

Status: `P3_6_MP01_R2_INTEGRATION_AUTHORIZED`

## 1. Purpose and authority

This contract authorizes PR-based integration of the independently reviewed P3.6 modal-only frontend correction.

It does not authorize any further product implementation or source modification. The worker commit is immutable for this integration.

Authoritative implementation contracts:

- `signalguard-rs/phase-3.6/prompts/P3.6-MP01.md`
- `signalguard-rs/phase-3.6/prompts/P3.6-MP01-R1.md`
- `signalguard-rs/phase-3.6/prompts/P3.6-MP01-R2.md`

Authoritative implementation report:

`signalguard-rs/phase-3.6/reports/P3.6-MP01-R2/41f7f6fa9779e282bfff5714c26965b833f69741.md`

Report commit:

`acdbbe47e9567e08474c4829bf3718f107f392bb`

Report blob:

`a82dac068c087fd1f13c1066e3c749bd52bd3dd4`

The report status is:

`P3_6_MP01_R2_IMPLEMENTATION_COMPLETE`

## 2. Independently verified pre-integration identity

Product repository:

`progeranna/signalguard-rs`

Target phase branch:

`refactor/dashboard-modules`

Exact target branch head before integration:

`09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`

Exact target base tree:

`0ad193d7aca4b668ba51232b7bf8362ddd066e10`

Worker branch:

`fix/modal-only-detail-navigation`

Exact worker commit:

`41f7f6fa9779e282bfff5714c26965b833f69741`

Exact worker tree:

`f629b6ea4339c92d03223c3bd8024cd4cb4571da`

Worker commit subject:

`fix(ui): restore modal-only detail navigation`

Verified relationship:

- worker commit is exactly one commit ahead of the target base;
- worker commit is zero commits behind;
- merge base is exactly the target base;
- target phase branch still resolves exactly to the target base;
- the worker commit contains exactly 26 changed paths: 21 modified and 5 deleted;
- every changed path belongs to the complete P3.6-MP01-R2 lease;
- no package, lockfile, Rust, API, CI, Docker, script, CSS, Timeline, ticker, query-key, or resource-identity path changed.

## 3. Independently reviewed product result

The worker tree implements the binding product-owner correction:

- `/` and `/dashboard` are the only visual console pages;
- `/symbols/:symbol` redirects with replacement semantics to `/dashboard`;
- `/anomalies` redirects with replacement semantics to `/dashboard`;
- market activation opens Dashboard-owned Symbol Detail;
- anomaly activation passes exact anomaly UUID and opens Dashboard-owned Anomaly Detail;
- All Anomalies rows open exact Anomaly Detail rather than Symbol Detail;
- Anomaly Detail resolves against the active mode summary and cannot retain stale cross-mode data;
- Back restores All Anomalies and exact row focus;
- the header symbol selector changes mode-scoped Dashboard selection without symbol-route navigation;
- obsolete standalone pages and route helpers are deleted;
- the active source has no prohibited standalone-route residuals;
- the backend `/anomalies` API is unchanged.

The implementation report records successful local gates:

- focused R2 suite: 6 files / 62 tests;
- full Vitest: 44 files / 591 tests;
- Node bundle-policy: 25 / 25;
- typecheck, lint, build, and bundle policy: pass;
- Rust: 379 passed, 0 failed; documented ignored service-backed tests only;
- browser matrix: desktop and mobile, eight screenshot hashes, zero unexpected console/page/unhandled errors;
- final raw JS: 387239 bytes;
- direct gzip: 110799 bytes;
- one initial JS asset and zero async JS assets;
- no Recharts graph, new dependency, or duplicate emitted module ID.

No GitHub status check or workflow run currently exists for the standalone worker commit. Therefore integration requires authoritative pull-request merge-ref CI before merge.

## 4. Integration role and tools

Execute this as an integration worker with authenticated GitHub access and local Git/`gh` access.

Local product repository:

`/Users/anion/Desktop/work/git-signalguard-rs/signalguard-rs`

Use a dedicated integration worktree when local merge-ref inspection is needed:

`/Users/anion/Desktop/work/git-signalguard-rs/worktrees/p36-mp01-integration`

Use an isolated connector checkout/worktree for the final report and status commit:

`/Users/anion/Desktop/work/connector-p36-mp01-integration`

Do not modify or stop the existing manual-QA environment, including its worktree, processes, containers, ports, PID files, or logs.

## 5. Immutable integration rules

Do not modify the worker branch or product source.

Do not create any additional product commit before merge.

Do not amend, rebase, reset, squash, cherry-pick, force-push, tag, or rewrite product history.

Do not retarget the PR to another branch.

Do not use squash merge or rebase merge.

Use a normal merge commit only.

Do not merge when any required identity, CI, tree, or scope check fails.

## 6. Preflight

Fetch all product refs and verify independently:

1. `refactor/dashboard-modules` resolves exactly to `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`.
2. `fix/modal-only-detail-navigation` resolves exactly to `41f7f6fa9779e282bfff5714c26965b833f69741`.
3. The worker commit has exact parent `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`.
4. The worker commit tree is exactly `f629b6ea4339c92d03223c3bd8024cd4cb4571da`.
5. The worker branch is exactly one ahead and zero behind the target branch.
6. The compare result has exactly the 26 paths recorded in the implementation report.
7. No existing open or merged PR already integrates this exact head into this exact base.
8. The implementation report exists on connector `main` with exact blob `a82dac068c087fd1f13c1066e3c749bd52bd3dd4`.

If the target branch moved, the worker branch moved, ancestry differs, or the report identity differs, stop with:

`P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_IDENTITY_DRIFT`

Do not rebase or adapt the worker commit.

## 7. Pull request

Open exactly one pull request:

- head: `fix/modal-only-detail-navigation`
- base: `refactor/dashboard-modules`
- title: `fix(ui): restore modal-only detail navigation`

The PR body must state:

- exact immutable base and worker SHA;
- the product-owner modal-only invariants;
- the 26-path scope and five deletions;
- local validation summary from the implementation report;
- that merge is forbidden until the authoritative PR merge-ref CI is successful;
- that a normal merge commit is required;
- that Phase 4 remains unauthorized.

Do not add unrelated labels, reviewers, milestones, or metadata unless repository policy requires them.

## 8. Authoritative PR CI

Identify the exact PR merge ref and its SHA.

Require the workflow run triggered by this PR and exact merge ref.

Verify from workflow/job metadata and checkout logs that the authoritative jobs test the exact PR merge ref, not only the worker head.

Require all repository-required jobs to complete successfully, including at minimum:

- frontend job: success;
- Rust job: success.

Inspect failed, cancelled, skipped, neutral, timed-out, or missing required jobs as failure.

Do not merge while CI is queued or in progress.

The tested merge-ref tree must equal:

`f629b6ea4339c92d03223c3bd8024cd4cb4571da`

Because the target base is the worker commit's direct parent, any different merge-ref tree is a blocker.

Also verify that the merge ref contains only the exact 26 worker paths relative to the target base.

When PR CI fails or the tested merge-ref identity cannot be proven, stop with:

`P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_CI`

Do not modify the worker branch to repair failures under this integration contract.

## 9. Merge

Only after every preflight and authoritative PR CI gate passes, merge the PR using a normal merge commit.

Do not squash or rebase.

The resulting merge commit must have exactly two parents in this order:

1. `09e0cbaa8cafd7c0523bb4ed539c01b2f7ad0b27`
2. `41f7f6fa9779e282bfff5714c26965b833f69741`

The merge commit tree must equal both:

- the successful tested PR merge-ref tree;
- `f629b6ea4339c92d03223c3bd8024cd4cb4571da`.

Verify that `refactor/dashboard-modules` resolves exactly to the resulting merge commit.

Do not delete the worker branch during the merge operation. Branch cleanup may occur only after all post-merge verification and report publication are complete.

## 10. Post-merge CI

The repository workflow includes push-triggered validation. Identify the push workflow run whose exact head SHA is the resulting merge commit on `refactor/dashboard-modules`.

Require all required frontend and Rust jobs to complete successfully.

Inspect checkout logs and prove that each required job checked out the exact merge commit.

When no successful exact-merge-SHA push run exists, stop with:

`P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_POST_MERGE_CI`

Do not rewrite or revert history automatically.

## 11. Final product verification

Verify all of the following:

- PR is merged;
- merge method was normal merge;
- merge commit has the exact two parents in the required order;
- merge tree equals `f629b6ea4339c92d03223c3bd8024cd4cb4571da`;
- target phase branch resolves exactly to the merge commit;
- worker commit is an ancestor of the target phase branch;
- target compare from the old base contains exactly one worker commit plus the merge commit and the exact worker tree change;
- no additional product path or commit was introduced;
- worker branch remains at `41f7f6fa9779e282bfff5714c26965b833f69741`;
- no force-push, tag, squash, rebase, or history rewrite occurred.

## 12. Connector integration report and status

Create or update exactly these connector paths in one connector commit:

1. Integration report:

`signalguard-rs/phase-3.6/reports/P3.6-MP01-R2-INTEGRATION/<MERGE_COMMIT_SHA>.md`

2. Phase 3.6 status:

`signalguard-rs/phase-3.6/control/STATUS.md`

Required connector commit message:

`docs(phase-3.6): record modal-only integration`

The integration report must include:

- original, R1, R2, implementation-report, and integration-contract identities;
- exact base, worker, PR number, PR merge-ref SHA, PR merge-ref tree, CI run ID, and job IDs;
- proof that CI tested the exact merge ref;
- exact merge commit, parent order, and tree;
- post-merge push CI run and job identities;
- exact 26-path changed/deleted inventory;
- final bundle/test evidence referenced from the implementation report;
- no-source-change/no-extra-product-commit proof;
- target and worker branch final states;
- PR merged state and merge method;
- no force-push/rebase/squash/tag/history-rewrite confirmation;
- cleanup state.

`STATUS.md` must record at minimum:

- state: `P3_6_MODAL_ONLY_CORRECTION_INTEGRATED_PHASE_4_NOT_AUTHORIZED`;
- terminal disposition: `P3_6_MP01_R2_INTEGRATION_COMPLETE`;
- final integrated product SHA and tree;
- old closed Phase 3.5 base;
- worker SHA;
- PR, merge-ref CI, merge commit, and post-merge CI identities;
- modal-only product invariants;
- final tests and bundle metrics;
- Phase 4 remains explicitly unauthorized;
- the next action is a separate Phase 4 planning/inventory contract against the new exact integrated SHA.

The connector report/status commit must contain only those two paths.

Push connector `main` normally. Do not force-push or rewrite connector history.

Download/read back both files from connector `main` and verify their exact blobs and commit.

## 13. Cleanup

After successful merge, post-merge CI, and connector report verification:

- verify the P3.6 implementation worktree and integration worktree are clean;
- remove the dedicated integration worktree;
- the implementation worktree may be removed only when clean and the worker commit is proven reachable from both the remote worker branch and integrated phase branch;
- prune worktree metadata;
- retain or remove the worker remote branch according to the repository's established cleanup policy, but report the exact result;
- do not touch unrelated worktrees or the existing manual-QA environment.

## 14. Terminal statuses

Return:

`P3_6_MP01_R2_INTEGRATION_COMPLETE`

only after authoritative PR merge-ref CI, normal merge, exact-tree verification, successful exact-merge-SHA push CI, connector report/status publication, read-back verification, and cleanup all succeed.

Return an exact blocker status instead when required:

- `P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_IDENTITY_DRIFT`
- `P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_CI`
- `P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_POST_MERGE_CI`
- `P3_6_MP01_R2_INTEGRATION_BLOCKED_BY_PUBLICATION`

Do not claim completion after a partial integration.